# 对话系统使用说明

面向"以后要往里加东西"的人（包括未来的我）。读完这一份就能：给新 NPC 加对话、放立绘、加新情绪、接任务系统。

---

## 1. 数据怎么流（先看这张图）

```
Assets/Data/Tables/对话表.csv            ← 你唯一要手改的地方
        │  菜单：修仙/从配置表生成资产
        ▼
Assets/Data/Generated/DialogueDefinition/*.asset     （一条对话 = 一个资产）
        │  自动跟着跑 / 或菜单：修仙/对话系统/收集对话资产
        ▼
Assets/resources/对话/对话库.asset        ← 运行时唯一入口（Resources 才能被运行时读到）
        │
        ▼
DialogueDatabase.取().取段(npcId, 分段)  →  DialogueUI 显示
```

> **为什么要「收集」这一步**：生成出来的资产在 `Assets/Data/Generated/` 下，**不在 Resources 里**，
> 运行时 `Resources.Load` 拿不到（这是本项目的既有约束）。所以摊平成一份库资产放在 Resources 下。
> `修仙/从配置表生成资产` 结束时已经会自动调一次收集，一般不用手点。

---

## 2. 给一个新 NPC 加对话（3 步）

**第 1 步**：在 `Assets/Data/Tables/对话表.csv` 里加行。

| 列 | 说明 |
|---|---|
| `id` | 本条唯一 id，建议 `dlg_<谁>_<第几段>` |
| `npcId` | 写 `NpcDefinition.id`（如 `npc_cunmin_chengnan`）。**留空 = 通用段**，谁都能用（见下） |
| `分段` | 同一 NPC 从 1 开始；`跳转` 也指到这里。同一个 `分段` 可以写多条（靠条件/优先挑一条） |
| `说话人` | 留空 = 用 NPC 自己的显示名 |
| `文本` | 这一段说什么 |
| `立绘` / `玩家立绘` | 资源名，见第 3 节。留空 = 不显示 / 用占位黄块 |
| `回答1..3` | 回答选项文字，**留空即没有这个选项**（最多 3 个） |
| `跳转1..3` | 选了对应回答跳到哪个 `分段`；**0 = 结束对话** |
| `需要标记` / `排除标记` | 条件，分号分隔，见第 5 节。留空 = 无条件 |
| `优先` | 同分段多条都满足条件时，取 `优先` 大的（**优先相同则 NPC 专属压过通用**） |
| `情绪` / `情绪强度` | 见第 4 节。留空 = 不表演情 |

**第 2 步**：跑菜单 **修仙/从配置表生成资产**（会顺带重建对话库）。

**第 3 步**：确认 NPC 的预制体/实例身上有 `NpcDialogue`（`Assets/resources/NPC/Human/` 下 83 个人类预制体已经全挂好了）。
没挂的：加 `NpcDialogue` 组件即可，它自己在 `Start` 里补一个「F 对话」的 `StationInteractable`。

### 通用段（npcId 留空）

`对话表.csv` 里 `npcId` 留空的行，**任何 NPC** 都能取到。典型用法：
给所有村民写一段"兜底第 4 段"，只有没写第 4 段的 NPC 才会走到它。
**注意**：同 `分段` 同时存在专属段和通用段时，专属段赢（按"优先→是否专属→id"排序）。

### 没有回答的段落

`回答1..3` 全空 → 界面给一个「继续 ▸」按钮：能进下一段就进，否则「结束」。
键盘上按 `继续键`（默认 `F`）或空格也等于点它。

---

## 3. 立绘怎么放

- 放这里：**`Assets/resources/立绘/`**（文件夹已建好，里面有 `说明.txt`）
- 文件名 = 对话表里 `立绘` 列的值，例如 `立绘` 列写 `成男村民` → 文件就是 `Assets/resources/立绘/成男村民.png`
- 找不到图时，界面显示**示意图里那个黄块 + 占位文字**（"人物立绘"/"玩家立绘"），不会报错 —— 所以美术没补图也能先用
- 玩家立绘同理：对话表 `玩家立绘` 列 + `Assets/resources/立绘/<名字>.png`

---

## 4. 情绪特效（可扩展）

对话表 `情绪` 列写名字，`情绪强度` 写倍率（1 = 标准，0.5 = 弱一半，留空按 1 算）。

**内置：**

| 名字 | 表现 |
|---|---|
| `震动`（也认 `震惊`/`惊讶`） | 整个对话框 + 立绘一起抖 0.45 秒 |
| `冒泡`（也认 `冒泡泡`） | 人物立绘下方冒出一串泡泡 |
| `发怒`（也认 `生气`） | 文本框闪红 |
| `害羞` | 文本框闪粉 |

**加新情绪（不用改表结构、不用改 `switch`）：**

```csharp
// 在任意初始化处（例如你自己的剧情脚本 Awake）注册一次
DialogueUI.自定义情绪["彩虹"] = (ui, 强度) =>
{
    ui.StartCoroutine(ui.冒泡(强度));      // 复用内置表现
    ui.StartCoroutine(ui.闪色(强度, Color.cyan));   // 或者自己组合
};
```

之后对话表里 `情绪` 列写 `彩虹` 就能用。写了一个没注册的名字：只会在 Console 里提示一句，不会崩。
`DialogueUI` 上的 `震动/冒泡/闪色` 都是 `public IEnumerator`，可以从外面直接起。

---

## 5. 任务管理器怎么接（接口已留好，本体没写）

**核心就一个静态标记集合**：`对话标记`（`Assets/Scripts/Data/对话标记.cs`）

```csharp
对话标记.添加("任务_采药_已接");        // 接任务
对话标记.添加一批("任务_采药_已接;见过村长");
对话标记.移除("任务_采药_已接");
bool 有 = 对话标记.具备("任务_采药_已接");
对话标记.清空();                        // 读档/重开时
对话标记.变化 += (标记, 新增) => { /* 需要的话刷新界面 */ };
```

**对话侧不用写代码**：对话表里同 `分段` 写两条，用标记区分即可。

例：村长第 1 段，接任务前 / 接任务后说不一样的话

| id | npcId | 分段 | 文本 | 需要标记 | 排除标记 | 优先 |
|---|---|---|---|---|---|---|
| dlg_cunzhang_01 | npc_cunzhang | 1 | 你来得正好，帮我去采三株灵芝。 | | 任务_采药_已接 | 0 |
| dlg_cunzhang_01b | npc_cunzhang | 1 | 灵芝采到了吗？ | 任务_采药_已接 | | 0 |
| dlg_cunzhang_01c | npc_cunzhang | 1 | 多谢道友！ | 任务_采药_已交 | | 10 |

规则：`需要标记` **全部**具备、`排除标记` **一个都不**具备 → 这条才算候选；多条候选按 `优先` 选大的。

**其它已经留好的口子：**

- `DialogueUI.当前NPC` / `NpcDialogue.当前对话中`：谁在跟玩家说话（过场、禁移动、禁攻击都能问它）
- `NpcDialogue.请求对话`（静态事件）：按 F 的那一刻广播，剧情系统可以先接一手再让 UI 打开
- `NpcDialogue` 上的 `交互距离 / 看向距离 / 只转头距离 / 最大偏角 / 交互键`：全 NPC 通用，预制体上可单独覆写

---

## 6. 提示与朝向（`NpcDialogue`）

- **提示**：复用建筑那一套 `StationInteractable` + `StationInteractor`，**没有第二套**。NPC 类型是 `对话`、键位 `F`、距离 3.5m，同屏多个时取**根节点更近**的那个。
  - **NPC 的排版（用户 2026-09-26 定的）**：**头顶只有 NPC 名字**（`StationName` 画布，白字加粗 + 深色描边、不带框），
    **「F 对话」小框在身侧**（`StationHint` 画布，圆角深色底 + 亮键帽 + 动作名"对话"）；
    小框沿**屏幕右方向**离身体 `StationInteractor.身侧距离`（默认 0.55m）、高度取肩胸之间（`顶 × 0.62`，夹在 1.2~2.2m），
    并且画布轴心设在**左边缘** —— 这样小框从身体旁边往外长，不会压住人。
  - **建筑保持老样子**：头顶一个「F 炼丹」（`提示文字 = 设施.标题`，没有名字画布）。
  - 实测：名字高度差 2.44m / 水平偏移 0.00m；小框高度差 1.51m / 水平距离 0.55m；跑远了两块一起隐藏。
- **朝向**：玩家进 `看向距离`(8m) 才看；距离 ≤ `只转头距离`(4.5m) 且所需偏角 ≤ `最大偏角`(70°) → **只转 Head 骨骼**；超了就交给身体（`NpcAiBase.转向`，带阻尼）；离开范围把头恢复原状。
- 头骨自动找名字含 `Head` 的骨骼（找不到退 `Neck`）；本项目村民是 `Head` / `NeckTwist01-02`。
- **偏角用的是项目自己的朝向公式**：`LookRotation(方向) * Euler(0, 模型朝向补偿, 0)`（和 `NpcAiBase.转向` 一模一样），所以头和身体对"正面在哪"的理解必然一致 —— 别改成 `transform.forward` 去猜，村民的根节点 forward 不是模型正面。

---

## 7. 踩过的坑（都修了，但别再踩）

1. **导入器不会清空单元格**：CSV 里把某一格改空、重新导入，旧值会留着（`ApplyRow` 里 `if (!string.IsNullOrEmpty(val))`）。**改了列内容（尤其变空）后，先删掉 `Assets/Data/Generated/DialogueDefinition/` 再重新生成**。
2. **同优先时通用段会盖掉专属段**：已经改成"专属优先"，但你自己写条件时也要留意 `优先` 别乱设。
3. **对话资产不在 Resources 下**：忘了跑"收集对话资产"→ 运行时找不到库，Console 会明确提示，照提示跑菜单即可。
4. **手改 CSV 的逗号数量**：字段数必须和表头一致（空列也要留逗号）。改完用脚本数一遍字段数最稳（本项目就被这个坑过一次：7 行错位）。
5. **用 pwsh 替换 C# 源码里的中文**：会把换行写成字面量 `` `n ``，导致后面的语句被吞进注释（能编译、但整行不生效）。**改源码用编辑器/编辑工具，别用 shell 拼中文**。
6. **`NpcInstance.IsDead` 在运行时刚 `Instantiate` 的 NPC 上是 true**（气血还没初始化）—— 判断"能不能对话"别用它。
7. **挡 AI 转身要挡在 `NpcAiBase.转向()` 源头**：只挡 `执行待机` 里的那一次，别的状态照样把身体转过去（实测偏角恒为 0、身体还是转了）。
8. **调试瞬移玩家会被胶囊挤开**：测试里把玩家 `transform.position` 硬设到 NPC 旁边，碰撞会把玩家推走，量出来的偏角是假的。要么挪 NPC 根节点，要么挑空地。
9. **NPC 僵在原地、完全没有 idle 动作** ✗→✓：**场景文件里存下来的实例**，Animator 的 playable 是断的 ——
   `normalizedTime` 在涨、`isInitialized=true`、`hasBoundPlayables=true`、`weight=1.0`、`cullingMode=AlwaysAnimate`，
   **但 87 根骨头 0.8 秒内 0 根动、姿势一个字节不变**。
   实测对照：同一实例 `Rebind() + Update(0f)` 后 17/87 根动；从预制体**新 Instantiate** 的实例本来就 23/87 根动
   → **预制体没坏，坏的是场景里那份序列化状态**。
   修法：启动时补一次 `Animator.Rebind(); Animator.Update(0f);`（`Assets/Scripts/Npc/Npc动画自检.cs`，
   `NpcDialogue.Start` 里也会兜一次）。修完复验：**9/9 个村民 14/87 根骨头在动、最大 2.31°**。
   注意：这份 idle 本来就**很含蓄**（2° 上下），觉得不够明显就换 `swagger`/`agree` 那种动作幅度大的当待机。
10. **点开对话是一块「空白幕布」，或者幕布和对话框叠在一起** ✗→✓（用户实测报的）：
   `StationInteractor.打开界面` 对**所有**类型都开界面，`界面预制体` 为空时就造一块占位幕布（全屏半透明黑 + 标题），
   而且它 `sortingOrder = 2500` 比当时的对话框(900) 高 → **幕布永远盖在对话框上面**；
   同时 `NpcDialogue` 也在同一帧响应 F → 两个系统抢同一个键，谁先谁后由组件执行顺序（未定义）决定，
   所以表现忽而是"只有幕布"、忽而是"幕布 + 对话叠着"。
   修法三条：① `StationInteractor` 遇到 `类型=对话` **不造幕布、也不用界面预制体**，直接回调 `NpcDialogue.打开对话()`
   （漏挂模块的老实例它会补一个）；② `NpcDialogue` 在玩家身上有 `StationInteractor` 时**不再自己处理 F**
   —— 交互器是全项目唯一的 F 入口（建筑 `F 炼丹`、NPC `F 对话`、同屏取离根节点更近的）；③ 对话框画布提到 `2600`
   （高于幕布 2500、低于暂停菜单 3000），万一两者同时存在也让对话框赢。
   实测：走同一条 F 路径后「**没有幕布** ✓、对话框显示 ✓、说话时头顶提示自动让开 ✓、关闭后 `有界面打开` 收回 ✓」，
   建筑那条路照旧出幕布 ✓（回归验过）。

---

## 8. 验收实测记录（2026-09-26 这一轮）

| 项 | 证据 |
|---|---|
| 编译 | 0 error |
| 村民 idle（阶段 0） | 修前 **0/87 根骨头动、0.000°**（状态时钟照走）→ 补 `Rebind+Update(0f)` 后 **9/9 个村民 14/87 根动、最大 2.310°** |
| 靠近提示 | 站村民 2.2m：提示画布文字 = `[字=F][字=正太村民]` |
| 只转头 | 横移 35°：决策`只转头`，**头转 35.0°、身体转 0.0°** |
| 超偏角转身 | 原地瞬转 120°：1 帧内决策`转身`、`禁止身体转向=False`；身体转 51.9° 后偏角降到 68° 又自动切回只转头 |
| 离开范围 | 离 20m：头回正 0.0°、解禁 |
| 按 F 出框 | 名字=`正太村民`、内容=对话表第 1 段原文、回答按钮 2 个（对应表里 2 个回答） |
| 选回答跳段 | 点 `有啊` → 内容变第 2 段 `哇——！我长大了也要飞！` |
| 震动 | 0.5s 内对话框最大偏移 **16.1 像素** |
| 冒泡 | 人物立绘子物体 6 → 13 |
| 未知情绪 | 只打一条提示日志，不崩 |
| 跳转 0 收尾 | 点完根节点 `activeSelf=False`，`当前对话中=null` |

录像：`cultivation/screenshots/dialogue_flow.mp4`、`head_turn_final.mp4`、`prompt_fixed.mp4`

---

## 9. 文件清单

| 文件 | 作用 |
|---|---|
| `Assets/Data/Tables/对话表.csv` | **你改这个** |
| `Assets/Scripts/Data/DialogueDefinition.cs` | 一条对话的数据结构（生成资产的类型） |
| `Assets/Scripts/Data/DialogueDatabase.cs` | 运行时对话库 + 取段/条件过滤 |
| `Assets/Scripts/Data/对话标记.cs` | 任务管理器接口：标记集合 + 条件判定 |
| `Assets/Scripts/UI/DialogueUI.cs` | 对话框（照示意图的比例程序化搭 UI）+ 情绪系统 |
| `Assets/Scripts/Npc/NpcDialogue.cs` | NPC 上的对话模块：F 提示、只转头、按 F 开框 |
| `Assets/Editor/Tools/DialogueDatabaseBuilder.cs` | 收集对话资产到 Resources |
| `Assets/resources/对话/对话库.asset` | 运行时入口（自动生成） |
| `Assets/resources/立绘/` | 立绘图片放这里 |
