using UnityEngine;

/// <summary>
/// 水蓝（ShuiLan）。
///
/// 动画：Idle/Specialidle/Walk —— 属于「兽类」那套规则（见 NpcAiAnimal）：
///   · 不会攻击玩家；好感 > _亲近_ 跟随、低于 10 躲避、挨打往反方向跑一段
///   · 待机时插播 Specialidle（啄食 / 刨地）
///   · 死亡没有动画 → 模型立刻消失 + 通用粒子消散
///
/// 性格：水边生物、灵巧。这里只调数值，行为逻辑全部继承，不重写任何状态。
/// </summary>
public class NpcAiShuiLan : NpcAiAnimal
{
    protected override void 取默认参数()
    {
        base.取默认参数();

        // ---- 性格：水边生物、灵巧 ----
        索敌范围 = 10f;          // 多远注意到玩家
        跟随距离 = 3.5f;          // 好感高时跟到多近停下
        回避距离 = 8f;          // 好感低时躲到多远
        亲近好感阈值 = 60f;      // 高于这个值才会跟着玩家
        行走倍率 = 0.5f;
        奔跑倍率 = 1f;          // 没有 Run 动作的会自动降级成 Walk

        // ---- 待机花样（啄食 / 刨地）----
        待机花样动作 = "Specialidle";
        待机花样间隔 = 3f;
        待机花样时长 = 1.4f;

        // ---- 死亡消散（大小走基类基准 3，这里只调颜色和数量）----
        死亡消散特效路径 = "";
        死亡消散颜色 = new Color(0.72f, 0.9f, 1f, 0.95f);
        死亡消散粒子数 = 46;
        死亡消散特效存活 = 2f;
        死亡后销毁延迟 = 1.2f;
    }
}