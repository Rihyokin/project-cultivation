

## Codely Structured Memories

### User

### Feedback

### Project
- [2026-09-19 00:48:04] 本机 tuanjie-cli 的 install-modules 命令损坏（无论模块 ID 均报 'undefined is not an object' JS 崩溃），安装编辑器模块需走手动路径：模块清单与官方下载地址见 %APPDATA%\TuanjieHub\releases.json，语言包为 PO 文件，按模块 ID 命名（如 zh-hans.po）放入编辑器 Editor\Data\Localization\。编辑器实际装在 D:\Tuanjie 2022.3.61t14（1.6.13/2022.3.61t14），而 CLI install-path 配置的 C:\Program Files\Tuanjie\Hub\Editor 已失效；2026-09-19 已手动装好简体中文语言包。
- [2026-09-19 01:42:24] 项目路径含全角冒号（D:\project：cultivation\…），编辑器内 .NET 反射中 Assembly.GetName()/CodeBase 相关 API 会抛 "String conversion error: Illegal byte sequence"；编辑器脚本里枚举 AppDomain 程序集时避免对每个 asm 调 GetName()，改用 Type.GetType("…, AssemblyName") 直接按程序集限定名查找。另：Tripo Bridge 已作为嵌入式包装在 Packages/com.tripo3d.unitybridge（v1.0.14，菜单 Tools/Tripo Bridge）。
- [2026-09-21 09:45:00] 控制管线已改用 MCP（取代此前挂掉的 HTTP 桥）：harness 配置行在 C:\Users\Rihyo\.dsh\profiles\desktop\cordis.patch.yml（id: mcp-unity，transport: stdio）。CLI 入口是 `codely serve unity-mcp --stdio|--http --unity-project-path <工程>`，它经 TCP 连编辑器内 cn.tuanjie.codely.bridge（端口在 <工程>/Temp/.com-unity-codely.json，本次为 63386；编辑器进程是 Tuanjie.exe）。该桥支持多客户端，stdio 与 HTTP(8765) 可同时连。工作区内运维脚本：.dsh\health.ps1（只读体检）、.dsh\serve-unity-mcp.ps1（起 HTTP 通道）、.dsh\mcp.ps1（HTTP 工具客户端）；旧文件桥 DshBridge.cs + .dsh\cmd.txt 保留为兜底。注意 DSH_HOME 的 profile 文件在工作区之外，写入需提权；本机 PowerShell 执行策略为 Restricted，跑 .ps1 需 -ExecutionPolicy Bypass。
- [2026-09-21 09:49:24] 【修正并接续上一条】上一条只说拉到 codely 是**不够的**：codely 的 tools/list 输出不符合 harness 支持的 JSON Schema 子集，而 @deepseek-ai/dsh-mcp-client 是「要么完整世代要么没有」——实测原始输出 **0/17 通过**，一个工具不合规则 17 个全丢，日志只报一条含糊的 `path:["tools",0,"inputSchema","type"] Invalid input: expected "object"`。子集权威定义在 @deepseek-ai/dsh-tools/lib/types/json-schema.js：CONSTRAINT_KEYWORDS = type/oneOf/properties/required/additionalProperties/items/enum/const，ANNOTATION_KEYWORDS = description/title/default/examples，**其余 key 一律违规（含 $schema、$ref、anyOf）且递归检查**；另有根必须 type:"object"、required 的名字必须在 properties 里、additionalProperties 必须是布尔、type 只能单字符串、enum/const 只能用于标量。codely 具体踩了四处：全部 17 个带 $schema；unity_editor 根是 anyOf（两个 object 变体）；unity_gameobject 的 properties.propertyValue 有嵌套 anyOf；7 个工具的 additionalProperties 是 schema 不是布尔。因此 harness 行最终为 command=node.exe、args=[D:\project：cultivation\.dsh\unity-mcp-proxy.mjs]、toolCallTimeoutMs=600000（Unity 编译超 60s 默认值）；垫片只改 tools/list 的 inputSchema 并递归消毒（根 anyOf 合并成 object schema、properties 取并集且 enum/const 取并集以免丢掉 unity_editor 的 action=resume、required 取交集、非布尔 additionalProperties 放宽为 true、不认识的形状退化成 {} 保证不因翻译失败而丢工具），其余消息透明转发。垫片把 codely/project 路径硬编码为默认值（路径含空格和全角冒号，靠 argv 传会被中间层按空格拆开）。离线校验台在 .dsh\_diag\：validate.mjs 运行时从 app.asar 现取 harness 真校验器（不随仓库分发副本，DSH 升级即自动跟随），fidelity.js 确认不是靠掏空 schema 换通过。**改了消毒逻辑用 --transform + validate.mjs 本地验证，不要靠重启 DSH 试。**
- [2026-09-21 10:23:15] 伤害公式扩展为 **2×2**（用户口述需求）。两个正交轴：`DamageNature{物理,特殊}` 决定走**暴击**还是**会心**；`AttackKind{普通攻击,主动神通}` 决定用**普攻系**还是**主动法术系**的加成/减免。四种组合：物理普攻(暴击+普攻)、特殊普攻(会心+普攻)、物理主动神通(暴击+主动法术)、特殊主动神通(会心+主动法术)。规则要点：特殊攻击**不经过暴击判定**、完全不吃暴击与暴击伤害，改做"和暴击判定一模一样"的会心判定（`免会心率 =(受击方会心抗性−攻击方会心)/受击方会心抗性`，≤0 必定会心），会心成功在第三步乘会心伤害；主动神通**不吃**普攻伤害加成、也**不受**对方普攻伤害减免，替换成主动法术伤害加成/减免。实现：新增 `Assets/Scripts/Combat/AttackSpec.cs`（枚举类型名 ASCII、成员中文，与 QualityTier/SpiritualRoot 一致）；`ICombatStats` 增加 会心/会心抗性/会心伤害/主动法术伤害加成/减免 共 5 项，`NpcInstance`、`PlayerCombatStats`、`AttributeCombatStats` 同步补齐；`CombatCalculator` 泛化为 `Resolve(attacker,defender,AttackSpec)`，旧 API `ResolveBasicAttack`/`ResolveGuaranteedHit`/`CalcBasicAttackDamage` 全部保留且等价（`default(AttackSpec)` 就是物理普攻），因此 `BasicSword01` 一行都没改。**特殊 ≠ 远程**：分类由调用方显式传 AttackSpec，不要用"有没有子弹"推断（用户明确说特殊只是"通常"远程）。已实测（Edit Mode + AttributeCombatStats 构造确定数值）：四组合 220/330/300/450，刻意让 暴伤2.0≠会伤3.0、普攻加成0.2≠主动法加成0.8 以隔离两轴；不触发加成时 110/110/150/150；物理从不触发会心、特殊从不触发暴击；编译 0 错误并按时间戳 + DLL 符号表双重确认。完整公式见 开发注意事项.md §十一。
- [2026-09-21 10:23:15] 【待策划确认 · 重要】免暴率/免会心率公式是"反转"的：分母是**受击方抗性**，所以 `攻击方暴击 ≥ 受击方暴击抗性` 时免暴率≤0 → **必定暴击**，而不是"更容易暴击"。对 NPC表.csv 全部 111 行实测：玩家暴击 0.15 → **104 行(93.7%)必定暴击**，只有 7 行是概率暴击；玩家会心 0.10 → **85 行(76.6%)必定会心**。NPC 抗性大多填在 0.02~0.16（另有练功木桩 0.5）。若想改成按概率，公式需换成 `实际暴击率 = 攻击方暴击 − 受击方暴击抗性` 这类形状——但现行形状是照策划说明实现的，**动之前必须确认**。
- [2026-09-21 10:23:15] 一条旧坑已失效 + 一次我自己的错误，都记下来：(1) 开发注意事项.md §5.6 原写"中文标识符不能出现在 exec_editor_script 里、会被损坏成 ?"——那是**挂掉的 HTTP 桥**的编码问题；换成垫片管线后实测中文变量名与中文枚举成员（`AttackSpec.特殊主动神通`、`走会心判定`）都能正常传回，§5.6 已改写。这与"`.cs` 必须带 UTF-8 BOM"是两回事，后者依然必须。(2) 我手工数 NPC表.csv 的列数时**错了一位**，把木桩的「普攻伤害加成/普攻伤害减免」报反了（实际是 加成 0、减免 0.1），据此给出的伤害算例全错。表本身没问题：36 列、111 行列数全对齐、无列错位。**以后一律用 `Import-Csv` 按列名取值，不要手工数下标。**

- [2026-09-21 10:50:26] 主动技能系统落地（快捷键 + 焚天炎术）。技能栏 `UIPanelData.主动技能` 6 格（神通/法宝/灵阵共用）→ **第 N 格 = 快捷键 N**（`KeyCode.Alpha1..6`，可加小键盘）。新增 `Assets/Scripts/Abilities/ActiveSkillCaster.cs`（挂 Player，212 行）：施放顺序 = 冷却 → 结算方式是否实现 → 是否需要锁定 → 灵力够不够 → 扣灵力/进冷却 → 生成特效 + 结算；任何一步不过只给 `UIPanelData.ShowHint` 提示、**绝不白扣灵力**；空槽静默；界面开着（`UiEscRegistry.AnyOtherUiOpen()`）或 `timeScale<=0` 时不接收快捷键（可用 `界面开着时禁止施放` 关掉）。新增 `Assets/Scripts/Abilities/AreaSkillRunner.cs`（128 行）挂在临时特效物件上，用 `Physics.OverlapSphere` 找半径内 `是敌对目标` 的 NPC，去重后逐个 `NpcInstance.ReceiveAttack(攻击方, new AttackSpec(伤害属性, AttackKind.主动神通, false, 伤害倍率))`，**作用半径填 0 则退化成只打锁定目标**；`持续时长 > 0` 时按 `灼烧间隔` 反复结算，否则一次性。`AttackSpec` 新增 `技能倍率` 字段（≤0 当作 1，保证 `default(AttackSpec)` 仍是安全的物理普攻），`CombatCalculator.CalcDamage` 末尾乘它。`ActiveDivineAbility` 新增 `ActiveSkillKind{未实现=0, 范围伤害=1}` + 结算方式/伤害属性/伤害倍率/作用半径/冷却时间/需要锁定目标/持续时长/灼烧间隔/特效资源路径，全部由 `主动神通表.csv` 新列按名映射。**`主动神通表.csv` 从 8 列扩到 17 列**，焚天炎术行 = 范围伤害/特殊/2.5/半径6/冷却8/需锁定/持续0/间隔1。实测（Play Mode）：玩家 57 攻击、1.05 主动法加成、1.3 会心伤害、2.5 倍率 → 每个木桩 **194.513**，一次 AoE **命中 3 个木桩合计 583.538**，灵气 160→100，冷却 8 秒期内再放被拒，未实现的寒冰诀被拒且不扣灵力。
- [2026-09-21 10:50:26] 【坑 · 重要】用户指定的 `Effect_13_DangerClose.prefab` **单独放出来是看不见的**：它 6 个粒子系统里 3 个爆炸层（`Effect_13_Explosion`/`_2`/`Distort`）全是 `rate=0 burst=0 playing=False`，是等着被撞击触发的占位层；唯一在播的 `Effect_13_Shell` 的 `localPosition=(0,0,30)`（炮弹从 30 米外飞入的写法）、拖尾又是 World 空间，粒子实际落在施放点 Z+30 / Y−90，**画面外且在地下**；特效包是 `NotScriptBased`，触发脚本根本不存在。**真正的火焰在子目录 `Effect_13_Base/Effect_13_Explosion.prefab`**（burst=1、生命 1.5 秒、尺寸 10），已把 `特效资源路径` 改成它。注意路径要写到**子目录那一层**：`.../Effect_13_DangerClose/Effect_13_Base/Effect_13_Explosion`。`Effect_13_Base/Effect_13_Trails.prefab` 是另一份拖尾可另作它用。
- [2026-09-21 10:50:26] 【坑】MCP 的 `unity_gameobject ensure_component` 在本项目**用不了**，报 `String conversion error: Illegal byte sequence encounted in the input.`（内部走了 `Assembly.GetName()` 之类反射，撞上路径里的全角冒号，与 cultivation/CODELY.md 里记的老坑同源，只是这次中招的是工具本身）。**改用 `exec_editor_script` 里 `AddComponent<T>()` + `EditorUtility.SetDirty` + `EditorSceneManager.SaveScene`。** 另外 `ParticleSystem` 直接继承 `Component` 而非 `Behaviour`，**没有 `.enabled`**，要判播放状态用 `isPlaying` / `gameObject.activeInHierarchy`。

- [2026-09-21 11:02:11] 主动神通表字段整理（用户要求 6 项）：(1) `灼烧间隔` → **`伤害间隔`**；(2) `作用半径` → **`范围`**；(3) 删掉 `特效方法id` **列** —— 但基类 `DivineAbilityDefinition` 上的 `特效方法id` **字段保留**了，因为 被动神通表.csv 也有一列同名，删字段会连带影响被动神通（该字段目前零消费者）；(4) `持续时长` 对齐特效动画时长 = `duration 0.1 + 最长粒子生命 1.5` = **1.6 秒**；(5) `伤害间隔` 我定 **0.5 秒**（1.6÷0.5 → 每目标 **4 跳**）；(6) 特效按 `范围` 缩放 X/Z。表从 17 列变 **16 列**。实测逐跳累计 `583.54 → 1167.08 → 1750.61 → 2334.15`（= 3 目标 × 4 跳 × 194.513）；缩放实测 范围6→`localScale.x=0.606`、范围12→`1.212`（= 范围÷9.9，精确）。**注意 `伤害间隔=0.5` 让单目标总伤害从一次性的 194.5 涨到 778（4 倍），要压回原量级把 `伤害倍率` 从 2.5 调到 ≈0.6。**
- [2026-09-21 11:02:11] 【坑 · 特效缩放】`Effect_13_Explosion` 的粒子系统是 `simulationSpace = World`，而 **World 空间下 transform 缩放只改变粒子的散布位置、不改变单个粒子的尺寸**（实测 scale 1/2/3 → 散布 6.19/12.81/18.88 米，但粒径恒为 9.94/9.95/9.97）→ 火焰只是被"摊开"而不是"变大"，大小根本跟不住范围。**必须在实例上把粒子系统切成 `Local`**，之后缩放才等比（外缘半径÷scale ≈ 10.5，基本是常数；特效本身静止，Local/World 观感无差别）。`ActiveSkillCaster.特效基准半径` = 该特效在 scale=1 时的视觉外缘半径（`Effect_13_Explosion` 实测 **9.9 米**），施放时 `localScale = (范围÷基准, 1, 范围÷基准)`，只缩 X/Z。**换特效资源要重新标定这个值。**
- [2026-09-21 11:02:11] 【运维坑】`主动神通表.csv` 一度被**第三方进程独占锁定**（连只读都失败；Unity 侧 `File.ReadAllText` 报 Sharing violation；删/改名都不行），write 工具和 PowerShell 全部写不进去，持续数分钟后自行释放。**判别方法**：同目录其它 csv 能正常写 → 只有目标文件被锁；Unity 也读不了 → 不是 Unity 占的（`AssetDatabase.LoadAssetAtPath` 那个"成功"读的是 Unity 缓存副本，不算数）。**别在原地死磕**：把内容先落到暂存文件（`.dsh/_staged/`），等锁释放后 `Copy-Item` 覆盖。

- [2026-09-21 11:30:00] 【纠错 · 推翻 10:50 那条】上一条判定「真正的火焰在子目录 `Effect_13_Base/Effect_13_Explosion`」并据此换了特效 —— **用户指出原特效是「陨石砸地」的效果，我判断错了**。复查原因：(a) 只观察了 **1.07 秒**，而 `Effect_13_DangerClose` 的 `Effect_13_Shell` 是 `duration=4 / rate=3` 慢慢发射的，**火焰 1 秒多才起来**（首次出现粒子实测 0.33 秒）；(b) 用的是 13 米高、fov 40 的**游戏俯视相机**，画面根本装不下空中部分。换成能看空中的侧面相机观察 6 秒后，同一 prefab 拍出来是一大片地面火焰。**教训：判断特效可见性必须用能看全上下的专用侧面相机 + 至少 4~6 秒窗口，不能只看 1 秒 + 游戏相机。** 按用户决定，`特效资源路径` 已改回整个 `Effect_13_DangerClose`。它的真实结构：`Effect_13_Shell`(duration=4 rate=3，尺寸仅 **0.25** → 基本不可见的"炮弹"，localPos=(0,0,30) rot=(90,0,0) velOTL=(0,-50,-150)) / `Effect_13_Trails`(`Effect_10_TrailSmoke`+`TrailParticle_2`，**loop=True 无限发射**) / `Effect_13_Explosion`×2 + `_2` + `Distort`(burst=1) / `Effect_13_ShellColider`(缩放 10)。`shape=Circle 半径 20` → 铺开约 40 米。**它自带"飞入→火焰"全过程、不需要任何脚本**（NotScriptBased），但**看不到陨石本体**。
- [2026-09-21 11:30:00] 重标定后的焚天炎术数值：`持续时长` **3 秒**（覆盖 Shell 飞入 + 火焰升起；拖尾是 loop，严格说不存在"动画结束"）、`伤害间隔` **0.75 秒**、`特效基准半径` **49.1 米**（原 9.9 是 Explosion 子层的值）。0.75 是刻意选的：**换特效、拉长时长之后仍保持 4 跳**，免得伤害总额又变一次。实测 `localScale.x = 0.1222`（= 6÷49.1，精确）、逐跳 `583.54→1167.08→1750.61→2334.15`（4 跳）、3.3 秒后结算体自动销毁。**单目标总伤害仍是 778（4×194.5），比一次性结算高 4 倍**；要压回去就把 `伤害倍率` 从 2.5 调到 ≈0.6。
- [2026-09-21 11:30:00] 备查：这个特效包**里有真正的陨石** —— `EffectsSet_2(ScriptBased)/Effect_10_MeteorStrike/`。其中 `Effect_10_MeteorStrike.prefab` 是**纯控制器**（0 粒子系统、0 渲染器），靠包里 `MultipleObjectsMake` 脚本 spawn `Effect_10_BigMeteor`/`Effect_10_SmallMeteor`；`Effect_10_BigMeteor` 是真陨石模型（4 渲染器、0 粒子系统）+ `ObjectMoveDestroy` 脚本，实测会水平移动但**需要自己接目标**。它是"范围流星雨"而非单点命中。抓帧 `meteorstrike_2.png` 能看到拖着长尾的火焰流星下落。别和 `Effect_13_DangerClose` 搞混。

- [2026-09-21 11:35:00] 【关键 · 缩放的代价】用户给出他看到的画面（从天而降的火柱 + 落地爆炸），我并排对比后确认：**按 `范围` 等比缩放会把「从天而降」的高度一起压扁**。`Effect_13_DangerClose` 的火柱靠很长的下落距离撑起（`Effect_13_Shell` 起点 `localPos z=30`），缩放是整体的，所以范围越小下落越短。实测三档：缩放 0.12（范围6）= 一小团火、**火柱几乎看不见**；0.25（范围12）= 火柱能看清；0.4（范围20）= 成排火柱砸下；1.0（范围49）= 最壮观。**这是"视觉严格等于判定范围"与"火柱要够长才好看"的固有矛盾，不是 bug。** 用户选择 **范围 = 12 米**（缩放 0.2444）。要"小范围 + 大火柱"只能解耦（特效固定 scale=1、伤害仍按 `范围`），代价是看着大判定小。**定这个值要开侧视相机在 1.2s / 2.5s 各拍一张，别只靠数字。**
- [2026-09-21 11:35:00] 焚天炎术最终数值（本轮）：`范围` **12 米**、`持续时长` **3 秒**、`伤害间隔` **0.75 秒**、`伤害倍率` **2.5**、`冷却` 8 秒、`消耗灵力` 60、`伤害属性` 特殊、`特效` 整个 `Effect_13_DangerClose`、`特效基准半径` **49.1**。实测 `localScale.x = 0.2444`（=12÷49.1 精确）、逐跳 `583.54→1167.08→1750.61→2334.15`（4 跳）、3.5 秒后结算体销毁、命中 3 个木桩。**单目标总伤害 778（4×194.5），比一次性结算高 4 倍**；要压回原量级把 `伤害倍率` 调到 ≈0.6。

- [2026-09-21 11:40:00] 【用户发现 · 特效放反了】用户指出焚天炎术的特效是**从地底往上砸向地面**而不是从天上往下。核实无误：`Effect_13_DangerClose` 原样生成时粒子**世界 Y = −100.8 ~ −0.1（全在地面以下）**，绕 X（或 Z）转 180° 后变成 **0.1 ~ 100.8（全在地面以上）**，绕 X 转 270° 则变成水平（−5.1~12.1）都不对。**这个 prefab 是"倒着做"的。** 修法：`ActiveSkillCaster` 新增 `特效旋转` 字段（默认 `(180,0,0)`），生成时 `Instantiate(prefab, 中心, Quaternion.Euler(特效旋转))`。修后实测粒子 7120 个全部 Y ≥ 0.1 ✔，火柱从天上笔直落下砸地。**教训：这个坑用俯视相机永远看不出来（"从下往上"和"从上往下"都是一团火），必须打粒子的世界坐标、或用能看到地面以下的侧视相机。** 我在这个特效上已经连错两次（先说"看不见"、又没发现是反的）。
- [2026-09-21 11:40:00] 补充：该效果粒子 Y 跨度有 **100 米**（因为 Y 不参与缩放），火柱会冲出俯视相机画面——这是"天火"的正常表现。修好后焚天炎术数值不变：范围 12 / 持续 3 秒 / 间隔 0.75（4 跳，583.54→2334.15）/ 倍率 2.5 / 基准半径 49.1 / 旋转 (180,0,0)。

- [2026-09-21 11:50:00] 游戏内 HUD 落地（需求：左下角功法图标 / 主动技能栏 / 气血灵力条；技能进冷却后**图标变暗 + 实时倒计时 + 暗部逐渐消退**）。新增 `Assets/Editor/Builders/HudBuilder.cs`（编辑器生成器，菜单 **修仙/生成游戏界面 HUD**，**幂等**，只重建 `HudCanvas`、不碰 `CharacterUI`/`PauseMenuCanvas`；顺带生成贴图资产 `Assets/Resources/UI/Hud/hud_circle.png`(128 圆、1px 抗锯齿) 与 `hud_square.png`(4×4 白)）+ `Assets/Scripts/UI/PlayerHud.cs`（运行时刷新，挂 HudCanvas）。`ActiveSkillCaster` 新增 `冷却总秒 / 冷却中 / 冷却比例 / 槽位可用` 四个查询。冷却实现：遮罩 = `Image.Type.Filled + Radial360 + fillOrigin=Top + fillClockwise`，`fillAmount` **直接吃 `冷却比例`**（1→0），所以暗部从 12 点起顺时针消退；倒计时 ≥1 秒显示整数、最后一秒显示一位小数。实测 0.4s → 剩余 7.60 / 比例 0.950 / fill 0.951 / 文字 "8"；3.5s → 4.43 / 0.554 / 0.554 / "5"；7.6s → 0.24 / 0.030 / 0.031 / "0.2"；8.6s 遮罩与文字一起隐藏。`HudCanvas` = ScreenSpaceOverlay **sortingOrder −1** 且**刻意不加 GraphicRaycaster**（不挡点击）。布局：功法名在圆上方、技能名在格下方、按键号在格右下。
- [2026-09-21 11:50:00] 【坑 · HUD 三条】(1) **`Image` 的 sprite 为空时 `fillAmount` 被完全忽略**（不管 type 是否 Filled）→ 进度条/遮罩永远满格。所以生成器必须先产出贴图资产，`PlayerHud.Awake` 里还做了"缺 sprite 就现生成"的兜底。(2) **`PlayerStatsDebugPanel`（F1 面板）是 IMGUI `OnGUI` 画的，且该组件就挂在 `Player` 身上** —— 想临时关它必须 `面板.Visible = false`，**绝不能 `面板.gameObject.SetActive(false)`**：那会关掉整个 Player，导致 `ActiveSkillCaster.Update` 不跑、**冷却永远停在初始值**；而 `FindObjectOfType`/`GameObject.Find` 都不找未激活对象，报出来的是"找不到 Player"，与真因隔好几层（我为此绕了三轮，最后靠 `unity_editor stop` 重置 Play 才恢复）。(3) HUD 是 ScreenSpaceOverlay，**`Camera.Render()` 抓不到它**，必须 `ScreenCapture.CaptureScreenshot` + `WaitForEndOfFrame`（帧末落盘）；1920×1080 里 HUD 只占左下角一条，要看细节得裁图（`Texture2D.LoadImage` + `ReadPixels`）。
- [2026-09-21 11:50:00] 【现状】技能/功法资产的 `图标` 字段**全是空的**，所以 HUD 现在用**品阶色**画色块（`UIEntryRow.TierColor`）；等美术把图标填进资产，HUD **不改代码**就会显示真图标。`结算方式 = 未实现` 的槽位（寒冰诀/雷影术/五行遁/御剑术）与法宝/灵阵会盖一层黑 62% 的**灰罩** —— 这和"冷却中"是两回事（灰罩=还做不了，冷却遮罩=刚放过）。

- [2026-09-21 12:05:00] 【HUD 改版 · 用户 4 项要求】把 HUD 从"圆 + 带名字"改成**方框 + 紧凑 + 悬停出幕布**：(1) 全部图形改**正方形**（连冷却遮罩一起）；(2) 更紧凑——布局改为「功法 68×68 @(20,58) ／ 技能 1~6 各 46×46、间距 5、@(98,80) 右缘 x=399 ／ 气血(20,38) 与 灵力(20,17) 各 379×15 横跨在下方」；(3) 换布局，气血/灵力从"技能栏右侧"挪到"整块下方横跨"，右端与技能栏对齐；(4) **不再显示功法和主动技能的名字**，改成**鼠标悬停弹出半透明「信息幕布」**（400×190 @(20,136)，默认隐藏），移开即消失。新增 `Assets/Scripts/UI/HudHoverTarget.cs`（实现 `IPointerEnterHandler`/`IPointerExitHandler`，`槽位` 0~5=技能、−1=功法）；`HudCanvas` 加上 `GraphicRaycaster`（场景本来就有 `EventSystem`），但**只有格子的「底」是 `raycastTarget=true`**，其余图都 false，所以不会抢别处的点击；幕布自身的图全 false，否则会挡住格子的悬停。冷却遮罩由 `Radial360` 改成 **`Vertical` + `fillOrigin=Top`**（方框用竖向扫比径向钟面更清楚）；冷却文字与按键数字都加了 `Outline` 深色描边（否则压在亮色图标上看不见——实测按键"1"在粉色底上原本看不清）。幕布正文按类型分支：功法／主动神通(已实现，列 消耗灵力·冷却·范围·伤害倍率·持续与结算间隔·是否需锁定)／主动神通(未实现)／灵阵／法宝／空槽不弹。
- [2026-09-21 12:05:00] 【悬停怎么自动测】不用真的动鼠标，走 uGUI 自己的管线即可：`RectTransformUtility.WorldToScreenPoint(null, 格子.position)` 算出屏点 → `EventSystem.current.RaycastAll(ped, 结果)` → `ExecuteEvents.GetEventHandler<IPointerEnterHandler>(结果[0].gameObject)` → `ExecuteEvents.Execute(处理器, ped, pointerEnterHandler)`。实测三个格子都正确解析到对应的 `HudHoverTarget`（射线命中「底」，处理器是对应格子根节点）。**注意 `EventSystem.current` 在 Edit Mode 下是 null**（普通 MonoBehaviour 在编辑器里不跑 `OnEnable`），必须进 Play Mode。另外 `ScreenCapture` **偶尔会抓到全空白的一帧**（实测出现过整片灰图），抓完要检查像素是否有变化、空白就重试。

- [2026-09-21 12:09:17] 【新需求 · 敌对照显血条】所有 `对主角好感度 < 0` 的 NPC **不需要锁定就常显血量 UI**。改 `Assets/Scripts/Npc/NpcIndicator.cs`：新增 `应显示血条` 属性（死亡且 `死亡时隐藏血条` → 不显示；否则 `IsLocked || (敌对常显血条 && npc.是敌对目标)`）+ 两个开关 `敌对常显血条`(默认开) / `死亡时隐藏血条`(默认开)。**关键：显隐改成在 `LateUpdate` 里每帧同步（`同步血条显隐()`），不再只在 `SetLocked`/`SetSelected` 里刷** —— 否则好感度中途变化、NPC 死亡/重生（练功木桩会立刻满血重生）都不会反映。实测四分支全过：木桩×3（好感−1、敌对、**未锁定**）→ 显示；临时友善NPC（+5）→ 隐藏；同一个改成 −5 → **即时显示**（无需重新锁定）；打死它 → 隐藏。血条尺寸仍是世界单位 `血条宽度 1.1 / 血条高度 0.13`（原来只为单个锁定目标设计，现在会同时出现多个，嫌小就调这两个字段）。注意每个 `NpcIndicator` 会建一个 WorldSpace Canvas，同屏敌对目标几十个时要考虑按距离/是否在屏幕内裁剪。
- [2026-09-21 12:09:17] 验证手法备忘：`NpcIndicator.血条根` 是**私有字段**，Play 脚本读不到；要判断血条是否真的显示，用层级查找 `npc.transform.Find("IndicatorRoot/HealthBar").gameObject.activeSelf`。要在不污染资产的前提下测"友善 → 敌对"的反向分支，用 `ScriptableObject.CreateInstance<NpcDefinition>()` 造一个临时定义 + `new GameObject` + `CapsuleCollider` + `NpcInstance` + `NpcIndicator`，**别去改共享的 NpcDefinition 资产**（Play 模式下改 ScriptableObject 资产会写回磁盘）。

- [2026-09-21 12:20:00] 【新需求 · 首次造成伤害时间】主动神通表新增一列 **`首次造成伤害时间`**（第 15 列，表从 16 列变 **17 列**），用来把"伤害生效时刻"对齐特效动画（陨石落地/火焰升起）。改 `ActiveDivineAbility`（新字段 + OnValidate 钳非负）、`AreaSkillRunner`（`初始化` 里 `下次结算时间 = Time.time + 首次`，**首次 > 0 时不再在施放瞬间立刻结算**；`Update` 逻辑不变）、`PlayerHud`（幕布多一行「首次伤害：N 秒」，幕布高度 190→215 免得截断）。时间轴：`t=首次` 第 1 跳 → 每 `伤害间隔` 一跳 → `t=持续时长` 销毁；**跳数 = 满足 `首次 + k×间隔 < 持续` 的 k 个数**。`首次 >= 持续` 时一跳都不会结算，此时 `AreaSkillRunner` 会打 `Debug.LogWarning` 明说（不静默失败）。焚天炎术定为 **首次 1 秒**（对齐火焰真正烧起来），于是结算时刻 `1.00 / 1.76 / 2.51`、**3 跳**（原来是 首次=0 的 4 跳）。A/B 实测：首次1 → 1.00/1.76/2.51；首次0（克隆）→ 0.01/0.76。**注意：单目标总伤害从 778（4跳）变成 583.5（3跳）；想补回 4 跳就把 `持续时长` 调到 3.75。**
- [2026-09-21 12:20:00] 【坑 · 让我白绕一轮】`练功木桩`（`monster_wooden_dummy`）在 `NPC表.csv` 里的 **`对主角好感度` 已被改成 `0`**（两轮前还是 `-1`）。于是它**不再是敌人**：焚天炎术"命中 0 个目标"、血条也不显示，表现得非常像"新功能坏了"。我因此误判新加的 `首次造成伤害时间` 没生效——实际是那一发**一个目标都没打到**（所以既没有血条变化也没有 `[AreaSkillRunner]` 日志），当时唯一能看到的掉血是旁边**飞剑的普攻**（81.225/54.15，正好不是 194.513，这就是分辨点）。**教训：拿木桩试招前先确认 `对主角好感度` 是负数；要稳定复现就在测试脚本里现造一个敌对目标（`ScriptableObject.CreateInstance<NpcDefinition>()` + `CapsuleCollider` + `NpcInstance`，设 `定义.属性[AttributeType.MaxHealth]` ≥ 500），别改共享资产（Play 模式改 ScriptableObject 资产会写回磁盘）。**

- [2026-09-21 12:35:00] 【用户报 3 个 bug · 查明根因】用户报：(1) 好感为负的 NPC 不显示血条；(2) 焚天炎术打不出伤害；(3) **新规则：锁定了攻击就该对锁定目标生效，无关好感度**。查明：**(1)(2) 都不是代码问题，是同一条数据没同步** —— 用户把 `NPC表.csv` 里 `monster_wooden_dummy` 的 `对主角好感度` 改回 -1，但**没跑导入器**，生成资产里还是 0（我实测：CSV=-1 / 资产=0 / 场景 3 个木桩 敌对=False），于是木桩不再是敌人 → 血条不显示 + 技能"命中 0 个目标"。**跑一次「修仙/从配置表生成资产」两个 bug 同时消失**（实测血条 `显示 OK`、焚天炎术每跳 194.513）。**(3) 已实现**：`AreaSkillRunner.结算一次()` 的敌对过滤改成 `if (!npc.是敌对目标 && npc != 单体目标) continue;` —— 范围内的敌人照打，**锁定目标无条件打**（和飞剑"手动右键锁定不看好感度"一致）。实测锁一个好感 +20 的友好目标放焚天炎术 → 照扣 500 血 ✔。
- [2026-09-21 12:35:00] 【新增兜底 · 配置表同步检查】因为"改 CSV 不生效"这个坑连着坑了两次，新增 `Assets/Editor/Data/DataTableSyncChecker.cs`（`[InitializeOnLoad]`）：**每次进 Play Mode 前**（`PlayModeStateChange.ExitingEditMode`）比对 CSV 与生成资产 mtime，不一致就 `Debug.LogWarning` 点名哪张表；另有菜单 **修仙/检查配置表是否已同步** 手动跑。配套在 `DataTableImporter` 加了 `public static List<string> 找出未同步的表()` 与 `表数量`（**只读**，带 **2 秒容差**，避免"改完马上导入"误报）。实测：touch 一下 `主动神通表.csv` 的 mtime → 立刻报 `检测到 1 张未同步 → 主动神通表` ✔；重新导入后恢复 ✔。**没有让导入器自动跑**，因为它会 `RewirePanelData()` 重写技能栏 + `SaveScene()`，自动执行有数据风险（§3.1 的老坑）。
- [2026-09-21 12:35:00] 【再次踩到的读法坑】判断 `NpcIndicator.应显示血条` **不能在 Edit Mode 读** —— `npc` 是在 `Awake()` 里赋的，而 Edit Mode 不跑 `Awake`，所以 `npc == null` → 永远返回 false，看起来像 bug。必须进 Play Mode 看。（同源坑：`EventSystem.current` 在 Edit Mode 也是 null。）另外发现用户已自行把焚天炎术调成 **首次=0.1 / 间隔=0.56**（我写的是 1 / 0.75）—— 实测首跳 t=0.10s、`0.1+0.56k<3` 正好 **6 跳**，与日志完全吻合，说明参数按用户的值在正常生效。

- [2026-09-21 12:50:00] 【新功能 · 受伤飘字】NPC 挨打时在旁边弹出伤害数字，**1 秒后消失**，**暴击/会心字号更大**。新增 `Assets/Scripts/Npc/DamagePopup.cs`（运行时现建 WorldSpace Canvas + Text，场景里不用摆东西；字体用内置 `Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")`，因为只有阿拉伯数字、不需要中文）。**关键设计：挂在事件上而不是各个攻击里** —— `NpcInstance.ReceiveAttack` 是所有伤害的唯一入口，新增 `public event Action<NpcInstance, AttackResult> 结算完成` 并广播，`NpcIndicator` 在 `Awake` 订阅（`OnDestroy` 退订）后调 `DamagePopup.弹出(飘字位置, 伤害, 暴击, 会心)`；**这样飞剑/普攻/神通全都自动有飘字**。选 `AttackResult` 而不是已有的 `Damaged` 事件，是因为后者只有扣血量、判断不了暴击。三档实测：普通 **34 号米白** `(1,0.96,0.86)` / 暴击 **46 号橙** `(1,0.7,0.16)` / 会心 **46 号青** `(0.52,0.86,1)`；动画 = 出现时 0.55→1 倍「砰」一下（0.09s）+ 先快后慢上浮 1.1 米 + 水平随机散开 0.5 米 + 最后 40% 淡出。`sortingOrder=200` 必须高于血条的 100。用 `Time.time`（受 timeScale 影响）所以暂停时一起定住。实测焚天炎术（一次 4 目标 × 每 0.56s 一跳 × 活 1s）**同屏最多 8 个**、之后残留 0 ✔。
- [2026-09-21 12:50:00] 【踩到的小坑】验证「普通命中」那一档时发现**看不出来**——因为玩家暴击/会心数值本来就高（§11.3 的反转公式），实际打起来几乎每一发都必定暴击/会心，所以只能直接调 `DamagePopup.弹出(pos, 128f, false, false)` 来确认普通字号。另外 `DamagePopup.弹出` 可以带 `null` 世界位置也没事，但 GameObject 必须用 `new GameObject(name, typeof(RectTransform))` 建——`Canvas` 需要 `RectTransform`。

- [2026-09-21 13:20:00] 【大功能 · NPC 的 AI 基类】按用户需求做了三型 AI。架构：`NpcAiBase`（抽象基类：感知/状态机/移动/动画降级/出手/受击/死亡/收服预留）→ `NpcAiAnimal` 兽类；`NpcAiCombatant`（抽象，索敌→追击→出手→脱战）→ `NpcAiDemon` 妖魔 / `NpcAiHuman` 人类。新增目录 `Assets/Scripts/Npc/Ai/`（5 个文件 + `NpcProjectile` 子弹）。**子类只实现 `决策()`**，其余全在基类。**自动装配**：`NpcInstance.Start()` 调 `NpcAiBase.确保(this)`，按 `NpcDefinition.类型` 加对应组件；**prefab 上已有 AI 就不动**（只有代码自动装的才灌类型默认值，否则会覆盖手调数字）；中立/未指定不加 AI。配置表 `NPC表.csv` **36→38 列**，新增 `类型`（兽类/妖魔/人类/中立/未指定）+ `索敌范围`，已按 id 前缀批量填好（beast→兽类11 / demon→妖魔54 / human→人类26 / npc·monster→中立20）。行为：兽类永不敌对（好感>60 跟随到 3 米、<10 躲开、挨打反方向跑 3 秒、无死亡动画靠粒子消散）；妖魔初始好感 ≤−10 视玩家为敌、进索敌范围就追上来打；人类好感 ≤9 才翻脸（= 10 及以上友好）。出手分工：Attack1 普通攻击、Attack2/3 主动神通，伤害在动画 `出手进度`(默认 0.4) 处结算；Attack2/3 可勾 `是施法`+填 `子弹特效路径` 走飞行道具。
- [2026-09-21 13:20:00] 【AI 的三个坑】(1) **好感度原来存在 ScriptableObject 上，运行时改会写回磁盘**（玩一次就把表里的初始值改脏）→ 改成资产只当初始值、运行时用 `NpcInstance.运行好感度` 副本，对外接口名不变。挨打掉好感在 `ReceiveAttack` 里做（每次 −20，**每次伤害结算都掉**，多段 AoE 会掉很多次）。(2) **`Physics.SphereCast` 从自己体内出发会命中自己** → NPC 一步不动且日志无提示；改成 `SphereCastAll` + 过滤自身碰撞体。(3) **攻击距离是「中心距」**，第一版妖魔填 2.2 而它被物理挡在 2.31 米 → `可出手()` 永远 false → 永远停在「接近」，看着像 AI 坏了。改为妖魔 3.2 / 人类 3.4。
- [2026-09-21 13:20:00] 【⚠️ 阻塞 · 未解决】**NPC 现在打不中玩家**，而且**不是 AI 的问题**：玩家 闪避=0.08/忽视闪避=0.05，妖魔 闪避=0.06/**忽视闪避=0**；`闪避率 = (受击方闪避 − 攻击方忽视闪避) ÷ 受击方闪避` → 妖魔打玩家 = (0.08−0)/0.08 = **1 = 100% 被闪避**，实测 30 次 0 命中。**只要攻击方忽视闪避为 0 而受击方闪避>0 就永远打不中** —— 与 §11.3 的「免暴率/免会心率公式反转」同类（拿受击方属性做分母 → 任何正数防御属性都变成完全免疫）。**这是数值设计决定，没有擅自改**，已给用户三个选项：①改公式为直接相减（推荐，和暴击修法一致）②给 NPC 配忽视闪避（治标）③临时用新加的 `NpcAttackConfig.必定命中` 开关。**开必定命中后整条链路实测通过**：妖魔接近(9m)→攻击(3.2m)→Attack3 命中 188×2 → 玩家血 420→86.6 ✔。

- [2026-09-21 14:10:00] 【用户报 5 个 bug · 全部处理】(1) **动画控制器是空壳**：`Assets/Animations/NPC/*.controller`（101 个）里**所有 state.motion 都是 null**，`BaiLuJing.controller` 引用 **0 个 GUID**；真片段在 `resources/NPC/<类>/<名字>_Animation/<名字>@<动作>.FBX` 里（FBX 内嵌子资产），`Gou` 更是整个目录没有动画文件。控制器结构本身是对的（`Action:Int + Speed:Float` + AnyState→每个状态）。新增 `Assets/Editor/Tools/NpcAssetFixer.cs`：菜单 **修仙/NPC 资产/检查动画控制器（只读）** 与 **修复动画控制器**，按名字接片段 + 修循环（Idle/Specialidle/Walk/Run/Fight 循环，攻击/死亡/受击一次性）。**坑：FBX 内嵌片段不能改 `AnimationClipSettings`（只读），必须改 `ModelImporter.clipAnimations[].loopTime` + `SaveAndReimport()`。** (2) **陷进地板**：`标定贴地()` 原来用**碰撞体**包围盒底部对齐 → 胶囊底在 pivot 之上时会把模型按进地里。改成用 `NpcBodyBounds.取最低点`（身躯渲染体，排除武器）。(3) **武器撑大碰撞体**：白鹿精胶囊 `r=1.3 h=2.6`（半径=半高=一个直径 3.4 米的球）→ 一靠近就把玩家挤开、永远够不到攻击距离。根因是按"含武器整体包围盒"撑的（武器渲染体沿 Z 3.38 米，身躯只有 1.96×1.63）。新增 `Assets/Scripts/Npc/NpcBodyBounds.cs`（按体积排掉细长条，**门槛 0.5 是量出来的**：身躯 9.9 / 武器 3.8 = 0.38，用 0.35 排不掉）+ 菜单 **修仙/NPC 资产/修复碰撞体**。实测白鹿精 r 1.3→**0.8**（含武器 1.19）✔。只读检查：**261 个 prefab 里 209 个需要重算**（破坏性操作，带确认弹窗）。
- [2026-09-21 14:10:00] 【用户报 5 个 bug · 续】(4) **`类型` 列里 `中立` 是我擅自批量填的**——用户已对照检查并把大部分改回 `人类`，**以用户改过的 CSV 为准**。教训：批量填数据列前先问，别替策划做分类决定。⚠️ 遗留：**没有攻击动画的「人类」会一直追着玩家但打不出来**（`可用出手方式()` 空 → `可出手()` 恒 false），应判定为"不参战"停待机。(5) **闪避公式已按用户要求重做**：老公式 `(受击闪避 − 攻击方忽视) ÷ 受击闪避` 拿受击方属性做分母 → 攻击方忽视闪避为 0 时闪避率恒为 **1（永远打不中）**。新公式 `闪避率 = 闪避 ÷ (闪避 + 忽视闪避 + 闪避基准)`，`CombatCalculator.闪避基准`（默认 1，表里填点数就改 100）与 `闪避上限`（默认 0.75）可调。实测：玩家 0.08/忽视 0 → 旧 100% / 新 **7.4%**；用户例子 闪避 100/忽视 350（基准 100）→ 旧 0% / 新 **18.2%**；闪避 1000/忽视 0 → 75%（被上限压住）；闪避 0 → 0%（必中，保持旧行为）。**永远到不了 0 也到不了 1**，单调无突变。

- [2026-09-21 14:45:00] 【#2 真正根因 · 我第一版判断错了】**用户一句「狗也一样陷地」把我拉回正轨**——狗的体型和命名跟白骨娘娘毫无共同点，所以原因一定更底层。真正原因是 Animator 状态的 **`writeDefaultValues`**：它为 true 时，进入状态会把**所有没被动画驱动的属性重置成默认值**；而这些状态 **`motion` 全是 null**（见 #1），于是**所有骨骼被重置成默认姿势 → 蒙皮网格塌陷成一团 → 姿势诡异 + 整个网格沉到地下**。和体型/命名/贴地算法都无关，所以所有 NPC 都会中招。**修复（已执行）**：`NpcAssetFixer.处理动画` 现在①把 `<名字>@<状态>.FBX` 接回每个状态（**385 个接上**，另有 315 个本来就接好）②对仍然没有片段的状态关掉 writeDefaultValues（保险，1 个）。结果：101 个控制器、1 个片段缺失（`XiaoYaoZi@Specialidle`）、2 个控制器无动画资源（`Long_texiao`/`ZhanQi`）。**另外：循环设置其实一直是对的**（Idle/Run/Walk/Fight 的 loopTime 本来就是 True，攻击/死亡/受击本来就是 False）——用户感觉"没设 loop"，真因是**片段根本没接上**。
- [2026-09-21 14:45:00] 【#3 已修 · 我第一版又错了】按「体积最大的渲染体」挑身躯是**错的**：白骨娘娘体积最大的是**飘带**（`BaiGuNiangNiang_02` 体积 8.63 > 身体 4.76），而飘带最低点在 **0.945 米高** → 拿它贴地会把 NPC 往下压 0.945 米。**改成按命名挑（用户给的规律）**：①排掉名字带 `wuqi`/`weapon`/`武器` 的 ②剩下的优先取**以 `_01` 结尾且数字后缀最少**的（`BaiGuNiangNiang_01`、`BLJ_01` 而不是 `BLJ_01_01`、`BaiXiongJing_01_01` 而不是 `_01_02`、`Changdaonv_01` 而不是 `ChangDaoNv_02`）③都不匹配（`gou`/`muji` 单网格）→ 取体积最大的 ④**安全阀**：第②步挑出来的高度若不到「所有非武器渲染体并集」的 60%，说明挑中小挂件 → 改用并集（实测 `BaiLuJing_02` 只挑出 **0.69 米**，并集 2.35 米 → 走安全阀修复）。包围盒用 `sharedMesh.bounds`（绑定姿势本地）算世界值，不用 `Renderer.bounds`（当前姿势的动态值，没动画时不可靠）。**已执行两轮，261 个 prefab 改了 272 个**：BaiLuJing_01 r1.3→**0.66**、BaiLuJing_02 h0.69→**2.35**、BaiXiongJing_01 1.62→**1.25**、BaiGuNiangNiang_01 1.19→**0.68**。注意胶囊参数是**本地空间**的，会被根缩放再乘一次（狗根缩放 10 → 本地 r=0.05 = 世界 0.5 米，是对的）。

- [2026-09-21 15:00:00] 【✅ NPC 战斗全部打通】用户确认「姿势基本恢复正常」，动画修复成功。**端到端验收**：动画修好 + 碰撞体修好 + 闪避公式换掉之后实测——妖魔打玩家的闪避率 **0.074**（旧公式 1.0 永远打不中）；妖魔 接近(9m)→攻击(3.2m)→**出手 3 次命中 3 次**，累计伤害 **506.3**，玩家血 **420→18**；动画 `Run`→`Attack2`→`Idle` **真的在切换播放了**（接片段之前根本不动）。**NPC 战斗正式可用**。配置表也已同步（用户跑过导入器：人类 43 / 妖魔 54 / 兽类 11 / 中立 3）。用户确认**人类全部有攻击动画**，所以「没攻击动画的人类会追着打不出来」这个顾虑不存在，不用补。`NpcAttackConfig.必定命中` 保留作调试手段。

- [2026-09-21 15:30:00] 【第一个物种专属 AI · 母鸡】新增 `Assets/Scripts/Npc/Ai/NpcAiMuJi.cs`，并给基类加了**物种专属脚本的反射自动装配**：约定类名 `NpcAi<模型名>`（`NpcAiBase.物种脚本()` 扫程序集自动登记，`Animal/Demon/Human/Combatant` 这些类型默认层不参与），装配优先级 = **物种专属 → 类型默认**。匹配写法：`MuJi_01` / `MuJi_01(Clone)`（前缀）、`beast_muji`（下划线包含，对应定义 id）。**所以以后加一个 NPC 的专属 AI 只要新建 `NpcAi<模型名>.cs`，不用改注册表也不用改 prefab。** 母鸡专属行为：①**啄食**——待机时在 `Idle` 和 `Specialidle` 之间按时长交替（它的控制器只有 Idle/Walk/Specialidle）②**下蛋**（默认关，配 `蛋预制体路径` 或监听 `下蛋` 事件）③死亡粒子消散（`Effect_33_DeathRevolution`，缩放 0.08 —— 特效包里的效果很大，小鸡要压小，先给个值看着调）。另外给基类加了 `死亡消散特效缩放` / `死亡消散特效存活`，并把消散特效生成在**身躯中心**而不是脚下。**实测全过**：物种匹配（`MuJi_01`→`NpcAiMuJi`，`Gou_01` 不误匹配）、装配成 `NpcAiMuJi`、啄食（Specialidle 在 0/4.9/8.6s 反复出现）、跟随（好感 90 → 到 3.5 米停）、躲避（好感 3 → 3.56→5.01）、死亡（特效生成 + 母鸡销毁 + **残留特效 0**）。
- [2026-09-21 15:30:00] 【新增文档】`AI开发注意事项.md`（工作区根目录）——**做 NPC AI 的入口文档**：现状 / 从哪入手（步骤清单）/ 关键约定（类名规则、类型阈值速查、受击 −20 好感）/ 必须先知道的坑（指向 开发注意事项.md 的 §0.5 §8.2 §8.3 §15.5-15.7 §16.1-16.5）/ 验收清单 / 还没做的（其余 108 个 NPC 还是类型默认 AI、涨好感没途径、灵阵法宝没接、收服组队空着、人类对话没接、子弹特效待导入、群体行为没做）/ 文件地图。

- [2026-09-21 16:10:00] 【改需求 · 通用消散特效】**用户要求取消资产包里的消散特效**（`Effect_33_DeathRevolution`），改成**一个通用的**：粒子炸开、然后随机逐渐消失。新增 `Assets/Scripts/Npc/Ai/NpcDissolveEffect.cs` —— **纯代码生成 ParticleSystem，不依赖任何美术资产**：Sphere 形状 burst 一批粒子，`startLifetime` 给随机区间（**这就是「随机逐渐消失」的来源**：每颗粒子寿命不同 → 在不同时刻各自缩小淡出），配 `colorOverLifetime` alpha 渐入渐出 + `sizeOverLifetime` 缩小 + 随机 z 轴自转；贴图和材质也是运行时生成的（柔边圆 + `Sprites/Default`，按顺序找可用 Shader）。**按体型自动缩放**（用 `NpcBodyBounds` 的身躯半径定粒子大小/初速/散布），粒子数 0 = 按半径自动算（24~160）。`NpcAiBase.播放消散特效()` 改为：**路径留空 = 用通用消散**（默认），填了才用资产 prefab，找不到资产会警告并回落通用。母鸡改成通用（暖色、40 颗，像散了一地羽毛）。实测：t=0.12s **40 颗**一起炸开 → t=0.47s 已向外散开 → t=1.02s **只剩 9 颗**（随机寿命依次消失）→ t=2.6s **残留 0** ✔。
- [2026-09-21 15:50:00] 【bug · 母鸡挨打飞天 · 已修】**现象**：母鸡被打一下 Y 从 0.02 一路涨到 **372 米**，而且**匀速爬升 ≈41 m/s** —— 匀速=每帧棘轮式上升。**根因**：`贴地()` 的向下射线 `从 position+3 往下 12 米` **先命中了【自己的胶囊顶】**，于是被摆到自己顶上；下一帧起点更高、又命中自己 → 每帧涨「胶囊中心高+半径」，正好是那个匀速值。**我修 `前方被挡()` 时排过自己，但漏了 `贴地()` 和 `视线通畅()`。** 现在三处统一走新的 `取首个外部命中()`（`RaycastAll` + 按距离排序 + 跳过自身碰撞体），不会再漏。**为什么只有母鸡中招**：狗的胶囊是薄片、射线往下第一个命中地面而不是自己；母鸡胶囊中心偏高就先命中自己 —— **这个 bug 跟体型强相关，只看一个案例会误判成"已修好"**。**顺带修**：`Animator.applyRootMotion` 实测 **261 个 prefab 里 127 个开着**（MuJi/Gou 开着，BaiLuJing 关着）；AI 用 transform 驱动移动、Root Motion 会和它打架（循环片段抵消、**一次性片段位移累积**）→ 运行时 `关掉RootMotion()` 兜底 + 新增菜单 **修仙/NPC 资产/修复 Root Motion** 批量改。⚠️ `LeiGong_02.prefab` **本身有 Missing Script**，`SaveAsPrefabAsset` 报错保存不了，得手动清理（运行时兜底能兜住）。**修复后实测**：鸡 Y `0~0.02`、狗最高 `0.02`、妖最高 `0.002` ✔。

- [2026-09-21 16:40:00] 【兽类一次做完 + 剔除仙鹤】**用户说动物都差不多，一次性做完全部**。关键决定：**共性行为提升到类型层** —— 把「待机花样（啄食/刨地）」从母鸡搬到 `NpcAiAnimal`，于是 10 只动物一次性全都有，母鸡瘦身到只剩「下蛋」。然后脚本批量生成 10 个物种文件（每个 ~40 行，只重写 `取默认参数()` 调性格数值）：`NpcAiGou`(亲人 亲近35) / `HouZi`(好动 花样1.6s) / `LingHe`(高冷 索敌12) / `MaoLv`(慢 奔跑0.7) / `MuJi` / `ShuiLan` / `SongShu`(胆小 回避10) / `TuZi`(最胆小 回避12) / `XiaoJi` / `XiaoLu`(警觉 索敌12)。**实测 10/10 全部装到自己的专属类、啄食 Specialidle 都生效**。**坑**：脚本生成 C# 时浮点字面量必须带 `f` 后缀（`2.5` 会被当 double 报 CS0664），第一版漏了 52 个错误。**仙鹤（`beast_xianhe`）被用户判定为非目标 NPC，已从 `NPC表.csv` 剔除**（111→110 行，兽类 11→10），同时删掉生成资产 `beast_xianhe.asset` 和 `NpcAiXianHe.cs` —— **注意导入器不会自动清理「表里已删行」的旧资产，必须手动删，否则留孤儿**。模型/动画资产保留。顺带给待机兜底链加了 `Run` 作为最后手段（原本是为只有 Run 的仙鹤，留着当通用兜底）。
- [2026-09-21 17:00:00] 【bug · 小鹿模型大小 · 已修】用户说"一部分 animal 模型大小可能有问题，比如小鹿"。量出来一看果然是：**其他动物根缩放都是 6~14，只有 XiaoLu_01 是 1** → 世界高度 **0.133 米**（比小鸡 0.265 还小一半，玩家是 2.62 米）。网格本身大小跟别人差不多，是**缩放忘了设**。修法：根缩放 1 → **8.6**（目标高 1.15 米，小鹿体型），并用 `NpcBodyBounds.算胶囊` **重算碰撞体**（胶囊是本地空间参数，缩放变了必须跟着重算，否则世界半径不对）。修后 1.15 米 ✔、世界半径 0.43 米 ✔。**这类问题不该靠肉眼发现**，所以在 `NpcAssetFixer` 加了菜单 **修仙 / NPC 资产 / 检查模型尺寸**：按类型分组取**中位数**，高度不到 1/3 或超过 3 倍就报出来（只读）。**全量体检结果**：兽类 0.27~1.82m / 妖魔 1.16~3.81m / 人类 0.92~3.44m / 坐骑 1.33~3.43m / 中立 `npc_chuansongdian_01` 7.18m（传送点，是建筑，正常）—— **只有小鸡 0.265m 被报出来，而那是合理的小**（工具只找离群值，天生就小的会误报，要人判断）。所以**全项目只有小鹿一个是真的错**。
- [2026-09-21 17:30:00] 【用户导入子弹特效包 · 已分类 + 已接线】**用户导入 Assets/resources/QFX/ProjectilesFX**（105 个特效 prefab / 4 个 PFX_*.shader / 3 个演示脚本 / 15 个主题）。**包的架构是"三件套 × 15 主题"**：Flash 枪口闪光 + Projectile 弹道 + Impact 命中爆炸；主题 = Ares/Comet/Curse/Cyber/DarkMagic/Demon/Kawaii/Lightning/Magic/Nature/Priest/StarlightFairy/Succubus/Vampire/Winter。目录：`Flashes`15 / `Impacts`15 / `Projectiles_Particles`15 / `Projectiles_Particles_Complete`(Projectiles+Weapons 各15) / `Projectiles_Objects_Complete`(Projectiles+Weapons 各15)。**关键**：①`**/Weapons/` 里挂的是**演示发射器**（朝鼠标、左键连发），**NPC 不能用**，要用同级的 `Projectiles/`；②`URP_HDRP_Gamma/*.unitypackage` 是 URP/HDRP/Gamma 补丁，**本项目 Built-in，不要导入**；③基础 shader 是 `PFX_*.shader`（Built-in）✓ 兼容。**实体弹丸自带 `PFX_ProjectileObject` 脚本**（字段 Speed/ImpactFX/ImpactOffset/FXToDeatch，**"飞行中一碰就爆"**）→ 用反射塞 Speed+ImpactFX 交给它飞，不引包也能编译。新增 `NpcAttackConfig.闪光特效路径 / 命中特效路径 / 闪光特效存活` + `NpcAiBase.生成子弹` 重写（生成闪光→弹道→按飞行时间结算）+ `NpcProjectile.设置飞行()`。**坑**：弹道**必须生成在自己碰撞体之外**，否则出生当帧就撞到自己销毁（实测 0.5 秒内没了）→ 加 `出膛余量()`（碰撞体半径+0.35）。**验证状态**：资源加载 ✔、脚本识别 ✔、伤害结算 ✔（420→13）；但飞行中弹道/闪光**还没采样到**，待下次补验。新增 `Assets/Editor/Tools/NpcEffectInventory.cs`（菜单 **修仙/资源整理/生成特效资源清单**）→ 输出工作区根的 `特效资源清单.md`（按「飞行道具/单次爆发/循环」给建议 + 直接可填的 Resources 路径）。**⚠️ 导入时 Unity 把活动场景换成了包的 Demo 场景**（`ProjectileFX_Demo.unity`，且 dirty）——跑任何存场景的操作都会存到 Demo 上；已帮用户切回 `3C_Testbed.scene`。
- [2026-09-21 17:30:00] 【待办 · 明天】①**先补验 QFX 施法流程**（只给一条出手方式配路径、加密采样看飞行中的弹道与闪光）；②再做**妖魔(54)/人类(43)的 AI**，建议先挑 2-3 个代表性的（白鹿精/白熊精/山贼）看有没有能提升到类型层的共性，再批量生成；③人类还要接对话/剧情；④收服组队（`NpcAiBase.收服()` 钩子已留）；⑤涨好感没有途径（兽类"好感>60跟随"实际触发不了）。
- [2026-09-21 17:45:00] 【清理不可用资源】按用户要求删掉 QFX 包里**用不上**的东西，释放 **4.3 MB**、`VFX_Prefabs` **105 → 75** 个 prefab：①`URP_HDRP_Gamma/`（3 个 unitypackage，**4.2 MB**，URP/HDRP/Gamma 补丁，本项目 Built-in 永远用不上、留着还有误导入把 shader 搞坏的风险）②`Projectiles_Objects_Complete/Weapons/`（15 个，实测是**空壳 + 演示脚本**：0 粒子、1 个子物件）③`Projectiles_Particles_Complete/Weapons/`（15 个，粒子效果 + 演示脚本，和 `Projectiles/` 重复）。**删的理由**：那俩脚本（`PFX_ProjectileObjectWeapon`/`PFX_ProjectileParticlesWeapon`）是**鼠标左键 + 朝鼠标射线**的 Demo 发射器，挂 NPC 身上会跟着玩家鼠标转。**保留** `Demo/`（演示场景 = 看 15 个主题的画廊，挑主题有用）和 `Post Processing Profiles/`；⚠️ 但 `Demo/Effects.prefab` 原来引用过被删的 Weapon prefab，现在有几个**断链条目**（Demo 不进构建，不影响游戏）。
- [2026-09-21 18:00:00] 【纠错 + 修复 · 删 Weapons 其实弄坏了 demo】用户问"这个 demo 是啥，能删吗"。查下来：**Demo/Effects.prefab 的 Effects 数组装的就是我删掉的那 15 个 *_Projectile_Weapon** —— 我先前判断"没引用"是**错的**，因为 **Unity 删掉 prefab 后，实例会作为【脱离的副本】留在原地：名字还在、prefab 链接没了**，所以 LoadAssetAtPath 拿到的是非空对象，看着像没坏。**已修复**：把 15 个引用重新接到幸存的 Projectiles_Particles_Complete/Projectiles/VFX_<主题>_Projectile，**15/15 全接上**。**修的时候又踩一个坑**：第一版**先把旧物件 DestroyImmediate 再读 o.name** → 读出来全空（销毁后访问属性）、一个都没匹配上、新的[0] 抛 IndexOutOfRange；**必须先记名字再销毁**（好在崩在 SaveAsPrefabAsset 之前，prefab 没被写坏）。**demo 是什么**：ProjectileFX_Demo.unity（注意在 ProjectilesFX/ 根下、不在 Demo/ 里）+ Demo/Effects.prefab（画廊容器，15 个主题各一份）+ DemoEffects.cs（**X/Z 切上一个下一个**，一次只激活一个）+ SimpleCameraController（WASD 飞相机）。**不进构建**（Build Settings 只有 StartScene/3C_Testbed/Village）→ 删了不影响游戏，但**是挑主题最快的预览方式**，建议留着。**预览三法**：A 打开 demo 场景→Play→X/Z 切；B 拖单个 prefab 进自己场景按 F 聚焦（实体弹丸一碰就爆，静止看不全）；C 查 特效资源清单.md 的 75 个。
- [2026-09-21 18:20:00] 【学习 QFX 飞弹机制 + 写文档】用户要求「学它怎么造飞弹，整理成文档，下次照着做」。**拆出的配方**：飞弹 = 根物件上挂 PFX_ProjectileObject(飞行脚本) + **Rigidbody**(`isKinematic=false`/`useGravity=false`/`drag=0`) + **SphereCollider**(`radius≈0.1`/`isTrigger=false`) + TrailRenderer×N + 主体粒子 + `Sparks_Along`(循环)。脚本本体极简：`FixedUpdate` 里 `transform.position += forward * Speed * dt` 飞；`OnCollisionEnter` 里生成 `ImpactFX`（沿法线外推 `ImpactOffset`）、把 `FXToDeatch`(=Sparks_Along) **脱离父级留在命中点**、销毁自己。**4 条硬要求**：①必须有刚体+非 trigger 碰撞体，否则不触发碰撞、穿墙 ②`useGravity=false` 否则往下掉 ③`isKinematic=false`（唯一运动来源，别再加第二个推它的东西）④方向靠 `transform.forward`，生成时必须 LookRotation。**三件套真相**：`VFX_X_Projectile`（粒子完整版）本身就是三合一，它的 `Glow` 分支 ≡ Flash、`Impact` 分支 ≡ Impact；实体弹丸 `*_Projectile_Object` **只含飞行部分**，闪光/命中要另外指定（这就是那两个字段的来由）。**两条路线**：A 借 QFX 实体弹丸（只填 3 个路径，命中靠物理碰撞、会被墙挡）；B 自己的 `NpcProjectile`（不依赖刚体碰撞体、按时间必达、会穿墙）—— 代码里**自动判断**：弹道自带 `PFX_ProjectileObject` 就走 A，否则走 B。**文档**：`飞弹制作文档.md`（工作区根）—— 含解剖、两条路线、给 NPC 做（改 `取默认参数()` 的 3 行配置）、**给玩家做**（现状：玩家普攻近战、技能是立即 AoE 无飞行；建议抽 `ProjectileLauncher` 让 NPC 和玩家共用，别复制）、8 条踩过的坑、验证清单、参数速查。**顺带修**：`Demo/Effects.prefab` 的断链（见上一条），并发现演示场景里 `VFX Game Object Projectiles/Effects_Projectiles_Objects` 那 15 个 `*_Object_Weapon` 是**场景里的脱离副本**（删 prefab 后留下的），属外观问题、不影响游戏。
- [2026-09-21 18:50:00] 【第一个妖魔 AI 落地 · 白鹿精飞弹】**用户给了 5 要素规格**（出生点/飞行路径/命中效果/能否被阻挡/消灭点）。新增 NpcAiBaiLuJing（**一个类覆盖 01/02/03 三个变体**，靠 定义.id 区分 —— 这套 _01/_02/_03 变体模式值得复用）：01/02 只有 Attack1 飞弹（冷却 3s、特殊普通攻击、左手末端 Bip01 L Finger0Nub、动画 50% 发射）；03 多一条 Attack2 主动神通（冷却 8s、弹体 1.6 倍、右手近似武器末端）。**实测**：出手 3 次全命中、每次 108.7 伤害、弹道逐帧采样 293 帧、单发直线飞 **7.4 米**全程、玩家 420→179.5、残留弹道 0。**代码改动**：NpcAttackConfig 加 挂点/挂点偏移/最大飞行距离/拦截层/弹体缩放/冷却/命中半径；NpcAiBase 加**骨骼挂点**（找挂点 按名字搜）、**每招独立冷却**（各招冷却 字典 + 该招就绪() 过滤候选，可出手() 不再看全局冷却）；NpcProjectile 加**直线不追踪模式**（设置直线飞行）+ 拦截层检测 + 消灭点淡化消散（复用 NpcDissolveEffect）。
- [2026-09-21 18:50:00] 【飞弹的两个关键判断】(1) **「无法被场景阻挡」和包里带 Rigidbody 的实体弹丸直接冲突** —— 那个靠 OnCollisionEnter 命中、**会被墙挡**；所以白鹿精用的是 **Projectiles_Particles/VFX_X_Projectile_Only（纯弹道）+ 自家 NpcProjectile（零物理）**。这也解释了包里 Only 后缀的用途：_Only = 只有弹道部分（配自己的飞行/命中），_Complete/Projectiles/ 里的三合一是原地一次播完的。(2) **「不追踪」不能实现成「飞到一个点」**：第一版那样写，**玩家躲开了还是会打中旧位置**；正确做法是「每帧沿固定方向前进 + 判断目标还在不在弹道上（命中半径），不在就继续飞、超距消散」。**顺带确认**：有效索敌范围 优先用**表里的 索敌范围**，改运行时字段 NpcAiBase.索敌范围 是没用的（改测试参数时要改 定义）。**未实测**：消灭点的 22 米消散（玩家没躲没触发）、拦截层（层为 0，等灵阵/法宝）。**武器末端**只能拿右手+偏移近似，模型里没有武器骨骼（武器是蒙皮网格挂根上）。
- [2026-09-21 19:20:00] 【用户发现 · 白鹿精动画完全不播 · 已修 · 系统性 bug】**根因：Animator.avatar 为空**，而片段是**人形片段**（isHumanMotion=True、150+ 肌肉曲线）—— 人形片段不直接驱动骨骼，靠 **Avatar** 把肌肉空间重定向到骨架；**Avatar 空 = 一根骨头都不动**。**为什么我漏了一整轮**：NpcAnimator.CurrentAction 在变、状态机在切、Console 干净 —— **但蒙皮完全不动**。我之前只验"状态在切"就以为动画好了。**教训：验动画必须验骨骼** —— nimator.GetBoneTransform(HumanBodyBones.LeftHand) 隔 0.12s 采样 6 次，位移 > 0.01m 才算在播（GetBoneTransform 只在人形 Avatar 下有效，顺便验证 Avatar 对不对）。**新增 Assets/Editor/Tools/NpcAvatarFixer.cs**（菜单 **修仙/NPC 资产/检查·修复 Animator 的 Avatar**，Avatar 从模型 FBX 反查）。**261 个 prefab 里 141 个 avatar 为空 → 补上 80 个**；**10 个 FBX 本来就没有 Avatar**（RongYanGuai / ShuangTouShe / Chibang / thxr，应该是 Generic 骨架）→ 待单独处理。**实测修复后**：白鹿精_01 vatar=BaiLuJing_01Avatar(Human)、GetBoneTransform(LeftHand)=Bip01 L Hand、**0.6 秒位移 1.609 米** ✅ 骨骼真的在动。**修工具时又踩一个坑**：LoadPrefabContents 出来的物件**不是资产**，AssetDatabase.GetAssetPath 返回空 → Path.GetDirectoryName("") 抛 Invalid path；路径要自己传进去。**噪音提醒**：prefab 上同时有 legacy Animation 组件，会刷 must be marked as Legacy 警告 —— **和动画不播无关**，别去查它。
- [2026-09-21 20:10:00] 【用户报 6 个问题 · 修了 5 个】①**白鹿精攻击距离太小**（丢飞弹的却只有 3.4 米近战距离）→ NpcAiBaiLuJing 攻击距离 **3.4 → 12**、索敌 15→18、脱战 24→26。②**动画扭曲** → 见下一条，未修（要设计决策）。③**Fight（战斗待机）从不播** → NpcAiBase 加 protected virtual string 取待机动作名()（默认 Idle），NpcAiCombatant 覆盖成 视玩家为敌 ? "Fight" : "Idle"；播待机动画()/停止移动动画() 都走它。④**Wound 不立刻播** → 两个原因：(a) 收到伤害通知 里有 受击动画冷却 0.45s 的门槛，**改成无条件立刻（重）播**；(b) ★**真 bug**：进入状态(受击) 内部会调 停止移动动画()，**把刚播的 Wound 覆盖成 Idle/Fight** —— 改成"**只有真的进待机才切待机动画**"，并把 进入状态 提到 受击表现() 之前。另加 待校正受击时长：进受击后下一帧用 **Wound 片段真实长度**覆盖硬直，保证"播完才允许动"。⑤**生成的敌对 NPC 背对着就开火** → 可出手() 加 朝向玩家够正(40度)：**没转过来之前不出手**。⑥**动画切换生硬** → 101 个控制器的 AnyState 过渡时长 **0.15 → 0.3 秒**。**实测**：交战中动作=Fight ✓、夹角=0 度 ✓、挨打**第 1 帧**就是 Wound/受击 ✓、Wound 期间只出现 受击/Wound（没偷偷接近/攻击）✓、再次受击仍 Wound/受击（重播）✓。
- [2026-09-21 20:10:00] 【待决策 · 动画扭曲（问题②）】**现象**：白鹿精动画播了但"扭曲、只大概看出是那个动作"。**排查结论**：vatar = BaiLuJing_01Avatar (isHuman=True) **是对的**（不是挂错 Avatar）。真正原因很可能是**人形重定向的固有失真**：骨架里有 **Bip01 L/R HorseLink** —— 在 Calf 和 Foot 之间**多一根关节**（反关节鹿腿，不是标准人腿链）。**人形 Avatar 假设腿链是 Thigh→Calf→Foot→Toe**，多一根 HorseLink 就只能近似映射 → 腿部/姿势扭曲。**两个选项**：(a) **改用 Generic 骨架**（把模型和片段的 nimationType 从 Human 改 Generic）→ 片段**原样播放、零重定向** → 不扭曲；代价是人形 API（GetBoneTransform）不能用、验证法要换成直接读某个 Transform；(b) 让美术确认 Avatar 的骨骼映射（尤其 HorseLink 映射到哪）。**建议**：妖魔里很多不是人形比例，可能整类都该考虑 Generic —— 但这是设计决策 + 要看画面判断，等用户定。
- [2026-09-21 21:00:00] 【用户报 4 条 · 修了 2 条半】①**全部改 Generic**——用户批准了，**但我没执行**（见下）。②**"所有人类"**——已确认：我的改动在 NpcAiBase（全部 NPC）+ NpcAiCombatant（**所有人类 + 妖魔**），**本来就覆盖所有人类**，不是只管已敌对的。用户原话："不是好感度为负的人类，而是所有的人类（因为每个人类都可以通过攻击让好感度变负）"。③**转向慢**——半修，见下。④**飞弹没轨迹 · 已修 · 已验证** ✅
- [2026-09-21 21:00:00] 【飞弹没轨迹 · 根因 + 修法 · 已验】**根因**：QFX 弹道 prefab 里**所有粒子 playOnAwake = False**、ateOverTime = 0；主系统和 Trail 只有 1 个 burst，而 Glow_Along / Sparks_Along / Sparks_Stretched_Along / Smoke_Along 这些**沿路粒子 burst = 0、rate = 0** —— **它们是靠包里的武器脚本反复调 Emit(1) 才出的**。我 Instantiate 之后没人调 → 飞过去什么都没有。**修法**（NpcProjectile）：①Awake 里 起播粒子() —— 全部 playOnAwake=true + Play(true)；把"rateMax<=0 且 burstCount==0"的归为**沿路粒子** ②补发沿路粒子(本帧距离) —— **按飞过的距离** Emit(1)（每米补发粒子 默认 8，跟速度无关所以快慢都均匀），直线模式和到达模式都调。**实测** ✅：Trail / Sparks_Along / Glow_Along / Sparks_Stretched_Along / Smoke_Along 全部出粒子，单帧最多 **59 颗**。
- [2026-09-21 21:00:00] 【转向慢 · 半修 · **未定位** ⚠️】**已做**：①NpcAiCombatant 加 Update() 覆盖，**每帧**朝向玩家（不再只在"进入攻击"那一下 + 不再只在待机状态；受击/死亡/未发现玩家时不转）②白鹿精 转向速度 540 → 900。**问题**：实测玩家以 4°/帧绕圈时**仍然跟不上**（最大夹角 171.7°、平均 39.8°）。**已排除**：转向() 实现干净（Quaternion.RotateTowards(rotation, 目标, 转向速度 * Time.deltaTime) 无隐藏提前返回，NpcAiBase.cs:804-815）；NpcAiCombatant.Update 确实在链上（NpcAiDemon : NpcAiCombatant 有 	ake待机动作名() 生效为证，且 grep 确认全链只有它覆盖 Update 且都调了 base）；攻击距离=12 生效 → 取默认参数() 确实执行 → 转向速度=900 应该也生效。**下一步要查**：运行时直接打印 转向速度 的真实值 + 状态（注意**基类的 private 字段 GetField 在派生类上找不到**，要用 BaseType 或改 protected）+ 在 Update 里打点确认每帧真的调到了 朝向玩家()。**怀疑**：900°/s 下 60fps 每帧能转 15°，追 4°/帧的目标绰绰有余 —— 实测却差 171°，说明**大部分帧根本没转**，大概率是 Update 里那个 if (!已交战 && !玩家在索敌范围()) return; 或状态判断把它挡掉了。
- [2026-09-21 21:00:00] 【⚠️ 待办 · Generic 骨架改造（用户已批准，未执行）】**目标**：把 NPC 的**模型 FBX + 动画 FBX** 的 nimationType 从 Human 改成 Generic，让片段**原样播放、零重定向**，解决白鹿精"动画扭曲"（根因是骨架有 Bip01 L/R HorseLink——Calf 和 Foot 之间多一根关节的反关节鹿腿，人形 Avatar 只能近似映射）。**为什么没做**：①风险高——**Generic 靠曲线路径名匹配**，如果模型和片段的层级路径不一致，**所有动画会彻底不动**（比现在更糟）②量大（261 prefab + 数百 FBX）③**必须先拿一个做试验并肉眼确认**。**建议流程**：只对 BaiLuJing_01 的模型 + 它的 _Animation 目录片段改 Generic → 清掉 prefab 上 Animator 的 avatar → Play 里用「骨骼位移」法验证动没动（Generic 下 GetBoneTransform 不能用，改成直接读 Bip01 L Hand 这个 Transform 的位置变化）→ 看画面。**注意**：Generic 下 Animator.avatar 为空是**正常的**（所以之前"141 个 avatar 为空"里如果混着 Generic 的，我的 NpcAvatarFixer 可能给他们硬塞了 Human avatar —— 但那批片段的 isHumanMotion=True，塞 Human 是对的）。
- [2026-09-22 09:50:00] 【飞弹缩体积 + 命中特效做好】**用户要求**：飞弹体积小一点、命中特效稍微做好一点。**改法**：①白鹿精普攻 弹体缩放 1 → 0.65、神通 1.6 → 1.1（保持"神通更大"的关系）②NpcAttackConfig 加 命中特效存活 / 命中特效缩放；白鹿精普攻 1.25、神通 1.6 ③**命中特效放到弹道的真实命中点**——NpcProjectile 新增 命中点 / 飞行朝向 两个属性（到达/被拦截时记下），回调里读 ④**朝向改成"迎着飞来的方向"**（-弹.飞行朝向），爆炸朝施法者那侧张开。**顺带修掉一个真 bug**：生成装饰特效 里原来传 Vector3.up 当朝向，而 **Quaternion.LookRotation(up, up) 是退化调用**（前向与上方向平行）→ Unity 报错并返回 identity → **闪光/命中特效的朝向一直是随机的**；现在竖直方向会换 Vector3.forward 当 up 轴。**实测** ✅：弹道缩放 0.65、命中特效离玩家判定点 **0.89 → 0.14 米**（原来因为"进到命中半径 1 米就算命中"而悬在身前）、缩放 1.25、朝向 (0.09,0.14,0.99) 指向施法者。
- [2026-09-22 09:50:00] 【做了一件事：命中点往身上拉】弹道是在"进到 命中半径 内"就判定命中的，所以弹道位置离目标身上还差几十厘米，爆炸会悬在身前。改法：Vector3.Lerp(弹.命中点, 目标判定点, 0.85f) —— 往目标身上拉 85%，爆炸才像"打在人身上"。生成装饰特效 顺手改成**返回 GameObject**，方便调用方继续调缩放。
- [2026-09-22 09:50:00] 【用户指出：其实已经是第二天了】我之前在 CODELY.md 里**自己编了时间戳**（一直写 2026-09-21），实际当天是 **2026-09-22**。**以后时间戳一律用 Get-Date 取真实时间，不再自己写。**
- [2026-09-22 09:53] 【bug · 白鹿精"不动" · **主因已修**】用户报"03 不动，且召唤 03 会让 01/02 也不动"。**查明是两件事，不是互相影响**（AI 代码里**一个静态字段都没有**，不存在跨实例共享状态 —— 我专门 grep 过）：**①"停在攻击距离边界上不动"** —— NpcAiCombatant.决策 里"走到 <= 攻击距离 就进待机"，而 可出手() 又要求"<= 攻击距离"，于是它**停在边界上、浮点误差判定够不着 → 永远站着不动也不出手**。实测 03 从 13 米走到**正好 12 米**（=攻击距离）然后卡死。**修法**：接近时停在 攻击距离 * 0.85（留 15% 余量）。**②执行待机() 每帧写 已交战 = false** —— 出手完回待机就"忘了在战斗"，下次出手还要重走"首次发现"（带 1.5 秒 重新索敌冷却）→ **打一下愣半天**。**修法**：待机不再清 已交战；"在战斗"只能由 决策 里的"玩家已脱战"解除。**验证** ✅：修复后三个变体**都是敌对、都在索敌范围内、可出手=True、状态=攻击**，16 秒内**三个都出手了**（01/02 普攻命中、03 出了 Attack1 + Attack2 神通 1695 伤害），同屏最多 3 发弹道、弹道有粒子、**残留 0**。
- [2026-09-22 09:53] 【⚠️ 未解决 · 出手频率偏低】修完上面两条后，16 秒里每个白鹿精**只出手 1 次**，而冷却只有 3 秒（理论上应该约 4~5 次）。**没定位到根因**：我四次用反射去打点 可出手()/各选出手方式()/各招冷却/当前出手/攻击结束时间，**每次都因为"基类的 private 成员在派生类上用 GetField 找不到"（遍历类型链的版本又在 IDictionary 转换处 NRE）而失败**。**下一步别再用反射** —— 直接在 NpcAiBase.收招()、决策()、可出手() 里**临时加 Debug.Log**，看每次出手后冷却到底写成了什么值、决策 有没有再走到 进入攻击()。**教训**：反射探针在"基类 private 字段"上是不可靠的，验证要打点就**打在代码里**。
- [2026-09-22 09:53] 【教训 · 反射探针的坑】Type.GetField/GetMethod **在派生类上找不到基类的 private 成员**（private 不被继承）；必须遍历 BaseType 链，或者干脆别用反射。这个坑我在同一天里踩了**三次**（两次导致诊断脚本 NRE）。
- [2026-09-22 10:07] 【★★★ 大 bug 定案 · 玩家死亡标记永久不清 → 全场 NPC 集体卡死】**用户报**："状态机经常莫名挂掉，打着打着忽然不动，而且只要有一个不动了，再召唤的也不会动"。**根因根本不在 NPC 状态机，在玩家**：`PlayerVitals.回满()` **不清 `已死亡`**，而**全项目没有任何地方会自动清它**（只有手动 `复活()`）。于是玩家一旦被打到 0 血，`已死亡` **永久为真**；而所有敌对 NPC 的 `决策()` 第一行就是 `if (玩家生命.已死亡) { 进入状态(待机); return; }` → **所有 NPC（含新召唤的）永久待机** ✓ 和用户描述完全一致。**修法**：`回满()` 同时清 `已死亡`（"回满"语义上就该是活的）。**验证** ✅：阶段1 让玩家真被打死（18.3 秒、NPC 出手 **5 次** ≈ 每 3.7 秒一次，符合 3 秒冷却）→ 死后 6 秒 NPC 出手 **0**（不打尸体是对的）；阶段2 `回满()` 后 `已死亡=False` → **8 秒内 NPC 出手 2 次，恢复** ✅ **不需要重新召唤**。
- [2026-09-22 10:07] 【★ 这个 bug 一举解释了我之前三个"未解之谜"】(1) **"出手频率偏低，16 秒只出手 1 次"** —— 不是 bug！是因为我测试时用 `回满()` 补血，但**玩家其实早就在第一击（1695 伤害 vs 420 血）就被打死了**，`回满()` 补了血却没清死亡标记 → 全场 NPC 立刻停手。真实频率是每 ~3.7 秒一次 ✓ (2) **"转向跟不上，171°"** —— 也是假的：`NpcAiCombatant.Update` 第一行 `if (玩家生命.已死亡) return;`，**玩家"死了"时 NPC 连转都不转**；再加上我测试时给 NPC 设了背对玩家的初始朝向（那一下就是 180°）。**用稳态角度重测**（跳过起转）：最大 **19.6°**、平均 **8.4°** ✅ 跟得住。(3) 所以转向**没有独立 bug**，昨天那次"半修"其实已经修好了。
- [2026-09-22 10:07] 【教训 · 别再被"测试脚手架自己造成的假象"骗】这一轮我连续误判了两件事（出手频率、转向），**根因都在我的测试脚本里**：用 `回满()` 补血却没意识到它不清死亡标记、给 NPC 设了背对玩家的朝向却按"最大值"判断跟随。**以后测 AI 行为**：①补血一律用 `复活()`（或确认 `回满()` 会清标记）②判断"跟不跟得上"要看**稳态**角度，别用含起转阶段的最大值 ③**每次测试前先打印玩家的 `已死亡` 状态** —— 玩家一死，整个 AI 系统就是静默停摆，什么现象都会像"AI 坏了"。
- [2026-09-22 10:21] 【新功能 · 玩家死亡→消散→死亡界面→重生】用户给了设计稿（"杀伐道途，终是棋差一招 / 你死了 / 重生"），**项目里原本没有这个 UI**，是我实现的。**新增两个文件**：①`Assets/Scripts/UI/DeathScreenUI.cs` —— 死亡界面。**照 `PauseMenuUI` 的做法运行时自己搭 Canvas**（项目没导 TMP，统一 uGUI 传统 Text）；`sortingOrder = 4000`（盖过暂停菜单 3000 和 HUD）；幕布 `(0,0,0,0.62)` 半透明；标题 64 号带描边、副标题 30 号、按钮亮黄 `(1,0.92,0.10)` + 黑字。**中文字体用 `Assets/Fonts/SimHei.ttf`**（`HudBuilder` 里也是这个，`PauseMenuUI` 有"留空自动找"的先例）。按钮只发 `点了重生` 事件，不自己决定去哪复活。②`Assets/Scripts/Player/PlayerDeathSequence.cs` —— 整条流程：死亡 → **原地播粒子消散**（复用 `NpcDissolveEffect.播放`）→ 藏 `Player_Visual` 的渲染体 + 禁用 4 个操作组件（`PlayerController`/`BasicSword01`/`ActiveSkillCaster`/`YufengFlight`，自动收集）→ **等 2 秒**弹界面 → 点重生 → 传送到**修炼小屋前** + 满血 + 还回模型和操作 + **重置全场敌对 NPC 的战斗状态** + 2.5 秒无敌保护。**"修炼小屋"在场景里叫 `cultivation room`**（根物件，位置 (12.4,-0.02,-1.37)），重生偏移 `(0,0.1,-3)` = 门前 3 米。**配套**：`NpcAiCombatant.重置战斗状态()`（清 `已交战` + `上次交战时间 = -999`，让它们重生后立刻能重新索敌）、`PlayerVitals.无敌` 标志（`受到伤害` 里判）。**实测** ✅：消散特效 ×1、渲染体 0/1、4 个操作组件全禁用；**t=1s 界面未显示、t=2.4s 已显示**（2 秒延迟对）；重生后位置 (12.4,0.07,-4.37)、血 420、已死亡 False、模型恢复、界面关闭；无敌 2.5 秒后自动结束；怪物 `已交战` 被重置（之后仍待机是**对的** —— 玩家已传送到 19 米外，超出索敌 14 米）。**抓图确认界面**：中文正常、布局和设计稿一致、幕布半透明。
- [2026-09-22 10:21] 【踩坑 · 抓 ScreenSpaceOverlay 的 UI】死亡界面是 ScreenSpaceOverlay，**`Camera.Render()` 抓不到**（只抓 3D 相机），必须用 `ScreenCapture.CaptureScreenshot` + `WaitForEndOfFrame`，而且**偶尔会抓到空帧要重试**（这次重试机制没触发，一次就成）。抓图路径用正斜杠 + 全角冒号要写成 `"D:/project\uff1acultivation/..."`。
- [2026-09-22 10:24] 【bug · 重生按钮点不了 · 已修】**根因**：`UIBuildUtils.CreateImage` **默认把 `raycastTarget` 设成 false**（这是做 HUD 时定的"省性能"约定：除了底座都关掉）→ 按钮的 targetGraphic 收不到射线 → **点上去"一个 UI 都打不到"**，而且 **Console 一点报错都没有**。**修法**：`DeathScreenUI` 里把按钮图和幕布的 `raycastTarget` 显式设成 `true`（幕布也要开，用来挡住射线、防止点穿到背后的 HUD/世界）。**验证** ✅：按钮位置 `RaycastAll` 结果是 `重生按钮(depth=3)` 在最上、`幕布(depth=0)` 在下，最上面就是按钮；走完整点击链路 → `已死亡=False`、位置 (12.4,0.06,-4.37)、界面关闭。**我的验证漏洞**：上一轮我只调了 `流程.执行重生()`，**从没真点过按钮**，所以漏了这个 —— 凡是"点按钮"的功能，验证必须走 `RaycastAll` + `ExecuteEvents` 这条链路，不能直接调回调。
- [2026-09-22 10:24] 【★ 通用坑 · 这个项目里做 uGUI 按钮必看】`UIBuildUtils.CreateImage` 出来的 Image **`raycastTarget = false`**。所以**任何要能点的东西**（按钮、可点格子、拖拽源）都要**显式打开** `raycastTarget = true`；反过来全屏遮罩/幕布也要开，否则阻挡不住穿透。排查手法：`RectTransformUtility.WorldToScreenPoint` 拿按钮屏幕坐标 → `EventSystem.current.RaycastAll` 看谁在最上面。
- [2026-09-22 11:13] 【用户改表 + 新任务：第二个功法「太虚炼气诀」】用户把功法表改了：`gongfa_taixu_lianqi`（太虚炼气诀）的 **普攻方法id → `basic_remoteattack_01`**，`gongfa_qingyun_jianjue`（青云剑诀）→ `basic_sword_01`（玩家现在用的）。**所以玩家当前修炼的是青云剑诀**，太虚炼气诀是要新做的。**规格**：40% 出手、3s 冷却、**攻速同步缩放动画与冷却**、右手骨骼末端出生、**持续追踪**（命中 / 目标超出神识范围 / 超 8s）、特殊普通攻击、不被场景阻挡但可被神通/法宝/灵阵阻挡。另要求**把「玩家」的措辞统一改成「锁定单位」**（以后 NPC 可能是召唤单位帮玩家打，伤害目标统一叫锁定单位）。
- [2026-09-22 11:13] 【★ 摸清的三条架构事实（做这个功法必须知道）】**①`Assets/Scripts/Combat/Weapons/<方法id>/<类名>.cs` 是普攻方法的约定**（`basic_sword_01` → `BasicSword01`），功法表 `普攻方法id` 列指向它。**②`BasicSword01` 根本不播玩家动画** —— 它是"飞剑自己飞出去穿目标"的行为，玩家角色身上没有任何攻击动画。**所以"播角色攻击动画"这套在玩家身上是全新的。** **③`PlayerAnimationController.Update` 每帧都写 `animator.speed = playbackScale`** —— 想用 `animator.speed` 实现"攻速缩放动画播放时间"会被它每帧覆盖 ✗ 必须给它加一个**攻击速度倍率**字段（乘进去）。玩家动画控制器只有一个 `PlayerLocomotion.controller`（Speed 1D BlendTree + Flying/FlyMoving + TakeOff/Land），**没有攻击状态**，得加。
- [2026-09-22 11:13] 【已完成 · 飞弹的「持续追踪」模式（第 1 块）】`NpcProjectile` 新增 `追踪` / `追踪转向速度`（默认 540°/s）/ `追踪终止距离`（= 策划说的"神识范围"）/ `最长追踪时间`（默认 8s）+ `设置追踪飞行(Transform 目标, 速度, 转向速率)`。每帧把 `飞行方向` 用 `Vector3.RotateTowards` 往目标拐（**限速转向**，所以目标快速横向拉开仍能甩掉），并带 `transform.rotation` 跟着方向转。终止条件：命中 / 目标超出追踪终止距离 / 超时 → 消散。**和直线模式的区别**：那个起飞锁死方向、躲开就打不中；这个会拐弯。**实测** ✅：①**故意朝反方向起飞** → 追踪模式**拐回来命中了**（最近 0.5 米）；②同样反方向的**直线模式命中 False**（不会拐弯）；③目标瞬移出终止距离 → **0.01 秒就放弃**。编译 0 错误。
- [2026-09-22 11:13] 【待做 · 第二个功法剩下的 4 块】(1) 把飞弹系统从「玩家」泛化成「**锁定单位**」（`NpcTargeting.LockedNpc` 是玩家的锁定来源；NPC 那边它的锁定单位是玩家 —— 契约要统一）(2) **复制 `ChangE_Animation` 里的 Attack1** 到 `Assets/Animations/技能动作/` 并适配成玩家普攻动作（`技能动作` 目录已有 4 个 .anim）(3) 给 `PlayerLocomotion.controller` 加攻击状态 + 给 `PlayerAnimationController` 加攻击速度倍率 (4) 写 `Assets/Scripts/Combat/Weapons/BasicRemoteAttack01/` —— 40% 出弹、3s 冷却、攻速同步缩放、右手骨骼末端出生、追踪飞弹、特殊普通攻击。
- [2026-09-22 11:17] 【新需求：施法动画（提前准备）】用户说**主动神通之后也要补施法动画** —— 焚天炎术以后可能要在技能开始时播 `Assets/Animations/技能动作/技能动作2.anim`。所以我把"播一段技能动作"做成了**普攻和施法共用的通用机制**，而不是给普攻写死。
- [2026-09-22 11:17] 【已完成 · 第 2 块：复制 ChangE 攻击动画 + 玩家的通用「技能动作」机制】**①复制动画**：`ChangE@Attack1`（人形、1.2s、494 曲线）→ **`Assets/Animations/技能动作/普攻_远程_01.anim`**（用 `Object.Instantiate(clip)` + `CreateAsset` 做成真·独立资产，连带曲线和片段设置一起搬，`loopTime=false`）。**②改控制器**：给 `PlayerLocomotion.controller` 加了参数 `Action`(Trigger) + 状态 `Action`（motion = 普攻_远程_01 作**占位**）+ `AnyState → Action`（条件 Action，过渡 0.12s）+ `Action → Locomotion`（`hasExitTime=true, exitTime=1`，过渡 0.15s，**播完自动回**）。**③`PlayerAnimationController` 加通用播动作 API**：`播动作(AnimationClip 片段, float 速度倍率)` / `停止动作()` / `动作播中` / `动作进度`(0~1，用来对齐"40% 出弹") / `动作播完` 事件。**核心手法**：运行时 `new AnimatorOverrideController(基控制器)`，把状态里的**占位片段换成传进来的片段** → **任意片段都能播，不用给每个技能往控制器里加状态**（焚天炎术以后直接 `播动作(技能动作2)`）。**④攻速缩放的关键修复**：`PlayerAnimationController.Update` 以前每帧写 `animator.speed = playbackScale`，会把外面的设置覆盖掉；现在改成 `animator.speed = playbackScale × (动作播放中 ? 动作速度倍率 : 1)` → **攻速 2 时动画播得快一倍**。**实测** ✅：普攻 1 倍速播完用时 1.38s（片段 1.2s + 进出过渡）、**右手骨骼位移 1.336 米**（动作真的在播）、**40% 节点耗时 0.5s**；**2 倍速用时 0.69s ≈ 一半**（攻速缩放生效）；**施法片段（技能动作2, 2.7s）也播完（2.88s）** → 普攻/施法共用同一套。编译 0 错误。
- [2026-09-22 11:17] 【踩坑】PowerShell 的 `String.Replace(a,b,count)` **重载在 .NET Framework 里不存在**（只有 .NET Core 有），用了会静默失败/报错 —— 想只替换第一处得自己 `IndexOf` + `Substring`，或者干脆用 `edit` 工具。
- [2026-09-22 11:20] 【已完成 · 「锁定单位」抽象】新增 `Assets/Scripts/Combat/ICombatTarget.cs`：`ICombatTarget` 接口（`根`/`判定点`/`已倒下`/`当前气血`/`名字`/`受到攻击(攻击方属性, 规则, 攻击方)`）+ 两个**薄适配器** `NpcTarget`（转发给 `NpcInstance.ReceiveAttack`）和 `PlayerTarget`（替玩家走 `CombatCalculator.Resolve` 再 `PlayerVitals.受到伤害`）+ `CombatTargets.取/从` 工厂。**故意不改 NpcInstance/PlayerVitals 内部**，风险最低。**术语规范落地**：伤害目标一律叫「锁定单位」；玩家普攻 → 锁定单位 = 被锁定的 NPC，NPC 打玩家 → 锁定单位 = 玩家，NPC 被召唤后打怪 → 锁定单位 = 它锁的那只怪。
- [2026-09-22 11:20] 【已完成 · 第二个功法的普攻 basic_remoteattack_01】新增 `Assets/Scripts/Combat/Weapons/BasicRemoteAttack01/BasicRemoteAttack01.cs`（按 `basic_sword_01 → BasicSword01` 的目录约定）。**行为**：按攻击键 → 对**锁定单位**出手 → 播`普攻动作` → **动画 40% 处**生成飞弹 → 飞弹**持续追踪**（超神识范围 / 超 8s 就消散）→ 命中造成**特殊普通攻击**。**攻速同步缩放**：`播动作(clip, 攻速系数)` 让动画快一倍（动画那一半）+ `实际冷却 = 基础冷却 ÷ 攻速系数`（冷却那一半，基础 3s）。**出生点 = 右手骨骼末端**（`HumanBodyBones.RightMiddleDistal`，退路 `RightHand`）。**不被场景阻挡**（`NpcProjectile` 零物理），可被`拦截层`上的神通/法宝/灵阵拦下。**实测** ✅：动作自动从 Resources 加载（1.2s）、**动画进度到 40% 时出弹**、飞弹飞了 3.28 米、**命中会心 70.4 伤害**（锁定单位 10000 → 9929.6）、攻速 1 → 冷却 3s、神识范围 14m。
- [2026-09-22 11:20] 【★ 两个坑】(1) **运行时 `AddComponent` 的组件拿不到 Inspector 引用** —— `PlayerAbilityLoader` 是反射 AddComponent 的，所以 `普攻动作`(AnimationClip) 必须在 `Awake` 里 `Resources.Load`。因此把动作**从 `Assets/Animations/技能动作/普攻_远程_01.anim` 挪到了 `Assets/resources/技能动作/普攻_远程_01.anim`**（`Assets/Animations/技能动作/` 里还留着 4 个施法用的 .anim，那些是给**场景里**的组件用的，不受这个限制）。**以后凡是运行时 AddComponent 的组件，所有资源引用都得走 Resources 或代码默认值。** (2) **`功法表.csv` 被别的程序锁着（`IOException: Sharing violation`）→ 导入器跑不了**！用户改的 CSV（太虚炼气诀 → `basic_remoteattack_01`）**没能同步到资产**，所以面板数据里太虚炼气诀的普攻方法id 还是 `basic_sword_01`。**要用户关掉 Excel 再跑「修仙/从配置表生成资产」**。这个"CSV 被占用"的坑在改表流程里会反复出现，值得单独记一条。
- [2026-09-22 11:20] 【待做 · 收尾】①**用户关掉 Excel → 跑导入器** → `PlayerAbilityLoader` 查到 `basic_remoteattack_01` 就会自动装上 `BasicRemoteAttack01`（绑定已加进 `普攻方法表`：`basic_remoteattack_01 → BasicRemoteAttack01`）②把 `NpcAiBase` 的 `玩家生命` 换成 `锁定单位`（`ICombatTarget`），让 NPC 的飞弹也走同一套术语 ③白鹿精的飞弹描述改成「锁定单位」措辞 ④测试时注意：`BasicSword01` 和 `BasicRemoteAttack01` 都绑 Mouse1，同时开着的会抢；功法切换会自动只装一个。
- [2026-09-22 11:22] 【已完成 · 功法表同步 + 功法切换打通】用户关掉 Excel 后导入成功（功法表更新 4、NPC表更新 110）。**发现并修掉一个关键问题**：`BasicSword01` 是**场景里序列化的组件**，不受 `PlayerAbilityLoader` 管理 → 它会和装载器新装的 `BasicRemoteAttack01` **抢 Mouse1** ✗。**修法**：把场景 Player 上的 `BasicSword01` **删掉**，交给装载器按「当前功法的普攻方法id」统一装卸（绑定表里两条都在）。**实测** ✅：当前功法=太虚炼气诀 → 已装载=`BasicRemoteAttack01`、`BasicSword01=False`（只剩新的）→ 出手出弹（日志用「锁定单位」）→ **命中会心 70.4**、木桩 10000 → 9929.6。
- [2026-09-22 11:22] 【已完成 · NpcAiBase 改用「锁定单位」】`NpcAiBase` 新增 `protected virtual ICombatTarget 锁定单位`（默认 `new PlayerTarget(玩家生命)`）+ `锁定判定点`；`生成子弹` 的目标点/追踪目标、`在目标结算` 的伤害全部改走 `锁定单位.受到攻击(自己, 规则, 自己)` —— **不再写死玩家**。**以后 NPC 被收服成召唤单位，只要覆盖 `锁定单位` 这一个属性就能去锁怪**，飞弹瞄准/追踪/结算/命中特效全都跟着走。**回归实测** ✅：白鹿精出手 3 次、命中 2 次（108.7），玩家血 420 → 285.9。编译 0 错误。
- [2026-09-22 11:22] 【★ 又一个"Play 模式挡住编辑操作"的坑】跑导入器时报 `InvalidOperationException: This cannot be used during play mode.` —— 我上一个 `exec_runtime_script` 测试**把编辑器留在 Play 模式**了。**跑导入器 / 改资产之前先 `unity_editor stop`**，或者用 `get_state` 确认 `playMode: stopped`。
- [2026-09-22 11:22] 【术语规范已写进文档】`飞弹制作文档.md` 加 §12「伤害目标一律叫锁定单位」+ §13「白鹿精 vs 太虚炼气诀 两种飞弹的差别」。**以后写飞弹描述别再写"玩家"，写「锁定单位」。**
- [2026-09-22 11:26] 【已完成 · 主动神通施法动作（通用机制，不只焚天炎术）】做了**数据驱动**的版本，所有主动神通都能配：①`ActiveDivineAbility` 加字段 `施法动作`（填 `Assets/resources/技能动作/` 里的文件名，不带扩展名）②`主动神通表.csv` 加 `施法动作` 列（**导入器按「列名 = 字段名」自动映射，没改导入器**），焚天炎术那行填 `技能动作2` ③`ActiveSkillCaster` 加 `动画` 引用 + `播施法动作(神通)`，在 `施放()` 开头调。**设计取舍**：是"播动作"而不是"等动画播完再结算" —— **出手/结算时机仍由表的「首次造成伤害时间」控制**，这样动画和伤害的对齐是策划可调的，不会被动画长度绑架。**施法动作不受攻速影响**（攻速只管普攻），固定原速。**实测** ✅：焚天炎术在槽位 0、它的施法动作=`技能动作2`、`尝试施放` 返回 True、日志 `[主动神通] 播施法动作「技能动作2」（2.7s）`、**当前动作 = 技能动作2、动画进度到 1（播完）**；同时焚天炎术本身照常结算（命中 3 目标累计 2917）。
- [2026-09-22 11:26] 【注意 · 动画资源挪位置了】**`Assets/Animations/技能动作/` 里的 4 个施法 .anim 全部挪到了 `Assets/resources/技能动作/`**（技能动作2/4、技能引导动作1/3），加上之前的普攻_远程_01。**原因：这些动作是运行时按表里的名字 `Resources.Load` 的，必须在 Resources 下**。以后加新的技能动作也放 `Assets/resources/技能动作/`。
- [2026-09-22 11:26] 【坑 · 我又踩了同一个】PowerShell 的 `String.Replace(a,b,count)` 三参重载在 .NET Framework 不存在 —— **今天踩了第二次**（`ActiveDivineAbility.施法动作` 和 `ActiveSkillCaster.动画` 两个字段都没写上，编译报 12 个错）。**教训：在 PowerShell 里改 C# 代码时，一律用「单行字符串替换」，别用 here-string + `-replace` 的组合，那个表达式会被 PowerShell 解析成 3 个参数。**改完必须 `refresh` 看编译结果，不能只看脚本输出。
- [2026-09-22 11:26] 【设计小记 · ActiveSkillCaster.动画 是懒加载的】我加了 `动画` 字段但**没在 `Awake` 里赋值**，而是靠 `播施法动作` 里 `if (动画 == null) 动画 = GetComponent<...>()` 兜底。实测这样也能工作（日志显示 Awake 后引用是空的，但施法时正常播了）。要更规范可以补个 Awake，但不补也没事。
- [2026-09-22 11:32] 【用户报 3 个问题 · 修好 2 个】(1) **普攻要点一下打一下** → 加 `BasicRemoteAttack01.自动出手`（默认 true）：只要锁定了单位、冷却好了就自己一直打，**不用按键**；按键仍可触发（`自动出手` 关掉就是纯手动）。**实测** ✅：12 秒内没按任何键，自动出弹+命中 3 次（冷却 3s 一轮）。(2) **动画会忽然跳起来** → 见下条，**只改善了一部分**。(3) **飞弹没有 y 轴，玩家飞起来后打地面单位会一直空中转圈** → ★**这是我写的 bug**：`NpcProjectile` 追踪分支里写了 `期望.y = 0f`（那是给白鹿精"贴地平飞"抄过来的），结果飞弹只会水平追、永远不爬升也不下降。**修法**：去掉那行，追踪用**真正的三维方向**。**实测** ✅：起点 8 米高、目标在地面 → 方向 y 最低 −0.442、弹体从 8 米俯冲到 1.478 米。
- [2026-09-22 11:32] 【⚠️ 未解决 · 普攻动画"忽然跳一下"】**查出来的事实**：`applyRootMotion = False` 且玩家 `transform.y` 没变 ✓，`RootT.y` 曲线值域只有 **1.967~2.01（差 0.043 米）→ 不是它**；但动作期间 **Hips 世界 Y 起伏 0.914 米**（待机基线 0.874 → 最高 1.789）。**判断是重定向的尺度问题**：嫦娥模型髋高约 2.0，玩家只有 0.874，动作一播髋骨被抬到嫦娥的高度。**试过的修法（不够）**：给 `PlayerAnimationController` 加 `动作锁髋骨高度`（默认开）—— 播动作期间在 `LateUpdate` 里把 Hips 的**本地 Y** 锁回动作前的值。**实测只从 0.914 降到 0.743（-19%），没解决** ✗ 说明主要位移来自父级链或肌肉曲线，不是 Hips 的本地 Y。**下一步建议（我倾向这个）**：**换一个动作片段**，而不是在代码里跟它较劲 —— 线索是：`技能动作2/4/技能引导动作1/3` 只有 **130 条曲线**（嫦娥的是 **494 条**），说明**"技能动作"那批很可能是按玩家自己的骨架做的** → **换成那批里的一个就不会有尺度问题**。
- [2026-09-22 11:38] 【✅ B 方案成功 · 普攻动画"跳一下"彻底解决】用户说"现在效果还可以，也可以再试一下 B 方案"。**做法**：不再锁 Hips 的本地 Y（只治 19%），改成**量髋骨的"世界 Y"偏离了多少，就把整个模型根反向平移多少** —— 不管位移从哪来（父级链 / 旋转 / 尺度差）都能抵消。**实测** ✅：Hips Y 起伏 **0.914 → 0.04 米（改善 96%）**；连播两次后模型根 `localPosition = (0, 0.003, 0)`、Hips Y 回到基线，**没有累积漂移**。实现要点：①补偿根 = `animator.transform`（= Player_Visual，**动画不会写它**）②`纯动画Y = 髋骨节点.position.y - 已施加偏移Y`（减掉上帧自己加的，**避免自我反馈**）③`已施加偏移Y = -(纯动画Y - 动作前髋骨世界Y)`④动作结束 / `停止动作` 时把 `补偿根.localPosition` 还原。
- [2026-09-22 11:38] 【★ 差点酿成大祸的坑 · 补偿根选错 = 数值发散】B 方案第一版把 `补偿根层数 = 0` 实现成了"**补偿根 = 髋骨自己**"（循环 `for i < 层数` 一层都不走）→ 于是**每帧既在动髋骨、动画又在写髋骨，两边打架** → **Hips Y 起伏炸到 127.5 米**（人直接飞出太阳系）。**教训**：做"反向补偿"时，**补偿的节点必须是动画不写的那个**。默认直接取 `animator.transform`，别用"往上走 N 层"的写法（`N = 0` 很容易变成"就是自己"）。
- [2026-09-22 11:38] 【我今天的第三次同类失误 · C# 字符串里写了 ASCII 引号】在 `[Tooltip("...治"忽然跳一下"...")]` 和验证脚本的 Debug.Log 里写了 `"忽然跳一下"`，把字符串截断 → `CS1003: Syntax error, ',' expected`。**今天因为这个问题踩了三次**。**以后在 C# 字符串内部一律用「」而不是 " "。**
- [2026-09-22 11:41] 【bug · 飞起来打一下普攻 → 移动动作变成跑步 · 已修】**根因**：我给 `PlayerLocomotion.controller` 加的 `Action` 状态，出口只有一条 **`Action → Locomotion`（写死回地面移动）**。所以飞着打一下普攻、动作播完就掉回 `Locomotion`（跑步），而不是回御风状态。**修法**：把出口改成**按飞行参数分别回正确的状态**（Unity 按顺序取第一条条件满足的）：
  `→ Yufeng_Forward`（`If(Flying) If(FlyMoving)`）→ `→ Yufeng_Idle`（`If(Flying) IfNot(FlyMoving)`）→ `→ Locomotion`（`IfNot(Flying)`），三条都 `hasExitTime=true, exitTime=1`（动作播完再走）、过渡 0.15s。**实测** ✅：真实起飞（`YufengFlight.强制御风 = true`）→ `Flying=True`、状态 `Yufeng_Idle` → 打普攻 → 动作中 `Action` → **播完 `Yufeng_Idle`（不再掉回跑步）**；落地后打完仍回 `Locomotion` ✓。
- [2026-09-22 11:41] 【★ 测试教训 · 别手动设被"每帧覆盖"的参数】我第一版验证时手动 `an.SetBool("Flying", true)` —— **没用**，因为 `PlayerAnimationController.Update` 每帧都会用 `YufengFlight.御风流程中` 覆盖 `Flying`/`FlyMoving`。结果 `Flying` 被冲成 false → 走 `IfNot(Flying)` → 回 `Locomotion` → 我一度以为"改了没用"。**要测就驱动真正的系统**（这里用 `YufengFlight.强制御风`），别去手动设它每帧写的 Animator 参数。
- [2026-09-22 11:50] 【场景 · 用 better ground 替换旧 Ground】用户放了一块 `Level/better ground` 要替换 `Level/Ground`，要求**顶部对齐**。**做法**：①量出旧顶 = **0**、新顶 = **0.2924** → 把 better ground **下移 0.2924**，对齐后误差 **0 米** ②旧 `Level/Ground` `SetActive(false)`（**保留在场景里没删**，随时能开回来）③**给 better ground 补碰撞体** —— 它原本**一个碰撞体都没有** ✗（旧 Ground 是有 MeshCollider 的，一关掉地面就没碰撞了，玩家会直接掉下去）：先给 2 个石头子物件各补了 `MeshCollider`（非凸，静态地形最准）。
- [2026-09-22 11:50] 【★ 发现 · "better ground" 其实是两块石头，表面不是平的】**它的 2 个子物件叫 `environment_stone_dabingshi_001_01/02`**（大饼石），一块是 68.9 米厚的石台、一块是 1 米厚的石板。**"顶部对齐"只对齐了包围盒的最高点，可走表面是拱形/倾斜的**：往下打射线实测——中心外 10 米处只能打到 **Y = −0.32 ~ −0.65**（不是 0），而**角落 (18,18) 完全打不到东西 → 会掉下去** ✗。**旧 Ground 是个 44×44 的平面盒**，所以这样替换上去玩家会"走斜坡 + 掉洞"。**解法**：在 better ground 下新建子物件 **`碰撞地板(平坦)`**（`BoxCollider`，中心 (0,−0.5,0)、尺寸 (44,1,44)、**顶部 Y = 0**，就是照抄旧 Ground 的碰撞盒）→ **视觉还是石头，碰撞是一块无缝平地面**。**复查** ✅：8 个采样点里 **7 个落在 Y=0 的平坦地板上**（剩下 1 个是打到站在那儿的 Player，正常）。**副作用**：石头表面局部比平地低，玩家在某些位置会**视觉上悬空几厘米**（业界常规做法，可接受；想要完全贴合就得用真正的地面网格）。
- [2026-09-22 11:53] 【场景 · 让景物落地】**现象**：用户说"场景中大部分地方都有点悬空（木桩/房屋/树/石头）"。**根因是我换地面造成的** —— 旧 Ground 顶面是 **Y=0（平的）**，而新换上的石头地面是**拱形**的（中间顶 0、四周最低 −0.65），所以原本"落在 Y=0"的东西就悬在石头表面之上。**做法**：逐个物件**贴到石头表面** —— 用射线打到石头（认 `dabingshi`），按"包围盒半径 ≤ 4 米算一个独立物件"的启发式把成组的 `Props_Trees/Rocks/Small` **拆成单个**（否则整组用同一个偏移量，边上还是悬空）→ 逐个下移。**结果**：163 个物件里 **138 个落地**（木桩 0.44~0.53、cultivation room 0.62、alchemy furnace 0.77、树 0.8~1.39 米），11 个本来就在地面、14 个脚下没石面。用了 `Undo.RecordObject`，用户可以在编辑器里 Ctrl+Z 撤销。
- [2026-09-22 11:53] 【★ 结论 · 这块石头当不了整块地面】复查发现 **140/163 贴地**，但**9 个物件脚下石面在 −3~−7 米、14 个脚下完全没有石头**（共 23 个）✗ **根因**：`better ground` 是**一坨拱形石头**，**包围盒**有 49.7×48.5 但**网格本身盖不满**（边角是空的，实测射线在 (18,18) 打不到东西）。所以这些物件悬在我加的那块**看不见的平坦碰撞地板**上。**这不是落地偏移能解决的 —— 是石头本身覆盖不了场地**。**给用户的三个选项**：(a) 把石头**放大/多摆几块拼起来**盖满场地（注意放大后要重新对齐顶部 + 重新落地）(b) **保留一块可见的平地面**（比如把旧 `Level/Ground` 重新打开当视觉地面），石头当装饰 (c) 把那 23 个物件**移进石头范围内**。**建议 (b)** —— 最省事、视觉和碰撞完全一致。**注意**：如果用户决定回到平地面，我这轮把 138 个物件下移了 0.4~1.4 米，需要一起还原（编辑器里 Ctrl+Z，或者告诉我，我按"重新贴到 Y=0"再跑一遍）。
- [2026-09-22 11:54] 【场景 · 把悬空物件移进石头范围 + 全部落地 · 完成】用户选 (c)"移动进来"。**做法**：对"脚下没石头 / 石面离得太远"的物件，从当前位置**朝石头中心逐步收（每步 0.5 米）找到第一个脚下有石头的位置**，水平移过去 + 垂直贴地。**结果**：23 个全部移进来（水平 0.5~2 米）。**最终复查**：163 个物件里 **162 个稳稳贴地（±8 厘米）**、**悬空 0、陷地 0**；关键物件全部严丝合缝（木桩 底 −0.487 = 石面 −0.487、cultivation room −0.663、refining furnace −0.418、alchemy furnace −0.800、deploy formation point −0.281）。
- [2026-09-22 11:54] 【★ 踩的坑 · 射线"打到石头"不等于"打到可走的地面"】**better ground 是一根 68 米高的石柱**，我第一版判断"脚下有没有地面"时只要射到石头就算数 → 结果在穹顶外面打到的是**石柱的斜坡侧面**（Y = −4 ~ −7），于是把 23 个物件**扔到了地面以下 4~7 米** ✗。**修法**：加一条 面 Y >= -1.5 才算"可走顶面"，侧壁一律不算；再把那 19 个被扔下去的**抬回 2.1~6.3 米**。**教训：判断"站在什么上面"时，必须限定"可走层"的高度范围，否则会把侧壁/底面当成地面。**
- [2026-09-22 11:54] 【★ 坑 · Collider.bounds 是物理同步时才更新的】我改完地板位置后**立刻**读 ox.bounds 和打射线，拿到的还是**旧值**（显示"压低了 1.2 米 → 顶部 Y = 0"，看起来像没生效）。**新开一次调用就对了**（显示 −1.2）。**以后在同一个脚本里改完碰撞体位置就别立刻读 bounds/射线**，要 Physics.SyncTransforms() 或者分开一次调用。
- [2026-09-22 11:54] 【最终状态 · ground 替换收尾】把"碰撞地板(平坦)"的顶面从 **0 压到 −1.2** —— 这样**石头比地板高的地方玩家就站在看得见的石头上**（实测中心 −0.483、东 10 −0.649、西 10 −0.317 全是石头），**石头覆盖不到的外圈由地板兜住**（−1.2）✓ 视觉和碰撞终于一致了。旧 Level/Ground 仍然关着没删（可随时开回来；但注意我这一轮把 138+19 个物件下移到石头表面了，**要回平地就得一起还原**）。
- [2026-09-22 12:41] 【修炼系统 · 第 1 块：数据层 · 已完成】用户给了设计文档（docx，我提取到 `境界与功法设计（从docx提取）.md`，609 行）+ 三张修炼小屋 UI 稿（闭关修炼/境界突破/转修功法），并指定**白鹿精 01/02/03 = 等级 1/4/10**。**做完了**：①**新建 `Assets/Data/Tables/境界表.csv`（90 级）** —— 数据**完全照抄文档主表**（不做公式外推，避免舍入漂移）；列：`境界名,序号,等级,大境界,层数,升级所需灵气,累计灵气,单次修炼灵气,突破类型,突破材料,材料数量` ②`CultivationTypes.cs` 加 `enum RealmTier`（九大境界）+ `enum BreakthroughKind`（小境界突破/大境界突破/满级），`RealmDefinition` 加 等级/大境界/层数/升级所需灵气/累计灵气/单次修炼灵气/突破类型/突破材料/材料数量 ③导入器 `Specs` 注册 `new TableSpec("境界表", typeof(RealmDefinition), null, null)` → **生成 90 个境界资产** ✓ ④`NpcDefinition` 加 `等级` 字段、`NPC表.csv` 加 `等级` 列（白鹿精 = 1/4/10，其余暂按 境界×10+1 粗填）⑤**修掉导入器一个精度 bug**（见下）。**实测** ✓：90 个资产、等级 1~90、各境界首层费用与文档完全一致（100/550/3025/16638/91506/503284/2768062/15224341/83733876）、等级 10 = 大境界突破、等级 90 = 满级。
- [2026-09-22 12:41] 【★ 修的 bug · 导入器整型也走 float → 大数值丢精度】`DataTableImporter.SetFieldValue` 里 `int` 和 `long` 都写成 `(long)ParseFloat(raw)`，而 **float 只能精确表示到 16,777,216**，境界表的灵气到 **23 亿** → 实测 `83733876 → 83733872`、`2368614941 → 2368614912` ✗。**修法**：整型先 `int.TryParse`/`long.TryParse`，解析不了再退回 float。**实测** ✓：修复后 `83733876 ✓`、`2368614941 ✓`；**手工核算**「前 8 境界升满 627042290 + 登仙首层 83733876 = 710776166」证明 **CSV 是对的**、之前资产里的 710776192 才是丢精度的结果。
- [2026-09-22 12:41] 【⚠️ 文档里 3 处核心数值互相矛盾 · 等用户确认，我没猜】(1) **两张灵气表冲突**：文档「各境界升级所需基准灵气」主表跨境界 **×5.5**（首层 100/550/3025/16638/91506…），而后面「首层升级灵气」表是 **×2.2**（100/220/484/1065/2343/5155/11341/24950/54890）✗ 而且「所需同级怪数」那张表用的是 **×2.2 的数值** ✗ **我按主表（×5.5）生成的**，因为主表自带完整 9×10 数据且内部自洽（层内 ×1.2、大突破 = 前一层 ×3）。(2) **文档自己的例子对不上主表**：正文写「炼气→筑基大突破：446÷2 = 223 只」，但主表的大突破是 **1290**（÷2 = 645 只）✗；446 只在 ×2.2 的体系下也不成立。(3) **单次修炼灵气用取整值还是原始值**：「取整值」列是 2/3/5/7/10/15/23/34/51，但「所需同级怪数」的除法用的是原始值 6.75/10.13/15.19/22.78/34.17/51.26 ✗ 我表里填的是**取整值**。
- [2026-09-22 12:41] 【⚠️ 另一个待确认 · NPC表 `境界` 列 vs 新的 `等级`】NPC表**本来就有 `境界` 列（取值 0~8 = 大境界序号）**，白鹿精是 **2/4/6**；但用户给的等级是 **1/4/10**。按文档「等级 1~10 = 炼气」，1/4/10 **都该是炼气（境界 0）**，和现有的 2/4/6 完全对不上 ✗ **需要用户明确**：`境界` 列是不是旧概念（该废弃/该跟着等级走）？还是它有别的含义？
- [2026-09-22 12:41] 【待做 · 修炼系统剩下的部分】(1) **`PlayerCultivation` 运行时**：总灵气 long / 当前等级 / 修炼次数 / 突破材料；升级判定走 `累计灵气 × K` 反查（等效基准灵气 S = T ÷ K）(2) **杀怪 → 修炼次数**：单只 = 基础 1 次 × 等级差系数（≤−5:0.2、−4~−3:0.5、−2~−1:0.8、0:1.0、+1~+2:1.5、+3~+4:2.0、≥+5:3.0）(3) **修炼小屋 UI 三个页签**（照三张稿）：闭关修炼（功法信息 + 当前境界 + 进度条 + 剩余修炼次数 + 「修炼一次」+「闭关」）、境界突破（突破所需物品 + 提交物品 + 确认突破）、转修功法（当前学会的功法列表 + 转修后境界预估 + 确认转修）—— 现有 `StationInteractable/StationInteractor` 已有 `StationKind.修炼` 和 `界面预制体` 插槽可以接 (4) **功法转换**：低→高 = 总灵气先扣 5% 损耗再按新 K 换算；高→低 = 境界不提升、灵气最多填满当前小境界（超出逸散）(5) **突破材料定名**（破境丹 / 特殊突破材料）并接到物品表 (6) **存档**：`SaveData/SaveApplier` 要加 总灵气/等级/修炼次数。
- [2026-09-22 12:45] 【修炼系统 · 用户答复 + 境界表改成公式驱动】用户答复：**「两张表矛盾是因为 k 值取得不一样……以公式为准，包括例子也是，以提供了的公式为准，你可以自行做校验」** + **「单次修炼灵气向上取整」** + **「npc表如果已经有境界了的话，那是我的失误，我以为还没有」**（= 承认 `境界` 列是他自己加的、他忘了）。**所以我把 `境界表.csv` 从「照抄文档表」改成「按公式算」**：**基准灵气(n,m) = ⟦100 × 1.2^(m−1) × 5.5^(n−1)⟧**（m=1..9，四舍五入 AwayFromZero）、**大突破 m=10 = 3 × 基准(n,9)**、**单次修炼灵气 = ⌈2 × 1.5^(n−1)⌉**。**校验结果**：各境界首层公式 vs 文档表 —— **前 6 个完全一致**（100/550/3025/16638/91506/503284），合体差 2、大乘差 11、登仙差 62（**文档表在中间步骤舍入累积的漂移**，相对误差 7e-8）→ 按"以公式为准"用公式值 ✓。**单次修炼灵气（ceil）**：2/3/5/7/**11**/**16**/23/**35**/**52**（文档"取整值"列是四舍五入 = 2/3/5/7/10/15/23/34/51，现在按用户要求改成向上取整）。**实测** ✓：90 个境界资产、等级 1~90、突破类型分布 = 小境界 81 / 大境界 8 / 满级 1、long 精度无损失（登仙第9层累计 2368616703 ✓）。
- [2026-09-22 12:45] 【⚠️ 仍未解决 · NPC表 `境界` 列 vs `等级` 列】用户说 `境界` 列是他自己加的（他忘了），但**没说要不要跟着 `等级` 走**。现状：白鹿精 `境界` = **2/4/6**（金丹/化神/合体），而用户指定的 `等级` = **1/4/10** → 按文档「等级 1~10 = 炼气」，三个都该是 `境界` = **0** ✗ **不一致**。**我的处理**：`等级` 按用户明确指示填 1/4/10 ✓、`境界` 列**原样没动**（避免擅自改游戏数据）。**待用户一句话确认**：`境界` 要不要改成 (等级-1)/10 推导（那样白鹿精会从 2/4/6 变成 0/0/0）？还是 `境界` 另有含义（比如"设计上的强度档位"而不是修为等级）？
- [2026-09-22 12:48] 【修炼系统 · 用户澄清「境界就是等级」→ 已统一】用户明确：**「境界就是等级，所有表中的境界都是等级」** —— 也就是**只有一个概念，不该有第二个列**。**所以我把上一轮自己加的 `等级` 列和 `NpcDefinition.等级` 字段全删了**，改成**直接拿 NPC表原有的 `境界` 列当等级（1~90）用** ✓ 并把 `境界` 的 Tooltip 改成「境界 = 修为等级（1~90）」。**数据**：白鹿精 01/02/03 = **1/4/10** ✓（用户指定）；**其余 107 个 NPC 原来的 `境界` 是 0~8**（看着像"第几个大境界"），按 `×10+1` 转成等级（0→1、1→11、…、8→81）→ 分布变成 1级×11、4级×1、10级×1、11级×7、21级×8、31级×26、41级×17、51级×14、61级×11、71级×8、81级×6 ✓ **实测** ✓：白鹿精=1/4/10 → 炼气第1/4/10层；白熊精=51 → 炼虚第1层；山贼_01=21 → 金丹第1层；母鸡=1 → 炼气第1层。
- [2026-09-22 12:48] 【⚠️ 待确认 · 别的表里"境界含义"的列】**用户说"所有表中的境界都是等级"** → 所以这些列的值也应该是**等级 1~90**：`主动神通表/被动神通表/功法表` 的 **`修炼门槛`**、`功法表` 的 **`最高可修炼境界`**、`兽宠表` 的 **`驯服境界要求`**。**现状看还算合理**：`最高可修炼境界` = 9/12/15/12（= 炼气第9层 / 筑基第2层 / …）✓、`修炼门槛` = 0/1/2/3/5 ✓ **但 `修炼门槛` 有 0，而等级从 1 起 —— 0 是"无门槛"还是该改成 1？** 等用户一句话。
- [2026-09-22 12:57] 【修炼系统 · 用户答复：所有门槛改成 1】把 `主动神通表 / 功法表 / 被动神通表` 的 **`修炼门槛` 全部改成 1**（主动 5 行、功法 4 行、被动 6 行）✓ `兽宠表.驯服境界要求` **没动** —— 它不叫「门槛」而且值 1/2/4 本来就都 ≥1（没有 0 的问题）✓
- [2026-09-22 12:57] 【★★ 重大发现 · 功法表有 3 列一直被静默忽略】`GongFaDefinition` 的字段名和 CSV 列名**对不上**：CSV 是 `修炼门槛 / 最高可修炼境界 / 难度等级`，而类里叫 `功法修炼门槛 / 功法最高可修炼境界 / 功法难度等级` ✗ **导入器是按「列名 = 字段名」映射的，对不上就静默跳过** ✗ → 也就是说 **`难度等级` 从来没进过代码**（而整个修炼系统的 K = D ÷ 100 全靠它！），我上一轮改的 `修炼门槛` 也**根本没生效**。**怎么发现的**：我写验证脚本时直接读 `d.修炼门槛` 报 `CS1061: 不存在` —— **幸好验证脚本是用真字段名读的，如果用反射或只看导入日志就会漏掉**。**修法**：把 `GongFaDefinition` 的 3 个字段**改名成和 CSV 一致**（`功法难度等级 → 难度等级` 等），并同步 `OnValidate` 内部引用 + `PlayerHud.cs` 的 2 处引用（全项目只有这 3 处外部引用）✓ 神通类（`DivineAbilityDefinition.修炼门槛`）和兽宠类（`SpiritPetDefinition.驯服境界要求`）的字段名**本来就是对的** ✓。**实测** ✓：功法 `修炼门槛` 全 = 1、`难度等级` = 100/260/300/600 → **K = 1.0/2.6/3.0/6.0**（正好对上文档参考值）、`最高可修炼境界` = 9/12/12/15 ✓ 主动/被动神通门槛全 = 1 ✓
- [2026-09-22 12:57] 【⚠️ 教训 · 配置表列名对不上字段名会被静默忽略】这个项目里「CSV 列名 → 字段名」是**按名字精确匹配**的，**写错名字不会报错、也不会警告，那列就是空气** ✗。**以后加列/改列名后，验证一定要用「真字段名」去读资产**（像 `d.修炼门槛` 这样，拼错会编译报错），**不能只看导入日志说"更新了 N 行"** —— 日志只表示写进去了，不表示每个字段都写对了。
- [2026-09-22 12:59] 【修炼系统 · 第 2 块：运行时核心 · 已完成】**新增 `Assets/Scripts/Player/PlayerCultivation.cs`（~400 行）**，挂在 Player 上、境界表已接 90 个资产 ✓。**核心模型（严格照文档）**：`总灵气` 是**与功法无关的通用积累**；`难度系数 K = 功法难度等级 ÷ 100`；`等效基准灵气 S = 总灵气 ÷ K`；拿 S 查境界表得当前等级；`单次修炼灵气` **不乘 K**（所以高难度功法同样一次修炼推不动境界、需要更多怪 —— 这正是设计意图）。**修炼次数内部存 float**（系数有 0.2/0.5/0.8 这种小数，用 int 会抹成 0；打 5 只低级怪 = 攒够 1 次）。**API**：`修炼一次()` / `闭关全部修炼()` / `记录击杀(怪物等级)` / `取等级差系数(差)` / `转修功法(新功法)` / `预估转修后境界(新)` / `查境界(S)` / `取累计(等级)` / `设置总灵气` / `设置修炼次数` + 事件 `修为变化/修炼了/等级变化/击杀获得次数`。**杀怪挂钩**：加在 `NpcInstance` 的死亡处（`Died` 之后调 `修炼.记录击杀(定义.境界)`）—— 挂这里而不是各 AI 里，**所有 NPC（含以后新加的）自动覆盖** ✓。**功法转换**：低→高 = 总灵气 ×0.95（扣 5% 损耗）再按新 K 算；高→低 = 把总灵气压到 `累计(当前等级) × 新K`（**境界不提升、差一步突破**）✓ 并会调 `PlayerAbilityLoader.Refresh()` 换普攻方法 ✓
- [2026-09-22 12:59] 【★ 修掉一个 off-by-one（差点带进正式版）】**表里 `累计灵气(n)` 的语义是「从 1 级升到 n+1 级所需的总灵气」**，所以「累计 <= S 的最大 n」对应的当前等级是 **n + 1** 而不是 n ✗ 我一开始少加了这一级，导致 **S 刚好等于某个累计值时卡在 100% 不升级**（实测 S=100 显示 1 级 100%、S=220 显示 2 级 100%、S=3370 显示 10 级 100%）✗ **修法**：`推定 = min(90, d.等级 + 1)`，再 `取境界(推定)`。**修复后实测** ✓：S=99 → 1 级 99%；**S=100 → 2 级 0%**；S=220 → 3 级 0%；**S=3370 → 11 级筑基第1层**；S=3920 → 12 级；S=140480 → 32 级元婴第2层；**S=2368616703 → 90 级登仙十层 100%（满级）** ✓ **边界全部精确**。
- [2026-09-22 12:59] 【修炼系统 · 实测通过的完整链路】(1) **杀怪掉次数** ✓：1 级玩家杀 1/4/10/20 级怪 → 差 0/+3/+9/+19 → 系数 **1 / 2 / 3 / 3** → +1/+2/+3/+3 次（完全符合文档的分段表）(2) **修炼一次** ✓ +2 灵气（不乘 K）(3) **闭关 50 次** ✓ +100 灵气 (4) **转修功法** ✓：总灵气 100000、太虚炼气诀(K=1.0、29 级金丹第9层) → 归元混元功(K=6.0) → 总灵气 95000（扣 5%）、**19 级筑基第9层**（符合"低→高境界会掉"）✓ `预估转修后境界` 与实际结果一致 ✓ 并自动卸掉了 `BasicRemoteAttack01` ✓ **遗留**：(a) **修炼小屋三页签 UI 还没做**（照三张稿）(b) **存档**要加 `总灵气`(long) + `修炼次数累积`(float) (c) **突破**（提交材料 + 确认突破）还没做，(d) 突破材料名字还没定。
- [2026-09-22 13:02] 【修炼系统 · 第 3 块：修炼小屋三页签 UI · 已完成】**新增 `Assets/Scripts/UI/CultivationUI.cs`（~520 行，照策划三张稿）** + **预制体 `Assets/Prefabs/CultivationUI.prefab`**，已挂到修炼小屋 `cultivation room` 的 `StationInteractable.界面预制体` 上（**原来是 null → 走占位幕布，现在换成真 UI**）✓ **集成方式**：`StationInteractor.打开界面()` 会 `Instantiate(界面预制体)`、`关闭界面()` 直接 `Destroy` —— 所以 **UI 的 Awake 必须"实例化即显示"，不能在里面调隐藏**（我一开始调了 `隐藏()`，会一片空白 ✗ 已修）。**三个页签**：①**闭关修炼** = 功法信息框 + 当前境界大字 + 等级 + 进度条 + 剩余修炼次数 + 「修炼一次」「闭关」②**境界突破** = 突破所需物品信息 + 「提交物品」「确认突破」③**转修功法** = 已学功法列表（4 门，当前那门带 ▶）+ 转修后境界预估 + 「确认转修」。**实测** ✅：右键交互 → 界面打开；文字 21 条全部正确（功法信息=太虚炼气诀/玄品/难度100 K=1.0、**当前境界=炼气第3层**、**第 3 级/共 90 级**、进度 30/144 20.8%、总灵气 250、剩余次数 7、单次+2、转修页 4 门功法带 K 值）；**按钮 12 个 raycastTarget 全部 = True**（提前避开了那个"按钮点不了"的坑）；三页切换正常；关界面正确销毁 ✓
- [2026-09-22 13:02] 【★ 又踩一次"UI 坐标从下往上"】页签原来写成 `y = 12 + i*76`，那是**以左下角为原点往上排** → 三个页签跑到**左下角且顺序反了** ✗ 改成 `y = 面板高 - 12 - 64 - i*76`（**从顶部往下排**，i=0 在最上面）✓ **实测坐标**：闭关修炼 y=584、境界突破 y=508、转修功法 y=432 ✓。**教训：uGUI 里 anchorMin/Max=0 时 anchoredPosition 原点是左下角，"从上往下排"一定要自己减。**
- [2026-09-22 13:02] 【修炼系统 · 还差的】(1) **存档**：`SaveData` 要加 `总灵气(long)` + `修炼次数累积(float)`，`SaveApplier` 要写回 (2) **突破是个壳**：物品表里**还没有破境丹/大境界突破材料的定义** ✗ 所以「提交物品」现在只记个数、「确认突破」在校验（灵气满 + 提交过）后直接放行一级 —— **等物品定名后要接真正的消耗** (3) 「已学会的功法」现在**用的是全部 4 门功法**（没有"已学会"的数据源 ✗），要加就得给 `UIPanelData` 加个已学列表 (4) 界面里 `转修后境界预估` 已接好 ✓ (5) 三张图在 `screenshots/cult_v2_1~3.png`。
- [2026-09-22 13:05] 【修炼系统 · 删掉两门未完成功法 + 加「已学功法」数据源 · 已完成】**用户指示**：「3，然后现在默认是所有功法已学，有两个还没做好的功法，归元混元决和寒冰法典就不要了」。**①删功法**：`功法表.csv` 删掉 `gongfa_guiyuan_hunyuan`（归元混元功）和 `gongfa_hanbing_daodian`（寒冰法典）→ 只剩 **太虚炼气诀(D=100/K=1.0) + 青云剑诀(D=260/K=2.6)** ✓ 这两门的普攻方法 `basic_palm_01` / `basic_ice_01` **都还没实现**，留着确实是坑 ✓ **顺手删掉了生成的 2 个 .asset**（**导入器不会自动清理已删行的旧资产**，这个坑之前踩过）✓ 查引用干净、无残留 ✓ **②加「已学功法」**：`UIPanelData` 新增 `List<GongFaDefinition> 已学功法` + `已学(g)` / `学会(g)` / `补齐已学功法(全部)`；`CultivationUI` 改成**以 `UIPanelData.已学功法` 为准**（`取可转修功法()`，面板为空才退回组件上的兜底列表）✓ 场景里 `UIPanelData`（CharacterUI）的已学功法已填 2 门、预制体的兜底列表也同步了 ✓ **实测** ✓：已学功法=2 门、`已学(太虚炼气诀)=True`、转修页**只列 2 行**（▶ 太虚炼气诀 / 青云剑诀）、功法行按钮=2 个、归元混元功和寒冰法典完全消失。
- [2026-09-22 13:05] 【踩坑 · 又一次 `.Replace` 插错位置导致重复特性】给 `UIPanelData` 插新字段时锚点选了 `public List<PassiveDivineAbility> 已停用被动`，结果**插在了它的 Tooltip 和字段之间** → 那个 Tooltip 变成了新字段的、我的 Header/Tooltip 重复 → `CS0619: Duplicate 'Tooltip' attribute` ✗ **教训：用 `.Replace(锚点, 新内容 + 锚点)` 插入时，锚点必须是「字段声明那一行」而不是「它的特性那一行」，否则会把上一个字段的特性抢过来。** 这个错误今天犯了两次（NpcDefinition.等级 那次也是）。
- [2026-09-22 13:11] 【bug1 · 御风升空/落地时放普攻高度错乱 · 已修】**根因是我那个髋骨补偿**：它每帧把模型根（`Player_Visual`）的 localPosition **绝对赋值**成"基准 + 补偿偏移" ✗，而**御风升空/落地时 `YufengFlight` 正在故意改变高度**（0.85 秒过渡）→ 补偿把这段高度**冻住并和飞行系统打架** ✗。**两处修法**：(a) 补偿从**绝对赋值改成只施加增量**（`localPosition += up * 增量`）—— 这样**别人对 localPosition 的改动原样保留** ✓ (b) **御风流程中（`御风.御风流程中`）整个跳过补偿**并把加过的偏移还回去 —— 那套高度由 `YufengFlight` 管，不该插手 ✓ **实测** ✅：地面上放普攻 → 髋骨世界 Y 起伏 **0.042**（补偿生效）；**御风升空中放普攻 → `Player_Visual` 本地 Y 范围 0.003~0.003（补偿确实跳过了）**、高度偏移 0.178→2.4 自由爬升 ✓ 落地后本地 Y 回到 0.003 ✓
- [2026-09-22 13:11] 【bug2 · 转修到青云剑诀后飞剑变成方块 · 已修】**根因是我自己引入的**：为了让功法切换能自动换普攻，我把场景里**序列化的 `BasicSword01` 删了**、改成 `PlayerAbilityLoader` 运行时反射 AddComponent ✗ —— 而 `BasicSword01.剑模型资源` 原来是**在 Inspector 里拖好的**，运行时装的组件**拿不到引用** → 走了"没接模型就 `CreatePrimitive(Cube)`"的兜底 ✗。**修法**：新建 **`Assets/resources/Weapons/base_sword.prefab`**（内含 `Assets/TripoModels/base_sword/base_sword.fbx` 的实例，**只引用不复制资源**），`确保飞剑存在()` 里加一句 `if (剑模型资源 == null) 剑模型资源 = Resources.Load<GameObject>("Weapons/base_sword");` ✓ **实测** ✅：转修后 `剑模型资源 = base_sword`、飞剑是真模型（MeshRenderer + 网格）而不是方块 ✓
- [2026-09-22 13:11] 【★ 教训汇总 · 运行时 AddComponent 的组件拿不到 Inspector 引用】这个坑今天踩了**三次**：`BasicRemoteAttack01.普攻动作`（→ 挪到 Resources + `Resources.Load`）、`BasicSword01.剑模型资源`（→ 建 Resources prefab 兜底）、`ActiveSkillCaster.动画`（→ 懒加载 `GetComponent`）。**规律很清楚：凡是运行时反射 AddComponent 的组件，它所有需要"拖引用"的字段都必须能在代码里自己找到**（Resources 路径 / 代码默认值 / 运行时懒查找）。**以后把某个组件从"场景序列化"改成"运行时装载"时，必须逐个检查它原来靠 Inspector 拖的引用。**
- [2026-09-22 13:13] 【bug · 飞剑方向错了 · 已修】**用户直接给了补偿参数 (225, 0, 0)**（说"之前也出过类似的错误，所以我直接给你补偿参数"）。`BasicSword01.模型朝向补偿` 从 `Vector3.zero` 改成 **`new Vector3(225f, 0f, 0f)`** ✓ **注意**：本组件现在是 `PlayerAbilityLoader` 运行时反射 AddComponent 装的，**代码默认值就是实际生效值**（改场景没用，场景里没这个组件）✓ **实测** ✅：转修到青云剑诀后 `模型朝向补偿 = (225, 0, 0)`、Model 的本地旋转 = (315,180,180) = `Quaternion.Euler(225,0,0)` 的等价欧拉角、角度差 < 1° ✓ 飞剑也正常打中了木桩（暴击 81.23）✓ **同类问题**：`BasicSword01` 上这个 `模型朝向补偿` 字段就是专门给"模型自身朝向不对"用的，以后换剑模型改它即可；`BasicRemoteAttack01` 没有这个概念（它是纯粒子飞弹）。
- [2026-09-22 13:19] 【修炼系统 · 第 4 块：I 键面板的「境界展示」接新系统 + 实时更新 · 已完成】**问题**：`UIRealmBar`（角色面板「境界」页右下那块）读的是**旧的**经验系统（`UIPanelData.当前经验` / `GetRequiredExp()` / `GetRealmName()`）→ 一直是 **0 / 100、炼气一层**的占位数据 ✗ **改法**：`UIRealmBar` 改成**以 `PlayerCultivation` 为准**（拿不到才退回旧经验显示），并**自己**在 `Start`/`Refresh` 里**腾出面板下半块、加一个多行信息区**（不动 `CharacterPanelBuilder`、不用重建面板 ✓）—— 做法：把 RealmName 收到 0.74~1.0、进度条到 0.60~0.64、Percent/Exp 到 0.46~0.58，然后在 0.02~0.44 放 Info 文字（字体从境界名那行借 ✓）。**信息区 5 行**：修为等级/大境界、已积攒修炼次数+单次修炼灵气、总灵气+等效基准灵气、当前功法+难度系数 K、下一级突破类型+材料。**实时更新两条路都接**：①订阅 `PlayerCultivation.修为变化`（修炼/杀怪/转修/升级都会触发）②保留每帧 `Refresh()` 兜底 ✓ **实测** ✅：面板激活后起步显示 0/100（旧占位）→ 设总灵气 2000、次数 99 → **未手动刷新**就变成 **境界名=炼气第9层、百分比=81.4%、进度=350/430**（手工核对：累计(8)=1650 ≤ 2000 < 累计(9)=2080 → 9 级 ✓、(2000-1650)/430=81.4% ✓）→ **✅ 自动实时更新生效**；再调 `记录击杀(30)` → 次数行**立刻**变 102（订阅那条路也通 ✓）。顺手给 `PlayerCultivation` 加了 `大境界名` 属性。
- [2026-09-22 13:19] 【测试教训 · 激活子物件不等于激活它】我验实时更新时连着两次判"没更新"✗：第一次只 `境界条.gameObject.SetActive(true)`（父级还关着 → `activeInHierarchy` 仍 false）；第二次激活了**根** `CharacterUI`（根本来就是开的，**中间那层「境界页」还关着** ✗）。**正确做法：从目标一路往上把每一层都 `SetActive(true)`** ✓ **另记**：关着的 UI 不跑 `Update` / `OnEnable`，所以"实时更新"要在**可见**的前提下测 —— 这不是 bug，是 Unity 的正常行为。
- [2026-09-22 13:23] 【修炼系统 · 第 5 块：存档优化 + 暂停页保存按钮 + 右上角提示 · 已完成】**①补存档字段**：`SaveData` 原来只存 气血/灵气/位置/功法id/神通/被动/属性快照 —— **修炼系统的东西一个都没存** ✗ 现在加了 `总灵气(long)` / `修炼次数累积(float)` / `境界等级(int)` / `已学功法(List<string>)`，`当前版本 1 → 2` ✓ **②存读接上修炼系统**：`SaveSystem.从角色采集` 抓 PlayerCultivation 的总灵气/次数/等级/境界名 + `UIPanelData` 的当前功法 id 和已学功法 id 列表；`应用到角色` 按 id 还原功法（给 `PlayerCultivation` 加了 `GongFaDefinition[] 功法表` + `取功法(id)`，已接 2 门），还原后还会调 `PlayerAbilityLoader.Refresh()` 把普攻组件换对 ✓ **③暂停页加按钮**：`PauseMenuUI` 从 3 个按钮变 **4 个** —— **「保存游戏」/ 设置 / 返回主界面 / 退出游戏** ✓ 点击走协程：先弹「保存中…」→ `WaitForSecondsRealtime(0.35)`（暂停时 timeScale=0，必须用 Realtime）→ 采集+写入 → 弹「已保存到槽位 N（境界名）」✓ **④新增 `Assets/Scripts/UI/ToastUI.cs`**：右上角轻提示，静态 `ToastUI.提示(文本, 时长)`，自己建 Canvas（sortingOrder 5000，盖过暂停菜单 3000）；淡入 0.15s / 停留 2.2s / 淡出 0.45s 后自毁；**多条从上往下排队**不互相盖；用 `Time.unscaledDeltaTime` 所以**暂停时也能动** ✓ `raycastTarget = false`（提示不该挡点击）✓ **实测** ✅：JSON 里 6 个关键字段都在；**存读往返完全正确**（88888 灵气 / 42.5 次数 / 金丹第4层 / 青云剑诀 / 已学 2 门 全部还原）✓ 同屏 2 条提示排队 ✓ 存档路径 `Application.persistentDataPath/saves/slot_N.json` ✓
- [2026-09-22 13:23] 【踩坑 · C# 里打了全角括号】验证脚本里写成 `+（面板.已学功法 != null ...` —— **全角 `（` 不是 `(`** → `CS1056: Unexpected character '（'` ✗。**今天中文全角符号相关的编译错已踩第 4 次**（前面还有：C# 字符串里的 ASCII 引号 ×3）。**在 C# 代码里只用半角符号；中文只出现在字符串和注释里。**
### Reference


