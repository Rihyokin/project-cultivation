# 子弹/弹道特效资源说明（QFX ProjectilesFX）

> 位置：`Assets/resources/QFX/ProjectilesFX/`
>
> **验证状态（2026-09-21 收工前）**：资源可加载 ✔、弹道自带脚本识别 ✔、
> 伤害照常结算 ✔（玩家 420 → 13）；但**"飞行中的弹道"和"闪光"还没在实测里被采样到**，
> 下次要补一次确认。原因大概率是：测试时把 3 条出手方式全改成了施法、只有第 1 条配了路径，
> 随机选招没轮到它；而 QFX 的弹道是「一碰就爆」，存活时间很短，采样窗口要更密。
> 导入日期：2026-09-21　共 **105 个特效 prefab**、4 个自定义 shader、3 个演示脚本

---

## 一、这个包是怎么设计的：**三件套 × 15 个主题**

包把一次「发射」拆成三段，每段一个 prefab，**同主题的三个拼起来就是完整一发**：

```
发射瞬间            飞行途中              命中瞬间
 Flash     +     Projectile      +      Impact
（枪口闪光）      （弹道/弹丸）          （命中爆炸）
```

**15 个主题**（每套三件都有）：

| | | | | |
|---|---|---|---|---|
| Ares 战神 | Comet 彗星 | Curse 诅咒 | Cyber 赛博 | Dark Magic 暗魔法 |
| Demon 恶魔 | Kawaii 可爱 | Lightning 闪电 | Magic 奥术 | Nature 自然 |
| Priest 圣光 | StarlightFairy 星灵 | Succubus 魅魔 | Vampire 吸血鬼 | Winter 寒冰 |

> **对项目的意义**：15 个主题正好可以分给不同属性/门派的敌人。
> 比如 妖魔 → `Demon` / `Curse` / `Dark Magic`，仙家 → `Priest` / `StarlightFairy` / `Nature`。

---

## 二、目录分类（照着这个挑）

| 目录 | 数量 | 是什么 | 怎么用 |
|---|---|---|---|
| `VFX_Prefabs/Flashes/` | 15 | `VFX_<主题>_Flash` 枪口闪光 | 发射瞬间在**出膛点**生成，一秒内销毁。纯装饰、不结算伤害 |
| `VFX_Prefabs/Impacts/` | 15 | `VFX_<主题>_Impact` 命中爆炸 | 命中点生成。通常**不用自己管** —— 见下面「实体弹丸」 |
| `VFX_Prefabs/Projectiles_Particles/` | 15 | `VFX_<主题>_Projectile_Only` **纯粒子弹道** | 粒子自己往前飞。要自己控制位置/结算 |
| `VFX_Prefabs/Projectiles_Particles_Complete/Projectiles/` | 15 | 粒子弹道**完整版**（带拖尾） | 推荐用这个当粒子弹 |
| `VFX_Prefabs/Projectiles_Objects_Complete/Projectiles/` | 15 | **实体弹丸**（有模型）+ `PFX_ProjectileObject` 飞行脚本 | ✅ **最省事**，见下面 |

### 两种弹道怎么选

| | 实体弹丸（`Projectiles_Objects_Complete/Projectiles`） | 纯粒子弹道（`Projectiles_Particles*`） |
|---|---|---|
| 外观 | 有实体模型（箭/球/飞镖…） | 纯粒子流 |
| 飞行 | **自带 `PFX_ProjectileObject` 脚本**，设 `Speed` 就自己飞 | 粒子自己飞 |
| 命中 | **自带 `ImpactFX` 字段，到点自动生成命中爆炸** | 要自己生成 |
| 接入成本 | **最低** —— 生成、设 forward 和 Speed 就行 | 要自己管位置和结算 |

> **推荐**：NPC 施法优先用**实体弹丸**那套，因为飞行和命中它自己全包了。

---

## 三、别碰的东西

**已在 2026-09-21 清理掉（释放 4.3 MB，VFX_Prefabs 105 → 75 个 prefab）：**

| 已删除 | 为什么删 |
|---|---|
| `URP_HDRP_Gamma/`（3 个 unitypackage，4.2 MB） | URP / HDRP / Gamma 的**补丁包**，本项目是 **Built-in 管线**，永远用不上；留着还有"误导入把 shader 搞坏"的风险 |
| `**/Projectiles_Objects_Complete/Weapons/`（15 个） | 里面是**空壳 + 演示发射器脚本**（0 粒子、1 个子物件），对应效果在隔壁 `Projectiles/` 里有 |
| `**/Projectiles_Particles_Complete/Weapons/`（15 个） | 粒子效果 + 演示发射器脚本，同样和 `Projectiles/` 重复 |

**为什么删**：`PFX_ProjectileObjectWeapon` / `PFX_ProjectileParticlesWeapon` 是**靠鼠标左键 + 朝鼠标射线发射**的 Demo 脚本 —— 挂到 NPC 身上会**跟着玩家的鼠标转**，是个实打实的坑。

**仍然保留、但别用：**

| 东西 | 说明 |
|---|---|
| `ProjectileFX_Demo.unity` + `Demo/` | **演示画廊** —— 见下面「八、怎么预览」。已修复断链，能正常用 |
| `Post Processing Profiles/` | 演示用后处理 profile，本项目没接 Post Processing v2 |

> ⚠️ **修复记录**：删 `Weapons/` 时把 `Demo/Effects.prefab` 的 15 个引用弄断链了
> （Unity 删 prefab 后，实例会作为**脱离的副本**留在原地 —— 名字还在、链接没了，
> 所以"看起来没坏"）。
> 已把 15 个引用重新接到幸存的 `Projectiles_Particles_Complete/Projectiles/VFX_<主题>_Projectile`，
> **15/15 全部接上**。

---

## 八、怎么预览这些效果

### 办法 A：用包的演示画廊（最快）

1. 打开场景 **`Assets/resources/QFX/ProjectilesFX/ProjectileFX_Demo.unity`**
2. 按 **Play**
3. 用 **X / Z** 键切换 15 个主题（`DemoEffects.cs` 一次只激活一个）
4. 相机用 **WASD** 飞（`SimpleCameraController`）

> 作者说要看和演示一致的效果，相机要开 **HDR** + 加 **Bloom（强度 3~4 / 阈值 1.1）**
> + **Color Grading 用 ACES**，或者直接用包里的 `Post Processing Profiles/`。

**这个 demo 是什么**：就是一个**画廊** ——
`Demo/Effects.prefab` 里塞了 15 个主题的粒子弹道各一份，`DemoEffects.cs` 一次只显示一个。

**能删吗**：能。它**不在 Build Settings 里**（构建列表只有 StartScene / 3C_Testbed / Village），
删了不影响游戏。但它**是挑主题最快的方式**，所以建议留着。

### 办法 B：把单个 prefab 拖进自己的场景

从 `VFX_Prefabs/` 里拖一个进 `3C_Testbed`，选中它、按 **F** 聚焦看就行。

> 注意**实体弹丸**（`*_Projectile_Object`）带 `PFX_ProjectileObject` 脚本，
> 它**一碰就爆**，静止摆着看不完整；想连飞行一起看，用 `NpcAttackConfig` 那套配好、
> 让 NPC 打你一发（或者用演示画廊）。

### 办法 C：查清单

`特效资源清单.md` 里有全部 **75 个** prefab 的路径、粒子数、循环与否、规模估计，
可以直接搜主题名（比如搜 `Demon`）挑。

---

## 四、怎么接到 NPC 施法上

一次完整的施法 = **闪光 + 弹道 + 命中**，对应 `NpcAttackConfig` 的三个路径字段：

```csharp
new NpcAttackConfig
{
    动作名 = "Attack3",
    伤害属性 = DamageNature.特殊,
    攻击类别 = AttackKind.主动神通,
    技能倍率 = 1.8f,
    出手进度 = 0.5f,
    是施法 = true,                                   // ← 打开才会走子弹流程

    闪光特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Flashes/VFX_Demon_Flash",
    子弹特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Projectiles_Objects_Complete/Projectiles/VFX_Demon_Projectile_Object",
    命中特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Impacts/VFX_Demon_Impact",
    子弹速度 = 18f,
}
```

运行时会：
1. 在出膛点生成**闪光**，朝目标（约 0.8 秒后销毁）
2. 在出膛点生成**弹道**，`forward` 指向目标
   - 如果它自带飞行脚本 → 设好 `Speed` / `ImpactFX` 交给它飞
   - 如果没有 → 用 `NpcProjectile` 带着它飞
3. 飞行时间 = `出膛点到目标的距离 ÷ 子弹速度`，到点结算伤害
4. 玩家掉血 + **伤害飘字**（飘字是自动的，见 `AI开发注意事项.md`）

> `Resources.Load` 用的是**相对 `Assets/resources` 的路径、不带扩展名**，
> 所以上面那些路径可以直接抄。完整清单见 `特效资源清单.md`（105 个 prefab 全在里面，
> 按「飞行道具 / 单次爆发 / 循环」分好类了）。

---

## 五、⚠️ 注意：导入时把工作场景换掉了

导入这个包时 Unity 自动打开了它的演示场景：

```
当前活动场景 = Assets/resources/QFX/ProjectilesFX/ProjectileFX_Demo.unity
```

**原来的 `Assets/Scenes/3C_Testbed.scene` 没坏**，但**当前活动场景是 Demo**。
后果：这时候跑任何会保存场景的操作（生成 HUD、导入配置表…）都会存到 **Demo 场景**上。

**回工作场景**：菜单 `File / Open Scene` → `Assets/Scenes/3C_Testbed.scene`。

---

## 六、推荐的首批搭配

按项目现有的敌人类型先挑几套（够用就行，不用 15 套全接）：

| 敌人 | 主题 | 理由 |
|---|---|---|
| 妖魔（白鹿精/白熊精…）| `Demon` 或 `Dark_Magic` | 名字直接对得上 |
| 妖术师/诅咒类 | `Curse` | |
| 人类修士（正道）| `Priest` 或 `StarlightFairy` | 仙家气派 |
| 山贼/火系 | `Magic` 或 `Comet` | |
| 冰系敌人 | `Winter` | |
| 雷系 | `Lightning` | |

---

## 七、包里自带的文档

| 文件 | 内容 |
|---|---|
| `Readme.txt` | 要开相机 **HDR** + Bloom(强度 3~4 / 阈值 1.1) + Color Grading ACES 才和演示一致；shader 用 Amplify Shader Editor 编辑 |
| `Particle_System_Readme.pdf` | 粒子系统说明 |
| `Credits.txt` | 素材来源 |
| `HDRP URP Gamma Readme.txt` | 补丁包说明（本项目用不到） |
