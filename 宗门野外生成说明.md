# 宗门野外（Sect_Wilderness）生成说明

> 场景 `cultivation/Assets/Scenes/Sect_Wilderness.scene`，生成器
> `cultivation/Assets/Editor/Builders/SectWildernessBuilder.cs`，
> 菜单 **`Cultivation / Build Sect Wilderness (v1)`**（幂等，可反复重跑）。
>
> 用户需求原文（2026-09-27）：
> 「这个场景的用处是**靠近宗门的野外**，需要你创建一张**比较大的地图（村庄的 4 倍大小左右）**，
> 要求使用**村庄的那种稍微有起伏**地图生成，你看着**河流怎么好看怎么放**，**树木组团放置**，
> 围合出一条**环合的主干道**，并且有**一些分支路**，路也是**基于材质来体现**而不是你创建一条，
> 做一个**生态感的森林**」
> 追加（同一轮）：「**石头太多了**，稍微减少一点，我强调了**树木组团放置**，你可以**先建好路网，
> 路中间的空间用组团的树木填充**，现在这样有点乱。可以**稍微留出一些空间用来放刷怪区**」

---

## 1. 一张图看懂

```
世界 XZ ∈ [−200, 200]（400×400m = 村庄 200×200 的 4 倍面积）
地形 GameObject 在 (−200, −8.5, −200)，高度图 0.25 ↔ 世界 y=0

        z=+200 ┌──────────── 北：四周环山（+20m）────────────┐
               │   ▲ 宗门道（出山口，碎石铺面 + 山口石）      │
               │        ╭─────── 环路（半径 97~167m）──────╮  │
               │   ╭───╯    ·林团·      ·林团·            ╰─╮│
   河流从西北 ─┼──▶╯   ·林团·   〔草甸〕   ·林团·           ╰┼──▶ 东南出图
   山口进来    │      ╲  ·林团·   ·林团·  ·林团·          ╱ │
               │       ╰────╮ 渡口路 / 空地路 / 上山路 ╭──╯  │
               └──────────── 南：四周环山 ──────────────────┘
                        水潭(半宽16m) 在 (30,−50)
```

| 项 | 值 |
|---|---|
| 地形 | 400×400m，高度图 **1025**（0.39m/格）· 地表 1024 · 细节 512（草 0.78m/格） |
| 高度 | 地形高 34m、基准 8.5m（**高度图 0.25 = 世界 y=0**） |
| 地表 4 层 | 草地 `grass009` · 林下地被 `dirt030` · 土路 `dirt036` · 沙石 `dirt022` |
| 草 | 9 种草 billboard（最后一种是**芦苇**，只刷水边），全图约 **48 万株** |
| 树 | **约 1670 棵**，34 个林团 + 110 棵散生 |
| 灌木/花草/石 | 620 / 300 / 66+60 |
| 场景文件 | ≈ **11.5 MB**（树的棵数决定它）· 地形资产 ≈ 9.6 MB |
| 出生点 | `(8, 地面+0.35, 160)` = 宗门道刚进谷的地方，朝南 |

---

## 2. 五步生成顺序（**顺序不能换**）

1. **准备()** —— 先造**路网**（环路 + 5 分支 + 2 野径）、8m 网格的**平滑高度场**、河的水位，最后**造林团**
   （因为"路中间的空档"得先有路才算得出来，地表刷材质也要用林密）
2. **建地形()** —— 高度 → 地表 → 草
3. **建河面()** —— 按水位拼水面带子（主河 + 支流）
4. **撒河岸 / 撒树 / 撒灌木 / 撒花草 / 撒散石 / 建山口**
5. **建刷怪区()** —— 只放空物体 + `SpawnZone` 线框标记

高度计算的**内部顺序**（在 `算高度()` 里，同样不能换）：

```
基础高度（噪声 + 4 个小丘 + 四周环山）
  → 路：压向【平滑高度】（不是 y=0！这是 400m 的野山，压平会切出深沟）
  → 河谷：只往下削到 (水位 + 1.8)
  → 河槽：刻到 (水位 − 河深)，跨路处留浅滩      ← 河必须最后，否则会被上面两步抹掉
```

---

## 3. 路网 = 地块边界（用户强调的"组团填充"）

用户要的顺序是「**先建好路网，路中间的空间用组团的树木填充**」。实现：

1. **环路**：按角度扫 22 个点，半径 = `132 + 20·sin(2θ+0.6) + 9·sin(3θ+2.3) + 6·sin(5θ+1.1)`
   → 不是正圆、也不撞进山里；再挂 5 条分支：
   **宗门道**（北出图，通向宗门）· **渡口路**（到水潭边）· **空地路**（西南林窗）·
   **上山路**（东北坡）· **猎径 / 野径**（强度 0.5 / 0.42，走进林子就淡掉）
2. **林团**：20m 间距的抖动点阵铺满谷地 → 筛掉 离路 <17m、贴河 <半宽+15、草甸/林窗/刷怪区、山脚
   → 剩下的就是"**被路围出来的空档**" → 打乱后**贪心取团心，彼此至少隔 30m**（团之间留缝，看过去是一团一团）
3. 每团按**生境**给主树种（这是"生态感"的来源，不是随机撒）：

| 位置 | 主树种 |
|---|---|
| 离河 < 60m | 水边林（阔叶 + 竹）七成 / 桃林三成 |
| 北坡 z > 62 | 松林 七成 / 古树 三成 |
| 东坡 x > 66 | 竹林 六成 / 阔叶 四成 |
| 西坡 x < −66 | 阔叶 六成 / 混交 四成 |
| 谷中 | 混交 六成 / 阔叶 四成 |

★ **团多、每团少**：总棵数不变时，"森林感"跟**团的覆盖面积**走，而**场景文件大小**跟**总棵数**走
（每棵 prefab 实例 ≈ 90 行 YAML）→ 宁可 34 个小团，也不要 20 个大团。

---

## 4. 河流

* 走向（21 个点）完全是按"好看"摆的：西北山口进 → 大 S 弯穿过谷地 → 东南出图，
  中途在 `(30,−50)` 把半宽放到 **16m**，就是一个**水潭**；另外从东北山坳下来一条**支流**汇入。
* **水位不是写死的**：`水位 = 平滑高度 − 0.9`，横向平滑 3 遍，再**强制单调下降**
  （水往低处流，水位有回升看着就是"水往山上爬"✗）。实测剖面：
  `19.1 → 18.7 → 16.9 → 13.6 → 9.1 → 4.7 → 1.5 → −0.3 → −1.3 → …→ −1.8 → −3.0`
* **河面网格逐顶点用各自的水位**（村庄那条是整条一个水平面）—— 野外的河要顺着山谷往下流。
* ★ **河槽要"宽而缓"**：第一版过渡带只有 `半宽+3`（≈8m 内掉 2.6m）→ 看着像**水泥渠** ✗；
  现在过渡带到 `半宽×2.6+9`（≈20m），水面坐在一个浅碗里，两岸是缓坡 ✓
* 四周的山是被河谷**自动削开**的：河谷那一步把地形往 `水位+1.8` 拉（`Min` 只往下削），
  所以河穿过山脚时会自然出现一条 V 形谷口，不用单独做"山口"。

---

## 5. 刷怪区（用户要的留白）

`刷怪区 = (-58,-56) / (58,48) / (-72,24) / (34,-74)`，半径 **26m**。

* 地形那边保证**一棵树都不长**（`可放()` 里硬性 `return false`，`空地权()` 里也算"空地"）→ 草和花照常长；
* 场景里生成 `野外环境/刷怪区/刷怪区_01..04` 空物体，挂 `Assets/Scripts/Core/SpawnZone.cs`：
  **只在 Scene 视图**贴地画一个红圈 + 中心十字（**无碰撞体、无 Update、Game 视图不画**）→ 以后好找；
* 想挪地方：改 builder 里的 `刷怪区` 数组重跑，或直接在场景里拖那个空物体（拖了之后重跑会被重置）。

---

## 6. 验收手法（★ 都踩过坑，别走弯路）

| 手法 | 怎么做 | 注意 |
|---|---|---|
| **看布局** | 临时相机正交俯视 400m 渲一张 PNG（`wild_plan` / `w3_plan` 就是这么来的） | 树影会显得像"黑斑"，别当成洞 |
| **★ 看草** | **必须进 Play Mode 用 `ScreenCapture.CaptureScreenshot`** | 离屏 `camera.Render()` **不画地形细节** —— 实测把 `detailObjectDistance` 从 0 改到 200，两点图**像素差 0.00000**，一点草都不多。数据里 48 万株是好的，只是那种渲染方式看不见 ✗ |
| **量地表贴图颜色** | `Graphics.Blit(贴图, RT)` 再 `ReadPixels` 求平均色（非可读贴图也能量） | 结果见 §7：**全是偏米黄的** |
| **挑树种** | 空场景里把候选树排成两行、逐个渲俯视图量树冠平均色 + 看形状 | 见 §7 |
| **验路/河/树** | Edit Mode 直接 `Physics` / 数学查询即可，不用进 Play Mode | 读 `Collider.bounds` 前记得 `Physics.SyncTransforms()` |

---

## 7. ★ 资源实测数据（这些结论是量出来的，不是猜的）

**地表贴图平均色**（都偏米黄，直接铺上去整张图发白）：

| 贴图 | 平均色 | 用途 |
|---|---|---|
| `grass009.bmp` | (74, 79, 40) | 草地底色 → 用 `diffuseRemapMax (0.74,0.94,0.74)` **压绿** |
| `dirt030.bmp` | (92, 78, 62) | 林下地被 → 压到 `(0.42,0.36,0.30)` 才是**深褐落叶层** |
| `dirt036.bmp` | (96, 84, 60) | 土路（不压，路要亮一点才看得见） |
| `dirt022.bmp` | (103, 94, 63) | 沙石/河滩（不压） |

**树种筛选**（树冠平均色 + 形状）：

| 树 | 平均色 | 结论 |
|---|---|---|
| `songshu_002_d` / `Green_005_d` / `Green_001_a` / `Green_005_a` | 绿 (26~32, 36~43, 0~8) | ✓ 主力 |
| `S1_shu001_tf` | (50, 58, 23) 偏亮的绿 | ✓ 松状，做"古树" |
| `S1_shu002_tf` | (21, 42, 5) | ✓ |
| `dashu_001_a` | (9, 19, 14) **很暗** | △ 少量做老树 |
| `qingduanzhu_001_a` | (41, 49, 11) | ✓ 竹林 |
| `taoshu_001_a` | (74, 45, 46) | ✓ 桃花粉，**只做点缀** |
| **`dashu_003_a`** | (49, 29, 1) **100% 暖色** | ✗ 纯橘黄秋树，**不进池子** |
| **`S1_shu003_tf`** | (88, 53, 38) 46% 暖色 | ✗ 也是秋树（还带块石头底座） |
| **`purple_001`** | (17, 24, 22) 太暗 | ✗ 远看一团黑 |
| **`zhangshu_01_a`** | 绿，但**形状是苏铁/棕榈状的放射叶** | ✗ 温带林子里非常出戏（原来占了 780 棵） |

---

## 8. 与村庄的关系 / 复用

* **同一套做法**：Terrain + 分层地表、材质"磨"出来的路、按目标高缩放树、`预制高()` 缓存、
  水面拼带子 + 法线直接写 up、`建目录()`/`启用全部部件()` 这些工具函数，都是从
  `VillageNatureBuilder` 搬过来的（两边都是自包含的静态类，**没有抽公共基类**，改一边别忘另一边）。
* **三处刻意的不同**：① 路压向**平滑高度**而不是 y=0；② 水面**逐顶点水位**（会顺着山谷下降）；
  ③ 树的碰撞体只给**细树干**（radius 0.34 / 高 3m），不是村庄那种整棵大胶囊 ——
  这片有 1600+ 棵，用大胶囊会让**御风/坐骑飞过树林被树冠一路顶起来**（见 `开发注意事项.md` §48.3）。
* **归生成器管**：整个 `野外环境` 根节点（含 `刷怪区`）。重跑 = 删掉它 + 删 `Assets/Art/WildernessTerrain/` 重建。
  **手动改过的东西会被重跑冲掉**，要留就挪到 `野外环境` 外面。

---

## 9. 装配现状

| 项 | 状态 |
|---|---|
| 根物体 | Directional Light · **野外环境** · Player · Main Camera · EventSystem · HudCanvas · CharacterUI · 暂停菜单 · SpawnPoint |
| 通用装配 | 已用 **`修仙 / 同步通用角色与UI到所有游玩场景`** 同步过（v1 时给它**补**的；`Sect_Wilderness.scene` 已加进 `SceneRigSyncer.游玩场景`）|
| Build Settings | 已加入 |
| 出生点 | `(8, 3.1, 160)`（宗门道北段，朝南）· 高度按地形取（地面 2.79m）|
| ⚠️ 直接按 Play 时 | 实测玩家会落在 **(0, 0.13, 0)**（谷地中央草甸），不是我摆的出生点 —— 场景里还有别的出生/传送逻辑在接管位置。**如果要在别处开，改那个逻辑或 SpawnPoint**，不是我这边生成器的锅 |

---

## 10. 相关文件

| 文件 | 作用 |
|---|---|
| `Assets/Editor/Builders/SectWildernessBuilder.cs` | 生成器（唯一入口，菜单 `Cultivation/Build Sect Wilderness (v1)`）|
| `Assets/Scripts/Core/SpawnZone.cs` | 刷怪区标记（只画 Scene 视图红圈）|
| `Assets/Art/WildernessTerrain/` | 生成的地形资产 / 4 个 TerrainLayer / 水面网格 / 水材质（**重跑会整个重建**）|
| `Assets/Editor/Tools/SceneRigSyncer.cs` | 通用角色与 UI 的同步（本场景已进 `游玩场景` 表）|
| `开发注意事项.md` §48/§49 | 空气墙（村庄）· 野外这次踩的坑 |
