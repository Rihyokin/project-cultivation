# 飞弹制作文档

> **这份文档的目的**：下次要"给某个 NPC 或玩家做一个飞弹"时，照这里直接做，不用再研究一遍。
> 结论来源：拆解了 QFX `ProjectilesFX` 包的飞行脚本与 prefab 结构（2026-09-21）。
>
> 相关：`子弹特效资源说明.md`（资源分类）、`特效资源清单.md`（75 个 prefab 的路径清单）、
> `AI开发注意事项.md`（NPC AI 总入口）

---

## 0. 一句话

**飞弹 = 一个带「飞行脚本 + 刚体 + 碰撞体 + 拖尾/粒子」的小物件。
它自己往前飞，撞到东西就把「命中特效」放出来然后自毁。**

我们这个项目里，**两条路都能走**：

| 路线 | 什么时候用 | 代码在哪 |
|---|---|---|
| **A. 借 QFX 的实体弹丸** | 主题能用包里现成的 15 套之一 | 已接好，只填 3 个路径 |
| **B. 用我们自己的 `NpcProjectile`** | 想自定义 / 包里没有的效果 | 已写好，挂上去就行 |

---

## 1. 飞弹的解剖（学自 QFX）

QFX 的实体弹丸 `VFX_<主题>_Projectile_Object` 长这样：

```
VFX_Demon_Projectile_Object            ← 根
├── ★PFX_ProjectileObject              飞行脚本
├── Rigidbody                          isKinematic=false, useGravity=false, drag=0
├── SphereCollider                     radius=0.1, isTrigger=false
├── Trail_1 / Trail_2                  TrailRenderer 拖尾（宽1 / 0.32~0.36 秒）
├── Projectile                         飞行主体粒子（一次，寿命 2s）
└── Sparks_Along                       一路的火花（**循环**）← 接给脚本的 FXToDeatch
```

### 飞行脚本的全部逻辑（就这么多）

```csharp
public class PFX_ProjectileObject : MonoBehaviour
{
    public ParticleSystem FXToDeatch;        // 飞行中拖着的粒子（作者笔误，应为 Detach）
    public float Speed = 15f;
    public GameObject ImpactFX;
    public float ImpactFXDestroyDelay = 2f;
    public float ImpactOffset = 0.15f;

    void FixedUpdate()
    {
        if (Speed == 0) return;
        transform.position += transform.forward * (Speed * Time.deltaTime);   // ← 就这么简单
    }

    void OnCollisionEnter(Collision collision)
    {
        var contact = collision.contacts[0];
        if (contact.otherCollider.name.Contains("Projectile")) return;   // 忽略弹丸互撞（靠名字，hacky）

        Speed = 0;
        var hitPosition = contact.point + contact.normal * ImpactOffset;  // 沿法线外推，别嵌进墙里

        if (ImpactFX != null) { Instantiate(ImpactFX, hitPosition, ...); Destroy(..., ImpactFXDestroyDelay); }

        FXToDeatch.transform.parent = null;    // ★把拖尾「脱开」，让它留在命中点自然消散
        FXToDeatch.Stop(true);
        Destroy(FXToDeatch.gameObject, ImpactFXDestroyDelay);
        Destroy(gameObject);
    }
}
```

### 从这个脚本能读出 4 条硬要求

1. **要 `Rigidbody` + 非 trigger 的 `Collider`** —— 否则 `OnCollisionEnter` 根本不触发，飞弹会穿墙
2. **`useGravity = false`** —— 不然它会往下掉，不是直线
3. **`isKinematic = false`** —— 脚本直接改 `transform.position`，靠 Unity 把 transform 同步给物理。
   （⚠️ 这和我们项目 §8.2 那条"别让 transform.position 和物理打架"看着冲突，但这里是**唯一的运动来源**、
   没有第二个组件在推它，所以没事。**别再加第二个会推它的东西**）
4. **方向靠 `transform.forward`** —— 所以生成时**必须把 forward 设成目标方向**

---

## 2. 三件套的真相（很重要）

拆开 prefab 层级会发现：**`VFX_X_Projectile`（粒子完整版）本身就是三合一**：

```
VFX_X_Projectile（粒子完整版）
├── Glow 分支      （发射瞬间的闪光）≡  VFX_X_Flash
└── Impact 分支    （命中爆炸）      ≡  VFX_X_Impact
```

也就是说 `Flashes/` 和 `Impacts/` 里的东西**是这三合一的拆件**。

**实践含义**：
- 用**实体弹丸** `VFX_X_Projectile_Object` 时，它**只含"飞行"部分** ——
  闪光和命中要**另外指定**（这就是我们代码里 `闪光特效路径` / `命中特效路径` 两个字段的来由）
- 用**粒子版** `VFX_X_Projectile` 时，三合一，但它**没有飞行脚本**（粒子原地发射），
  要靠 `NpcProjectile` 带着它飞

---

## 3. 路线 A：借 QFX 的实体弹丸（推荐，最省事）

**只要填 3 个字符串**，其余全自动。

```csharp
new NpcAttackConfig
{
    动作名 = "Attack3",
    伤害属性 = DamageNature.特殊,
    攻击类别 = AttackKind.主动神通,
    技能倍率 = 1.8f,
    出手进度 = 0.5f,

    是施法 = true,                       // ★打开才会走飞弹流程
    子弹速度 = 16f,
    闪光特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Flashes/VFX_Demon_Flash",
    子弹特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Projectiles_Objects_Complete/Projectiles/VFX_Demon_Projectile_Object",
    命中特效路径 = "QFX/ProjectilesFX/VFX_Prefabs/Impacts/VFX_Demon_Impact",
}
```

路径规则：**相对 `Assets/resources`、不带扩展名**。
`特效资源清单.md` 里 75 个的路径都能直接抄。

**15 个主题**任选：`Ares / Comet / Curse / Cyber / Dark_Magic / Demon / Kawaii / Lightning /
Magic / Nature / Priest / StarlightFairy / Succubus / Vampire / Winter`
（注意命中/闪光里是 `Dark_Magic`，实体弹丸里是 `DarkMagic` —— **没有下划线**，抄清单别手打）

---

## 4. 路线 B：用我们自己的 `NpcProjectile`

包里没有想要的、或者想用自己做的特效时用这条。

**`NpcProjectile` 不依赖刚体和碰撞体** —— 它自己按直线插值飞，到达就回调。
所以用在**任何特效 prefab** 上都可以。

```csharp
var 弹道 = Instantiate(你的特效prefab, 出膛点, Quaternion.LookRotation(方向));
var 飞 = 弹道.AddComponent<NpcProjectile>();
飞.设置飞行(目标点, 速度);
飞.到达时 += () => { 结算伤害(); 生成命中特效(); };
```

它和路线 A 的区别：

| | 路线 A（QFX 实体弹丸） | 路线 B（`NpcProjectile`） |
|---|---|---|
| 飞行 | 包自带脚本（`FixedUpdate` + 物理碰撞） | 我们自己的插值 |
| 命中判定 | **物理碰撞**（撞到才有） | **到达目标点**（按时间） |
| 需要刚体/碰撞体 | **要** | 不要 |
| 命中特效 | 包自己放 | 自己放 |
| 会不会打偏 | 会被障碍挡住（更真实） | 直线飞到底（会穿墙） |

> **要"会被墙挡住"就用 A，要"必定到达"就用 B。**
> 目前代码里是：**弹道 prefab 自带飞行脚本 → 走 A；否则 → 走 B**（自动判断，不用手选）。

---

## 5. 给 NPC 做飞弹：照着做

1. 挑主题（或自己做一个特效 prefab 放进 `Assets/resources/` 下）
2. 打开那个 NPC 的 `NpcAi<名字>.cs`
3. 在 `取默认参数()` 里把某一条出手方式改成施法：

```csharp
protected override void 取默认参数()
{
    base.取默认参数();

    // 第 3 条出手 = 施法飞弹
    攻击方式[2].是施法 = true;
    攻击方式[2].伤害属性 = DamageNature.特殊;
    攻击方式[2].攻击类别 = AttackKind.主动神通;
    攻击方式[2].技能倍率 = 2f;
    攻击方式[2].出手进度 = 0.5f;

    const string 包 = "QFX/ProjectilesFX/VFX_Prefabs/";
    攻击方式[2].闪光特效路径 = 包 + "Flashes/VFX_Demon_Flash";
    攻击方式[2].子弹特效路径 = 包 + "Projectiles_Objects_Complete/Projectiles/VFX_Demon_Projectile_Object";
    攻击方式[2].命中特效路径 = 包 + "Impacts/VFX_Demon_Impact";
    攻击方式[2].子弹速度 = 16f;
}
```

> ⚠️ `攻击方式` 默认只有 3 条（Attack1/2/3），**而且只有 Animator 里真有的动作才会被选中**
> （`可用出手方式()` 会过滤掉没有对应动画的）。所以别把飞弹配到一条没有动画的动作上。

4. 进 Play，让 NPC 打你，看是否：出膛点有闪光 → 有东西飞过来 → 命中点有爆炸 → 你掉血 + 飘字

---

## 6. 给玩家做飞弹

**现状**：玩家的普攻是近战（`BasicSword01`），主动技能走 `ActiveSkillCaster` + `AreaSkillRunner`
（**立即结算的范围伤害，没有飞行过程**）。所以玩家的"飞弹"目前**没有**。

**怎么做**（两种粒度）：

### 6.1 最省事：给主动神通加一个"飞弹"结算方式

在 `ActiveDivineAbility` 上加一个 `结算方式 = 飞弹`，然后 `ActiveSkillCaster.施放()` 里
把 `AreaSkillRunner` 换成"生成一发飞弹、到达后再结算"：

```csharp
// 生成飞弹（照路线 A 或 B），到点后：
目标.ReceiveAttack(自己, new AttackSpec(神通.伤害属性, AttackKind.主动神通, false, 神通.伤害倍率));
```

**好处**：和 NPC 那套复用同一个 `NpcAttackConfig` 思路，数据表加一列 `首次造成伤害时间` 就能对齐动画。

### 6.2 更贴做法：抽一个通用的 `ProjectileLauncher`

把"生成闪光 → 生成弹道 → 按飞行时间/碰撞结算 → 放命中特效"抽成一个静态工具，
**NPC 和玩家都调它**，只是"攻击方"和"受击方"换一下：

```csharp
ProjectileLauncher.发射(new 发射参数 {
    出膛点, 目标, 速度,
    闪光路径, 弹道路径, 命中路径,
    攻击方 = 自己, 受击方 = 目标, 规则 = spec,
    命中后 = (结果) => { ... }      // 玩家要飘字/演出就挂这里
});
```

> 现在 `NpcAiBase.生成子弹` 里已经是这套逻辑了，只是写死在 NPC 基类里。
> **建议下次做玩家飞弹时先把它抽出来**，别再复制一份。

---

## 7. 踩过的坑（都在这个项目里真实发生过）

| 坑 | 现象 | 解法 |
|---|---|---|
| **弹道生成在自己身上** | 飞弹出生当帧就撞到施法者、**0.5 秒内消失**，等于子弹根本没飞出去 | 出膛点**沿方向前移**（`NpcAiBase.出膛余量()` = 碰撞体半径 + 0.35）。QFX 弹丸是"一碰就爆"，必须让开 |
| **没有 Rigidbody / collider 是 trigger** | 走路线 A 时飞弹**穿墙**、`OnCollisionEnter` 不触发 | 刚体 + 非 trigger 碰撞体，缺一不可 |
| **useGravity 忘了关** | 飞弹飞出去就往下掉 | `useGravity = false` |
| **`transform.forward` 没设** | 飞弹朝固定方向飞（通常是世界 +Z），不朝目标 | `Instantiate` 时 `Quaternion.LookRotation(方向)` |
| **反射取字段写错名字** | 速度和命中特效**静默不生效**（`设字段` 找不到就跳过） | 包里的字段名是 `Speed` / `ImpactFX` / `ImpactOffset` / `FXToDeatch`（**Deatch 是作者笔误**）。改包版本后要重新核对 |
| **`FXToDeatch` 没接** | 命中时 `NullReferenceException` | 实体弹丸上它接的是 `Sparks_Along`，自己搭的话记得接 |
| **只配了弹道没配闪光/命中** | 飞过去"啪"一下就没了，很单薄 | 三件套一起配；`VFX_X_Projectile` 里能查到对应的 Flash / Impact |
| **用了 `**/Weapons/` 里的 prefab** | NPC 的飞弹**跟着玩家的鼠标转** | 用同级 `Projectiles/` 里的；`Weapons/` 已从项目删掉 |

---

## 8. 验证清单

```
[ ] 出膛点有闪光
[ ] 有东西（模型或粒子）真的飞出去，朝向目标
[ ] 飞行中拖尾/火花跟着走
[ ] 命中点在"目标身上"而不是"施法者脚下"（生成点没让开就是这个症状）
[ ] 命中点有爆炸特效
[ ] 受击方掉血 + 伤害飘字
[ ] 飞弹不会卡在自己身上、不会穿墙（路线 A）、不会永远不结算（路线 B）
[ ] 连发几发：不会互相撞、不会漏结算、不会越攒越多（看有没有残留 NpcProjectile_*）
```

**现成的测法**（不用手点鼠标）：

```csharp
var ai = 妖.GetComponent<NpcAiBase>();
foreach (var c in ai.攻击方式) c.是施法 = true;     // 全部走飞弹，方便观察
ai.攻击方式[0].子弹特效路径 = "...";               // 只给第 1 条配，避免随机选招看不出效果
// 然后等它自己打你，同时每 0.05 秒统计一次：
//   FindObjectsOfType<Transform>() 里名字以 NpcProjectile_ / NpcFx_ 开头的数量
```

> ⚠️ **采样要密**：QFX 弹丸是"一碰就爆"，加上飞得快，
> 0.1 秒的采样窗口**可能一次都抓不到**（我就在这上面误判过一次）。
> 想看清楚就把 `子弹速度` 调小（比如 6），或把目标放远一点。

---

## 9. 参数速查

| 参数 | 在哪 | 说明 |
|---|---|---|
| `是施法` | `NpcAttackConfig` | **开关**，不开就一直走"立即命中" |
| `子弹速度` | `NpcAttackConfig` | 米/秒。也决定结算延迟 = 距离 ÷ 速度 |
| `闪光特效路径` | `NpcAttackConfig` | 出膛点，纯装饰 |
| `子弹特效路径` | `NpcAttackConfig` | 弹道。**留空 = 只做延迟结算** |
| `命中特效路径` | `NpcAttackConfig` | 塞给实体弹丸的 `ImpactFX`；路线 B 时自己放 |
| `闪光特效存活` | `NpcAttackConfig` | 默认 0.8s |
| `出手进度` | `NpcAttackConfig` | 动画播到多少比例时**发射**（对齐施法动作的关键） |
| `出膛余量()` | `NpcAiBase` | 弹道往前让开多少，默认自动算 |
| `取出膛点()` | `NpcAiBase` | 出膛位置，默认胸口（约 70% 身高）。**想接骨骼挂点就改它** |

---

## 10. 【已落地】白鹿精：第一个会飞弹的 NPC

`NpcAiBaiLuJing`（一个类覆盖 3 个变体，靠 `定义.id` 区分）。
**这是"照本文档做一个飞弹"的第一个真实样板**，下次做别的照抄。

| 变体 | 普攻 Attack1 | 主动神通 Attack2 | 出生点 | 弹体 |
|---|---|---|---|---|
| 01 / 02 | 飞弹，冷却 **3s** | — | **左手末端** `Bip01 L Finger0Nub` | 1× |
| 03（王）| 飞弹，冷却 **3s** | 飞弹，冷却 **8s** | 普攻左手 / 神通**右手**（武器末端近似）| 神通 **1.6×** |

两招都在**动画 50%** 发射，命中造成**特殊普通攻击**（`AttackSpec(特殊, 普通攻击)`）。

### 5 个要素在这里怎么落地的

| 要素 | 落地方式 | 实测 |
|---|---|---|
| **1 出生点** | `挂点 = "Bip01 L Finger0Nub"`（骨骼名，`取出膛点()` 按名字找）| ✅ 出膛点 (-0.65, 1.61, 9.19)，距本体 1.73m |
| **2 飞行路径** | `NpcProjectile.设置直线飞行()` —— 起飞锁方向，**之后不追踪** | ✅ 逐帧 293 帧，直线飞 7.4 米全程 |
| **3 命中效果** | `在目标结算()` → `AttackSpec(特殊, 普通攻击)` + 自动飘字 | ✅ 命中 3 次 × 108.7 伤害 |
| **4 能否被阻挡** | **用 `Projectile_Only`（纯粒子）+ 自家飞行器 —— 完全没有刚体/碰撞体，所以墙挡不住**；只检测 `拦截层` | ⏳ 层为 0（等灵阵/法宝做出来才能测）|
| **5 消灭点** | `最大飞行距离 = 22` → 超了就**粒子消散 + 自己缩小淡出** | ⏳ 实现好了，但玩家没躲所以没触发过 |

### ⚠️ 两个重要判断

**1. 为什么不用包里带 Rigidbody 的「实体弹丸」**
你要的「**无法被场景阻挡**」和实体弹丸**直接冲突** —— 那个靠 `OnCollisionEnter` 命中，
**会被墙挡下来**。所以这里用 **`Projectiles_Particles/VFX_X_Projectile_Only`（纯弹道）**
+ 我们自己的 `NpcProjectile`（纯数学插值、零物理）。

> 这也解释了包里那个 `Only` 后缀的用途：
> `VFX_X_Projectile_Only` = **只有弹道部分** → 配自己的飞行和命中；
> `VFX_X_Projectile`（在 `_Complete/Projectiles/` 里）= **三合一**（发射+飞行+命中一次播完）→ 只适合原地放。

**2. 「不追踪」的正确实现不是「飞到一个点」**
第一版我写成"飞到起飞时锁定的那个点"，结果**玩家躲开了还是会打中旧位置** ✗
正确做法（现在是这个）：
```
每帧沿固定方向前进 → 判断「目标还在不在弹道上」（命中半径）
  ├── 在  → 命中
  └── 不在 → 继续飞 → 超过 最大飞行距离 → 消散
```

### 还没做的（等你后面做灵阵/法宝时补）

- **拦截层现在是 0（谁都拦不住）**。等灵阵 / 法宝做出来，把它们放到一个 Layer，
  再填进 `攻击方式[i].拦截层`，飞弹就会被自动拦下 + 消散
- **武器末端**现在只能用「右手 + 偏移」近似 —— 模型里**没有武器骨骼**
  （武器 `BLJ_xx_01` 是蒙皮网格挂在根上）。想精确要么让美术加一根武器末端骨骼，
  要么改 `取武器末端()` 用网格包围盒的尖端

---

## 11. 命中特效要放对（命中点 / 朝向 / 缩放）

三个容易错的点，都在 `NpcAiBase.在目标结算()` 里：

**1. 位置：用弹道的真实命中点，别用目标身上**
```csharp
Vector3 命中点 = 弹 != null ? 弹.命中点 : 目标点;
```
`NpcProjectile` 在**到达/被拦截时**把 `命中点` 记下来，回调里读。目标会动，直接放目标身上会飘。

**2. 但要往身上拉一点**
弹道是"进到 `命中半径`（默认 1 米）内"就判定命中的 —— 所以它的位置**离身上还差几十厘米**，爆炸会**悬在身前**。
```csharp
命中点 = Vector3.Lerp(弹.命中点, 目标判定点, 0.85f);   // 往身上拉 85%
```
实测：拉之前离判定点 0.89 米，拉之后 **0.14 米** ✅

**3. 朝向：迎着飞来的方向**
```csharp
Vector3 朝向 = -弹.飞行朝向;    // 爆炸朝施法者那侧张开
```

> ⚠️ **坑：`Quaternion.LookRotation(up, up)` 是退化调用**
> 前向和上方向平行时 Unity 会报错并返回 identity —— 特效朝向就变随机的。
> 竖直方向要换个 up 轴：
> ```csharp
> Quaternion 旋转 = Mathf.Abs(Vector3.Dot(前, Vector3.up)) > 0.999f
>     ? Quaternion.LookRotation(前, Vector3.forward)
>     : Quaternion.LookRotation(前, Vector3.up);
> ```

缩放用 `NpcAttackConfig.命中特效缩放`（`生成装饰特效` 返回 GameObject，调用方自己乘）。

**白鹿精的实际取值**（可以直接参考）：弹体 `0.65`（普攻）/ `1.1`（神通），
命中特效 `1.25`（普攻）/ `1.6`（神通）—— **弹体缩小、爆炸做大**，打击感靠命中特效撑。

---

## 12. 术语规范：伤害目标一律叫「锁定单位」

> 策划原话：*"以后有关伤害的统一以**锁定单位**作为目标 —— NPC 假如要打我，那就是 NPC 以我为锁定目标，这样比较规范。"*
> 原因：以后 **NPC 也可能作为召唤单位帮玩家打怪**，那时候"敌人"就不一定是玩家了。

**所以飞行路径 / 命中效果 / 消灭点 这些描述里，别再写"玩家"，写「锁定单位」。**

### 代码里的落点

`Assets/Scripts/Combat/ICombatTarget.cs` —— 接口 + 两个薄适配器：

| 适配器 | 包谁 | `受到攻击` 转发到 |
|---|---|---|
| `NpcTarget` | `NpcInstance` | 它自己的 `ReceiveAttack(攻击方属性, 规则)` |
| `PlayerTarget` | `PlayerVitals` | `CombatCalculator.Resolve(...)` → `PlayerVitals.受到伤害(...)` |

工厂：`CombatTargets.取(obj)` / `CombatTargets.从(go)`。

**故意不改 `NpcInstance`/`PlayerVitals` 内部**，适配层很薄，风险最低。

### 谁是谁的锁定单位

| 场景 | 锁定单位 |
|---|---|
| 玩家普攻 | 被锁定/选中的那只 **NPC** |
| NPC 打玩家 | **玩家** |
| NPC 被收服成召唤物后打怪 | 它**锁定的那只怪** |

### 代码里的两个入口

- **玩家侧**：`BasicRemoteAttack01.锁定单位` ← `NpcTargeting.LockedNpc`
- **NPC 侧**：`NpcAiBase.锁定单位`（默认 = 玩家）。**被收服成召唤物时，覆盖这个属性**让它去锁怪 ——
  飞弹瞄准 / 追踪目标 / 命中结算 / 命中特效 全都走它，**不用改别的**

## 13. 白鹿精 vs 太虚炼气诀：两种飞弹的差别

| | 白鹿精（`basic_remoteattack_01` 以外）| 太虚炼气诀普攻（`basic_remoteattack_01`）|
|---|---|---|
| 飞行 | **直线匀速、不追踪** | **持续追踪**（限速转向）|
| 结束 | 飞到锁定点 / 超 22 米消散 | 命中 / 超出**神识范围** / 超 **8 秒** |
| 出生点 | 左手骨骼末端（03 是右手近似武器末端）| **右手骨骼末端** |
| 角色动画 | 无（NPC 走 Attack1）| **播玩家攻击动作，40% 处出弹** |
| 冷却 | 3s（03 神通 8s）| 3s，**被攻速缩放** |

代码侧就是 `NpcProjectile` 的两个模式：`设置直线飞行()` vs `设置追踪飞行()`。