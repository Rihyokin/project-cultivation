# 村庄场景生成说明（`village.scene`）

> **这份文档给谁看**：下一个要动村庄场景的 AI / 人。
> 它是一份**操作手册**（怎么生成、每个数字在哪、怎么改、怎么验收）；
> 踩过的坑与"为什么不能那样做"在 `开发注意事项.md` **§46**（那份是错题本，两篇配套看）。

---

## 0. 一句话

菜单 **`Cultivation / Build Village Nature (v4)`**
（`Assets/Editor/Builders/VillageNatureBuilder.cs`）**幂等重建**整个村庄环境：

```
地面(Terrain) · 村路 · 河流 · 农田 · 森林 · 院墙 · 院门 · 草丛
```

用户**手摆的 25 个建筑不归它管** —— 生成器只负责"绕着它们走"。
重建时会先 `DestroyImmediate` 旧根节点 `村庄环境`，并把 `Assets/Art/VillageTerrain/` 整个删掉重建，
所以**可以随时重跑**（不会重复堆叠），但**不要**把手工调整做在这个根节点里面（会被冲掉）✗。

**它还顺手删掉场景里的 `S1_chengmen01_tf`**（旧城门）—— 那个位置换成了自己生成的院门。

---

## 1. 现状速查（改之前先看这张表）

| 项 | 值 |
|---|---|
| 地形 | `200 × 200 m`，左下角 `(-115, -90)`，高 24m 基准下沉 6m；高度图 **1025²**、地表 **1024²**、细节 **256²** |
| 地形图层 | 4 层：草地 `grass009` / 石板 `dimian001` / 土路 `dirt036` / 沙石 `dirt022` |
| 地形草 | 6 种 `Environment/.../Textures/Grass/Grass0xxwx.tga`（带 alpha 的植物 billboard）|
| 河流 | 中心线 11 点折线，半宽 4.6m，河深 1.7m，水面 y = **−0.55**（刻进地形，水面是独立网格）|
| 村路 | 5 条**中心线**（不铺网格），全部刷在地表材质里 |
| 农田 | 中心 `(-68, -3)`，整体**顺时针斜 14°**；2 列 × 3 行台地，台高 0.35~1.05 |
| 森林 | 4 片矩形林区，树按"目标高 3~11m"缩放，共 ~230 棵 + ~360 丛灌木 |
| 院墙 | 自己生成，围 `X[-18,20] Z[33,72]`，厚 0.45m 净高 2.45m，两坡瓦压顶 + 沿线墙垛 23 + 转角垛 4 |
| 院门 | 自己生成，位置 `(-1.7, 33)`，洞宽 3m |
| 碰撞体 | 手摆的 24 个物件各补了 BoxCollider（共 102 个，取**本地包围盒**）；空气墙 17 段（高 16m、无渲染器）|
| 生成资产 | `Assets/Art/VillageTerrain/`（地形 + 图层 + 材质 + 河面/垄面网格），**约 8 MB** |
| 场景 | `Assets/Scenes/village.scene`，已在 Build Settings，玩家出生点 `(-18, 0.4, -30)`（村口路面）|

---

## 2. ★ 地面：Terrain + 材质（这套方案的核心）

### 2.1 为什么用 Unity Terrain，而不是一块平面

四件事只有 Terrain 能一次给全：

1. **真实起伏**（村外缓缓起坡 + 外围一圈矮丘，视野有边界感）；
2. **分层混合的地表**（草/石板/土/沙按权重混，路和河岸是"混"出来的不是"贴"出来的）；
3. **便宜的草**：地形细节层（detail）用 billboard 撒草，比摆模型省几个数量级；
4. **真正的地面碰撞**（`TerrainCollider`）—— `village.scene` 原来**一块地面都没有**，玩家会直接掉下去。

### 2.2 尺寸与分辨率（**2ⁿ+1 是硬约束**）

```csharp
const float 地形X0 = -115f, 地形Z0 = -90f, 地形边长 = 200f;
const float 地形高 = 24f;            // 高度图 0..1 对应 24m
const float 地形基准 = 地形高 * 0.25f; // = 6m，见 2.6
const int 高度分辨率 = 1025;          // ★ 必须是 2ⁿ+1
const int 贴图分辨率 = 1024;          // ★ 必须是 2ⁿ
const int 细节分辨率 = 256;           // ★ 必须是 2ⁿ
```

> ★★ **`heightmapResolution` 写成 641（不是 2ⁿ+1）时，Unity 会自己 snap 成 1025**，
> 而你的 `float[641,641]` 数组跟 terrainData 对不上 → **`SetHeights` 静默失败** →
> 整张高度图全 0 → 地形整体落到 `y = -6`，**整个村子沉到地下** ✗（2026-09-26 实际踩过）
>
> 所以 `建地形()` 里**一律回读实际分辨率再算数组**：
> ```csharp
> int 实高图 = 数据.heightmapResolution;      // 回读！
> 数据.SetHeights(0, 0, 算高度(实高图));      // 用实际值算
> ```
> 地表/细节同理各有一行告警。

### 2.3 高度：**五步顺序不能换**（`算高度()`）

逐格算，顺序有讲究：

| 步 | 做什么 | 为什么在这个位置 |
|---|---|---|
| ① | 起伏 `噪声(x,z) × 5.0 × 起伏权` + 外围矮丘 `远² × 7.5` | 基础地貌 |
| ② | **村内压平**：`h *= SmoothStep(InverseLerp(2,10,离村))` | 手摆的建筑都在 y=0，村内必须恒 0；村内矩形 = `村内 = Rect(-52,-52,86,124)`（X[-52,34] Z[-52,72]）|
| ③ | **村路压平**：`h = Lerp(h, 0, 路权)`（6m → 1.5m 过渡）| 土路要能走，不能有坡 |
| ④ | **农田台地**：按"到田块矩形的距离"插值到 `台高` | 梯田 |
| ⑤ | **河道最后刻**：`h -= 河深 × k` | ★ 放前面会被②③乘掉/压平 —— 桥和水车都在村里，结果**整段河床是平的、水面被地面盖住，俯视根本看不到河** ✗ |

> 一句话记住：**②③④ 是"压平"，⑤ 是"开凿" —— 开凿必须最后**。

### 2.4 地表分层（`算地表()`：4 层 + 权重合成）

| 层 | 贴图 | 平铺 | 用在哪 |
|---|---|---|---|
| 0 草地 | `Dibiao/grass009.bmp` | 7m | 底子（默认）|
| 1 石板 | `Dibiao/dimian001.bmp` | 5m | 大殿院子（方正）+ **村内铺装**（噪声圆角边界，半径 15~23m）|
| 2 土 | `Dibiao/dirt036.bmp` | 6m | **村路磨痕** + 田块土 + 河岸过渡 |
| 3 沙石 | `Dibiao/dirt022.bmp` | 7m | 河岸河床（贴着河，`河半宽 + 3.2m` 内）|

合成规则（顺序有意义）：

```csharp
土   = max(土, 沙 * 0.45f);
石板 *= 1f - Clamp01(土);     // ★ 有土的地方就不铺石板 —— 路是"磨穿"铺装的
石板 *= 1f - 沙;
草   = Clamp01(1f - max(沙, max(土, 石板)));
归一化 → [草, 石板, 土, 沙]
```

### 2.5 草的密度是按"格"算的（**最容易烧显卡的一处**）

`SetDetailLayer` 每格存的是**株数**（整数）。地形 200m / 细节 256 → **一格 0.78m（0.61 m²）**：
**写 1 就是 1.6 株/m²**。第一版按"感觉"写 26 → 一格 26 株 ≈ 42 株/m² ✗ 百万级 billboard。

现在：**每一格用哈希选一种草**刷 1~3 株（6 种原型**不能**同一格全刷，那是 6 倍密度），
并按位置调密度：

| 位置 | 株数 |
|---|---|
| 院子 | 0 |
| 村里 | 1 |
| 路面上（`离路 < 2.2m`）| 0 |
| 田里 | 0 |
| 河边（`河半宽+0.8` 内）| 0；（`+4m` 内）3 |
| 其他 | 2 |

顺序：`SetDetailResolution(256, 8)` → `detailPrototypes = ...` → `SetDetailLayer(...)`（这个顺序不能反）。

### 2.6 高度基准：Terrain 的 y 是**负的**

Unity 高度图存不下负高度，而河床要挖到 −1.7m，所以：

```csharp
高[j,i] = Clamp01((h + 地形基准) / 地形高);      // 高度图里 0.25 ↔ 世界 y=0
go.transform.position = new Vector3(地形X0, -地形基准, 地形Z0);   // ★ 地形整体下沉 6m
```

**忘了下沉 → 地面整体抬 6 米，建筑全埋进去** ✗。
需要世界高度时一律用工具函数：

```csharp
static float 地形高度(Vector2 世界XZ);   // = 地形.transform.position.y + GetInterpolatedHeight(u,v)
```

---

## 3. 村路：在**地面材质**里"磨"出来（不要另铺路面网格）

走过两段弯路（详见 `开发注意事项.md` §46.6）：**刷地形→太糊**、**铺网格→太假**。
定稿是刷地形，但把"像贴上去的"这几点治掉：

```csharp
float 半宽 = 1.2f + 路噪(wx, wz) * 1.7f;        // 2.4~4.2m，低频噪声 → 边缘参差
float 土权 = SmoothStep(InverseLerp(半宽 + 2.6f, 半宽 * 0.3f, 离路));
土权 *= 0.55f + 0.45f * 磨损(wx, wz);           // 中频噪声 → 有的地方踩亮了、有的还剩草
土 = max(土, Clamp01(土权) * 0.92f);            // 上限 0.92 → 底下透出来
```

* `路噪(x,z)` = 低频 Perlin（0~1，波长几十米）：**路宽摆动 + 村内铺装的圆角边界**
* `磨损(x,z)` = 中频 Perlin（0~1）：**踩踏的深浅**
* ⚠️ **别再往"明显"调**：把磨损抬到 0.72 / 让草沿路让开 4m，就又变回"贴上去的条带" ✗（用户明确否过）

**路线**（`路` 数组，只存中心线）：

| # | 路线 | 备注 |
|---|---|---|
| 1 | **主路**：`S1_caowu02_tf`（村门口 (-19,-40)）→ 贴村西侧绕 → **院门 (-1.7,33)** | 用户指定。**必须贴西侧绕**：`S1_fangzi0x_zm` 那几栋连成一片，直着走会穿房子 ✗ |
| 2 | 东西街：`(48,13) → (10,11) → (-0.5,10) → (-74,22)` | 东边出村 + 过桥去农田，同一条街 |
| 3 | 村南环西路：`(-7,-46) → (-33,-18) → (-12,12.5)` | 接村门口 |
| 4 | 支路①：主路 → `S1_fangwu01_tf`（(-23,4) 东门口）| 用户指定 |
| 5 | 支路②：主路 → `S1_fangwu03_tf`（(-24,27) 东门口）| 用户指定 |
| 6 | 田埂小路：由**田块局部坐标**换算（跟着田一起斜）| 走两列田之间 |

---

## 4. 农田：局部坐标 + 整体旋转 + 台地

### 4.1 位置与倾斜（都是用户圈/指过的）

```csharp
static readonly Vector2 田心 = new Vector2(-68f, -3f);   // 整片田的旋转中心（世界 XZ）
const float 田角 = 14f;                                   // 顺时针（俯视）倾斜
```

> 用户 2026-09-26：「农田的位置也有问题，不是在这里，而是在这里」（Scene 视图里圈了块地）
> → 「我是想稍微让他们往右侧倾斜一点点」→ 他画的框本身就是斜的（右上角低 9m/36m ≈ 14°）。

### 4.2 关键做法：**田块存的是局部坐标**

```csharp
struct 田块 { float X0, Z0, X1, Z1, 台高; }     // ★ 相对 田心 的局部坐标，不是世界坐标！
static readonly 田块[] 田 = {
    new 田块(-18f, -19f,  -2f,  -7f, 1.05f), new 田块( 1f, -19f, 18f,  -7f, 0.85f),
    new 田块(-18f,  -4f,  -2f,   8f, 0.80f), new 田块( 1f,  -4f, 18f,   8f, 0.60f),
    new 田块(-18f,  11f,  -2f,  19f, 0.55f), new 田块( 1f,  11f, 18f,  19f, 0.35f),
};
```

两个换算函数（**所有"是不是在田里"的判断都要先转局部**）：

```csharp
static Vector2 到田局部(Vector2 世界);      // 绕 田心 反向转 田角
static Vector2 田局部到世界(Vector2 局部);  // 正向转
```

用到它们的地方（改田的位置/角度时**只需要改 `田心` 和 `田角`，下面全自动跟着**）：
`算高度()` 的台地、`算地表()` 的田土、`在田里()`（树/灌木避让）、田埂小路、`建稻田()`。

### 4.3 田里的东西（`建稻田()`）

**整片田挂在一个转了 `田角` 的父节点下**，子物体全用局部坐标摆 —— 田埂/稻垄/田水自动跟着斜，不用逐个算旋转：

```csharp
组.position = new Vector3(田心.x, 0f, 田心.y);
组.rotation = Quaternion.Euler(0f, 田角, 0f);
// 子物体：局部 (x,z) → 世界采地形高度 → 回填 y
Vector3 局部到世界点(float lx, float lz);
```

| 元素 | 做法 |
|---|---|
| 田埂 | 每块 4 条细长 Cube（土材质 `dirt036`），比田面高 0.12m |
| 稻垄 | 每块沿**局部 X** 一条条，间距 0.9m，垄面抬高 0.30m；共用 `垄面.asset`（单位四边形）+ `localScale` 拉伸，**不要每行 new Mesh**（会撑大 .scene）|
| 稻材质 | `Grass064wx.tga` 走 **cutout（AlphaTest）**，`mainTextureScale = (42,1)` → 看着是一行行稻子 |
| 田水 | 一块半透明 Quad（**纯色、不带贴图**！带 `terrain_water` 那张偏白贴图会把整片田提亮成白板 ✗）|

---

## 5. 河流

* 中心线 `河`（11 点）：北 → 西南，**必须穿过石桥 `S1_qiao01_tf`(-37,17) 和水车 (-39,-21)** —— 走向是"以现有建筑为准"反推的；
* 河道**刻进地形**（`河深 1.7m`，`河半宽 4.6m`，外侧 3.5m 斜坡）；
* 水面：沿中心线铺一条**带状网格**（`河面.asset`），y = −0.55；
  * ★ **法线显式写 `Vector3.up`**（程序拼的带子绕序不可靠，`RecalculateNormals` 一反过来就从上方看不见）；
  * 材质：`Standard` 半透明、**`_Cull = 0`（双面）**、`glossiness 0.35`（高光太高整条河一片死白 ✗）；
* 河岸：69 块 `Environment2/Stone/*` 石头沿两岸随机撒（y 压在水面下 0.2m）。

---

## 6. 院墙 / 院门：**自己生成** + 复用 S1 材质

> 用户：「院墙效果还不错，但太规整了，而且风格不相符……干脆把 `S1_chengmen01_tf` 删了，自己做一个院门，风格统一一下」

* **不能用 `S1_qiangmian01_zm.prefab`**：它的**根节点自带旋转**，用 `LookRotation(走向)` 会覆盖掉 → 墙横 90° ✗；而且厚 2.14m。
* **风格统一靠材质**（几何自己生成）。材质清单（都在 `Assets/Environment/Models/Materials/Scene/Materials/`）：

| 用在哪 | 材质 |
|---|---|
| 墙身 / 墙基 | `S1_chengmen01e_tf`（素净石墙，可平铺）|
| 墙垛 / 门垛 / 转角垛 | `S1_qiangmian01_zm_001`（带雕饰石墙面）|
| 压顶 / 门顶 / 垛帽 | `S1_jianzhu01_zm_001`（**大殿那套深青瓦**）|
| 过梁 / 门匾 | `S1_chengmen01b_tf`（深色木梁）|
| 门扇 | `S1_jianzhu01_zm_015`（带竖排字的木门板）|
| 台阶 / 门槛 | `S1_jianzhu01_zm_013`（石阶）|

* **"不那么规整"靠三件事**：① 压顶是**两坡瓦顶 + 正脊**（`建瓦顶()`：两个绕走向滚转 ±倾角 的盒子 + 一根脊）；② 沿线每 **5~7m** 一个墙垛（间距带随机）；③ 折点处再加转角垛。
* **贴图平铺的坑**：立方体每面 UV 都是 0..1，长条盒子会把整张贴图**拉成一条** ✗
  → `建盒子(..., 平铺米每格, 只横铺)` 用 **`MaterialPropertyBlock`** 设 `_MainTex_ST`（别 `new Material`，会造一堆材质实例）。
* **院门**（`建院门()`，位置 `院门 = (-1.7, 33)`）：门垛 1.1×3.0（带基座+瓦帽）→ 木过梁 → 门匾 → 两坡瓦顶（洞宽 3m、出檐 1.6m）→ 两扇木门（留 6cm 门缝）→ 门槛 + 两级踏步（朝南边村路）。

---

## 7. 植被：**按"目标高度"缩放**

各资源原生大小差 3 倍以上（`S1_shu001` 原生 **24.4m**、`guanmu` 只有 1.8m、`mogu_03` 有 **9.4m**），
所以**不要写缩放系数**，一律：

```csharp
go.transform.localScale = Vector3.one * (目标高 / 预制高(prefab));
// 预制高()：把临时实例的 scale 归 1 再量 Renderer 包围盒（结果缓存）
```

* 树 16 种（`Environment2/Tree/S1_shu00x` + `Assets/Environment/Tree/*.FBX`），目标高 **3~11m**；
  * 已排除黄叶/枯树/红叶那几种（用户参考图画的是**绿林**）；要秋景再加回来；
* 灌木/花草 11 种（`guanmu_*`、`YYZmantuoluo001`、`Env1/Grass/*`），目标高 0.4~1.7m；
* **不放蘑菇**（用户明确讨厌 `Mogu/*` 那套粉色巨伞 ✗）；
* 撒法：每片林区撒若干"丛心"，围着丛心正态散布（比全场均匀自然）；`可放()` 负责避让河/路/田/村/院 + 最小间距；
* 树补 `CapsuleCollider`（原地没有碰撞体）。

---

## 8. 配置速查（改这些地方就够）

| 想改什么 | 改哪里 |
|---|---|
| 地形大小/精度 | `地形X0/Z0/地形边长/地形高/高度分辨率/贴图分辨率/细节分辨率`（注意 2ⁿ+1、2ⁿ 约束）|
| 村内压平范围 | `村内`（Rect）|
| 地面植被/坡度 | `算高度()` 第①步的系数、外围矮丘 |
| 地面材质配比 | `算地表()` 的各层权重 + `建图层们()` 的贴图/平铺 |
| 草的密度 | `刷草()` 里的 `株` |
| 村路走线 | `路`（Vector2 数组，中心线，世界坐标）|
| 路的宽窄/深浅 | `算地表()` 里 `半宽`、`磨损` 系数、`0.92` 上限 |
| 河 | `河`、`河半宽`、`河深`、`水面高` |
| 农田位置/角度 | **`田心` + `田角`**（其他全自动）|
| 田块大小/台高 | `田`（⚠️ **局部坐标**）|
| 林区 | `林区`（Rect）|
| 树种/目标高 | `撒树()` 的 `树种` |
| 院墙尺寸 | `建院墙()` 表头的常量（厚/净高/基高）|
| 院门 | `院门` 位置 + `建院门()` 表头的常量 |

---

## 9. 怎么改 & 怎么验收

### 9.1 改代码的**铁律**

1. 改完 `.cs` **先 `unity_editor.refresh`（等编译）再跑菜单** —— 直接执行会跑**上一版程序集**；
   而且**热重载是异步的**：`edit` 完立刻执行有时赶在编译前（本次吃过一次：河道改完"没生效"）。
2. 生成器**幂等**，改完重跑即可；**别手工改 `村庄环境` 里的东西**（重跑会被删）。
3. 需要"落地位置"时用 `地形高度(世界XZ)`，**不要假定 y=0**。

### 9.2 渲图验收（本项目的标准手法）

在编辑器里临时建一台相机 `cam.Render()` 到 `RenderTexture` → 存 PNG 到 `cultivation/screenshots/`，
然后**看图**。三种常用机位：

| 用途 | 机位 |
|---|---|
| **顶视 + 网格**（对用户手绘图最有用）| `(中心x, 200, 中心z)` 看正下方，正交 `orthographicSize = 半高`；再在 PNG 上画 10m 格线 + 原点红线 |
| 鸟瞰 | 东南上方 `(95, 70, -95)` 看 `(-8, 2, 16)`，FOV 40 |
| 平视 | 站在路上/村口，FOV 45~55 |

**世界 ↔ 像素的换算**（顶视正交时必须先写下来，否则没法对照）：

```
世界X = 机位.x - 半高 + 像素x / 图宽 * (半高*2)
世界Z = 机位.z - 半高 + 像素y / 图高 * (半高*2)     // 图像 y 从下往上
（Unity 相机 up = 世界 +Z 时：图上 = +Z、图右 = +X）
```

> 想从**用户给的截图**反推世界坐标：找两个已知物体（例如石桥、某栋建筑）当基准做线性映射，
> 注意 2.5D 透视会让远处的尺度变小，只能当粗估。

### 9.3 平面视（Play Mode）验收 UI/主角

`ScreenCapture.CaptureScreenshot(路径)` 能抓到**含 ScreenSpaceOverlay 的 HUD** 的整幅游戏画面
（用 `unity_screenshot` 的 `capture_main_camera` 抓不到 overlay canvas ✗）。
注意村庄场景的 `PlayerStatsDebugPanel` **默认是开的**（F1 开关），截图前先 `enabled = false`，否则盖住画面。

---

## 10. 碰撞体与空气墙（2026-09-26 补）

### 10.1 给场景里已有的物件补碰撞体（`补场景碰撞体()`）

用户手摆的 **24 个建筑/道具原来一个碰撞体都没有**（只有石桥自带 2 个 MeshCollider），玩家能直接穿过去 ✗。
现在 `Build Village Nature (v4)` 里会顺手补上：**每个渲染器加一个 `BoxCollider`**，取它自己的
**本地包围盒**（`mesh.bounds` / `smr.localBounds` / `renderer.localBounds`）。

> ★ 不能用"整个物体的世界 AABB"：这些建筑不少转了角度，AABB 会肥出一大圈
> （斜着的 `S1_fangzi02_zm` 世界 AABB 有 18×19m，实际才 10×12m → 会挡掉半条街）✗

幂等：自己那层已经有 `Collider` 就跳过。实测新加 **102 个 BoxCollider**（跳过 2 个已有）。

### 10.2 空气墙（`建空气墙()` + `空气墙轮廓`）

照 3C_Testbed 里 `Boundary` 的套路：**只有 `BoxCollider`、没有渲染器**（不可见但挡人）。
比 `Boundary` 省事的地方：**每条边只放一个拉长的盒子**（边走直线，一段就够，不用每 1.5m 一段）。

| 项 | 值 |
|---|---|
| 轮廓 | `空气墙轮廓`（17 点闭合折线，世界 XZ，顺时针）|
| 厚度/高度 | 厚 1.0m，**高 16m**（盒子 y −2~14）—— `御风 2.4m`、`坐骑最高 5.64m` 都拦得住 |
| 挂在哪 | `村庄环境/空气墙/空气墙段_00..16`（跟环境一起重建）|
| 实测 | 周长 358m；从村中心朝东打射线，y=10/13 命中 `空气墙段_05` @59.5m、y≥15 打得出去 ✓ |

**轮廓的形状 = 用户画的黄圈**：把**村子本体 + 大殿院子 + 东边那片林子**围起来；
**农田 / 石桥 / 水车 / 河道上游留在墙外**（和图上一致）。
> ⚠️ 也就是说**现在走不到农田和石桥**（它们在墙外）。要让它们可达：把 `空气墙轮廓` 的西半边往西扩
> （或在那几段留缺口），改一串坐标重跑菜单即可。

### 10.3 ★ 补了碰撞体之后的连带效果：飞过房子会被"抬"到屋顶

`GroundProbe.向下取地面()` 会取**脚下最高的那个碰撞体**。以前建筑没有碰撞体 → 探到的都是地形；
现在有屋顶了 → **御风/坐骑飞过房子时，"地面"变成屋顶，角色会被抬到「屋顶 + 高度」**。

* 如果这就是你要的（不能穿房、飞过时贴着屋顶走）→ 不用动；
* 如果不要这个行为（希望飞行只看**地形**）→ 两步：
  1. 把建筑（或只把它们的碰撞体所在物体）放到 **layer 2 `Ignore Raycast`**；
  2. `GroundProbe` 里的射线 mask 从 `~0` 改成 `Physics.DefaultRaycastLayers`（= 不含 layer 2）。
  这样**碰撞照旧**（layer 只影响查询 mask，不影响物理碰撞），但地面探测不再被建筑挡住 ✓

---

## 11. 场景装配现状 / 已知问题

| 项 | 状态 |
|---|---|
| 手摆建筑 | 25 个（用户摆的），生成器只避让、不管理 |
| Player / Main Camera / EventSystem / HudCanvas / CharacterUI / 暂停菜单 / SpawnPoint | **已同步**（`SceneRigSyncer`，2026-09-26 起 `village.scene` 已加进它的 `游玩场景` 表）|
| Build Settings | 已加入（`village.scene`）|
| 玩家出生点 | `(-18, 0.4, -30)`（村口路面，朝村里）|
| `UIPanelData` | 神通 11 / 坐骑 9，和其它场景一致 ✓ |
| **F1 调试面板** | 默认**关闭**（`PlayerStatsDebugPanel.启动时显示 = false`），F1 开合 |
| **UI 画布（HudCanvas / CharacterUI）** | 编辑器里**默认不显示**（`Canvas.enabled = false`，见 `开发注意事项.md` §47），运行时由 `UICanvasBoot.Awake()` 自动打开；想在编辑器里看一眼用菜单 **修仙 / 编辑器里显示或隐藏 UI 画布** |
| 水面材质 | `Standard` 透明 + 双面；离远了看不出流动（要动效得换 shader 或加 UV 滚动）|
| 场景边缘 | 200m 之外是空的（外围矮丘只能弱化边界感）|
| 相机 | 跟随脚本在（`TopDownCamera`），但**村庄场景的滚轮调参**是同步时保留的本场景值 |

---

## 12. 相关文件

| 文件 | 作用 |
|---|---|
| `Assets/Editor/Builders/VillageNatureBuilder.cs` | **本套生成器的全部实现**（唯一入口）|
| `Assets/Art/VillageTerrain/` | 生成资产：`village_terrain.asset`、4 个 `.terrainlayer`、水/墙/门材质、`河面.asset`、`垄面.asset` |
| `Assets/Editor/Tools/SceneRigSyncer.cs` | 通用角色/UI 同步（`village.scene` 已在表内）|
| `开发注意事项.md` **§46** | 这一套的**踩坑记录**（地形基准、河道顺序、分辨率 snap、路面弯路…）|
| `screenshots/village_hud.png` | Play Mode 实机画面（HUD + 主角）|
| `screenshots/f2_tilt.png` / `f2_aerial.png` | 农田倾斜后的俯视 / 全村鸟瞰 |
