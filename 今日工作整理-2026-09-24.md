# 今日工作整理 · 2026-09-23 深夜 ~ 09-24 凌晨

> 这份是**给人看的摘要**：这次一口气做了 31 个提交，这里按主题把它们串起来，
> 附"实测证据 / 遗留 / 怎么自己验一遍"。
> **踩坑的技术细节仍在 `开发注意事项.md`**（§34~§41），本文只讲"做了什么、现在什么状态"。
>
> 涉及场景：`3C_Testbed`、`Sect`、`Demon-Suppressing Tower`（镇妖塔）；
> 仓库：`github.com/Rihyokin/project-cultivation`。

---

## 0. 一屏概览：这次做了 6 块

| # | 主题 | 结果 | 主要文件 |
|---|---|---|---|
| 1 | **坐骑系统收尾**（9 只坐骑的摆位 / 动作 / 朝向） | 每只坐骑一条配置，能骑、能跑、能贴地 | `MountRider.cs`、`坐骑表.csv` |
| 2 | **镇妖塔内景**（新场景内容） | 八面墙 + 连续螺旋楼梯 + 八角环楼板，塔内可走上去 | `Demon-Suppressing Tower.scene`、`ZhenYaoTa.prefab` |
| 3 | **相机与遮挡表现** | 塔里滚轮抬高 ×1.5；建筑透明 / 玩家描边变成**一张表**决定哪个场景生效 | `TopDownCameraZoom`、`OcclusionTransparency/Outline`、`场景特效开关` |
| 4 | **通用装配同步**（约定 + 工具） | 一条菜单把 UI / 角色 / 相机铺到三个游玩场景 | `SceneRigSyncer.cs` |
| 5 | **传送系统** | 两对场景间传送（3C↔Sect、Sect↔塔）+ 塔内「出塔 / 下一层（占位）」 | `Teleporter.cs` + 场景接线 |
| 6 | **坐骑页 3D 预览** | 不再横向拉伸、9 只都在播 idle | `UIMountPage.cs` |

---

## 1. 坐骑系统（9 只）

* **摆位改成"每只坐骑一条"**（`MountRider.坐骑摆位表`）：偏移 / 朝向 / 缩放 / 朝向跟随方式 /
  跟随速度 / 骑乘高度 / 是否贴地 —— 全部一只一配，删掉了全局偏移那套。
  * 地面坐骑（狼骑、灵鹿、熊猫）：`贴地=true`，`偏移.y = −骑乘高度`，跑起来跟着起伏的地面走
  * 飞行坐骑（灵翅、巨龙、上古神龙）：`贴地=false`
  * **朝向跟随三种**：`分离`（坐骑朝移动方向、玩家朝锁定方向）/ `硬跟玩家`（翅膀）/ `平滑跟玩家`（神龙 45°/s）
* **动作表**（`坐骑动作表`）：待机 / 跑动 / 出生 / 消失，各有动作名和**速度**；
  支持"一个动作兼四个阶段、只改速度"；下坐骑播 `Die`（不叫 `LeveUp`）。
* **灵鹤**（原「战骑」`mount_zhanqi_01`）：改名 + 套用碧水兽那套 + 用代码现做 idle/run。
* **巨龙**：加 90° X 旋转修正模型轴向。
* 上/下坐骑与御风**双向互斥**；下坐骑时坐骑的 `CapsuleCollider` 必须全关
  （不然骑手会被顶飞 —— 实测弹到 33m 高）。

> ⚠️ `坐骑表.csv` 改动要跑一次菜单 **修仙 / 从配置表生成资产** 才生效。
> 灵鹤的**介绍文案**目前还是旧的「战阵之骑。披甲衔枚，与主人同进退。」（未改）。

## 2. 镇妖塔内景（`Demon-Suppressing Tower`）

* 八面墙（127 段）+ **连续螺旋楼梯**（半径 10.4~13.4，每级升 0.250m，共 123 级、约 400°，"螺旋升天"）。
* 楼板 / 顶盖换成**真正的正八边形环网格**（程序化生成 128 顶点 / 64 面，内外边界按内切半径 + cos22.5°），
  结构上不再互相重合，碰撞体用 `MeshCollider`。
* 楼梯板朝向修正：板子的 3.0m 边必须**沿半径**（`localRotation = Euler(0, atan2(x,z)+90, 0)`），
  原来少转 90° 时走道只有 0.78m 宽。
* **关键坐标**：塔内地板面 **y = 4.94**（整塔悬空），玩家 / SpawnPoint 摆在 **(0, 4.99, −9)**；
  新加玩家一定要摆到地板面上，否则掉下去。
* 相机：塔里滚轮抬高上限 **×1.5**（`softMax 1.95 / hardMax 2.1`）。

## 3. 遮挡表现 & 场景特效开关表

| 特效 | 组件挂在哪 | 生效场景 |
|---|---|---|
| **建筑透明**（相机↔玩家连线上的建筑变透明） | `Main Camera` | **镇妖塔** |
| **玩家描边**（被挡住时给玩家描边） | `Player` | **Sect** |

两者原来靠"哪个场景挂组件"控制，但通用装配同步会覆盖 Player / Main Camera → 组件被冲掉
（Sect 的描边就是这么丢过一次）。**现在改成一张表**：`Assets/resources/场景特效开关.asset`，
组件铺到所有游玩场景、`Awake` 里查表决定自己关不关。
**以后要把某个特效挪到另一个场景，只改这张表，不要动场景里的组件。**

## 4. 通用装配同步（约定 + 工具）

菜单 **修仙 / 同步通用角色与UI到所有游玩场景**（`Assets/Editor/Tools/SceneRigSyncer.cs`）：

* 源场景 `3C_Testbed` → 目标 `3C_Testbed` / `Sect` / `Demon-Suppressing Tower`
* **总是覆盖**：`Player` `Main Camera` `EventSystem` `HudCanvas` `CharacterUI` `暂停菜单`
* 只在缺失时补：`SpawnPoint`；平行光不同步
* **覆盖时保留各场景自己的位置**（Sect 玩家 (−83.6,18.1,89.9)、塔里 (0,4.99,−9)）
* **保留各场景的相机调参**（塔的 ×1.5 靠这条）
* 特效开关不再按场景搬（表接管了）

实测三场景 UI 完全一致：`HudCanvas 64 / CharacterUI 628 / 坐骑 9 只`。

## 5. 传送系统

| 场景 | 光圈 | → 去向 |
|---|---|---|
| 3C_Testbed | `3ctestbed to sect1` (−7.96, 0, −11.28) | Sect 的 `sect1 to 3c` |
| Sect | `sect1 to 3c` (−18.23, −0.02, 27.54) | 3C 的 `3ctestbed to sect1` |
| Sect | `sect2 to tower` (−88.33, 14.21, 91.53) | 塔的 `tower to sect2` |
| 塔 | `tower to sect2` (0.30, 5.03, 9.04) | ①「进入下一层」**暂未开放** ②「出塔」→ Sect 的 `sect2 to tower` |

* 走进光圈 → 弹「是否传送？」（确认框是代码现建的，不需要美术资产）
* **落点就是对面那个传送点本身**（旁边那些 `TeleportTarget_*` 标记物已全部删除）
* 跨场景传送交给 `DontDestroyOnLoad` 的宿主执行 —— 第一版用光圈自己的协程，
  被 `LoadSceneMode.Single` 连组件一起销毁，**玩家永远留在新场景的存档位置**（差 91m）；
  改完实测落点距离 **0.000m** ✓
* 已知行为：落在对面光圈上会**立刻再弹一次确认框**（按取消即可）

## 6. 坐骑页 3D 预览（最后一件事）

用户报「模型被横向拉伸 + 都没挂 idle 动画」。**四个独立原因**：

1. `RenderTexture` 写死 512×512，而预览图矩形是 **1190×331** → 横向拉宽 3.6 倍
   → 改成按预览图矩形比例建贴图（长边取 max(512, 矩形长边)，`Update` 里复核）
2. 9 只里有 **6 只** prefab 挂着**旧版 `Animation` 组件** → Mecanim 不绑 clip、骨架一根不动
   → `Instantiate` 后销毁它
3. 面板打开时 `Time.timeScale = 0`，而 Animator 默认吃缩放时间 → 姿势冻住
   → 预览实例改 `AnimatorUpdateMode.UnscaledTime`
4. `NpcAnimator` 要等自己的 `Start` 才就绪，`Instantiate` 当帧 `PlayAction` 返回 false
   → 每帧重试

**实测**：`RT = 1190×331 = 预览图`；畸变纵横比 **0.9998**；9 只全部在动
（灵翅 19/29 骨骼、狼骑 28/52、碧水兽 25/41、灵鹤 14/47、巨龙 39/83、上古神龙 234/274）。
录屏：`cultivation/screenshots/mount_page_all9.mp4`（9 只巡览 13 秒）。

> 预览区是 1190×331 的**很宽**画幅，模型按高度取景，所以看起来比"被拉宽时"**窄** —— 这才是正常比例。

---

## 7. 遗留 / 未做 / 已知行为

| 项 | 状态 |
|---|---|
| 塔内「进入下一层」 | **占位**（`暂未开放`），等第二层盖好再接 |
| 传送落到光圈上会立刻再弹框 | 已知行为，可选修：加 1.5s 静默 / 落点挪出 2m |
| 灵鹤的介绍文案 | 还是旧的「战阵之骑…」 |
| Sect 相机上的 `OcclusionTransparency` | 暂缺（表里 Sect 本来就不生效）；跑一次同步会带上 |
| UI 里的坐骑传送选择器 | **用户明确推迟**（「算了，这是个大工程」）|
| 玩家描边的**视觉**效果 | 只验到"组件在跑、副本已建并启用"，没做过"被挡住时肉眼看见描边"的抓帧 |

## 8. 怎么自己验一遍

| 想验什么 | 怎么做 |
|---|---|
| 三场景装配是否一致 | 菜单 **修仙 / 同步通用角色与UI到所有游玩场景**（会打印每个根的"覆盖/补"）|
| 配置表改了没生效 | 菜单 **修仙 / 检查配置表是否已同步**；要写入就 **修仙 / 从配置表生成资产** |
| 传送 | 进 Play，走进任一光圈 → 点按钮 → 看 Console 的 `[传送] 玩家已放到「…」` 和落点距离 |
| 特效开关 | 进 Play，看 Console 有没有 `[场景特效开关] …不生效 → 关闭组件` |
| 坐骑页预览 | 进 Play，按 `I` → 切到「坐骑」页 → 9 只挨个看；或直接看录屏 `mount_page_all9.mp4` |

## 9. 提交清单（31 条，按时间倒序）

```
坐骑页预览    20b67baf  RT比例/旧Animation/UnscaledTime/每帧重试
              090d4811  UI 整体同步（HudCanvas/CharacterUI/… 进"总是覆盖"）
               f294f17a  场景特效开关表
               c459a986  描边挂回 Sect 玩家
传送          a05a7aa4  跨场景传送改用 DontDestroyOnLoad 宿主（落点距离 0.000m）
              1d1da992  玩家装配补回 Sect / 塔
              56925255  四个传送点改为直接落在对面传送点上
              305d17ba  3C→Sect 落点改到传送点本身
              650fe0a1  删 Sect 多余光圈 + 修正落点名
              1b12f38b  按用户命名重配四个传送点
              f8c73a4e  第三对传送：Sect 桥上 ↔ 3C_Testbed
              2060789a  Teleporter 组件 + 接线 + Build Settings
镇妖塔        3ab7ecd9  楼板/顶盖改正八边形环网格
              72e1d461  透视改为只在塔生效
              7f4e58a8  塔相机抬高 ×1.5
              c9a7ca3f  楼梯板朝向修正 + 新增遮挡透视
              436c7778  通用装配同步约定 + 工具
              b3b29075  八面体 + 连续螺旋楼梯
              0ae8b5fb  塔内景大厅
坐骑           d00dd302  灵鹤改名 + 代码现做 idle/run
              8289db3c  灵鹤套碧水兽
              6be33cfd  地面坐骑（狼/鹿/熊猫）贴地 + 各自骑乘高度
              115d71aa  巨龙轴向 +90° X
              b923e707  朝向跟随改成每只自己配
              44220fd2  Player_Visual 归零（用户确认）
              aef527cd  动作支持"兼四阶段、只改速度"；下坐骑播 Die
              5fec5aae  摆位改成每只一条
              059b1eed / 12e5e8b3  删场景里遗留的摆位参考物
              b75997e7  摆位高度上抬半个身位
              66f30377  补记：SaveScene 写的是编辑器内存状态
```
