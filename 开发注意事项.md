# 开发注意事项（踩坑记录）

> 这份文件是给**以后接手这个项目的人或者 AI** 看的。
> 里面每一条都是实际踩过的坑，不是理论建议。
>
> 位置：`D:\project：cultivation\开发注意事项.md`（工作区根目录，不在 Assets 里）

---

## ★ 开工前必读：给新对话的第一段话

如果你是**新开一段对话**来接这个项目，先看这几条，能省掉一晚上的弯路：

### 0.1 这个项目【没有版本控制】——先去初始化 git

2026-09 的某天晚上，一次"清理重复字段"的 PowerShell 脚本因为行范围判断写错，
**一口气删掉了 630 行 `NpcBrain.cs`**，没有 git、没有备份，只能靠重建救回来。

**开工第一件事：`git init` + 配 Unity 的 `.gitignore`**
（忽略 `Library/` `Temp/` `obj/` `Logs/` `UserSettings/` `*.csproj` `*.sln`，
只跟踪 `Assets/` `Packages/` `ProjectSettings/`）

### 0.2 改大文件前先复制一份

哪怕只是 `Copy-Item X.cs X.cs.bak`。改完确认没问题再删。

### 0.3 用 `edit` / `write` 工具改代码，不要用 PowerShell 拼多行

见 1.3。这一条我踩了**至少四次**。

### 0.4 类名一律 ASCII，成员名用中文

**这个项目的约定是**：类名 / 文件名全 ASCII（`NpcInstance` / `UiEscRegistry`），
public 成员用中文（`当前气血` / `刚关闭过()`）。

我曾经把一个协调器命名成中文 `界面开关记录`，
编译直接报 `鐣岄潰寮€鍏宠褰 does not exist` —— Unity 认不出中文文件名对应的类。
**别开这个头。**

### 0.5 【最坑的一条】改了配置表**不会**自动生效

**游戏读的是 `Assets/Data/Generated/` 下的资产，不是 `.csv`。**
改完 `Assets/Data/Tables/*.csv` 必须跑一次菜单 **修仙 / 从配置表生成资产**。

忘了跑的典型症状：**「明明改了表，游戏里一点变化都没有」** ——
然后就会开始怀疑代码坏了。真事：

> 把 `NPC表.csv` 里练功木桩的 `对主角好感度` 从 0 改回 -1，但没重新导入
> → 资产里还是 0 → 木桩不再是敌人
> → **「好感为负的 NPC 不显示血条了」+「焚天炎术打不出伤害了」两个 bug 同时冒出来**，
> 看着完全像代码回归，实际只是一条数据没同步。

现在有 `Assets/Editor/Data/DataTableSyncChecker.cs` 兜底：

- **每次进 Play Mode 之前**自动比对 CSV 与生成资产的修改时间，不一致就
  `Debug.LogWarning` 点名是哪张表
- 也可以随时手动跑：菜单 **修仙 / 检查配置表是否已同步**（全部同步就报个数）
- 判定带 **2 秒容差**，避免"改完马上导入"被误报

> 它是**只读**的，不会改任何东西 —— 导入器本身会 **重写技能栏（`RewirePanelData`）
> 并保存场景**，所以绝不能让导入器自动跑。

---

## 一、写文件：编码是头号杀手（本项目踩了 4 次）

### 1.1 所有 `.cs` 必须带 UTF-8 BOM

**症状**：`分类` 变成 `鍒嗙被`、`相机` 变成 `鐩告満`，编译报
`error CS0103: The name '鍒嗙被' does not exist`。

**原因**：某次编辑把文件写成了 GBK，或者写了无 BOM 的 UTF-8。

**规矩**：
- 用 `edit` / `write` 工具写 `.cs` **之后**，补一次 BOM：
  ```powershell
  $enc = New-Object System.Text.UTF8Encoding($true)
  [System.IO.File]::WriteAllText($路径, [System.IO.File]::ReadAllText($路径, [System.Text.Encoding]::UTF8), $enc)
  ```
- **绝对不要**用 `Set-Content` / `Out-File` 写 `.cs`（默认编码不是 UTF-8）

### 1.2 所有 `.csv` 必须带 UTF-8 BOM

**症状**：中文在 Excel / 编辑器里全变乱码（文件本身是好的）。

**原因**：中文 Windows 上，无 BOM 的 UTF-8 会被当 GBK 读。

**规矩**：
- 项目里 `Assets/Data/Tables/*.csv` **全部带 BOM**，新写的也必须带
- 写 CSV 用 `new UTF8Encoding(true)`，不是 `false`

### 1.3 PowerShell 里 `\n` 不是转义符

**症状**：往 C# 里拼代码时，`"a\nb"` 会写成一个**字面的反斜杠+n**，
结果后面的代码被 `//` 注释吞掉，编译报"某某名字不存在"。

**踩过的实例**：
```
// 坐骑不算 NPC，单独处理 —— 不写进 NPC 表\n        foreach (var 分类 in ...)
                                          ^^^^ 字面 \n，foreach 被注释吞了
```

**规矩**：
- 在 PowerShell 里拼多行，用**反引号 n**（`` `n ``），不是 `\n`
- 或者干脆**别用 PowerShell 拼多行代码**——用 `edit` / `write` 工具
- 全局替换 `\n` 会毁掉 C# 里的 `'\n'` 字符字面量，**不要全局替换**

### 1.4 写完文件自检

改完 `.cs` 后，扫一眼有没有异常字符：
```powershell
Select-String -Path $文件 -Pattern '鍒|鐩|锛|鈥'   # GBK 乱码的典型字
```

---

## 二、验证：不要读 Editor.log 的尾巴（本项目被误导 5 次以上）

### 2.1 `Editor.log` 是只追加的，旧报错会一直在

**症状**：明明已经修好了，`Get-Content ... | Select-Object -Last 5 | Where error CS`
还是报同样的错，于是反复"修复"已经修好的东西。

**规矩**：判断编译是否成功，**按可靠性从高到低**：

1. **时间戳对比**（最可靠）
   ```powershell
   # dll 比源文件新 = 编译成功
   (Get-Item "...\Library\ScriptAssemblies\Assembly-CSharp.dll").LastWriteTime `
     -gt (Get-Item "...\Assets\Scripts\X.cs").LastWriteTime
   ```
2. **日志增量**（次可靠）—— 刷新前记行数，刷新后只看新增部分：
   ```powershell
   $前 = (Get-Content $log).Count
   # ... refresh ...
   $新 = (Get-Content $log)[$前..((Get-Content $log).Count-1)]
   $新 | Where-Object { $_ -match 'error CS' }
   ```
3. **试跑一个菜单** —— 能跑通说明 Editor 程序集是好的（但**不能证明**运行时程序集好了）

**绝对不要**只看日志最后几行就下结论。

### 2.2 改名/移动脚本后的报错是正常的

**症状**：把 `A.cs` 改名成 `B.cs` 之后，日志立刻出现
`error CS2001: Source file 'A.cs' could not be found`。

**原因**：文件系统改名和 Unity 重新编译之间有窗口期，Unity 会报一次旧的路径。

**规矩**：**改名后忽略这一条**，等下一轮编译自然消失。别去"修"它。

---

## 三、有副作用的操作：验证之前先想清楚（本项目出过一次数据事故）

### 3.1 出过的事故

为了"验证菜单是否已经摘掉"，跑了一次那个菜单。
**明明知道它会整表覆盖 CSV**，结果：

- `NPC表.csv` 里 20 处好感度被改回旧值
- 已经被排除的 mount 6 行又回来了

**正确做法**：查编译产物时间戳就知道菜单摘没摘掉，**根本不需要真跑**。

### 3.2 规矩

**跑任何会写数据的命令之前，先问三句**：

1. 这个操作会改什么？（改 CSV？改 prefab？改场景？删文件？）
2. 有没有**只读**的办法达到同样目的？（查时间戳、读配置、看 dump）
3. 如果一定要跑，**备份了吗**？

**生成器类工具一次跑完就该摘掉菜单**，别留着——留着就是雷。
（`NpcTableBuilder` 已经因此被删除，`NPC表.csv` 现在手工维护）

---

## 四、别从元数据推断"有问题"（本项目错了 3 次）

| 我的判断 | 实际 |
|---|---|
| 战骑贴图是 DXT1 无 Alpha，肯定显示异常 | 实机显示**正常**，那个模型不需要 Alpha 镂空 |
| 93 个材质"配不到贴图" | 修完 shader 后 **353 个材质都有贴图**，只有 91 个是框架垃圾材质 |
| 母鸡碰撞体"高 0.07 太小了" | **是对的**——prefab 根缩放是 12，本地 0.06 × 12 = 世界 0.7 |

**规矩**：
- 看到数值/格式"不合理"，**先查单位和空间**，不要急着"修正"
- 判断"显示是否正常"，**看实际渲染结果**，不要从文件格式推
- 加"防呆 clamp"之前想清楚：原始值是不是其实对的？

---

## 五、本项目特有的坑

### 5.1 特殊目录

| 目录 | 说明 |
|---|---|
| `Assets/resources/` | **小写也是 Unity 的 Resources 目录**（大小写不敏感）。`Resources.LoadAll<GameObject>("NPC")` 能一次拿到全部 NPC prefab |
| `Assets/TripoModels/` | **不要动**，接收 Tripo 生成的模型 |
| `Assets/Scripts/` | 只放我们自己写的脚本（8 个目录：Abilities/Combat/Core/Data/Npc/Player/Save/UI） |
| `.dsh/` | 控制通道脚本（`health.ps1` 体检 / `serve-unity-mcp.ps1` 起 HTTP / `mcp.ps1` HTTP 客户端 / `uni.ps1` 文件桥驱动 / `cmd.txt`+`result.txt`），打包前要删 `DshBridge.cs` |

### 5.2 prefab 的根缩放不一定是 1

母鸡是 **12**。所有"用世界坐标算本地尺寸"的地方都要除以 `lossyScale`。
（`Renderer.bounds` 是世界空间，`Collider` 的尺寸是本地空间）

### 5.3 `Renderer.bounds` 在实例化的那一帧是无效的

全是 0。所以"摆物件 → 贴地对齐"必须**分两步**（先 Build 再 Align）。

### 5.4 让物件可被左键选中 / 右键锁定，需要三样

1. **碰撞体**（`CapsuleCollider` 最稳，`CharacterController` 不可靠）
2. **`NpcInstance`**（`RaycastNpc()` 用 `GetComponentInParent<NpcInstance>()`）
3. **`NpcIndicator`**（画光环 + 血条 —— **没有它，选中了也看不出来**，表现为"选不中"）

### 5.5 控制通道：MCP 优先，文件桥兜底

**主通道 = MCP（stdio）**。harness 按配置自动拉起 MCP 服务器，AI 直接拿到原生工具：

```
mcp__unity__unity_editor      unity_scene     unity_gameobject   unity_console
mcp__unity__unity_asset       unity_menu      unity_screenshot   unity_gameview
mcp__unity__unity_job         unity_dialog    unity_package      unity_bake
mcp__unity__exec_editor_script   exec_runtime_script   execute_custom_tool
```

链路：

```
harness ──stdio──> .dsh\unity-mcp-proxy.mjs ──stdio──> codely serve unity-mcp
                                                             │ TCP :63386
                                                             ▼
                                         编辑器内 cn.tuanjie.codely.bridge
                         （端口从 cultivation/Temp/.com-unity-codely.json 发现，
                           **编辑器必须开着**）
```

- node：`D:\Tuanjie Cowork\cli\bin\win32-x64\node.exe`
- codely：`D:\Tuanjie Cowork\cli\bin\win32-x64\codely.exe`
- 注册行：`C:\Users\Rihyo\.dsh\profiles\desktop\cordis.patch.yml` 里的 `id: mcp-unity`
  —— **注意这个文件在 DSH_HOME，不在工程目录里**，改它要提权
- `toolCallTimeoutMs` 设成了 10 分钟（默认 60 秒，Unity 编译/烘焙不够用）

**体检（只读）**：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\project：cultivation\.dsh\health.ps1"
```
> 本机 PowerShell 执行策略是 Restricted，跑 `.ps1` 必须带 `-ExecutionPolicy Bypass`，
> 否则报「在此系统上禁止运行脚本」。dot-source 也可以（见 `.dsh\uni.ps1` 的写法）。

#### 5.5.1 【坑】为什么中间必须有 `unity-mcp-proxy.mjs`

**症状**：DSH 日志里反复出现
```
[mcp-client] mcp-client(unity): connection attempt failed:
  path: ["tools", 0, "inputSchema", "type"]
  message: Invalid input: expected "object"
```
然后重试 10 次放弃，**17 个 Unity 工具一个都看不到**。

**原因**：`dsh-mcp-client` 遵循「**要么完整世代，要么没有**」——工具列表是全有或全无的。
而 harness 只支持一个很窄的 JSON Schema 子集：

```
CONSTRAINT_KEYWORDS = type, oneOf, properties, required,
                      additionalProperties, items, enum, const
ANNOTATION_KEYWORDS = description, title, default, examples
```

**其余 key 一律违规**（包括 `$schema`、`$ref`、`anyOf`），而且**递归**检查，不只是根。
另外：根必须 `type:"object"`、`required` 的名字必须在 `properties` 里、
`additionalProperties` 必须是布尔、`type` 只能是单个字符串、`enum`/`const` 只能用于标量。

codely 的原始输出实测 **0/17 通过**：

| 问题 | 涉及工具 |
|---|---|
| 带 `$schema` | **全部 17 个** |
| 根是 `anyOf` 而非 `type:"object"` | `unity_editor` |
| **嵌套** `anyOf` | `unity_gameobject` 的 `properties.propertyValue` |
| `additionalProperties` 是 schema 不是布尔 | 7 个 |

**规矩**：codely.exe 是闭源二进制，所以不要试图"修"它。
`.dsh\unity-mcp-proxy.mjs` 只改 `tools/list` 响应的 `inputSchema`，递归消毒到子集内，
其余消息透明转发。它**只放宽不收紧**（非布尔 `additionalProperties` → `true`），
认不出来的形状退化成 `{}`（合法），保证不会因为翻译失败而让工具消失。

**验证改了没改对——不要靠重启试**（重启一次成本很高）：

```powershell
node .dsh\unity-mcp-proxy.mjs --transform raw-toolslist.json fixed.json
node .dsh\_diag\validate.mjs fixed.json      # 期望 17/17 通过
```

`validate.mjs` 用的是 **harness 自己的校验器**（运行时从 `app.asar` 现取，所以
DSH 升级后约束变了会立刻暴露）。详见 `.dsh\_diag\README.md`。
`fidelity.js` 用来确认不是靠"掏空 schema"换来的通过。

#### 5.5.2 兜底通道

**文件桥**（`DshBridge.cs` + `.dsh/cmd.txt` / `result.txt`），MCP 没起来时用：

```powershell
# 命令写进 .dsh\cmd.txt，结果读 .dsh\result.txt
refresh | menu:<路径> | console:clear|get[:N] | play:on|off | save | shot[:路径] | bird[:高,半径] | dump
```

**执行前必须先把 Unity 窗口拉到前台**（Win32 `SetForegroundWindow`），
否则 Unity 会限制 `EditorApplication.update` 的调用频率。

**可选 = MCP over HTTP**（`127.0.0.1:8765`），想在 harness 之外手动点工具时用：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".dsh\serve-unity-mcp.ps1" -Background
powershell -NoProfile -ExecutionPolicy Bypass -File ".dsh\mcp.ps1" -Tool unity_editor -Json '{"action":"get_state"}'
```

三条通道可以同时连（桥支持多客户端，互不踢掉）。

### 5.6 中文标识符**可以**出现在 `exec_editor_script` 里（2026-09-21 起）

**这条老坑已经失效了。** 旧记录说会被损坏成 `?`，那是挂掉的 HTTP 桥的编码问题。

2026-09-21 换了垫片管线（`unity-mcp-proxy.mjs` → codely → TCP 桥）之后实测：

```csharp
string 中文变量 = "中文标识符存活";
var spec = AttackSpec.特殊主动神通;      // 中文枚举成员也能直接访问
return 中文变量 + spec.走会心判定;
// → 中文变量=中文标识符存活 | spec=特殊主动神通 | 走会心判定=True
```

所以**不必**再把中文逻辑绕进 `.cs` 文件、用 ASCII 菜单项调用了。

> 【更正】`.cs` **不带 BOM 也能编译**（实测：全项目的 `.cs` 都是无 BOM 的 UTF-8，
> 编译 0 错误、中文日志正常）。**必须带 BOM 的只有 `.csv`** ——
> `NPC表.csv` 等配置表一旦丢了 BOM，导入器读到的中文列名/名字就会乱码。

### 5.7 ★ `Resources.Load` 会挑到「和 prefab 同名的模型文件」

`Assets/resources` 里**同一个目录下同时有 `X.FBX` 和 `X.prefab`** 时：

```csharp
Resources.Load<GameObject>("NPC/Demon/YeZhu/YeZhu")      // → YeZhu.FBX  ✗（不是预制体！）
Resources.Load<GameObject>("NPC/Demon/YeZhu/YeZhu.prefab") // → null      （带扩展名没用）
Resources.LoadAll<GameObject>("NPC/Demon/YeZhu/YeZhu")   // → 两个都给你 ✓
```

拿到 FBX 的后果**极隐蔽、完全不报错**：FBX 根节点上**没有 `NpcInstance`**、
**没有 AnimatorController** → 这个 NPC 的 `移动速度` 读出 0、`有动作("Attack1")` 永远是 false
→ **走不动、也放不出招**（战阵真灵那次就是「站在那儿发呆」）。

本工程实测 **240 个真灵定义里有 17 个**是这种摆法。

**规矩：按路径实例化 NPC 一律走 `NpcPrefabs.加载(路径)`**
（它用 `LoadAll` + 「挑带 `NpcInstance` 的那个」判定，240/240 全对），
**不要**再直接 `Resources.Load<GameObject>(路径)`。

### 5.8 开局弹「Tuanjie Editor Mixusage Warning」——是提示，不是故障（2026-09-23 排查完毕）

打开工程时弹：

> Your Project has license mixusage, some functions may not work properly.

**结论：不影响本地开发。** 那句 "some functions may not work" 指的是**要联网授权**的功能
（登录 / 云构建 / 发布 / Hub 部分服务）；编辑器、编译、Play Mode、玩法一律不受影响。
（实测：弹着它照样跑完粒子验证和 HUD 验证。）

**根因在哪**——`%LOCALAPPDATA%\Tuanjie\Editor\Editor.log` 里把文件一个个列出来了：

```
The project has mix usages which violates EULA, .meta file Assets/_TerrainAutoUpgrade/layer_xxx.terrainlayer.meta
```

共 **223 行，全部在 `Assets/_TerrainAutoUpgrade/`**，工程里其它地方一个都没有
（用 `Select-String "mix usages which violates EULA"` 数一下就 223，按目录归类只有这一个目录）。

那个目录是**编辑器升级地形时把内嵌图层导出**留下的临时产物（223 个 `.terrainlayer` + 223 个 `.meta`，214 KB）。
排查时确认了它是**纯废物**：

| 检查 | 结果 |
|---|---|
| 三个场景里 `Terrain:` / `TerrainCollider:` / `TerrainData:` | **0 / 0 / 0**（工程里根本没地形） |
| 同一份 YAML 的对照项（证明提取没失败） | `Transform:` 745、`Camera:` 3、`BoxCollider:` 77 |
| 全工程 8624 个非 meta 文件里对那 223 个 guid 的引用 | **0** |
| 有没有 `.cs` 提到 `_TerrainAutoUpgrade` | 没有 |

**已于 2026-09-23 挪到工程外**：`D:\project：cultivation\_backup_TerrainAutoUpgrade\`（447 个文件）。
挪完 `unity_editor refresh` → **0 error / 0 warning**，场景物体数不变，`Shader`/材质无丢失。
（工程没有 git，所以是**移动**不是删除，随时能移回来。）

> ⚠️ **两个坑**
> 1. **这个弹窗不是新出现的**。昨天那份 `Editor-prev.log` 里同样是 223 行，
>    而且那个窗口昨天弹了 **11 次**、今天只弹 1 次 —— 遇到这种提示先翻 `Editor-prev.log`
>    对比一下，别急着怀疑是刚改的代码弄坏的。
> 2. 清掉这批之后，合规检查**可能会再报下一批**（比如 `Assets/resources/` 下有 1922 个
>    Unity 格式 GUID 的第三方资源包）。**再报也仍然只是提示**，不影响编译和游玩。

---

### 5.9 ★ 给一个模型换贴图，结果**全工程同类模型全变了**（2026-09-23）

**现象**：在场景里选中一个模型，把它材质的贴图换掉 → 所有用到那类模型的地方一起变了。

**根因：环境模型的材质不是"场景引用"的，是 FBX 导入器"按名字全工程搜"出来的。**

看 `environment_stone_dabingshi_001_a.FBX.meta`：

```
materials:
    materialName: 0      # 按名字找材质
    materialSearch: 1    # 搜索范围：全工程
    materialLocation: 0  # 外部材质（External Materials）
```

也就是说 `.mat` 是**一份全工程共用的资产**，谁都不"拥有"它。两个证据：

1. **按 guid 搜场景，一个都搜不到** —— `environment_stone_dabingshi_001_01.mat` 的 guid
   只出现在它自己的 `.meta` 里，`3C_Testbed.scene` / `Sect.scene` 里都没有。
   （所以"改这个材质会影响谁"**看文件是看不出来的**，只能在编辑器里遍历 `Renderer`。）
2. **FBX 里的子网格名 = 材质名**。`..._a.FBX` 的 `internalIDToNameTable` 里子网格叫
   `environment_stone_dabingshi_001_01` / `_02`，材质文件正好叫这两个名字。
   于是 `dabingshi_001_a / _b / _c / _d / _e …` 的**同名子网格全部解析到同一份材质**。

**实测影响面**（数 `Renderer.sharedMaterials`）：

| 场景 | 用到这两个材质的渲染体 |
|---|---|
| `Sect.scene` | **14 个**（`dabingshi_001_a/b/…` 各 2 个槽） |
| `3C_Testbed.scene` | 2 个（`better ground` 的两个 MeshRenderer） |

所以改一次 `.mat` = 改这 16 个地方，**跟"预制体实例"没关系**。

#### 只想换一个怎么办

**核心：让那一个实例"不共用"——给它一份自己的材质。**

1. Project 里选中原材质 → **`Ctrl+D` 复制** → **改成不重名的名字**
   （如 `environment_stone_dabingshi_001_01_雪地.mat`）。
   ⚠️ **必须改名**：材质是"按名字被搜出来"的，同名副本可能被别的模型捡走。
2. 在**复制出来的那份**上换贴图 / 改参数。
3. 回场景，选中**那一个**实例 → 展开它的 `MeshRenderer` → 把
   `Materials → Element N` 拖成新材质。字段左边出现**蓝线** = prefab override（只属于这一个）。
4. `Ctrl+S` 保存场景。

> ⚠️ **三个别做**
> · **别改原材质**（那是共享资产，正是这次"全变"的原因）。
> · **别点 Inspector 的 `Apply`**（会把 override 写回源，又变全工程）。
> · **复制时别保持同名**。

> 反过来的需求（"整类都换、只有某一个保持旧的"）：改原材质，然后在那**一个**实例的槽位上覆盖回旧材质。

> 想让某个模型**以后有独立身份**（贴图/材质能单独配）：右键 → `Create` → **Prefab Variant**，
> 在变体里换材质。和 §22 里 NPC 变体的管法一致。

---

## 六、改名规范

### 6.1 NPC 的 AI 脚本用**表里的完整 id**

```
母鸡      id = beast_muji            → NpcBrain_beast_muji.cs
野猪      id = demon_yezhu           → NpcBrain_demon_yezhu.cs
铁匠      id = npc_tiejiang          → NpcBrain_npc_tiejiang.cs
```

连前缀一起用，这样脚本名、`NpcDefinition` 资产名、表里的 id **三者完全一致**。

**不要**用英文意译（`Hen` / `Boar`）——项目里的目录、动画、控制器用的都是拼音。

### 6.2 改名要用 `AssetDatabase.MoveAsset` 或者连 `.meta` 一起移

只移 `.cs` 不移 `.meta` 会让 GUID 变化，**prefab 上的组件引用会断**。

用文件系统移的话：
```powershell
Move-Item $新路径 $旧路径
Move-Item ($旧路径 + ".meta") ($新路径 + ".meta")
```

---

## 七、一句话检查清单

写代码前问自己：

- [ ] 这个文件 Java/C# 要 BOM 吗？CSV 要 BOM 吗？
- [ ] 我用 PowerShell 拼多行了吗？（改掉）
- [ ] 我是不是准备跑一个会改数据的命令来"验证"？（先找只读办法）
- [ ] 我判断"有问题"的依据是实际结果，还是文件格式/元数据？
- [ ] 改名了吗？（改名后的 `CS2001` 是正常的，别管）
- [ ] 改完编译了吗？（看**时间戳**，不看日志尾巴）
- [ ] 改大文件前备份了吗？改完核对行数了吗？

---

# 八、NPC / AI 相关的坑（2026-09 那一晚踩出来的）

## 8.1 "能被选中"靠的是 NpcIndicator，不是碰撞体

一个 NPC 要能**左键选中 / 右键锁定 / 显示血条**，需要四样：

| 组件 | 作用 |
|---|---|
| `Collider`（`CapsuleCollider`） | `Physics.Raycast` 要打得中 |
| `NpcInstance` | `RaycastNpc()` 用 `GetComponentInParent<NpcInstance>()` 认人 |
| `NpcInstance.定义`（NpcDefinition） | 没接的话血量是 0，一碰就死 |
| **`NpcIndicator`** | **画选中绿环 / 锁定红环 / 血条** |

**最容易漏的是 `NpcIndicator`** —— 没有它，选中其实成功了，
但画面上毫无变化，表现为"根本选不中"。

### 两个鼠标键的契约（`NpcTargeting`）

| 操作 | 点到 NPC | 点到空白 |
|---|---|---|
| **左键**（`选中键`） | 选中（绿环） | **取消选中** |
| **右键**（`锁定键`） | 锁定（红环 + 血条） | 取消锁定 |

- 「点空白取消选中」是补上的：以前左键只有 `if (npc != null) Select(npc)`，
  点空地什么都不做，**绿环撤不掉**。现在统一走 `点击选中()` → `Select(RaycastNpc())`，
  `RaycastNpc()` 返回 `null` 时 `Select(null)` 就是取消（`Selected == npc` 时直接 return，不会重复发事件）。
- **点空白只撤选中，不动锁定** —— 两个语义本来就是分开的（见开头）。
- 左键加了 `点在界面上()`（`EventSystem.IsPointerOverGameObject()`）的挡板：
  HUD 是 `ScreenSpaceOverlay`，`Physics.Raycast` 打不到它（见 9.5），
  不挡的话**点技能格子会顺手把选中也取消掉**。
  实测：场景里"全屏且 `raycastTarget=true`"的 UI 有 **0 个**（HUD 底色全是 `raycastTarget=false`），
  所以这个挡板不会误伤正常选中。
- 设施界面开着时整个 `Update` 提前 return（`StationInteractor.有界面打开`），
  所以开着炼丹炉点空白不会误撤选中。

### 血条什么时候显示

`NpcIndicator.应显示血条` 的规则（2026-09-21 起）：

```
死亡（且 死亡时隐藏血条 = true）        → 不显示
否则：被锁定 或 （敌对常显血条 且 好感度 < 0） → 显示
```

也就是说 **敌对 NPC（`对主角好感度 < 0`）不需要锁定就常显血条**；
不敌对但被右键锁定的，照样显示。

两个开关都在 `NpcIndicator` 上：`敌对常显血条`（默认开）、`死亡时隐藏血条`（默认开）。

**关键实现点**：血条显隐是在 `LateUpdate` 里**每帧同步**的，
不能只在 `SetLocked` / `SetSelected` 里刷 —— 否则好感度中途变化、
或者 NPC 死亡/重生（练功木桩会立刻满血重生）都不会反映出来。
早期版本就是只在 `SetLocked` 里 `SetActive`，所以血条只有锁定后才出现。

实测（Play Mode，四个分支都过）：

| 情况 | 好感 | 敌对 | 锁定 | 死亡 | 应显示 | 实际 |
|---|---|---|---|---|---|---|
| 练功木桩 ×3 | −1 | 是 | **否** | 否 | 是 | **显示** ✔ |
| 临时友善 NPC | +5 | 否 | 否 | 否 | 否 | 隐藏 ✔ |
| 同一个改成敌对 | −5 | 是 | 否 | 否 | 是 | **显示** ✔（无需重新锁定，即时生效） |
| 把它打死 | −5 | 是 | 否 | **是** | 否 | 隐藏 ✔ |

> 血条尺寸仍是 `血条宽度 = 1.1` / `血条高度 = 0.13`（世界米）。
> 原来是给"只显示一个锁定目标"设计的，现在会同时出现多个，
> 觉得小就调这两个字段 —— 是按**世界尺寸**算的，不随相机距离变。

> 另外注意：每个 `NpcIndicator` 都会建一个 **WorldSpace Canvas**。
> 场景里敌对 NPC 一多，同时点亮的 Canvas 就多（各自一份 draw call）。
> 目前练功场只有几个木桩，无所谓；以后同屏几十个敌对目标时要考虑按距离/是否在屏幕内裁剪。

`CharacterController` **本身就能被射线打中**，所以不需要再加一个 `CapsuleCollider` 去"修"。

## 8.2 【血的教训】不要同时挂 CharacterController 和 CapsuleCollider

母鸡最初只有一个 `CharacterController`，用 `transform.position +=` 移动，**一切正常**。

后来为了"能被选中"给它**又加了一个 `CapsuleCollider`**，于是：
- `CharacterController` 自己维护一套内部位置
- AI 又在直接改 `transform.position`
- 两个碰撞体互相顶 → **"走一下就卡住"**

**由此引发了一整晚的连锁误诊**：我以为是 `CharacterController` 有问题 →
删掉全部 257 个 → 又手写 `CapsuleCast` 探路 → 探到地面一步不动 →
换 `Rigidbody` → 飘起来 → …

**正确做法（二选一，绝不并存）**：
- 只留 `CharacterController`，移动用 `controller.Move()`
- 或 只留 `CapsuleCollider` + `Rigidbody`，移动用 `velocity` / `MovePosition()`

**关键**：别让 `transform.position` 和物理组件互相打架。

## 8.3 水平移动不要拿【整条胶囊】去扫

用 `CapsuleCast` 拿整条胶囊探水平方向时，**胶囊的下半部分天然贴着地面**，
每次探都命中 Ground 且 `distance = 0` → `可走 = 0` → **NPC 一步都动不了**。

日志里长这样：
```
[NPC] 移动被挡住：障碍=Ground(MeshCollider)  距离=0
```

要用的话，改成在**腰高放一个球**（`SphereCast`）探，永远碰不到地面。
**或者干脆用 Unity 的 Rigidbody / CharacterController，别自己写。**

## 8.4 自己写的重力：用射线，不要用胶囊扫

`CapsuleCast` 向下找地面时会和地面重叠（同样是 `distance = 0`）。
改成**从脚底向上抬 0.1 米后打一条 `Raycast`**，没有体积重叠问题，简单可靠。
（记得滤掉自己身上的碰撞体 —— 射线从自己的 Collider 内部出发会把自己也算命中。）

## 8.5 FBX 动画默认【不循环】

`ModelImporter` 导入的 `AnimationClip` 默认 `loopTime = false` ——
**播一次就停在最后一帧**。表现是"人在移动，但动画定住了"。

要用 `ModelImporter.clipAnimations` + `SaveAndReimport()` 把
`Idle / Specialidle / Walk / Run / RideIdle / FloatIdle / Fight` 这些设成循环
（一次性动作 `Attack* / Death / Wound / Jump / Dodge` 保持不循环）。

工具：`Assets/Editor/Tools/AnimLoopSetup.cs`（已跑过，352 个片段全部设好了）

## 8.6 【改代码里的字段默认值，对已有 prefab 没有任何影响】

组件是 `AddComponent` 加进 prefab 的，那一刻字段就已经**按当时的默认值序列化**进去了。
之后你改代码里的 `= 5f` → `= 2f`，**已有 prefab 里还是 5**。

所以：
- 让安装工具**显式赋值**（`脑.挨打扣好感 = 5f;`），别指望改默认值
- 或者写工具批量把 prefab 里的值刷一遍

## 8.7 玩家的 GameObject 上【没有 NpcInstance】

所以 NPC 的"攻击目标"**不能用 `NpcInstance` 类型存** ——
`玩家.GetComponent<NpcInstance>()` 永远是 `null`。

后果：每帧都在「追击 → 目标为空 → 待机 → 发现玩家 → 追击」之间横跳，
画面上敌人**原地抽搐**（而且只有它想动的时候才看得出来）。

**正确做法**：目标存 `Transform`（追谁的位置），另外用 `bool 目标是玩家` 标记类型，
NPC 目标才额外存一个 `NpcInstance`。

## 8.8 状态机的"早退"会让连续受击没反应

```csharp
protected void 切状态(脑状态 新)
{
    if (当前状态 == 新) return;      // ← 已经在逃跑就什么都不做
```

动物第一下被打 → 进"逃跑"、跑一段停下回头看；
第二下再调 `切状态(逃跑)` **被无视** → 它一直站着。

**需要一个"强制重进状态"**（重跑一次 离开/进入 钩子、重置计时），
在"连续受击要重新开始逃跑"这类场景调用。

## 8.9 多段攻击不要每次都切"受击"状态

飞剑是「穿体」多段攻击，一秒打中好几次。
如果每次受击都 `切状态(受击)`，而僵直结束又回 `待机`，就成了：
```
追击 → 受击(停) → 待机(站着) → 发现玩家 → 追击 → 又被打中 …
```
**原地不停抽搐**。

改成：受击动画照播（加节流），但**不打断当前行为**；
只有从非战斗状态被打中才真正进僵直。

## 8.10 飞剑的瞄准点不要用 `col.bounds.center`

`Collider.bounds` 是物理 / 蒙皮结算出来的值，**和渲染帧不同步**。
敌人一移动它就一跳一跳，而绕行的 8 字轨迹锚在这个点上 →
剑跟着跳 → **看起来像有两把剑**。

用 `transform.position + 固定高度` 再做一次指数平滑。

---

# 九、UI 相关的坑

## 9.1 多个界面都用 ESC → 必须有协调器

角色面板（I 键）和设施界面（右键）都用 ESC 关闭，暂停菜单也用 ESC 开关。
**同一帧里多个脚本的 Update 执行顺序是不保证的**，于是：

- 一次 ESC 既关了面板、又弹了暂停菜单
- 更糟：暂停菜单记下的"原来的 timeScale"正好是面板设的 0，
  退出暂停时恢复成 0 —— **游戏永远卡在暂停**

**现成的解法**：`Assets/Scripts/UI/UiEscRegistry.cs`
- 任何界面关闭时调 `UiEscRegistry.NotifyClosed()`
- 暂停菜单开之前查 `AnyOtherUiOpen()` 和 `JustClosed()`

**用时间戳（`Time.unscaledTime`）而不是布尔标记** —— 布尔标记又会变成"谁先跑谁赢"。
用 `unscaledTime` 是因为暂停菜单会把 `timeScale` 设成 0，普通时间会冻住。

## 9.2 `timeScale` 恢复时绝不恢复成 0

```csharp
原时间缩放 = Time.timeScale;
if (原时间缩放 <= 0.001f) 原时间缩放 = 1f;      // 别把 0 当成"原来的值"
...
Time.timeScale = 原时间缩放 > 0.001f ? 原时间缩放 : 1f;   // 双保险
```

否则只要有任何一条路径让"原值"取到了 0，游戏就永久暂停。

## 9.3 WorldSpace Canvas 必须自己设缩放

`new GameObject(..., typeof(Canvas))` 出来的 WorldSpace Canvas 默认 `scale = 1`，
一个 150×40 的 canvas 在世界空间就是 **150 米宽**，字大得没边。

```csharp
rt.sizeDelta = new Vector2(300f, 70f);
rt.localScale = Vector3.one * 0.01f;      // 300 × 0.01 = 世界 3 米宽
```

也**别随便挂成玩家的子物件** —— 玩家的缩放/旋转会一起作用到它。

## 9.4 不要重复 `AddComponent<Text>()`

```csharp
var go = new GameObject("文字", typeof(Text));   // 已经挂了 Text
var t = go.AddComponent<Text>();                  // ← 又加一次 ✗
t.text = "x";                                     // ← t 是 null → NullReferenceException
```

Unity 拒绝重复添加并返回 `null`。要么创建时给类型、要么之后 AddComponent，
**不要两样都做**。

## 9.5 全屏 UI 挡不住 `Physics.Raycast`

uGUI 的全屏 `Image`（`raycastTarget = true`）只挡 **uGUI 的射线**，
挡不住游戏自己的 `Physics.Raycast`。

界面开着时要在**代码里显式挡**：
```csharp
if (UiEscRegistry.AnyOtherUiOpen()) return;   // 别让鼠标穿透到场景里
```

---

# 十、这个项目现在的状态（2026-09 收工时）

## 10.1 已完成

- **NPC 表**：`Assets/Data/Tables/NPC表.csv`，111 行，手工维护
  （生成器已删除——它会整表覆盖，是颗雷）
- **359 个 NPC prefab**：都有 `NpcInstance` + `CapsuleCollider` + `NpcIndicator`
  → **能被选中 / 锁定 / 受击 / 显示血条**（就是木桩那套基础功能）
- **352 个循环动画**已设好 Loop
- **四个设施**：修炼房屋 / 炼丹炉 / 炼器炉 / 布阵点，
  靠近 + 右键 → 打开对应界面（目前是空白幕布占位）
- **暂停菜单**：ESC 开关，设置 / 返回主界面 / 退出游戏
- **控制通道**：MCP / stdio（主）+ `DshBridge.cs` + `.dsh/cmd.txt`（兜底，打包前记得删 `DshBridge.cs`）
- **伤害公式（2026-09-21 扩展）**：`CombatCalculator` 已支持 2×2 四种攻击
  （物理/特殊 × 普通攻击/主动神通），旧飞剑路径行为不变。见 §十一。
  **尚未接入**：`防御` 仍不参与结算、玩家仍无受伤入口、飘字/好感度事件仍零订阅。

## 10.2 【AI 已全部撤掉】—— 明天要重做

那一晚手忙脚乱地写了 `NpcBrain` 基类 + `NpcBrainAnimal` + `NpcBrain_beast_muji`，
但因为 8.2 / 8.3 / 8.4 那几个坑连着翻车，最后**全部删除**，只留基础功能。

**设计意图（已经和用户确认过的需求）**：

- **动物**：通常只有 Idle / Specialidle / Walk(Run)，没有死亡动画（死了走粒子消散）。
  行为由好感度驱动：> 60 跟随玩家；< 10 躲避与逃跑。挨打降好感。
- **妖魔**：初始好感度 < -10，天生敌对。进索敌范围就锁定玩家主动攻击，
  出追击范围放弃。有 Attack* / Death / Wound / Idle / Walk / Run。
- **人族**：同妖魔但部分 Attack 是施法动作（要出子弹，等特效包）。
  初始好感度 ≥ 10，被削到 10 以下才翻脸。
- **以后要接**：人族对话 / 剧情；人族与妖魔可被收服并召唤（帮玩家打玩家锁定的目标）

**命名规范**：子类脚本用 NPC 表里的**完整 id**
（`NpcBrain_beast_muji.cs` / `NpcBrain_demon_yezhu.cs`）

## 10.3 还没做的

- **git**（最重要，见 0.1）
- NPC 的物理碰撞（现在会穿墙 —— 用户选择暂时接受）
- 子弹特效（等资产包）
- 单个 NPC 的数值微调

---

# 十一、伤害计算公式（2026-09-21 扩展为 2×2）

## 11.1 唯一入口

`Assets/Scripts/Combat/CombatCalculator.cs`。
**任何普攻/神通都调用这里，不要各自实现一份。**

一次攻击由两个**正交**的轴描述（`Assets/Scripts/Combat/AttackSpec.cs`）：

| 轴 | 取值 | 决定什么 |
|---|---|---|
| 伤害属性 `DamageNature` | 物理 / 特殊 | 走**暴击**还是**会心** |
| 攻击类别 `AttackKind` | 普通攻击 / 主动神通 | 用**普攻**还是**主动法术**的加成/减免 |

组合出四种攻击：

| | 普通攻击 | 主动神通 |
|---|---|---|
| **物理** | 物理普通攻击（暴击 + 普攻系） | 物理主动神通（暴击 + 主动法术系） |
| **特殊** | 特殊普通攻击（会心 + 普攻系） | 特殊主动神通（会心 + 主动法术系） |

```csharp
var r = CombatCalculator.Resolve(攻击方, 受击方, AttackSpec.特殊主动神通);
```

旧接口保持等价：`ResolveBasicAttack(a,d)` == `Resolve(a,d,AttackSpec.物理普通攻击)`，
`default(AttackSpec)` 就是物理普通攻击。

> **特殊 ≠ 远程。** 远程带子弹那类「通常」归特殊，但归类由调用方显式传
> `AttackSpec` 决定，**不要用「有没有子弹」去推断**。

## 11.2 三步公式

**第一步 · 命中**（四种完全一致）
```
闪避率 = (受击方闪避 − 攻击方忽视闪避) / 受击方闪避
  ≤ 0 → 必中（受击方闪避为 0 也是必中）
  > 0 → Random.value >= 闪避率 才命中
```

**第二步 · 加成判定**（按「伤害属性」二选一，互斥）
```
物理 → 免暴率   = (受击方暴击抗性 − 攻击方暴击)   / 受击方暴击抗性
特殊 → 免会心率 = (受击方会心抗性 − 攻击方会心)   / 受击方会心抗性
  ≤ 0 → 必定触发；> 0 → Random.value >= 该概率 才触发
  （抗性为 0 时也视为必定触发）
加成倍率 = 触发 ? (物理 ? 攻击方暴击伤害 : 攻击方会心伤害) : 1
```

**特殊攻击不会经过暴击判定，也完全不吃暴击属性/暴击伤害；物理攻击反之不吃会心系。**

**第三步 · 伤害**（按「攻击类别」二选一）
```
普通攻击 → 攻击方攻击 × (1 + 攻击方普攻伤害加成     − 受击方普攻伤害减免)     × 加成倍率
主动神通 → 攻击方攻击 × (1 + 攻击方主动法术伤害加成 − 受击方主动法术伤害减免) × 加成倍率
```

**主动神通不会吃到普攻伤害加成，也不受对方普攻伤害减免影响。**
减免堆到 100% 以上按 0 倍处理，不倒扣血。

## 11.3 【重要】免暴率 / 免会心率是「反转」的

公式的分母是**受击方的抗性**，所以不是「暴击越高越容易暴」，而是：

> **攻击方暴击 ≥ 受击方暴击抗性 → 免暴率 ≤ 0 → 必定暴击。**

当前数值下这条影响**非常大**（2026-09-21 对 NPC 表 111 行实测）：

| 谁打谁 | 结果 |
|---|---|
| 玩家暴击 0.15 vs NPC 暴击抗性 | **104/111 行（93.7%）是必定暴击**，只有 7 行是概率暴击 |
| 玩家会心 0.10 vs NPC 会心抗性 | **85/111 行（76.6%）是必定会心** |

NPC 表的抗性大多填在 0.02 ~ 0.16（还有 1 行 0.5 的木桩），而玩家的暴击/会心正好压在上面。

**要改成"按概率"**，公式得换成 `实际暴击率 = 攻击方暴击 − 受击方暴击抗性` 这类形状。
**动之前先确认这是不是策划要的** —— 现在的形状是照策划说明实现的。

## 11.4 目前只有一条扣血路径

```
飞剑 BasicSword01.执行穿体结算()
  → NpcInstance.ReceiveAttack(attacker, spec)
    → CombatCalculator.Resolve(...)
      → NpcInstance.TakeDamage(伤害, alreadyMitigated: true)
```

- 出手节奏：`实际穿体间隔 = 0.15s ÷ 攻速系数`（`BasicSword01.cs`）
- `NpcInstance.TakeDamage(amount, alreadyMitigated: false)` 里有一层 `amount − 防御`，
  但**唯一调用方传的是 `true`**，所以 **`防御` 目前完全不参与结算**（死代码）。
  按现在的数值，一旦接回减法模型：长老防御 90 > 玩家暴击伤害 63，
  `Max(0, …)` 会打成 **0 伤害**。要接防御得先定模型
  （减法 / 百分比 / `攻击 × 攻击/(攻击+防御)`）。
- **玩家没有受伤入口**：`PlayerVitals` 只有扣灵气，气血只能被回满 / 存档恢复改写。
- `NpcInstance.Damaged/Died/Revived`、`BasicSword01.OnHitLanded`
  **在代码里零订阅者**（飘字 / 音效 / 挨打扣好感都还没接）。
  血条不受影响 —— `NpcIndicator` 是每帧轮询 `HealthPercent`。

## 11.5 属性消费情况

会被读取的：攻击、暴击、暴击抗性、暴击伤害、**会心、会心抗性、会心伤害**、
闪避、忽视闪避、普攻加成、普攻减免、**主动法术加成、主动法术减免**、攻速。

**只被赋值、从不被读取的**：防御、真实 / 真实抗性 / 真实伤害、冷却缩减、治疗加成、
被动法术伤害加成 / 减免。

## 11.6 怎么验证（不要靠肉眼读代码，也不要靠进 Play）

`CombatCalculator` 全是纯静态函数，**在 Edit Mode 直接测**：

```csharp
var 攻 = new AttributeSet();
攻[AttributeType.Attack] = 100f; 攻[AttributeType.CritDamage] = 2f; 攻[AttributeType.InsightDamage] = 3f;
var A = new AttributeCombatStats(攻);
var D = new AttributeCombatStats(守);
CombatCalculator.Resolve(A, D, AttackSpec.特殊主动神通);
```

关键是把**两个轴隔离**验证：让 `暴击伤害 ≠ 会心伤害`、`普攻加成 ≠ 主动法术加成`，
四种组合的数值应当两两可区分。示例期望值（`攻击=100 普攻加成0.2 主动法加成0.8
普攻减免0.1 主动法减免0.3 暴伤2.0 会伤3.0`，且抗性设为必定触发）：

```
物理普攻 220   特殊普攻 330   物理主动神通 300   特殊主动神通 450
不触发加成时：物普 110  特普 110  物神 150  特神 150
```

---

# 十二、主动技能系统（快捷键施放）

## 12.1 结构

| 东西 | 在哪 |
|---|---|
| 技能栏数据 | `UIPanelData.主动技能`（6 格，**神通 / 法宝 / 灵阵 共用**） |
| 施放器 | `Assets/Scripts/Abilities/ActiveSkillCaster.cs`，挂在 Player 上 |
| 范围伤害执行体 | `Assets/Scripts/Abilities/AreaSkillRunner.cs`，挂在临时生成的特效物件上 |
| 配置表 | `Assets/Data/Tables/主动神通表.csv` →导入器→ `ActiveDivineAbility` 资产 |

**第 N 格 → 快捷键 N**（`KeyCode.Alpha1..6`，可同时支持小键盘）。
按键读取在 `ActiveSkillCaster.Update` 的 `Input.GetKeyDown(快捷键[i])`。

### 范围技能打谁：敌人 + 锁定目标（锁定目标无视好感度）

`AreaSkillRunner.结算一次()` 的筛选规则：

```
范围内的【敌人】（对主角好感度 < 0）           → 打
【锁定目标】                                  → 一定打，无关好感度
其他非敌对 NPC                                → 跳过
死亡目标 / 已经打过的（同一 NPC 多个碰撞体）   → 跳过
```

「锁定目标无视好感度」是 2026-09-21 按用户要求加的：
**玩家既然锁定了它又按了技能，就该对它生效** —— 和飞剑
「鼠标手动右键锁定的目标不看好感度」的规则一致。
（只有 `需要锁定目标 = 是` 的技能才有锁定目标；范围填 0 的单体路径本来就不筛选好感度。）

实测：锁一个 `对主角好感度 = 20` 的友好目标放焚天炎术 → 照样扣血 ✔。

## 12.2 施放流程（每一步不过都只提示，**绝不白扣灵力**）

```
冷却 → 结算方式是否实现 → 是否需要锁定 → 灵力够不够
     → 扣灵力、进冷却 → 生成特效 + 结算伤害
```

- 空槽 → 静默返回，不提示
- 法宝 / 灵阵 → 提示「还没做」（目前只实现了主动神通）
- 界面开着（`UiEscRegistry.AnyOtherUiOpen()`）或暂停（`timeScale <= 0`）→ **不接收快捷键**
  （跟 `NpcTargeting` 的既有做法一致；想改就关掉 `界面开着时禁止施放`）

## 12.3 主动神通表新增的列

| 列 | 说明 |
|---|---|
| `结算方式` | `未实现` / `范围伤害`。**没填 = 未实现**（按下去只给提示） |
| `伤害属性` | `物理` / `特殊` → 决定走暴击还是会心（见 §十一） |
| `伤害倍率` | 伤害 = 攻击 × (1 + 加成 − 减免) × 加成倍率 × **伤害倍率** |
| `范围` | 米。**填 0 = 退化成只打锁定目标**。特效会按这个值同步缩放（见 12.4） |
| `冷却时间` | 秒 |
| `需要锁定目标` | 是 / 否 |
| `持续时长` | 秒。**0 = 一次性**；> 0 则每「伤害间隔」再结算一次。有特效时对齐特效动画时长 |
| `首次造成伤害时间` | 秒。施放后隔多久才结算**第一次**伤害。**专用来把伤害生效时刻对齐特效动画**（陨石落地、火焰升起的那一刻）。填 0 = 施放瞬间立刻结算 |
| `伤害间隔` | 秒。首次结算之后，每隔这么久再结算一次 |
| `特效资源路径` | **Resources 相对路径**（`Assets/resources` 之下，不含扩展名） |

伤害的时间轴（三个字段的配合）：

```
t=0                     特效生成
t=首次造成伤害时间        第 1 跳
t=首次 + 伤害间隔         第 2 跳
t=首次 + 2×伤害间隔       第 3 跳 …
t=持续时长               特效销毁（未开始的跳不会发生）
```

所以**跳数 = 满足 `首次 + k×间隔 < 持续` 的 k 的个数**。
`首次 >= 持续` 时一跳都不会结算 —— 这种情况 `AreaSkillRunner` 会打一条
`Debug.LogWarning` 明说，不会静默失败。

实测 A/B（临时敌对 NPC，`持续 3 / 间隔 0.75`）：

| 首次造成伤害时间 | 实际结算时刻 |
|---|---|
| **1 秒** | **1.00s / 1.76s / 2.51s**（3 跳） |
| **0 秒** | **0.01s / 0.76s**（立即，之后每 0.75 秒） |

导入器按列名映射到 `ActiveDivineAbility` 的同名 public 字段 —— **加新神通只要加一行**。
没填的格子导入器会跳过，字段保持 C# 默认值（所以未填的行天然是「未实现」）。

> 早先还有一列 `特效方法id`（`vfx_xxx`），**已删除** —— 它从来没有消费者。
> 注意 `特效方法id` 这个**字段**仍留在基类 `DivineAbilityDefinition` 上，
> 因为 `被动神通表.csv` 也有一列同名，删字段会连带影响被动神通。

## 12.4 特效怎么摆：大小跟着【范围】，朝向要手动翻

施放时按 `缩放 = 范围 ÷ 特效基准半径` 设特效的 `localScale = (缩放, 1, 缩放)`
（**只缩 X/Z，Y 保持原样**）。`特效基准半径` 是 `ActiveSkillCaster` 上的字段，
含义是「该特效在 scale = 1 时的视觉外缘半径」，**换特效资源就要重新标定**：

| 特效 | 实测基准半径 |
|---|---|
| `Effect_13_DangerClose`（焚天炎术现用） | **49.1 米** |
| `Effect_13_Base/Effect_13_Explosion` | 9.9 米 |

**关键：必须同时把粒子系统改成 Local 空间。** 这些特效是 `simulationSpace = World`，
而 World 空间下 transform 缩放**只改变粒子的散布位置、不改变单个粒子的尺寸**：

| scale | 粒子最远水平距离 | 最大粒径 |
|---|---|---|
| 1 | 6.19 m | 9.94 |
| 2 | 12.81 m | **9.95** |
| 3 | 18.88 m | **9.97** |

→ 火焰只是被「摊开」而不是「变大」，大小跟不住范围。
把实例上的粒子系统切成 `Local` 之后缩放才是等比的（外缘半径 ÷ scale ≈ 常数）。
特效本身是静止的，Local / World 观感上没有区别。

实测联动（`特效基准半径 = 49.1`）：

| 范围 | 特效 localScale.x | 期望 |
|---|---|---|
| 6 | 0.1222 | 6 ÷ 49.1 = 0.1222 ✔ |
| 12 | 0.2444 | 12 ÷ 49.1 = 0.2444 ✔ |

### 【重要】缩放的代价：它会把「从天而降」的高度一起压扁

`Effect_13_DangerClose` 的「火柱砸地」是靠**很长的下落距离**撑起来的
（`Effect_13_Shell` 起点在 `localPos z = 30`，拖尾拉出一整条）。
缩放是**整体等比**的，所以范围越小、下落越短：

| 缩放 | 对应范围 | 视觉 |
|---|---|---|
| 0.12 | 6 米 | 一小团火，**火柱几乎看不见** |
| **0.25** | **12 米** | **火柱能看清**（当前取值） |
| 0.4 | 20 米 | 成排火柱砸下，最像"天火焚天" |
| 1.0 | 49 米 | 最壮观 |

**这是"视觉严格等于判定范围"与"火柱要够长才好看"之间的固有矛盾**，不是 bug。
要做成"小范围 + 大火柱"，只能把视觉与判定解耦（特效固定 scale = 1、
伤害仍按 `范围` 结算），代价是看着大、实际判定小。

> 换更小/更大的特效时，用同一台**侧视相机**在 1.2 秒和 2.5 秒各拍一张来定这个值，
> 别只靠数字。

### 【重要】朝向：`Effect_13_DangerClose` 是「倒着做」的，必须翻 180°

原样生成时**粒子全部在地面以下**，看起来是「从地底往上冒」而不是「从天而降」。
把粒子的**世界坐标**打出来一眼就看出：

| 朝向 | 粒子世界 Y | 结果 |
|---|---|---|
| 原样（identity） | **−100.8 ~ −0.1** | 从地底往上 ✗ |
| **绕 X 转 180°** | **0.1 ~ 100.8** | **从天而降砸向地面** ✔ |
| 绕 Z 转 180° | 0.1 ~ 100.8 | 同样正确（只是横向漂移方向相反） |
| 绕 X 转 270° | −5.1 ~ 12.1 | 变成水平，不对 ✗ |

所以 `ActiveSkillCaster` 上有 `特效旋转` 字段（默认 `(180, 0, 0)`），
生成时用 `Instantiate(prefab, 中心, Quaternion.Euler(特效旋转))`。
**换特效资源时这个值也要跟着改。**

> **这个坑极其隐蔽**：粒子系统各项参数看着都正常，只有把粒子的**世界坐标**打出来，
> 或者用能看到地面以下的侧视相机，才看得出整段动画在地面之下。
> 俯视相机里「从下往上」和「从上往下」都只是一团火，根本分不出来。
> —— 我在这里来回错了两次，一次说"看不见"、一次没发现是反的。

> 附带：本效果粒子的 Y 范围有 **100 米**（Y 不参与缩放），所以火柱会冲出
> 俯视相机的画面。这是「天火」该有的样子，不是 bug。

## 12.5 焚天炎术当前数值（`ability_fentian_yanshu`）

| 项 | 值 |
|---|---|
| 结算 | 范围伤害 · **特殊** · 主动神通 → 走**会心**判定 + 主动法术加成 |
| 伤害倍率 | 2.5 |
| 范围 | **12 米**（缩放 = 12 ÷ 49.1 = 0.2444） |
| 消耗灵力 | 60（灵气上限 160 → 满灵气可放 2 次） |
| 冷却 | 8 秒 |
| 需要锁定 | 是 |
| 持续时长 | **3 秒**（覆盖 Shell 飞入 + 火焰升起那段；拖尾层是 `loop`，严格说没有「动画结束」） |
| 首次造成伤害时间 | **1 秒**（对齐到火焰真正烧起来的那一刻） |
| 伤害间隔 | **0.75 秒** → 每目标 **3 跳** |
| 特效 | `Effect_13_DangerClose`（整个 prefab），基准半径 49.1 米，**旋转 (180,0,0)** |

实测：玩家打敌对目标，每个目标每跳 **194.513**
（`57 攻击 × 1.05 主动法加成 × 1.3 会心伤害 × 2.5 倍率`）。
结算时刻实测 **1.00s / 1.76s / 2.51s**（= 首次 1 + 每 0.75 秒）✔，
`localScale.x = 0.2444`（= 12 ÷ 49.1）✔，3 秒后结算体自动销毁 ✔。

> ⚠️ **单目标总伤害 583.5（3 跳 × 194.5），比一次性结算高 3 倍。**
> 要压回原来的量级，把 `伤害倍率` 从 2.5 调到 ≈0.85 即可。
> 想把跳数补回 4 跳，把 `持续时长` 调到 `3.75`（首次仍在 1 秒）。
>
> 【变更记录】加 `首次造成伤害时间` 之前是 `首次=0 / 4 跳 / 总 778`；
> 现在 `首次=1 / 3 跳 / 总 583.5` —— **首跳晚了 1 秒对齐动画，代价是少了一跳**。

## 12.6 【坑 · 我判断错过一次】别用「1 秒窗口 + 游戏俯视相机」判断特效看不看得见

**这条是纠错记录。** 我曾把 `Effect_13_DangerClose.prefab` 判定为「单独放出来什么都看不到」，
并据此把 `特效资源路径` 改成了它的子层 `Effect_13_Base/Effect_13_Explosion`。**结论是错的。**

错在哪：

- 只观察了 **1.07 秒**。而这个特效的 `Effect_13_Shell` 是 `duration = 4` 秒、
  `rate = 3` 慢慢发射的 —— **火焰 1 秒多才起来**（首次出现粒子实测 0.33 秒）
- 用的是 13 米高、fov 40 的**游戏俯视相机**，画面本来就装不下空中的部分

**正确做法：换一个能同时看到地面和空中的侧面相机，观察至少 4~6 秒。**
换成侧面相机后同一个 prefab 拍出来的就是一大片地面火焰。

`Effect_13_DangerClose.prefab` 的真实结构：

```
Effect_13_DangerClose.prefab
├── Effect_13_Shell         localPos=(0,0,30) rot=(90,0,0) duration=4 rate=3 尺寸 0.25
│                           velocityOverLifetime = (0,-50,-150)  ← 从 30 米外飞入的「炮弹」
├── Effect_13_Trails        ← 拖尾容器：Effect_10_TrailSmoke / TrailParticle_2（loop=True，无限发射）
├── Effect_13_Explosion ×2  burst=1 生命 1.5 尺寸 10     ← 撞击 / 地面爆炸层
├── Effect_13_Explosion_2   burst=1 生命 1.0
├── Effect_13_Distort       burst=1 生命 1.5
└── Effect_13_ShellColider  缩放 10
```

要点：

- **它自带「飞入 → 火焰」的完整过程，不需要任何脚本**（包是 `NotScriptBased`）
- **看不到「陨石本体」**：`Effect_13_Shell` 的粒子尺寸只有 **0.25**，基本不可见；
  画面上真正的主体是拖尾 + 爆炸层烧出来的地面火焰
- `shape = Circle 半径 20` → 发射铺开约 40 米，所以基准半径高达 **49.1 米**
- 两个拖尾层 `loop = True` → **永远不会「结束」**，只能靠 `持续时长` 掐掉
- 子目录 `Effect_13_Base/` 下另有 `Effect_13_Explosion.prefab`（单次爆发，基准半径 9.9 米）
  与 `Effect_13_Trails.prefab`，可以单独取用

> **备查**：想要真正的「陨石从天而降」，这个特效包里是有的 ——
> `EffectsSet_2(ScriptBased)/Effect_10_MeteorStrike/`（含 `Effect_10_BigMeteor` /
> `Effect_10_SmallMeteor` / `*HitEffect`）。但它是 **ScriptBased**，靠包里的
> `MultipleObjectsMake` / `ObjectMoveDestroy` 驱动，是「范围流星雨」而非单点命中；
> 拆单颗用需要自己接目标。别和 `Effect_13_DangerClose` 搞混。

## 12.7 【坑】`unity_gameobject ensure_component` 在本项目用不了

MCP 的 `unity_gameobject` 加组件会撞上项目路径里的全角冒号：

```
String conversion error: Illegal byte sequence encounted in the input.
```

它内部有一步走 `Assembly.GetName()` 之类的反射。**改用 `exec_editor_script` 里
`player.AddComponent<T>()` 再 `SetDirty` + `SaveScene`**（Roslyn 脚本没这个问题）。
这和 `cultivation/CODELY.md` 里记的老坑同源，只是这次中招的是工具本身。

## 12.8 怎么验证

`CombatCalculator` 是纯静态函数，Edit Mode 就能测；
但快捷键、特效、`Physics.OverlapSphere` 要在 **Play Mode** 测：

```csharp
var caster = FindObjectOfType<ActiveSkillCaster>();
caster.目标管理器.Lock(木桩, true);   // 需要锁定的技能
caster.尝试施放(0);                   // 等价于按「1」
```

**`尝试施放(槽位)` 是 public，所以不用模拟按键就能测完整条链路。**

### 【测试注意】练功木桩不一定是敌人

`焚天炎术` 只打**敌人**（`NpcInstance.是敌对目标` = `对主角好感度 < 0`）。

`练功木桩`（`monster_wooden_dummy`）在 `NPC表.csv` 里的 `对主角好感度`
**一度被改成 `0`** —— 于是它不再是敌人：技能"命中 0 个目标"、血条也不显示，
表现非常像"技能坏了"。**拿它试招之前先确认那一列是负数。**

> 我就是被这个坑了一轮：以为新加的 `首次造成伤害时间` 没生效，
> 实际是那一发**一个目标都没打到**（所以没有任何 hp 变化和日志），
> 而当时唯一能看到的掉血是旁边飞剑的普攻。

**要稳定复现，就在测试脚本里现造一个敌对目标**（别去改共享资产）：

```csharp
var 定义 = ScriptableObject.CreateInstance<NpcDefinition>();
定义.对主角好感度 = -5f;
定义.属性[AttributeType.MaxHealth] = 500f;
var go = new GameObject("TempEnemy");
go.AddComponent<CapsuleCollider>();
var 敌 = go.AddComponent<NpcInstance>();
敌.定义 = 定义;
go.AddComponent<NpcIndicator>();
caster.目标管理器.Lock(敌, true);
```

> 另外：Play Mode 下改 **ScriptableObject 资产**（比如 `定义.对主角好感度`）**会写回磁盘**。
> 要改数值请用 `Instantiate` 出来的临时副本。

视觉验证：Play Mode 里把相机 `Render()` 进 RenderTexture 再存 PNG
（`DshBridge.cs` 的 `shot` 就是这套写法），然后看图 —— 比读日志可靠。

---

# 十三、游戏内 HUD

## 13.1 结构

| 东西 | 在哪 |
|---|---|
| 生成器（编辑器） | `Assets/Editor/Builders/HudBuilder.cs`，菜单 **修仙 / 生成游戏界面 HUD** |
| 运行时刷新 | `Assets/Scripts/UI/PlayerHud.cs`，挂在场景里的 `HudCanvas` 上 |
| 悬停转发 | `Assets/Scripts/UI/HudHoverTarget.cs`，挂在每个格子上 |
| 贴图资产 | `Assets/Resources/UI/Hud/hud_square.png`（生成器自动产出） |

布局（左下角一小块，参考分辨率 1920×1080。**全部方框，不显示任何名字**）：

```
┌────┐ ┌─┐┌─┐┌─┐┌─┐┌─┐┌─┐
│功法│ │1││2││3││4││5││6│     ← 名字改成「鼠标悬停弹幕布」
└────┘ └─┘└─┘└─┘└─┘└─┘└─┘
气血 ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
灵气 ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
修炼 ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓     ← 距「下一次修炼次数」的进度（见 13.7）
```

| 部件 | 位置 / 尺寸 |
|---|---|
| 功法 | `(20, 80)` 68×68 |
| 技能 1~6 | `(98, 102)` 起，46×46，间距 5 → 右缘 x = 399 |
| 气血 / 灵力 / 修炼 | `(20, 59)` / `(20, 38)` / `(20, 17)`，379×15 |
| 信息幕布 | `(20, 160)`，400×190（默认隐藏） |

> **【2026-09-22】底部加「修炼」条时整块往上挪了 21**：原来是 气血 38 / 灵力 17 /
> 功法 58 / 技能 80 / 幕布 136，现在每个都 +21（修炼落在最底下的 17）。
> 顺序和用户给的标注图一致：**气血 → 灵力 → 修炼**。

- `HudCanvas` 是 **ScreenSpaceOverlay，sortingOrder = -1** → 角色面板 / 暂停菜单会盖在它上面
- 有 `GraphicRaycaster`（悬停需要），但**只有格子的「底」是 `raycastTarget = true`**，
  其余图一律 false —— 所以 HUD 不会抢走别处的点击
- 生成器是**幂等**的：重复执行会先删掉旧 `HudCanvas` 再重建，
  且**不碰 `CharacterUI` / `PauseMenuCanvas`**

## 13.2 冷却表现（这次需求的核心）

`ActiveSkillCaster` 给 HUD 提供了三个查询：

| 接口 | 含义 |
|---|---|
| `冷却剩余秒(槽位)` | 还剩几秒 |
| `冷却总秒(槽位)` | 这次冷却一共几秒（读神通资产的 `冷却时间`） |
| `冷却比例(槽位)` | **1 = 刚进冷却，0 = 冷却完毕** |

HUD 把 `冷却比例` **直接喂给遮罩的 `fillAmount`**：

- 遮罩是 `Image.Type.Filled` + `FillMethod.Vertical` + `fillOrigin = Top`
- 于是暗部从顶部开始往下盖满，再**自下而上逐渐消退**（1 → 0）
  （方框图标用竖向扫比径向的"钟面扇形"更清楚；径向也在 `HudBuilder` 里留着注释可换）
- 中央 `Text` 实时显示剩余秒数：**≥1 秒显示整数，最后一秒显示一位小数**（如 `0.2`）
- 冷却结束后遮罩与文字一起隐藏
- 冷却文字和按键数字都加了 **`Outline` 深色描边** —— 否则压在亮色图标上会看不清

实测（焚天炎术，8 秒冷却）：

| 时刻 | 剩余 | 比例 | 遮罩 fill | 倒计时文字 |
|---|---|---|---|---|
| 0.4s | 7.60 | 0.950 | **0.951** | `8` |
| 3.5s | 4.43 | 0.554 | **0.554** | `5` |
| 7.6s | 0.24 | 0.030 | **0.031** | `0.2` |
| 8.6s | 0 | 0 | 隐藏 | 隐藏 |

## 13.3 【坑】`Image` 没有 sprite 时 `fillAmount` 会被完全忽略

不管 `type` 是不是 `Filled`，**sprite 为空时 `fillAmount` 不起任何作用** ——
进度条永远满格、冷却遮罩永远是个满圆。（`NpcIndicator.cs` 里也记过同一条。）

所以生成器会先生成一张贴图资产再用：

- `hud_square.png`（4×4 纯白）—— 给格子底、图标底、灰罩、冷却遮罩、条状进度**全部**用

`PlayerHud.Awake` 里还有一层兜底：**哪个填充图缺 sprite 就现场生成一张**。

## 13.4 鼠标悬停的信息幕布

不显示名字，改成**鼠标移到格子上弹出一块半透明幕布**，移开就消失。

实现只需要三样：

1. Canvas 上有 **`GraphicRaycaster`**，场景里有 **`EventSystem`**（`3C_Testbed` 本来就有）
2. 每个格子上挂 **`HudHoverTarget`**（`槽位`：`0~5` = 主动技能，`-1` = 功法）
3. 格子里**只有一个** `raycastTarget = true` 的图（用的是「底」），
   所以射线会打到「底」，再沿父级往上找到 `HudHoverTarget`

幕布内容按类型分支：

| 槽位内容 | 幕布正文 |
|---|---|
| 功法 | 当前修炼功法 / 每级所需经验 / 可修炼境界 / 普攻方法 + 介绍 |
| 主动神通（已实现） | 属性 · 结算方式 / 消耗灵力 / 冷却 / 范围 / 伤害倍率 / 持续与结算间隔 / 是否需锁定 + 介绍 |
| 主动神通（未实现） | `主动神通 · <属性> · 【尚未实现】` + 介绍 |
| 灵阵 | `灵阵【尚未实现】` + 有效范围 / 持续时间 / 目标筛选 / 布置消耗灵气 |
| 法宝 | `法宝【尚未实现】` |
| 空槽 | 不弹幕布 |

> 幕布自己的图全部 `raycastTarget = false`，否则它会挡住下面格子的悬停。
> 幕布位置固定在 HUD 上方（不跟着鼠标跑），换格子时内容替换、位置不跳。

## 13.5 图标从哪来

- 技能图标 = `主动技能[i] as IPanelEntry` 的 `DisplayIcon`
- 功法图标 = `当前功法.DisplayIcon`
- **目前这些资产的 `图标` 字段全是空的**，所以 HUD 退化成用「品阶色」画一个色块
  （`UIEntryRow.TierColor`）。等美术把图标填进资产，HUD **不改代码**就会显示真图标。
- `结算方式 = 未实现` 的槽位（寒冰诀等）、以及法宝 / 灵阵，会盖一层黑 62% 的**灰罩**。
  **这和"冷却中"是两回事**：灰罩 = 这东西还做不了，冷却遮罩 = 刚放过正在转 CD。

## 13.6 怎么验证

HUD 是 **ScreenSpaceOverlay**，所以 **`Camera.Render()` 抓不到它**（overlay 在所有相机之后
直接画到屏幕）。必须用 `ScreenCapture.CaptureScreenshot`：

```csharp
ScreenCapture.CaptureScreenshot(路径);
yield return new WaitForEndOfFrame();   // 它是在帧末落盘的，要等
yield return null;
```

想看细节就把截图**裁一块再存**（`Texture2D.LoadImage` + `ReadPixels`），
否则 1920×1080 里 HUD 只占左下角一小条，根本看不清。

> `ScreenCapture` **偶尔会抓到全空白的一帧**（实测遇到过一张整片灰的图）。
> 稳妥做法是抓完检查一下像素是否有变化，空白就重试几次。

**悬停怎么自动测**（不用真的动鼠标）：走 uGUI 自己的管线就行 ——

```csharp
Vector2 屏点 = RectTransformUtility.WorldToScreenPoint(null, 格子.position);
var ped = new PointerEventData(EventSystem.current) { position = 屏点 };
var 结果 = new List<RaycastResult>();
EventSystem.current.RaycastAll(ped, 结果);            // 命中「底」
var 处理器 = ExecuteEvents.GetEventHandler<IPointerEnterHandler>(结果[0].gameObject);
ExecuteEvents.Execute(处理器, ped, ExecuteEvents.pointerEnterHandler);   // 等价于鼠标移上去
```

> 注意 `EventSystem.current` 在 **Edit Mode 下是 null**（普通 MonoBehaviour 在编辑器里不跑
> `OnEnable`），必须进 Play Mode 才测得到。

> 【坑】`PlayerStatsDebugPanel`（F1 数值面板）是 **IMGUI `OnGUI`** 画的，
> 而且**这个组件就挂在 `Player` 身上**。
> 想临时关掉它有两个办法：`面板.Visible = false`，或者 **`面板.enabled = false`**
> （IMGUI 只在组件 enabled 时才跑 `OnGUI`，实测好使、更省事）。
> **千万不要 `面板.gameObject.SetActive(false)`**：那会把整个 `Player` 关掉，
> 于是 `ActiveSkillCaster.Update` 不再跑、**冷却永远停在初始值**，
> 而 `FindObjectOfType` / `GameObject.Find` 又都找不到未激活对象，
> 报出来的是"找不到 Player"这种和真正原因隔着好几层的错。
> （我在这上面绕了三轮。）

---

## 13.7 【新功能】左下角「修炼次数进度条」

用户原话：
> 「每杀一只怪就记录一下还要多久才攒够一次修炼次数，在左下角新增了现存修炼次数
> 以及本次修炼次数还需要杀多少怪物的进度条信息。**杀完一只怪进度条实时增长**」

### 数据从哪来（没有新加任何"击杀计数"）

**不需要单独记「杀了多少只」**。`PlayerCultivation.修炼次数累积` 本来就是个
**float**：整数部分 = 现存次数，小数部分 = 距离下一颗的进度。
`NpcInstance.TakeDamage()` 在气血归零时调 `修炼.记录击杀(定义.境界)`
（§14.2 说的"挂在事件上"那套），所以：

```
杀怪 → 记录击杀() → 修炼次数累积 += 1 × 等级差系数 → HUD 每帧读它 → 进度条自己就动了
```

HUD 侧**只读**，不碰任何业务逻辑。

### 条怎么画：**显示值"追"真值**，不是直接画真值

`PlayerHud.刷新修炼进度()` 里维护一个 `显示修炼累积`，按 `修炼追赶速度`（默认 1.6 次/秒）
往真实值爬。用户原话：

> 「如果单次增长大于一次修炼进度的话进度条满了就清空重新跑一下，比如单次长了 3.5 次
> 修炼次数，进度条最开始是 0，那就从 0 到 100 跑满 3 次，第四次跑到进度条一半」

**实测（一次 +3.5，`hud_xiulian_chase.mp4`）**：

| 时刻 | 显示值 | 条 | 文字 |
|---|---|---|---|
| t=0.00 | 0.000 | 0% | 修炼次数 0　下一颗 0% |
| t=0.63 | 1.000 | **满** | 修炼次数 1　下一颗 100% |
| t=0.81 | 1.000 | 0%（停顿结束，重跑） | 修炼次数 1　下一颗 0% |
| t=1.44 | 2.000 | **满** | 修炼次数 2　下一颗 100% |
| t=2.25 | 3.000 | **满** | 修炼次数 3　下一颗 100% |
| t=2.44 | 3.000 | 0% → 继续跑 | 修炼次数 3　下一颗 0% |
| 结束 | 3.500 | **50%** | 修炼次数 3　下一颗 50% |

= 跑满 3 次 + 第 4 次跑到一半 ✓ 和用户描述的一模一样。

三条规则：

| 情况 | 行为 |
|---|---|
| 进度跑满一格 | 停在 1.0 **`修炼满格停顿`（0.18 秒）**，让"满"这一下看得见，然后归零重跑 |
| 真值**变少**（修炼一次 / 闭关消耗） | `显示修炼累积` **立刻落下**，不做倒放动画（倒着跑很怪） |
| 首帧 / 读档 | 直接对齐真值，**不重放**（否则一进游戏就自己跑一遍） |

左边那个「修炼次数 N」用的是**显示值**的整数部分，不是真值 ——
这样数字和条是**同步**跳的（条满 → 数字 +1），不会出现"数字已经 4 了条还在跑第 3 格"。

**实测（真杀一只 1 级怪，`hud_kill_grow.mp4`）**：真值 3.500 → 4.500，
显示值 3.500 →（跑满）4.000 → 4.500，文字 `修炼次数 3` → `修炼次数 4`。

### 调参入口

| 想改什么 | 字段（`HudCanvas` 上的 `PlayerHud`） | 默认 |
|---|---|---|
| 条跑多快 | `修炼追赶速度`（次/秒） | 1.6 |
| 满格停多久 | `修炼满格停顿`（秒） | 0.18 |
| 条的颜色 | `修炼色` | 金 `(0.86, 0.72, 0.32)` |
| 数据源 | `修为`（留空自动 `FindObjectOfType`） | 自动 |

### 【坑】三个条的文字都要描边

血/灵力条**几乎永远是满的**，白字压在实色上没事。但**修炼条经常是半格** ——
白字正好骑在"金填充 / 深底"的交界上，不描边就糊掉。
`HudBuilder.建条()` 里统一 `加描边(文字.gameObject)`，三个条一起用，风格也一致。

---

# 十四、受伤飘字（伤害数字）

NPC 挨打时在旁边弹出一个伤害数字，**1 秒后自动消失**；
**暴击 / 会心的字号更大**（34 → 46），颜色也不同。

## 14.1 结构

| 东西 | 在哪 |
|---|---|
| 飘字本体 | `Assets/Scripts/Npc/DamagePopup.cs`（**运行时现建**，场景里不用摆任何东西） |
| 谁触发 | `NpcInstance.结算完成` 事件 → `NpcIndicator.处理结算` |
| 开关 | `NpcIndicator.显示受伤飘字`（每个 NPC）、`DamagePopup.开关`（全局） |
| 样式 | `DamagePopup.默认样式`（存活时长 / 上浮高度 / 字号 / 颜色 / 层级） |

## 14.2 关键：挂在事件上，而不是各个攻击里

`NpcInstance.ReceiveAttack` 是**所有伤害的唯一入口**（飞剑普攻、主动神通都走它），
所以在那里广播一次 `结算完成`，**所有伤害来源就自动都有飘字**，不用各自接一遍。

事件带的是完整的 `AttackResult`，所以拿得到 `暴击` / `会心`；
已有的 `Damaged` 事件只有「实际扣了多少血」，判断不了是不是暴击。

> 反过来写（让 `NpcInstance` 直接 new 一个飘字）会把「模型」和「表现」绑死，
> 而且 `NpcInstance` 就反过来依赖 `NpcIndicator` 了。
> 现在是**视图订阅模型**，方向才是对的。

## 14.3 三档表现（实测）

| 情况 | 字号 | 颜色 |
|---|---|---|
| 普通命中 | **34** | 米白 `(1, 0.96, 0.86)` |
| **暴击**（物理） | **46** | 橙 `(1, 0.70, 0.16)` |
| **会心**（特殊） | **46** | 青 `(0.52, 0.86, 1)` |

动画：出现时从 0.55 倍「砰」一下弹到 1 倍（0.09 秒），
期间**先快后慢地往上飘 1.1 米**，同时朝水平随机方向散开最多 0.5 米
（免得连续几下完全叠在一起），最后 40% 时间淡出。

## 14.4 几个实现细节

- **字体用 Unity 内置的**：`Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")`。
  伤害数字只有阿拉伯数字，用不上中文，所以**不用往 `Resources` 里塞字体资产**。
  （项目里既有做法，见 `StationInteractor.取字体()`。）
- **WorldSpace Canvas，`sortingOrder = 200`** —— 必须比血条的 `100` 高，
  否则数字会被血条挡住。
- **朝向**用和血条同一套 billboard 算法。
- **用 `Time.time` / `Time.deltaTime`（受 `timeScale` 影响）** —— 暂停时数字一起定住。
- **飘字不跟随 NPC**：生成后就留在原地往上飘（常规做法，NPC 跑了数字不追）。
- 每个飘字是一个**独立的 Canvas**（≈ 1 个 draw call）。焚天炎术一次打 4 个目标、
  每 0.56 秒结算一次、数字活 1 秒 → 实测**同屏最多 8 个**。
  以后目标数量上去了要考虑对象池。

## 14.5 怎么验证

三档并排看一眼最直观：

```csharp
DamagePopup.弹出(pos - 玩家.transform.right, 128f, false, false);  // 普通 34 号
DamagePopup.弹出(pos,                        456f, true,  false);  // 暴击 46 号
DamagePopup.弹出(pos + 玩家.transform.right, 789f, false, true);   // 会心 46 号
```

数数量和生命周期：每帧读 `FindObjectsOfType<DamagePopup>().Length`。
实测施放焚天炎术后**同屏最多 8 个**，再等 1.5 秒残留 **0** ✔。

> 注意：玩家的暴击 / 会心数值很高（见 §11.3），所以实际打起来
> **几乎每一发都是暴击或会心**，很难看到「普通」那一档 ——
> 想确认普通字号得用上面这种直接调 `弹出` 的办法。

---

# 十五、NPC 的 AI

## 15.1 架构

```
NpcAiBase（抽象基类）
│   感知 / 状态机 / 移动 / 动画降级 / 出手 / 受击 / 死亡 / 收服预留
├── NpcAiAnimal        兽类
└── NpcAiCombatant（抽象）  索敌→追击→出手→脱战 的公共实现
    ├── NpcAiDemon     妖魔
    └── NpcAiHuman     人类
```

| 文件 | 作用 |
|---|---|
| `Assets/Scripts/Npc/Ai/NpcAiBase.cs` | 基类 + `NpcAiState` + `NpcAttackConfig` |
| `Assets/Scripts/Npc/Ai/NpcAiCombatant.cs` | 会打人的两类的公共逻辑 |
| `Assets/Scripts/Npc/Ai/NpcAiAnimal.cs` / `NpcAiDemon.cs` / `NpcAiHuman.cs` | 三种类型的差异 |
| `Assets/Scripts/Npc/Ai/NpcProjectile.cs` | 施法用的飞行道具（子弹） |

**子类只实现 `决策()`** —— 决定「下一步想进哪个状态」，
怎么走、怎么播动画、什么时候结算伤害全在基类，避免三份重复代码。

## 15.2 自动装配（不用给 200 个 prefab 手动挂）

`NpcInstance.Start()` 会调 `NpcAiBase.确保(this)`：按 **`NpcDefinition.类型`** 自动加对应的 AI 组件。

- **prefab 上已经手动放了 AI 就不动它**（`确保` 直接返回），方便单独调参
- 只有「代码自动加的」才会灌类型默认值（`应用类型默认值()`），
  否则会把 prefab 上手调的数字覆盖掉
- 类型是「未指定 / 中立」→ **不加 AI**（村民、练功木桩就是这类）

## 15.3 配置表新增的两列

`NPC表.csv` 从 36 列变 **38 列**：

| 列 | 说明 |
|---|---|
| `类型` | `兽类` / `妖魔` / `人类` / `中立` / `未指定`。**成员名要和 `NpcKind` 枚举写得一模一样** |
| `索敌范围` | 米。0 = 用该类型自己的默认值 |

已按 id 前缀批量填好：`beast→兽类(11)` / `demon→妖魔(54)` / `human→人类(26)` / `npc·monster→中立(20)`。

## 15.4 三种类型的行为

| | 兽类 | 妖魔 | 人类 |
|---|---|---|---|
| 初始好感（表里） | 10~30 | −10 ~ −50 | 10 |
| **视玩家为敌的阈值** | **永不**（只会躲） | **≤ −10** | **≤ 9**（= 好感 10 及以上才友好） |
| 好感 > 60 | **凑过来跟随**，到 3 米停下 | — | — |
| 好感 < 10 | 玩家靠近就**躲开** | — | — |
| 挨打 | **朝反方向跑 3 秒** | 播 Wound + 短硬直 | 同妖魔 |
| 玩家进索敌范围 | 只是「注意到」 | **锁定玩家、主动上前打** | 好感掉下去之后才打 |
| 死亡 | 没动画 → **粒子消散** | 播 Death → 4 秒后销毁 | 播 Death → 5 秒后销毁 |
| 其他 | — | — | `可对话`、`收服()` 预留 |

**出手分工（策划约定）**：`Attack1` 走**普通攻击**，`Attack2` / `Attack3` 走**主动神通**
（默认 Attack2 = 物理主动神通，Attack3 = 特殊主动神通）。
伤害在**动画播到 `出手进度`（默认 40%）时**才结算 —— 和 `首次造成伤害时间` 是同一个思路。
`Attack2` / `Attack3` 可以勾 `是施法` + 填 `子弹特效路径`，走 `NpcProjectile` 飞行道具。

## 15.5 【坑】好感度曾经写在 ScriptableObject 上

原来 `NpcInstance.对主角好感度` 直接读写 `NpcDefinition.对主角好感度` ——
**那是个资产，运行时改会写回磁盘**，玩一次就把表里的初始值改脏了。
（这个坑这个项目已经踩过一次，见 §0 的"改数据要小心"。）

现在改成：**资产上的值只当初始值**，运行时用 `NpcInstance.运行好感度` 这份副本，
对外接口名不变（`对主角好感度` / `是敌对目标` 照旧），所以其他脚本不用改。

挨打自动掉好感在 `ReceiveAttack` 里做，`每次受击降好感` 默认 **20**（策划要求"极大程度降低"）。
⚠️ 这是**每次伤害结算**都掉 —— 焚天炎术那种多段 AoE 会掉很多次。

## 15.6 【坑】`SphereCast` 从自己体内出发会打到自己

`前方被挡()` 一开始用 `Physics.SphereCast`，而球心起点在自己身上 →
**每次都命中自己的胶囊** → NPC 一步都不动，日志里还什么都看不出来。

改成 `Physics.SphereCastAll` + **过滤掉自己身上的碰撞体**。

## 15.7 【坑】攻击距离是「中心距」，填小了会死循环

第一版妖魔 `攻击距离 = 2.2`，而它被物理挡在 **2.31 米**进不去
→ `可出手()` 永远 false → 永远停在「接近」，看着像"AI 不工作"。

**`攻击距离` 是两个 pivot 的中心距**，要比双方碰撞体半径之和大一截。
现在妖魔 3.2 / 人类 3.4。

## 15.8 ⚠️ 阻塞：NPC 现在**打不中玩家**（闪避公式问题）

实测数据：

| | 闪避 | 忽视闪避 |
|---|---|---|
| 玩家 | **0.08** | 0.05 |
| 妖魔 | 0.06 | **0** |

`闪避率 = (受击方闪避 − 攻击方忽视闪避) ÷ 受击方闪避`
→ 妖魔打玩家 = `(0.08 − 0) ÷ 0.08 = **1**` → **100% 被闪避**，30 次一次没中。

**只要攻击方的「忽视闪避」是 0、而受击方闪避 > 0，命中率就是 0。**
这和我之前标记过的「免暴率 / 免会心率公式反转」（§11.3）是同一类问题：
**拿受击方的属性做分母**，导致任何正数的防御属性都变成完全免疫。

**这是数值设计决定，没有擅自改。** 三个选项：

1. **改公式**（推荐）：`闪避率 = 受击方闪避 − 攻击方忽视闪避`，直接相减、clamp 到 [0,1]。
   和暴击/会心的修法一致，也符合"0.08 = 8% 闪避"的直觉读法。
2. **给 NPC 配「忽视闪避」**（数据改动）：让 NPC 的忽视闪避 ≥ 玩家的闪避。
   治标不治本，玩家闪避一涨又打不中了。
3. **临时开关**：`NpcAttackConfig.必定命中` 勾上 → 无视闪避。
   **这只是为了先验证 AI 链路**，不是长久方案。

## 15.9 怎么验证

```csharp
var 妖 = Instantiate(Resources.Load<GameObject>("NPC/Demon/BaiLuJing_01/BaiLuJing_01"), 位置, Quaternion.identity);
var ai = 妖.GetComponent<NpcAiBase>();       // Start 之后会自动装上 NpcAiDemon
ai.状态                                      // 待机 / 接近 / 攻击 / 受击 / 死亡
ai.出手 += (a, 配置, 结果) => Debug.Log(配置.动作名 + " " + 结果.伤害);
```

实测（Play Mode）：

| 验证项 | 结果 |
|---|---|
| 自动装配 | 妖→`NpcAiDemon`、兽→`NpcAiAnimal`、人→`NpcAiHuman` ✔ |
| 妖魔 | 接近(9m) → 攻击(3.2m) → 出手 Attack3 命中 188 ✔ |
| 妖魔伤害（开必定命中） | 玩家血 420 → 86.6 ✔ |
| 野兽跟随 | 好感 80 → 凑到 3 米停下 ✔ |
| 野兽躲避 | 好感 5 → 远离 ✔ |
| 野兽挨打逃跑 | 好感 20→0，逃跑中 → 距离 6.71 → 9.41 ✔ |
| 人类翻脸 | 好感 10（友好）→ 挨打降到 −10 → 视为敌 → 攻击 ✔ |
| 中立/木桩 | 不装 AI ✔ |

---

# 十六、NPC 资产修复（5 个 bug）

## 16.1 【bug】动画控制器是空壳，片段从来没接上

**这是"部分 NPC 动画没做好处理"的真正原因**，比 loop 设置严重得多：

- `Assets/Animations/NPC/*.controller`（101 个）里**所有状态的 `motion` 都是 `null`**
- 实测 `BaiLuJing.controller` **引用了 0 个 GUID** —— 它是个只有状态名和过渡的空壳
- 真片段在 `Assets/resources/NPC/<类>/<名字>_Animation/<名字>@<动作>.FBX` **里面**
  （不是 `.anim`，是 FBX 内嵌的子资产）
- `Gou` 更彻底：整个目录只有模型 `Gou.FBX`，**一个动画文件都没有**

好消息是**控制器的结构本身是对的**：参数 `Action:Int + Speed:Float`，
`AnyState → 每个状态` 的过渡，`Action Equals N` 切换 —— 这套机制没问题，缺的只是片段。

**修复工具**：菜单 **修仙 / NPC 资产 / 修复动画控制器**
（先跑 **检查动画控制器（只读）** 看规模）。它会：

1. 按名字找到 `<名字>_Animation` 目录和 `<名字>@<状态名>.FBX`
2. 把片段接回对应状态
3. 顺手修循环：**`Idle` / `Specialidle` / `Walk` / `Run` / `Fight` → 循环**，
   `Attack*` / `Death` / `Wound` / `Dodge` / `Deffence` → 一次性

> 【坑】FBX 内嵌的片段**不能**直接改 `AnimationClipSettings`（只读），
> 必须改 **`ModelImporter.clipAnimations[].loopTime`** 再 `SaveAndReimport()`。
> 所以这个工具会重新导入一批 FBX，慢是正常的。

## 16.2 【bug·已修·根因是 Write Defaults】NPC 姿势诡异 + 陷进地板

**我第一版判断错了**（以为是贴地算法 / 挑错身躯），用户一句"**狗也一样陷地**"
把我拉回正轨 —— 狗的体型和命名跟白骨娘娘毫无共同点，所以原因一定更底层。

**真正的原因**：`Animator` 的状态有个 **`writeDefaultValues`** 开关。
它为 **true** 时，进入该状态会把**所有没被动画驱动的属性重置成默认值**。
而这些状态 **`motion` 全是 `null`**（见 16.1）—— 于是：

> 进入 Idle → 没有任何片段驱动骨骼 → **所有骨骼被重置成默认姿势** →
> 蒙皮网格当场塌陷成一团 → 看起来姿势诡异，而且**整个网格沉到地面以下**。

和体型、命名、贴地算法都无关，所以**所有** NPC 都会中招。

**修复**（已执行）：`NpcAssetFixer.处理动画` 现在做两件事：
1. 把 `<名字>@<状态>.FBX` 里的片段接回每个状态（**385 个状态接上**）
2. 对**仍然没有片段**的状态关掉 `writeDefaultValues`（**保险**，实测 1 个）

**执行结果**：101 个控制器，接上 **385** 个片段（另有 315 个本来就接好）、
1 个片段缺失（`XiaoYaoZi@Specialidle`）、2 个控制器没有动画资源（`Long_texiao` / `ZhanQi`）。

> **循环设置其实一直是对的**：实测 `Idle/Run/Walk/Fight` 的 `loopTime` 本来就是 `True`，
> `Attack*/Death/Wound` 本来就是 `False` —— 所以「0 个 FBX 需要改循环」。
> 用户感觉"没设 loop"，真正原因是**片段根本没接上**，不是 loop 标志错了。

> 剩下的 `标定贴地()`（用身躯渲染体最低点，而不是碰撞体底部）依然是对的，
> 它是**第二道保险**，防止模型 pivot 与脚底不一致时贴地出问题。

## 16.3 【bug·已修】武器的包围盒被算进碰撞体

白鹿精的胶囊原来是 `height 2.6 / radius 1.3` —— 半径等于半高，等于一个球，
乘根缩放 1.3 后直径 **3.4 米**。于是它一靠近就把玩家挤出去，**永远够不到攻击距离**。

**第一版我按「体积最大的渲染体」挑身躯，是错的**：
白骨娘娘体积最大的是**飘带**（`BaiGuNiangNiang_02`，体积 8.63 > 身体 4.76），
而飘带最低点在 **0.945 米高** → 拿它贴地会把 NPC 整个往下压 0.945 米。

**现在按命名挑（用户给的规律）**：

1. 先排掉名字带 `wuqi` / `weapon` / `武器` 的
2. 剩下的里面优先取**名字以 `_01` 结尾、且数字后缀最少**的那个
   （`BaiGuNiangNiang_01` ✓、`BLJ_01` ✓ 而不是 `BLJ_01_01`、
    `BaiXiongJing_01_01` ✓ 而不是 `_01_02`、`Changdaonv_01` ✓ 而不是 `ChangDaoNv_02`）
3. 都对不上（`gou` / `muji` 这种单网格）→ 取体积最大的
4. **安全阀**：如果第 2 步挑出来的高度不到「所有非武器渲染体并集」的 60%，
   说明挑中的是小挂件 → 改用并集
   （实测 `BaiLuJing_02` 挑出来只有 **0.69 米**，并集是 2.35 米 → 走安全阀 ✓）

包围盒用 mesh 的**绑定姿势本地包围盒**（`sharedMesh.bounds`）再算世界值，
不用 `Renderer.bounds`（那是当前姿势的动态值，没播动画时不可靠）。

**已执行**：261 个 prefab，**改了 272 个**（两轮），关键结果：

| prefab | 身躯 | 旧胶囊 | 新胶囊 |
|---|---|---|---|
| BaiLuJing_01 | `BLJ_01` | r=1.3 h=2.6 | **r=0.66 h=2.32** |
| BaiLuJing_02 | `BLJ_02_01` | r=1.25 h=2.51 | **r=1.18 h=2.35** |
| BaiXiongJing_01 | `BaiXiongJing_01_01` | r=1.62 h=3.24 | **r=1.25 h=2.5** |
| BaiGuNiangNiang_01 | `BaiGuNiangNiang_01` | r=1.19 h=2.62 | **r=0.68 h=2.15** |

> 注意胶囊参数是**本地空间**的，会被根缩放再乘一次
> （狗根缩放 10 → 本地 r=0.05 其实是世界 0.5 米，是对的）。

## 16.4 【修正】类型列不该由我擅自决定

我按 id 前缀把 `npc` 那 19 个批量标成了 `中立` —— **这是越权**，
用户已经对照检查并把大部分改回了 `人类`，`NPC表.csv` 以用户改过的为准。

> **教训**：批量填数据列前先问一句，别替策划做分类决定。
> 而且改了 CSV 记得跑导入器（见 §0.5，有自动检查）。

⚠️ 还没做的一件事：**没有攻击动画的「人类」会一直追着玩家但打不出来**
（`可用出手方式()` 为空 → `可出手()` 恒 false）。这类 NPC 应该判定为"不参战"、
停在待机而不是追人。下次补。

## 16.5 【bug·已修】挨打之后直接飞天（射线打到自己）

**现象**：母鸡被打一下，Y 从 0.02 **一路涨到 372 米**，而且是**匀速爬升**（≈41 m/s）。

匀速爬升 = **每帧棘轮式上升**，指向 `贴地()`：

```csharp
Vector3 起点 = transform.position + Vector3.up * 3f;
if (Physics.Raycast(起点, Vector3.down, out var hit, 12f, 地面层, ...))
    目标Y = hit.point.y + 贴地基准偏移;   // ✗ 射线先命中了【自己的胶囊顶】
```

射线从 `+3` 往下打，**最先命中的是自己的碰撞体顶部**，于是被摆到自己的顶上；
下一帧起点更高、又命中自己 —— 每帧涨「胶囊中心高 + 半径」，正好是那个匀速值。

> **我在 `前方被挡()` 里修过一次「排除自己」，但漏了 `贴地()` 和 `视线通畅()`。**
> 现在三处统一走 `取首个外部命中()`（`RaycastAll` + 按距离排序 + 跳过自己的碰撞体），
> 不会再漏。

**为什么只有母鸡中招、狗和妖魔不飞**：狗的胶囊是「薄片」（下半部分几乎贴地），
射线往下第一个命中的是**地面**而不是自己；母鸡的胶囊中心偏高，就先命中了自己。
所以这个 bug **跟 NPC 体型强相关**，换个 NPC 就可能复现 —— 不能只看一个案例就收工。

**顺带修的**：`Animator.applyRootMotion`。实测 **261 个 prefab 里 127 个开着**
（`MuJi_01` / `Gou_01` 都开着，`BaiLuJing_01` 是关的）。
AI 用 `transform` 自己驱动移动，Root Motion 会和它打架 ——
循环片段（Idle/Walk）位移每轮抵消看不出来，**一次性片段（Wound/Attack）位移会累积**。
两边都处理了：运行时 `NpcAiBase.关掉RootMotion()` 兜底 +
菜单 **修仙 / NPC 资产 / 修复 Root Motion** 批量改资产。

> ⚠️ `LeiGong_02.prefab` 因为**本身有 Missing Script**，`SaveAsPrefabAsset` 会报错保存不了
> —— 这个 prefab 有问题，得手动清理。运行时的兜底开关能兜住它。

**实测（修复后）**：鸡 Y `0 ~ 0.02`、狗最高 `0.02`、妖最高 `0.002` ✔ 都不飞天。

老公式 `(受击方闪避 − 攻击方忽视闪避) ÷ 受击方闪避` **拿受击方属性做分母**，
导致攻击方忽视闪避为 0 时闪避率恒等于 **1**（100% 闪避，永远打不中）。

新公式（`CombatCalculator.闪避基准` / `闪避上限` 可调）：

```
闪避率 = 闪避 ÷ (闪避 + 忽视闪避 + 闪避基准)
```

**`闪避基准` 和表里的数值同量级**：表里填小数就用 1（默认），填点数就改成 100。

| 闪避 | 忽视 | 旧 | 新 |
|---|---|---|---|
| 0.08（玩家） | 0 | **100%** | **7.4%** |
| 0.08 | 0.05 | 37.5% | 7.1% |
| 0.06（妖魔） | 0.05 | 100% | 5.7% |
| 0.5（木桩） | 0 | 100% | 33.3% |
| **100** | **350**（基准100） | 0% | **18.2%** ← 用户举的例子 |
| 100 | 0 | 100% | 50% |
| 1000 | 0 | 100% | **75%**（被上限压住） |

## 16.6 【已修】闪避公式：两个大数算出 0~1 的概率

特点：**永远到不了 0 也到不了 1**（除非闪避为 0 = 必中），单调、无突变。

### 端到端验收（2026-09-21）

动画修好 + 碰撞体修好 + 闪避公式换掉之后，整条 NPC 战斗链路实测：

| 项 | 结果 |
|---|---|
| 妖魔打玩家的闪避率 | **0.074**（旧公式是 **1.0**，永远打不中） |
| 妖魔行为 | 接近(9m) → 攻击(3.2m) → **出手 3 次、命中 3 次** |
| 伤害 | 累计 **506.3**，玩家血 **420 → 18** |
| 动画切换 | `Run` → `Attack2` → `Idle` **真的在播了**（接片段之前根本不动） |

> **NPC 战斗正式可用。** 用户已确认「姿势基本恢复正常」。
> `NpcAttackConfig.必定命中` 那个临时开关现在不需要了，但保留着当调试手段。

## 16.7 【bug·已修】`Animator.avatar` 为空 → 动画完全不播（而且极难发现）

**现象**（用户发现的）：白鹿精站在那儿**一动不动**。

**根因**：`Animator.avatar = <空>`，而这些片段是**人形片段**
（`clip.isHumanMotion = True`、150+ 条**肌肉曲线**）。

> 人形片段**不会直接驱动骨骼** —— 它靠 **Avatar** 把「肌肉空间」的曲线重定向到实际骨架。
> **Avatar 为空 → 映射不上 → 一根骨头都不动。**

**为什么极难发现**（我漏了一整轮）：

| 你以为的"在播" | 实际 |
|---|---|
| `NpcAnimator.CurrentAction` 在变 | ✅ 正常（它读的是 `Action` 整数）|
| `Animator` 状态机在切换 | ✅ 正常 |
| Console 没报错 | ✅ 干净 |
| **蒙皮在动** | ❌ **完全不动** |

**所以「动画在播」的验证必须验骨骼**：

```csharp
var 左手 = animator.GetBoneTransform(HumanBodyBones.LeftHand);
// 隔 0.12 秒采样 6 次，算最大位移；> 0.01 米才算真的在播
```
（`GetBoneTransform` 只在**人形 Avatar** 下有效 —— 顺便也验证了 Avatar 对不对。）

**修复**：菜单 **修仙 / NPC 资产 / 修复 Animator 的 Avatar**。
Avatar 从**模型 FBX** 里取（靠「SkinnedMeshRenderer 用的 mesh 在哪个 FBX」反查）。

**执行结果**：261 个 prefab 里 **141 个 avatar 为空** → 补上 **80** 个；
另 **10 个 FBX 里根本没有 Avatar**（`RongYanGuai` / `ShuangTouShe` / `Chibang` / `thxr`，
应该是 Generic 骨架）→ 这些要单独看。

> 【噪音】prefab 上**同时**有个 legacy `Animation` 组件，它会刷
> `The AnimationClip 'Attack1' used by the Animation component must be marked as Legacy`。
> **这条警告和"动画不播"没关系**，别被它带跑（我一开始就在查它）。

---

**写完这些，晚安。** —— 那晚的 AI

---

## 16.8 【新功能】玩家死亡 → 消散 → 死亡界面 → 重生

**文件**：`Assets/Scripts/Player/PlayerDeathSequence.cs`（流程）+ `Assets/Scripts/UI/DeathScreenUI.cs`（界面）
**挂在**：`Player` 上（引用留空都会自动找）

### 流程

```
玩家倒下
  → 原地播粒子消散（复用 NpcDissolveEffect）
  → 藏 Player_Visual 的渲染体 + 禁用操作组件
  → 等 2 秒
  → 弹死亡界面（半透明幕布 + 标题 + 你死了 + 重生按钮）
  → 点「重生」
      → 传送到修炼小屋前（cultivation room 前方 3 米）
      → 满血复活、还回模型和操作、2.5 秒无敌
      → 重置全场敌对 NPC 的战斗状态
```

### 几个要点

- **中文字体**：`Assets/Fonts/SimHei.ttf`。项目**没导 TMP**，统一用 uGUI 传统 `Text`。
  `DeathScreenUI` 的 `字体` 留空会自动去 `Assets/Fonts` 找。
- **排序**：死亡界面 `sortingOrder = 4000` > 暂停菜单 `3000` > HUD。幕布会挡射线，防止点穿到 HUD。
- **「修炼小屋」在场景里叫 `cultivation room`**（根物件）。`PlayerDeathSequence.重生点` 留空会自动找它，
  找不到才退回 `SpawnPoint`。重生偏移 `(0, 0.1, -3)` = 门前 3 米。
- **传送时要先关 `CharacterController`**，否则它会把你拽回原位。
- **`NpcAiCombatant.重置战斗状态()`**：清 `已交战` + `上次交战时间 = -999`。
  不调的话重生后怪会停在待机发呆（要等下一次重新索敌冷却才动），看着像 AI 卡死。
- **★ `UIBuildUtils.CreateImage` 默认 `raycastTarget = false`**（做 HUD 时定的省性能约定）。
  所以**任何要能点的东西都要显式 `raycastTarget = true`** —— 按钮、可点格子、拖拽源；
  **全屏遮罩/幕布也要开**，否则挡不住射线穿透。
  症状是"**按钮点不了，但 Console 一点报错都没有**"。
  排查手法：`RectTransformUtility.WorldToScreenPoint` 拿按钮屏幕坐标 →
  `EventSystem.current.RaycastAll` 看谁在最上面。
  验证"能点"必须走 `RaycastAll` + `ExecuteEvents` 这条链路，**别直接调回调**（那样测不出射线问题）。
- **抓这个界面的图**：ScreenSpaceOverlay 的 UI **`Camera.Render()` 抓不到**，
  要用 `ScreenCapture.CaptureScreenshot` + `WaitForEndOfFrame`，偶尔抓到空帧要重试。

## 16.9 【bug·已修】地面放焚天炎术「陷进地里」—— 动作的贴地补偿

**文件**：`Assets/Scripts/Core/PlayerAnimationController.cs`（只动了这一个）

**症状**：不御风、站在地面放焚天炎术时人往下沉；御风时反而没事。

**先厘清一件事**：玩家 `Transform` 全程**不动**（实测 Y 恒为 −0.4431，
`isGrounded` 恒为 true）—— 沉的是**模型**，不是角色控制器。别往重力 / 碰撞体那边查。

### 根因

`技能动作2` 是从别的角色身上**重定向**过来的（和 16.2 的 Write Defaults 是同一类问题：
骨骼被写成了不属于本骨架的高度）。而那段"贴地补偿"代码**从来没生效过**：

> `记录髋骨高度()` 只在**两只脚都拿不到**时才给 `髋骨节点` 赋值 →
> 正常路径下 `髋骨节点` 恒为 `null` → `LateUpdate` 开头 `if (髋骨节点 == null) return;`
> → `已施加偏移Y` **全程恒为 0**。

所以之前所有"已修"的说法都是假的：那段代码一次都没跑。**判据引用要各自独立兜底。**

### 判据选谁（三个都实测过，别猜）

| 判据 | 焚天施法 | 普攻_远程_01 |
|---|---|---|
| 全网格最低顶点 | −0.25（陷） | +1.75（悬空） |
| 最低脚骨 | −0.064 | +1.048 |
| 最低趾骨 | −0.080 | +1.009 |

- **不能用"全网格最低顶点"**：这角色身上挂着**垂到脚边的长袍下摆 / 佩剑**，
  它们的顶点才是全局最低点（实测最低点恒在 `Y=−0.64`、离左脚 0.9 米）。
  钉住它 = 脚离地、人整个悬空。
- **不能用脚骨**：脚骨只是脚踝，会随脚踝转动失真（差 4 倍）。
- **结论**：判据 = **脚那一撮网格顶点** —— 用 `boneWeights` 取每个顶点的主骨骼，
  只留主骨骼 ∈ {脚、脚趾、小腿} 的（本角色 7893 顶点里筛出 744 个）。

### 四个坑（每个都是实测出来的）

1. **基准不能等到 `播动作` 里才量**。`播动作` 会 `animator.runtimeAnimatorController = 覆盖控制器`，
   Animator 重绑会把姿态搅一下，那一刻量到的脚底比真正站着时高 **0.166 米**，
   于是整套补偿把人托高 0.166 米。改成**每帧空闲时**记 `待用基准参照Y`，
   而且必须在 `维护贴地补偿()` **之后**采样（收招那一帧偏移是在里面还回去的）。
2. **增量要加在世界坐标**（`补偿根.position`）。补偿根 `Player_Visual` 的
   `lossyScale = 1.8`，加在 `localPosition` 上收敛点虽然还是对的，但每帧只走一部分 ——
   普攻那种「0.2 秒抬 1.75 米」会留下 **0.18 米**滞后。
3. **补偿根必须是「动画不写的那个物件」**（= Animator 所在物件 `Player_Visual`）。
   曾经设成髋骨自己，动画写、我每帧也写 → 数值发散，Hips Y 炸到 127 米。
4. 采样开销可忽略：`BakeMesh` + `GetVertices(List)`（List 重载免 GC）7893 顶点 **0.085 ms/帧**。

### 一个开关：`允许下拉 = false`

**只治「陷下去」，不治「浮起来」。** 因为 `普攻_远程_01` 是拿**嫦娥的浮空施法动作**
重定向来的：整段悬空约 1 米、腿是收起来的，硬拉回地面会变成**蹲坐在地上**
（实测贴地后髋骨 −0.84 米）。素材库里真正站在地上的替代动作（按贴地后髋骨偏离排序）：
`WuNiang@Attack1` 0.085 米、`XiaoYaoZi@Attack1` 0.131 米。

> **用户已决定**：不换普攻片段、也不管长袍下摆（见下）。所以这两个旋钮都保持现状，
> 谁要动请先问。

### 验证（走真实施法路径 `ActiveSkillCaster.施放`，不是直接调 `播动作`）

- 施法全程玩家 `Transform` Y 不变；`isGrounded` 恒 true
- 脚底高度残差 **0.01~0.02 米**（原来沉 0.25 米）
- 收招后模型根回到原位
- 御风升空 / 飞行 / 落地全程正常，落地精确回到 −0.4631

### 明确不修的两件事（用户拍板）

1. 施法时**长袍下摆**比站着时多插进地面约 0.2 米 —— 这角色穿拖地长袍，
   站着时下摆本来就在地面下约 0.15 米，属于美术表现，不动。
2. `普攻_远程_01` 仍然悬空约 1 米（嫦娥的浮空动作），不换片段。

> **教训**：这次绕了很久，因为一直想「用一个公式同时满足两个互相矛盾的动作」。
> 正确顺序是——**先各自量清楚每个动作的原始偏差（脚骨 / 趾骨 / 网格最低点三个判据都量），
> 再决定判据**；不要先写补偿公式再回头解释为什么数不对。

## 16.10 【改动】凭虚御风：按住 Shift → **按一下切换**

用户原话：「**飞行的长按 shift 修改为 shift 切换**」。
（原来是「按住 Shift 才飞」—— 飞久了要一直按着，累。）

改在 `Assets/Scripts/Abilities/YufengFlight.cs`，**只动这一个文件**
（动画由 `御风.状态变化` 事件 + `御风流程中` 那个 bool 驱动，速度由 `PlayerController` 读 `御风.御风中`，
**都不读 Shift**，所以不用改别处）：

```csharp
public KeyCode 切换键  = KeyCode.LeftShift;    // 按一下起飞
public KeyCode 切换键2 = KeyCode.RightShift;   // 左右 Shift 都认
public bool 想飞 { get; private set; }         // 玩家「想飞」的开关
public void 切换()                             // 抽成公开方法：按键 / UI 按钮 / 测试都能用
```

### 四条容易漏的规矩（都按这个实现了）

| 情况 | 行为 | 为什么 |
|---|---|---|
| **升空 / 落地过渡中再按** | **忽略** | 不然刚起飞就被自己按掉，动作会来回抽搐 |
| **灵气不足时按"开"** | **不接受**（保持关闭） | 否则会「按了关 → 灵气回满 → 自己又飞起来」 |
| **灵气耗尽自动落地** | **`想飞` 一起关掉** | 同上：不然灵气一回就自动复飞 |
| **`强制落地()`**（开面板 / 死亡 / 卸组件） | **`想飞` 一起关掉** | 关面板 / 复活之后不该自己飞起来 |

### 实测（逐帧驱动 `切换()`，走真实施法路径）

| 步骤 | 结果 |
|---|---|
| 按一下 | `想飞=True` → 下一帧 `升空` → **0.86 秒**后 `御风`，高度 2.40 ✓ |
| 升空中再按一下 | 仍是 `想飞=True / 升空` ✓（被忽略） |
| 再按一下（御风时） | `想飞=False` → `落地` → **0.71 秒**后 `地面`，高度 0 ✓ |
| 静置 2 秒（灵气已回满） | 仍 `地面 / False` —— **不会自己又飞** ✓ |
| 灵气=0 时按"开" | 被拒绝，保持 `地面 / False` ✓ |
| 御风 3 秒（把 `回复倍率` 置 0 才量得到） | 灵气 160.0 → 159.7，**实耗 0.301** = 0.1/s × 3 ✓ |
| 灵气清零 | 立刻 `落地` 且 `想飞=False` ✓ |

> **测试踩的坑**：`PlayerVitals.回满()` 会把**灵气也补满**，测试里每帧调它会把消耗全盖掉；
> 而且玩家有 **灵力回复 4/秒**（`PlayerDefaultStats`），比御风的消耗还大。
> 想量消耗必须**同时**把 `回复倍率` 置 0 —— 这点也适用于以后任何"持续消耗"的验证。

> ℹ️ **一个已知但【用户明确说不用管】的数值现象**：
> **灵力回复 4/秒，御风只耗 0.1/秒** → 飞行时灵气其实在**净涨**（+3.9/秒），
> 所以「**灵气耗尽自动落地**」这条规则**实战中基本永远不会触发**，飞等于免费。
> 用户原话：「**不用管灵力消耗这块的事情**」—— **别去调它**。
>
> 代码里那条自动落地路径本身是**对的、也验过**（见上表最后两行），只是暂时用不上；
> 以后真要收费，把神通表里的「维持消耗灵力」提到 4 以上即可。

## 16.11 【新功能】打死怪之后，粒子**落到地上再被玩家吸收**

用户原话：
> 「杀死怪之后会有粒子炸开然后消散的动画，现在让这些粒子**不要消散，而是落到地上**，
> 然后**等待 1s**，之后**飞向玩家**，在玩家身上消失，产生一种**被玩家吸收**的感觉」

### 四段

| 段 | 行为 |
|---|---|
| 1 **炸开** | 从身躯包围盒中心炸出一批粒子（起手和老的消散同量级） |
| 2 **落地** | 往下掉，**碰到地面就停住**（不再各自淡出） |
| 3 **等待** | 全部落地后原地停 **1 秒**（用户定的） |
| 4 **飞向玩家** | 加速飞向玩家胸口，**越近越小**，贴到身上就消失 = 被吸收 |

新文件 `Assets/Scripts/Npc/Ai/NpcAbsorbEffect.cs`（纯代码生成，不依赖美术资产；
材质/贴图复用 `NpcDissolveEffect.取材质()` —— 那个方法为此从 private 改成 public）。

**开关**：`NpcAiBase.死亡粒子被玩家吸收`（**默认勾着**）。去掉勾 = 老行为（各自随机淡出）。

### 为什么这四段必须自己写，不能用 ParticleSystem 的模块

「**落地停住 → 再朝玩家飞**」是**逐粒子**的分段行为：
- `velocityOverLifetime` 是**整团一个向量**，做不到每颗粒子朝各自方向飞向玩家
- 「落地」是**世界坐标上的一个碰撞点**，模块里根本没有

所以把粒子**完全接管**：

```csharp
main.startSpeed = 0f;          // ★ 粒子自己一步都不动
main.gravityModifier = 0f;     // ★ 重力也自己算
// 每帧：GetParticles → 手动积分位置 → SetParticles
```

> **两个容易漏的点**：
> 1. 手动写 `position` 之后，**ParticleSystem 还会用 velocity 再积分一次** ——
>    所以每帧都要把 `particle.velocity` 清 0，否则等于动了两遍。
> 2. **`colorOverLifetime` 必须关掉**。老消散的「各自随机淡出」就是它干的；
>    吸收版要粒子**全程是实的**，直到飞进玩家身上才消失。

### 实测（时间线）

```
击杀 t=0
 t≈0.7s   全部落地（人形怪 0.66 秒 / 境界 90 胖怪 0.87 秒）
 t≈0.7~1.7s  原地停 1 秒          ← 用户要的那 1 秒
 t≈1.7s   开始飞向玩家
 t≈2.09s  全部吸完、特效物件自己销毁（境界 90 是 2.35 秒）
```

证据：`screenshots/absorb_30_burst.mp4`（第三轮：境界 1 人形 + 境界 90 胖怪各一次，
3 倍慢放）、`absorb_apex_lv1.png` / `absorb_apex_lv90.png`（最高点那一下的静帧）、
`absorb_20_small.mp4`（第二轮的老证据）、
`absorb_20_landed_demon_bingpoguai_04.png`（粒子趴在地上那一下）。

### 一个必须加的细节：**越近越慢**，否则会「擦过去绕回来」

第一版给的是「全速 16 米/秒直冲玩家」，实测有一颗粒子**从玩家旁边擦过去**：
距离 2.02 → **3.06** → 才绕回来吸掉 ✗
现在期望速度 = `min(吸附最快, 距离 × 减速系数)`（默认 6）→ 远处全速、近处减速，
是「被吸住」而不是「弹过去」。

### 尺寸与散布：**吸收版不吃那个 3 倍缩放**

用户看完第一版说：「**现在的粒子做的太大，炸的太远了，就怪物死后的原地一小圈就行了**」。

根因是**复用了老消散的尺寸体系**：`NpcAiBase.播放消散特效()` 算出来的
`半径 = 身躯半径 × 死亡消散特效缩放(3)` —— 那个 **3 倍是给老的「爆发式消散」用的**
（炸得又大又远、各自淡出）。吸收版拿它当基准，粒子就变成半米多一团、飞出去两三米 ✗

现在：**吸收版直接传「身躯半径」，尺寸/散布全部由 `NpcAbsorbEffect` 自己按身躯半径算**：

| 项 | 以前（第一版） | 现在 |
|---|---|---|
| 传给特效的半径 | 身躯半径 **× 3** | **身躯半径** |
| 单颗粒子大小 | 身躯半径 × 0.48~1.2 | 身躯半径 × **0.10~0.22** |
| 初始散布 | 半径 × 0.85 | 半径 × **0.7** |
| 粒子数（自动） | 半径 × 260，上限 160 | **按境界算**（见下节），16 ~ 150 |

### 【第三轮】粒子数按境界 + 炸开改成"按高度反推"

用户看完第二版说：
> 「粒子是**直接落地**的，增加一点点**炸开的感觉**，范围没必要像一开始那样大，
> 但是也别太小气了，**npc 境界越高，炸出来的粒子数量越多**」

#### (1) 粒子数按境界

`NpcAbsorbEffect.按境界算粒子数(境界)` = `clamp(round(14 + 1.25 × 境界), 16, 150)`：

| 境界 | 1 | 20 | 50 | 81 | 90 |
|---|---|---|---|---|---|
| 粒子数 | 16 | 39 | 76 | 115 | 126 |

单个 NPC 想自己定数量的，填基类的 `死亡消散粒子数 > 0` 覆盖。
**实测峰值数 = 预期数**（境界 1/50/90 → 16/76/126，一颗不多一颗不少）。

#### (2) ⚠️【坑】"炸开初速 = 身躯半径 × 系数"对**小体型**等于没有炸开

第二轮把 `爆开系数` 定成 2.2，看着像调好了，其实**没验证过**。第三轮实测发现：

```
身躯半径 0.4（人形怪） → 初速 = 0.4 × 2.2 = 0.88 米/秒
重力 20 → 弹起高度 = v²/(2g) = 0.88² / 40 ≈ 0.019 米 ≈ 2 厘米
```

**2 厘米 = 肉眼完全看不出炸开**，粒子还是"直接落地" —— 正是用户抱怨的那件事。
（第二轮之所以觉得 2.2"太大了"，是因为当时还在乘那个 3 倍，半径是 1.2~1.5，
同样公式算出来能弹半米多。）

**修法：先定"要弹多高"，再用 `vy = √(2gh)` 反推初速。**

```csharp
目标高度 = 爆开高度 + 每级爆开高度 × (境界-1) + 身躯半径 × 身躯高度加成
vy = √(2 × 重力 × 目标高度 × 随机(0.55~1.15))     // 逐粒子不同，整团才有层次
vh = vy / sin(仰角) × cos(仰角)                    // 仰角 = lerp(45°, 90°, 上抛比例)
```

这样**大怪小怪都能看到同样明显的炸开**，身躯大小只作为"再加一点"的加成项。

#### (3) 【坑】"落点硬夹回圈里"会让粒子落地变成一个**圆环**

第一版是落地时把超圈的粒子拉回圈边（§16.11 上面那条"落点硬夹"）。
现在初速变大了，如果还靠硬夹，**所有飞远的粒子会一起贴在圆周上** → 看起来像个环 ✗

**修法：在发射那一刻就把横向初速压回圈内**（预测落点 = 横向初速 × 滞空时间）：

```csharp
滞空 = vy/重力 + √(2 × (出生离地 + 弹起高度) / 重力)
出生偏移 = 粒子出生点离中心的水平距离     // ★ 必须扣掉，否则球外侧出生的会飘出圈
想落 = 圈 × 随机(0.25~1.0) - 出生偏移
if (vh × 滞空 > 想落) vh = 想落 / 滞空
```

落地时的硬夹**保留成兜底**，并加了 `被拉回圈内数` 计数器好验证。

**实测（`被拉回圈内数` 应为 0）**：

| 境界 | 身躯半径 | 粒子数 | 弹起 | 落点半径实测 | 圈 | 被夹回 | 下落耗时 |
|---|---|---|---|---|---|---|---|
| 1 | 0.4（人形） | 16 | 0.71 米 | 0.12 ~ 0.47 | 0.60 | **0/16** | 0.66 秒 |
| 50 | 0.4 | 76 | 0.97 米 | 0.03 ~ 0.58 | 0.60 | **0/76** | 0.72 秒 |
| 90 | 0.8（胖怪） | 126 | 1.71 米 | 0.06 ~ 1.17 | 1.20 | **0/126** | 0.87 秒 |

整段四段流程自己走完并销毁：**2.09 秒**（境界 1）/ **2.35 秒**（境界 90）。

### 调参入口（都在 Inspector 上，都在 `NpcAbsorbEffect` 上）

| 想改什么 | 字段 | 默认 |
|---|---|---|
| **粒子大小** | `大小系数`（乘身躯半径） | 0.10 ~ 0.22 |
| **炸多高** | `爆开高度`（米） | 0.40 |
| 境界带来的额外高度 | `每级爆开高度`（米/级） | 0.0035 |
| 大体型额外高度 | `身躯高度加成`（乘身躯半径） | 0.5 |
| 炸开的仰角 | `上抛比例`（0=45°，1=90°） | 0.7（≈76°） |
| **落地那圈多大**（"范围"主开关） | `落点半径系数`（乘身躯半径） | 1.5 |
| 粒子出生散多开 | `散开半径系数`（乘身躯半径） | 0.7 |
| 掉得快不快 | `重力` | 20 |
| **停多久** | `落地等待` | **1 秒**（用户定） |
| 吸得多快 | `吸附加速度` / `吸附最快` / `减速系数` | 60 / 16 / 6 |
| 什么时候算吸到 | `到达距离` | 0.5 米 |
| 吸进去时缩多小 | `吸附末端缩放` | 0.25 |
| 粒子个数 | 基类的 `死亡消散粒子数`（0 = 自动按境界） | 0 |

> ⚠️ **`爆开系数` 这个字段已经删掉了**（第三轮），别再找它；
> 现在控制"炸多开"的是 `爆开高度` + `落点半径系数` 两个。

> ⚠️ **别再动 `死亡消散特效缩放`**：那个 3 倍现在只影响**老的爆发式消散**
> （`死亡粒子被玩家吸收` 取消勾选时才会走到）。

> **哪些地方不受影响**：玩家自己死亡（`PlayerDeathSequence`）、飞弹飞到消灭点的淡化
> （`NpcProjectile.消散消失`）**仍然用老的 `NpcDissolveEffect`** ——
> 它们不该"飞向玩家被吸收"。只有**NPC 死亡**这一条走了新的吸收版。

## 17. 战阵真灵（formation spirits）

**一句话**：妖魔 / 人类可以变成**真灵**上阵，缩小一半、半透明幽灵化，
主动打玩家锁定的目标，但不会跑出玩家的**神识范围**；没目标时回自己的阵位，
朝向和玩家一致、跟着玩家走。

### 涉及的文件

| 文件 | 作用 |
|---|---|
| `Assets/Scripts/Formation/SpiritFormationLayout.cs` | 九宫格 ↔ 站位的唯一对照表（UI 和运行时都读它） |
| `Assets/Scripts/Formation/SpiritFormationManager.cs` | 挂在 **Player** 上：差量生成/销毁、幽灵化、神识范围 |
| `Assets/Scripts/Formation/FormationSpirit.cs` | 真灵 AI（继承 `NpcAiBase`） |
| `Assets/Scripts/UI/UISpiritFormationBar.cs` / `UISpiritSlot.cs` | 九宫格 UI |
| `Assets/Shaders/GhostSpirit.shader` + `Assets/resources/Materials/GhostSpirit.mat` | 真灵外观 |
| `NpcDefinition.模型资源路径`（`NPC表.csv` 新增列） | 哪个 NPC 能当真灵（填了才行） |
| `UIPanelData`：`已获得真灵` / `战阵站位` / `待上阵真灵` | 数据层（上阵流程和神通装备完全同构） |

### 数据与流程

- **谁是候选**：`NPC表.csv` 里**妖魔 / 人类**且 `模型资源路径` 非空（导入时按预制体上
  `NpcInstance.定义.id` 反查自动填好）。
- **谁能上阵**：`UIPanelData.已获得真灵`。
  **当前 = 所有 demon / human（240 个：妖魔 148 + 人类 92）** ——
  也就是 `NPC表.csv` 里填了「模型资源路径」的那些，
  由 `DataTableImporter.RewirePanelData` **自动收集**。
  > 240 = 一个蒙皮版本一行的结果（见 §21）：单个定义的 33 个 + 按蒙皮拆出来的 207 个。
  >
  > **排序 = `UIPanelData.比真灵`**（导入器和运行时共用同一个比较器）：
  > ① 妖魔在前、人类在后 → ② 同一族的挨在一起 → ③ 族内**由弱到强**（境界升序）→ ④ id 兜底。
  > 实测：240 条里「人类后面又出现妖魔」= 0 次，家族被拆成不相邻段 = 0 段。
  > 用户原话：「如果可以的话最好做一下排序，让同一族的 npc 在一起，人类不要和 demon 混在一起」。
  > 【用户明确要求】包括**土地公、铁匠这种 NPC** —— 所有 demon / human 完成 AI 之后
  > 都能进真灵列表供战阵选择。我一度擅自收窄成"只收有专属 AI 脚本的怪"，被明确纠正了 ✗
  > **别再自行加过滤条件**。
  >
  > 顺带一个坑：`NpcAiBase.找物种脚本` 的最后一条兜底规则是「名字里含 `_关键字`」，
  > 而 `demon_gou**tou**junshi` 里正好含 `_gou` → 会被认成**狗**的专属 AI。
  > 拿脚本推断来过滤就会误伤，这也是别用脚本判定过滤的理由之一。
- **几个同时上阵**：`SpiritFormationLayout.可上阵数 = 5`。
- **九宫格是以玩家为中心的阵**（用户明确更正过两轮，别再改回去）：
  - **正中间那一格 = 玩家自己**（`SpiritFormationLayout.玩家格 = 4`），标签写「玩家」、点不动
  - **上面一行 = 玩家正前方**（+Z 是玩家朝向），下面一行 = 身后 → 所以能放真灵的是**外围 8 格**
  - 站位偏移：正前 3.8 米 / 前侧 (±3,3) / 正侧 ±3.8 米 / 后侧 (±3,−3) / 正后 −3.8 米
  - `已上阵数量` 只数 8 格里的；上满 5 个之后，**剩下还空着的那几格显示「已满」**并点不动
- **操作**：列表行上点「上阵」→ 进入待上阵 → 点任意空格子放进去；行上点「下阵」撤下。
  和神通的装备是同一套交互。

### 运行时行为（`FormationSpirit`）

**★ 最重要的一条：真灵和敌对 NPC 用的是同一套战斗 AI。**
`FormationSpirit` 继承的是 **`NpcAiCombatant`**（不是 `NpcAiBase`）——
也就是妖魔 / 人类那套「接近 → 出手 → 脱战」。真灵**只覆盖「敌人是谁」和「没敌人时站哪」**：

| 覆盖点 | 真灵的实现 | 基类默认 |
|---|---|---|
| `敌人` | 主人锁定的那只怪 | 玩家 |
| `敌人还活着` / `敌人靶` | 目标没死 / `NpcTarget(目标)` | 玩家 |
| `可以开打` | **只要有目标就开打**（不看好感度） | 好感度 ≤ 阈值 |
| `敌人在索敌范围()` | 目标离**主人**没超出「追击半径」 | 玩家在自己的索敌范围里 |
| `敌人已脱战()` | **恒 false**（脱战只由"目标跑出追击半径"决定） | 到敌人距离 > 脱战范围 |
| `视玩家为敌` | false | 好感度判定 |
| `决策()` | 选目标 + 主人跟丢兜底，然后**调 `base.决策()`** | 完整的战斗决策 |
| `执行接近()` | 追目标（带冲刺倍率 + 追击半径夹取）/ 回阵位 | 直线走向敌人 |
| `执行待机()` | 没敌人时朝主人；**有敌人什么都不做**（交给基类每帧朝敌） | 播待机 |

> **为什么非改不可**（用户提的、我一开始没想到的路子）：
> 最初真灵是自己把「决策 / 接近 / 可出手 / 朝向」**重写了一遍**，
> 结果和怪的行为各自跑偏 —— 实测**侧翼格的真灵离目标 0.8 米、攻击距离 2.5 米，
> 却永远不出手**（它那句"待机朝主人"和"转向目标"互相打架，朝向判定永远过不去）✗
> 用户原话：「我感觉你现在好像是专门给真灵写了一整套办法？」
> 收敛成「只有敌人不同」之后，这类偏差**从结构上不可能再出现**：
> `NpcAiBase` 里新增 `敌人 / 敌人还活着 / 敌人靶 / 到敌人距离 / 朝向敌人()`，
> `NpcAiCombatant` 里新增 `可以开打 / 敌人在索敌范围 / 敌人已脱战`（默认全部 = 玩家那套），
> **敌对 NPC 的行为完全不变**（回归实测：10 米生成 → 接近 → 2.49 米出手 → 打掉玩家 406.8 血 ✓）。

- **无敌**（用户明确要求「无限血量」）：`NpcInstance.无敌` 标志，
  `SpiritFormationManager.生成()` 给真灵打开。`TakeDamage` 直接返回 0，
  所以**漏过来的 AoE / 场景伤害 / 好感度改错都打不死它**，也不会误发「击杀掉修炼次数」。
  实测注入 999999 伤害 → 实扣 **0**、气血不变、没死 ✓
  （碰撞体本来也全关了，这是第二道保险。）
- **追击半径**（`SpiritFormationManager.追击半径`，默认 **20 米**）：
  真灵肯为主人跑多远。**这一条也修过一个 bug** —— 以前用的是「神识范围」（默认只有 14 米），
  于是用户锁了一只 20 米外的怪，三只真灵**连目标都不认**（`目标 = null`）、原位站桩 ✗
  现在和敌对 NPC 的「脱战范围 20~22 米」一个量级。填 0/负数退回旧行为。
  > ⚠️ 现实里玩家自己也**只能锁住 14 米内的怪** —— `BasicSword01.脱锁距离 = 索敌半径 = 神识范围`，
  > 超过就 `ClearLock()`。所以「真灵不去打 20 米外的怪」有时其实是**锁已经掉了**，不是真灵的锅。
  > 排查时先看 `NpcTargeting.LockedNpc` 还在不在。
- **回位 / 跟随**：阵位 = `玩家.position + 玩家.rotation × 站位偏移`（玩家本地坐标，
  以玩家为圆心、半径约 3.8 米的一圈，见上表）。**没目标时**站桩朝向 = 玩家的朝向。
- **真灵之间的分离**（`SpiritFormationManager.LateUpdate` 里的 `分离真灵()`）：
  任意两只见的水平距离小于 `真灵最小间距`（默认 1.4 米）就**互相推开重叠量的一半**，
  单帧限速 `单帧最大推移`（0.12 米）防抖，推完各自重新贴地。
  实测 5 只一起打同一个目标，10 秒内最小间距 **1.378 米**，不会挤成一坨。
- **跟丢兜底**：离主人超过 `跟丢距离`（25 米，覆盖御风飞走 / 传送 / 重生）直接瞬移回阵位。
- **重新索敌冷却 = 0**：主人一锁定就立刻开打。基类那 1.5 秒是给野怪在索敌边界反复横跳用的，
  真灵的目标是玩家点名的、没有"发现"这个过程。

### 真灵列表：排序 + 右侧滚动条（用户要求）

- **为什么要有**：真灵表从 96 条涨到 **240 条**之后，列表一屏只放得下 8 行 ——
  用户原话「在真灵选择的右侧加一个滚动条方便快速找到需要的 npc 真灵」。
- **滚动条**：`UIBuildUtils.AddVerticalScrollbar(scroll)`，
  在 `CharacterPanelBuilder.CreateScrollList(..., 带滚动条: true)` 里只给 `SpiritList` 打开
  （其它 6 个列表不动，免得白改一遍视口宽度）。
  - 轨道 18px 宽、贴在面板右侧、顶部和视口一样让开 34px 的标题；
    视口右边界同步收进 24px（滚动条宽 + 6 间距）
  - 可见性 = `AutoHide`（**不用** `AutoHideAndExpandViewport`：那个要靠 ugui 自己算视口扩展，
    这里直接构建时把视口宽度算死，更可控）
  - **两个小坑**：① `UIBuildUtils.CreateImage` 默认 `raycastTarget = false` ——
    轨道和滑块都要显式打开，否则**看得见拖不动**；② 滚动条节点要建在 Viewport **之后**，
    才能在射线检测里压住列表行（否则拖滑块会点到行上的「上阵」按钮）。
- **它长这样**：轨道是 18% 黑（叠在灰色面板上只暗一点点），滑块是橙色 `(0.78,0.52,0.33)`。
  实测像素采样：滑块 `(198,135,92)`、轨道 `(168,168,168)`（面板底色 184），
  内容高 9128px / 视口 314px → 滑块占轨道的 **3.4%**，滚到底时滑到底部 ✔
- 证据：`screenshots/sp_10_scrollbar.mp4`（滚一遍的慢放录像）、
  `sp_11_list_top.png`（妖魔开头）/ `sp_12_list_human.png`（妖魔→人类交界）/ `sp_13_list_bottom.png`（人类结尾）

### 十二个坑（都实测过）

0. **「不要挤到一块儿」不要用物理碰撞做**。真灵的碰撞体是**故意关掉的**
   （无敌 / 不挡路 / 选不中 / 不参与玩家 AoE），而且它们的移动是**直接写 `transform.position`**，
   挂 Rigidbody 会和 AI 的走位打架。正确做法是**成对的位置修正**（`分离真灵()`，在 LateUpdate），
   推完再贴地。见上面「真灵之间的分离」。

1. **★ 要序列化的 MonoBehaviour 必须独占一个「文件名 = 类名」的文件**。
   Unity 只认**文件名同名的那一个类**是该文件的主类；**同一个文件里的其它 MonoBehaviour
   在编辑期 `AddComponent` 看着完全正常，但场景一旦从磁盘重新加载（进出 Play、重启编辑器）
   就会变成 `<Missing!>` 脚本** —— 而且不报任何错，只是功能静默失效。

   - 战阵踩的版本：`UISpiritFormation.cs` 里塞了 `UISpiritSlot` + `UISpiritFormationBar`，
     文件里**一个同名类都没有** → 进 Play 后 `GetComponentsInChildren<UISpiritFormationBar>()`
     返回 **0 个**，九宫格是个空壳。
   - **顺带挖出一个存量 bug**：`UIActiveSkillSlot` 一直和 `UIActiveSkillBar` 挤在
     `UIActiveSkillBar.cs` 里 → 实测 **18 个主动技能格子里 14 个变成 Missing**
     （神通页 6/6 全坏），也就是「神通页点装备格子没反应」。
     已拆成 `UIActiveSkillSlot.cs` / `UISpiritSlot.cs` / `UISpiritFormationBar.cs`，
     进出 Play 之后 Missing = 0。
   - 全项目文本扫描（`class X : ... MonoBehaviour` 且 X != 文件名）现在只剩 **`UIEntryRow`**
     （在 `UIEntryList.cs` 里）。它**只在运行时 `AddComponent` 且 `hideFlags = DontSave`**，
     从不序列化，所以安全 —— 但**以后要把它挂进 prefab 的话必须先拆文件**。
   - 排查手法：`GetComponentsInChildren<Transform>(true)` 遍历，
     `GetComponents<Component>()` 里有 `null` 就是 Missing。
2. **`NpcIndicator` 在 `Awake` 里就把血条建好了**。只 `enabled = false` 不够 ——
   它的 `LateUpdate`（负责同步显隐）也不再跑，于是那条血条**永远挂在真灵头顶**
   （实测截图里三只真灵都顶着红血条）。正确做法：先 `SetSelected(false); SetLocked(false);`，
   再把它生成的 `IndicatorRoot` 整个拆掉。
3. **真灵必须关掉「前方探障」**（`前方探障距离 = 0`，在 `FormationSpirit.Awake` 里设）。
   阵位在主人正前方，真灵要走到阵位就得从主人身上穿过去；探障一挡，
   **卡在离主人 1.68 米处永远不动**（状态一直「接近」、每帧位移 0）。
   真灵本来就是可以互相穿过的灵体，不探障更合理。
4. **必须给冲刺倍率**。主人奔跑 6 m/s，白鹿精只有 **1.5 m/s** —— 不加速就永远追不上，
   越落越远。`冲刺倍率 = 4`（落后越多走得越快，平滑过渡）。
5. **`NpcAiBase.立即命中()` 是写死打玩家的**，真灵用它就会打主人。
   所以 `FormationSpirit` **自己实现 `结算伤害()`**，一律结算到 `锁定单位`。
   （顺带：`出手` 事件只能在声明它的类里 Invoke，所以基类加了 `广播出手()` 给子类用。）
6. **拆 prefab 自带的 AI 要用 `DestroyImmediate`**。`Destroy` 是帧末生效，而
   `NpcInstance.Start` 里那句 `NpcAiBase.确保()` 会 `GetComponent` 到"正在等待销毁"的敌对 AI
   → 它以为"已经有 AI 了"、不再装真灵 AI。（本工程 prefab 上其实没挂 AI，这里是保险。）
7. **★【bug·已修】真灵借不到「这个物种自己的招式」，拿到的是一套冷却为 0 的通用招式。**
   用户报的现象是「**战阵真灵的冰魄怪一直在放神通**，但按设定应该有 12 秒冷却」。

   根因：真灵身上挂的是 `FormationSpirit`，而 `NpcAiBase.确保()` 一看到"已经有 AI"
   就**不会**调 `应用类型默认值()`；于是基类 `Awake` 里那句 `确保攻击方式()` 给真灵填的是
   三条**通用默认招式** —— `Attack1` 普攻 / `Attack2` 神通 / `Attack3` 神通，
   **冷却全是 0** ✗。表现就是：冰魄怪真灵无限神通、白鹿精真灵不放飞弹（变成了近战）、
   白熊精真灵爪击没有冷却。

   修法：`FormationSpirit.借用物种招式()` —— 临时 `AddComponent(NpcAiBase.选脚本(自己))`，
   调一次 `应用类型默认值()` 让**物种自己的 `取默认参数()`** 把招式表算出来，
   把 `攻击方式 / 攻击距离 / 攻击间隔倍率 / 奔跑倍率 / 行走倍率 / 转向速度 /
   模型朝向补偿 / 需要视线 / 视线遮挡层` 抄到真灵身上，再 `DestroyImmediate` 掉临时组件。
   同帧建同帧拆，`Start` 永远不会跑到它。

   实测（`exec_runtime_script` 读真灵的 `攻击方式`）：

   | 真灵 | Attack1 | Attack2 | Attack3 |
   |---|---|---|---|
   | 冰魄怪 | 冷却 **2** | 冷却 **12**（施法·冰弹·挂点 `Bip01 L Hand`） | 动作名空 → 不可用 |
   | 白鹿精 | 冷却 3（施法·飞弹·挂点 `Bip01 L Finger0Nub`） | — | — |
   | 白熊精 | 冷却 2.5 | 冷却 **10** | — |

   > **别抄 `索敌范围` / `脱战范围`** —— 真灵的追击范围走「追击半径」，
   > 由 `SpiritFormationManager` 管，抄过来只会误导。
   >
   > 配套：`FormationSpirit.取出膛点()` 现在**先调基类实现**（基类会按招式配置里的「挂点」
   > 去找出生点骨骼），只在基类返回 `Vector3.zero` 时才退回"胸口高度"。
   > 不这么改的话，冰弹仍然从胸口出来、白鹿精的飞弹也不从手指出来。
8. **★【bug·已修】真灵"另写一整套战斗逻辑"= 它和敌对 NPC 的行为会各自跑偏。**
   这条是**用户点出来的**：「我感觉你现在好像是专门给真灵写了一整套办法？」

   我当时把「决策 / 接近 / 可出手 / 朝向」在 `FormationSpirit` 里重写了一遍，
   于是踩了这个坑：真灵**侧翼格 / 后排格**的离目标 **0.8 米**、
   攻击距离 2.5 米，却**一下都不打** —— 因为
   `执行待机()` 那句"朝主人"和 `决策()` 里那句"转向目标"**每帧互相打架**，
   `可出手()` 的朝向判定（40°）永远过不去；只有正前方那一格能出伤 ✗

   修法（**结构上根治**，不是补那一句朝向）：把「敌人是谁」抽成虚属性，
   让真灵**继承 `NpcAiCombatant`**、只覆盖敌人相关的几个点（见上面那张表）。
   现在真灵和怪走的是同一行 `可出手()` / 同一行"每帧朝敌"。
   实测 4 只真灵（0/1/2/3 格）全部 **偏 0°**、全部出手，10 秒打掉目标 4315 血 ✓

   > 回归验证（改完必须跑）：敌对 NPC 不锁任何目标、自己按好感开打 ——
   > 10 米生成 → `接近` → 2.49 米 `攻击` → 打掉玩家 **406.8** 血 ✓ 行为不变。
9. **★★【坑·已修·最贵的一个】`Resources.Load<GameObject>(路径)` 会挑到「和 prefab 同名的 .FBX」。**
   本工程 `Assets/resources` 里**同一个目录下同时有 `X.FBX` 和 `X.prefab`** 的摆法有 **17 个 NPC**。
   `Resources.Load` 优先把那个 **FBX** 给你，而**它不报任何错**：

   | 少了什么 | 表现 |
   |---|---|
   | 根节点上**没有 `NpcInstance`** | `定义` 为空 → `移动速度` 读出 **0** → **一步都走不动** |
   | **没有 AnimatorController** | `有动作("Attack1")` 永远 false → **一招都放不出来** |

   合起来 = 「真灵站在那儿发呆，也不过来也不打」，日志一片干净。
   实测中招的 17 个：野猪·幼 / 铁匠 / 逍遥子 / 神秘人 / 李阿翠 / 岳来峰 / 胡一鸣 / 玄冥子 /
   游方道士 / 兵（山贼）/ 危月燕 / 冥妖一~三 / 鬼气将 / 赤蟒精三 / 李村医
   （id：`demon_yezhu_01`、`npc_tiejiang`、`npc_xiaoyaozi`、`npc_shenmiren`、`npc_liacui`、
   `npc_yuelaifeng`、`npc_huyiming`、`npc_xuanmingzi`、`npc_youfangdaoshi`、`human_shanzei_01`、
   `npc_licunyi_01`、`demon_weiyueyan_01`、`demon_mingyao_01~03`、`demon_guiqijiang_02`、`demon_chimangjin_03`）

   **修法**：`NpcPrefabs.加载(路径)` —— 用 `Resources.LoadAll<GameObject>(路径)`
   把这一路径下的资产全取出来，**挑带 `NpcInstance`（且 `定义` 非空）的那个**。
   实测 240 个真灵定义**全部选对**（FBX 根节点上没有 `NpcInstance`，这是可靠的判据）。

   > **带扩展名没用**：`Resources.Load("xxx.prefab")` 实测返回 **null**，所以只能 LoadAll + 组件判定。
   > 任何**按路径实例化 NPC** 的地方都要走 `NpcPrefabs.加载()`，别再直接 `Resources.Load`。
10. **真灵不能用「到敌人的距离」判脱战**（`FormationSpirit.敌人已脱战() => false`）。
   基类那套是 `到敌人距离 > 脱战范围`。但真灵的敌人是**怪**，不是主人 ——
   主人和怪贴在一起、真灵还在 20 米外往回赶时，到敌人的距离能到 40 米，
   会被判定成"脱战" → `已交战 = false` → **永远站在那儿发呆** ✗
   真灵的脱战判据只有一条：目标跑出**主人的追击半径**（那时 `目标` 直接算 null、走回位分支）。
11. **★【bug·已修】模型缩了一半，`攻击距离` 没缩 —— 真灵站在够不着的距离挥砍。**
   用户报「**冰魄怪的 attack1 看着动作怪怪的**」，量下来根因在这儿：

   | 项 | 原尺寸（prefab 1.2 倍） | 真灵（×0.5 = 0.6 倍） |
   |---|---|---|
   | `Attack1` 手臂前伸（相对胸口，峰值 59%） | **0.68 米** | **0.34 米**（手也跟着短了一半） |
   | `攻击距离`（中心距） | 2.5 米 | **2.5 米（没缩）✗** |

   于是真灵站在 **2.5 米**外、用 **0.34 米**的手去挥 ——
   实测**命中那一刻右手指尖离目标表面还有 1.08 米**，看着就是在打空气。

   **修法**：`SpiritFormationManager` 生成时按 `模型缩放倍率` 一起缩
   （新增字段 `攻击距离随缩放`，1 = 全缩（默认），0 = 不缩 = 旧行为）：
   ```csharp
   float 距离系数 = Mathf.Lerp(1f, 模型缩放倍率, 攻击距离随缩放);
   真灵.攻击距离 *= 距离系数;
   真灵.出膛高度 *= 距离系数;      // 弹道退化到"胸口高度"时也别高过头顶
   ```

   修完实测（`攻击距离` 2.5 → **1.25 米**）：

   | 靶子 | 中心距 | 手到靶面 |
   |---|---|---|
   | 白熊精·王（包围盒 2.5 米宽） | 1.24 米 | **0.00 米**（贴上了）✓ |
   | 冰魄怪·幼（包围盒 1.7 米宽） | 1.25 米 | **0.24 米**（修前 1.08 米）✓ |

   > **凡是"缩放一个会打架的单位"都要同时问一句：它的距离参数缩不缩？**
   > 移动速度 / 冷却 / 碰撞半径这类不缩是对的（要跟得上主人），
   > 但**"手能伸多长"必须跟着缩**。

12. **★★【坑·已修】AI 在攻击动画播放期间转向 = 把动画自带的「转体」抵消掉。**
   用户报「**冰魄怪的 attack1 看着动作怪怪的 / 右手刺歪了**」，根因是这个（不是动画坏了）：
   攻击动作里自带转体（挥臂时身子跟着转，手才正好扫/刺向正前方），
   而 `NpcAiCombatant.Update()` 每帧把根节点转回目标 —— **动画转一点、AI 纠回去一点**。

   实测（玩家在起手瞬间被挪到怪的右侧 4 米）：

   | | 攻击过程中根节点转了多少 |
   |---|---|
   | 旧行为 | 12 帧 **+40.6°**，累计 **+90°** |
   | 新行为 | 全程 **0°** ✓ |

   修法：`NpcAiBase.攻击期间锁定朝向`（默认勾）+ `转向()` 里的闸门（**所有 NPC 统一生效**），
   外加 `进入攻击()` 里**先瞬时对准再进状态**的顺序。详见 §20。

   > 这个坑还有一层**副作用**：我前面量"手臂往哪挥"时，坐标系就是被它污染过的
   > （量的是根节点朝向，而根节点当时正被 AI 每帧纠着）——
   > **凡是"AI 在动的同时量某个相对量"，先问一句：我拿来当基准的那个东西，AI 是不是也在改它？**

### 无敌 / 不挡路 / 不显示血条（用户拍板）

真灵就是**灵体**：关掉全部碰撞体（玩家选不中、玩家自己的 AoE 打不到、怪也打不到、
走路不互相挤）、拆掉血条光环、好感度改成 +1000（否则玩家普攻的自动索敌会把自己的真灵当敌人）、
`NpcInstance.无敌 = true`（**打不死**，用户明确要求"无限血量"）。
实测：注入 999999 伤害 → 实扣 **0**、气血不变、不死亡 ✓

### 外观：`Cultivation/GhostSpirit`

半透明（`_Color.a = 0.70` = 「加 30% 透明度」）+ **边缘菲涅尔冷光** + **自下而上的波纹流动**，
`ZWrite Off`、不投影。材质在 `Assets/resources/Materials/GhostSpirit.mat`，
**颜色 / 亮度 / 边缘光强度 / 流动速度全在 Inspector 上**，实时可调。
shader 里刻意只用 ASCII 标签 —— 因为"shader 因为编码问题编译不过"是极贵的失败模式。

### 存档

`SaveData.战阵站位`（9 个 id 的字符串数组，版本 3）已接进 `SaveSystem` 的采集 / 还原，
实测往返：摆 3 个 → 采集 → JSON → 清空（场上 0）→ 还原（3 个各自回到原格子、材质正确）。

> **已知缺口**：真灵没有寻路（直线走），复杂地形会蹭墙 —— 和 NPC 一样，等真有需要再说。
> 「已获得的真灵」= 全部 demon / human（见上），**没有"收集进度"这个概念**了。

### 兽宠系统：已取消

用户决定**不要兽宠**了，`兽宠表.csv` / `SpiritPetDefinition` / 生成的资产
以及导入器里的那一行都**已删除**。战阵真灵就是它的替代品。

`NpcAiBase` 里当年给兽宠 / 召唤单位铺的**空架子也按用户要求一起删掉了**（用户原话「一起删了吧」）：

| 删掉的东西 | 说明 |
|---|---|
| `NpcAiState.被收服 = 6` | 枚举成员（没人再切到这个状态；状态不入存档，删了不影响存档） |
| `NpcAiBase.已被收服` / `IsTamed` | 从来没被任何地方读过 |
| `NpcAiBase.收服()` / `放生()` | 从来没被任何地方调过 |
| `NpcAiBase.执行被收服()` + `Update` 里的 `case NpcAiState.被收服:` | 空实现 |
| 若干注释里的「被收服成召唤单位」 | 改写成「战阵真灵（`FormationSpirit`）」 |

> 战阵真灵**不走这条路** —— 它是 `FormationSpirit` 自己覆盖 `决策 / 可出手 / 锁定单位 / 结算伤害`。
> 删完之后重新编译 0 错误、进 Play 冒烟测试正常（见下）。
>
> **删完的冒烟测试**（`exec_runtime_script`，6 秒）：在玩家前方 8 米生成一只 `冰魄怪·幼`，
> 实测 `接近 → 攻击 → 待机`，距离 8.00 → 5.66 → 3.35 → 2.49 米，命中玩家 411 点，
> `Missing 组件 = 0`。也就是状态机（被我删掉一个 case 的那个 switch）照常工作 ✓

### 怎么验证

- UI：点「上阵」→ 点外围空格子 → 九宫格出现真灵名 + 色条；**正中间那格写「玩家」且点不动**；
  上满 5 个之后剩下还空的格子变「已满」
- 世界：真灵以玩家为中心站成一圈（上方 = 你前方、下方 = 身后）、比玩家小一半、半透明、无血条无影子
- 战斗：锁定一只怪 → 真灵一起围上去打（木桩掉血），**玩家不掉血**，**真灵之间保持 ≥ 1.4 米**
- 神识范围：把怪挪到 40 米外 → 真灵立刻放弃、回待机
- 跟随：玩家瞬移 8 米 → 真灵几秒内归位（**包括正后方那格，会直接穿过玩家**），朝向始终与玩家一致

## 18. 白熊精：纯近战（按动画进度出伤）

**一句话**：`Attack1` 是普攻、`Attack2` 是主动神通，**两招全是物理**；
出伤**按动画进度**在「武器砸到人身上」那一刻结算，**命中特效和出伤同一时刻**播。

### 文件

| 文件 | 改动 |
|---|---|
| `Assets/Scripts/Npc/Ai/NpcAiBaiXiongJing.cs` | **新增**：白熊精专属 AI（一个类覆盖 01~04 四个变体） |
| `NpcAnimator.取动作长度(动作名)` | **新增**：按动作名查片段长度（修 Attack2 播不完的 bug，见下） |
| `NpcAiBase.攻击中()` / `立即命中()` | 出伤时刻不变（`出手进度`）；`立即命中` 以前**一个特效都不放**，现在统一放命中特效 |

### 招式（策划给的）

| 招 | 动画 | 类别 | 属性 | 倍率 | 冷却 | 出手进度 |
|---|---|---|---|---|---|---|
| 普攻 | `Attack1` | 普通攻击 | **物理** | 1.0 | 2.5s | **0.52** |
| 主动神通 | `Attack2` | 主动神通 | **物理** | 2.0 | **10s** | **0.78** |
| — | `Attack3` | 不用（动作名留空） | | | | |

### 出手进度是【实测】出来的（换模型/改动画要重新量）

在 Play 里把白熊精的 `Attack1` / `Attack2` 逐帧采样，记「武器最远能伸到身前多少米」：

| 动画进度 | Attack1 前伸 | Attack2 前伸 |
|---|---|---|
| 0.26 ~ 0.41 | 2.1 ~ 2.3 | — |
| **0.52** | **3.44 ← 峰值** | — |
| 0.57 ~ 0.68 | 1.2 ~ 1.5（收招） | 2.5 → −0.8（往后抡） |
| **0.78** | — | **4.01 ← 峰值** |
| 0.94 ~ | 2.3 | 3.0（收招） |

峰值那两个比例就是**武器砸到人身上**的瞬间，直接拿来当 `出手进度`。
**怎么量**：`Object.Instantiate` 一只白熊精 → 禁掉它的 AI → `NpcAnimator.PlayAction("Attack1")` →
每帧烘焙武器网格、把 8 个角变换到熊的本地坐标取最远 Z。

### ⚠️ 为什么【不做】「用武器网格实时判定」

**我做过一版，被用户否掉了**（原话：*"既然你出伤都没有按照网格碰撞来，那么特效应该配合出伤，
在出伤时播放特效，就不用网格碰撞检测了"*）：

1. 那个判定本质上是「**胸口点落在一个大 AABB 里**」，而熊的武器盒有 2~4 米大
   —— 等价于距离判定，并不比按进度出伤更"准"，但更难预期；
2. **特效必须跟出伤同步**。网格判定下出伤发生在"盒子刚碰到人"的那一帧，
   可能早于武器视觉上砸到的那一刻，特效就会提前闪 ✗
3. 每帧烘焙顶点 + 每帧碰盒的开销（~0.05ms/帧 × 每只怪）也省掉了。

> 实测数据（武器前伸多少米）**没有浪费** —— 它正好用来定 `出手进度`。
> 同理，`NpcBodyBounds.取武器实时包围盒()` 那套也一起删了（要用的话按 §18 的量法重写即可）。

### ⚠️ 【bug·已修·老代码就有的】Attack2 播不完

用户报的：*"白熊精的 attack2 明明有将近 1.25 秒，但是它播不完"*。

**根因**：`攻击中()` 用 `Animator.GetCurrentAnimatorStateInfo(0).length` 当"这一招多长" ——
但那一帧动作刚 `SetInteger` 切过去、**状态还没切完**，读到的是**上一个状态**的长度：

- 战斗里 NPC 的待机是 **`Fight` = 1.333 秒** ← 就是用户看到的"将近 1.25 秒"
- Attack2 = **1.867 秒** → 在 1.333 秒被 `收招()` 砍断 ✗
- Attack1 = 1.4 秒，和 Fight 只差 0.07 秒 → **一直没人看出来**

**修法**：新增 `NpcAnimator.取动作长度(动作名)`，**按"我请求播的那个动作"**去控制器里查片段长度。
受击那条（`Wound`，0.733 秒）有同一个毛病，一并修了 —— 以前挨打会僵直 1.3 秒而不是 0.733 秒。

**实测（修完）**：`Attack1` 状态持续 1.403~1.406 秒（片段 1.4）、`Attack2` 持续 **1.873 秒**（片段 1.867）✓

### 站位：中心距 2.5 米

武器最远伸到身前 **3.44 米**（实测）。

> ⚠️ **【坑·已修】近战的「攻击距离」要按观感定，不是按「判定框够不够得着」定。**
>
> 第一版我按"武器能伸 3.44 米"填了 **3.2**（判定完全打得到，45 秒 10/10 命中、
> 命中距离稳定 3.19 米）—— **但用户一眼就看出「武器没打到玩家身上」**：
> 武器尖只是刚够到人，等于站在极限距离用武器尖捅空气 ✗
>
> 改成 **2.5 米**后：武器能伸 3.44 米，等于**整条武器从玩家身上扫过去**。
> 实测 Attack2 在 **72%~86%** 这段窗口里武器盒**一直盖着玩家人心**；
> `screenshots/bx_02_attack2_78.png` 能看到棍子横着压在玩家身上。
> 站桩距离也从 3.19 变成 **2.495 米**，命中记录同样是 2.496 米。
>
> 底线是别穿模：熊胶囊半径 ~1.45 + 玩家 0.4 = 1.85 米，所以 2.5 米安全。

### 冷却的【真实含义】

`冷却` 是**从收招（动画播完）开始算**的，所以两次命中之间的实际间隔 ≈
**动画长度 + 冷却 + 后摇**：

- 普攻：1.4 + 2.5 + 0.25 ≈ **4.15 秒一下**（实测 4.07）
- 神通：1.9 + **10** + 0.25 ≈ **12.15 秒一下**（实测 12.26）

**想让"两次神通正好隔 10 秒"，把 `神通冷却` 调到 8 左右。**

### 实测（45 秒 + 真掉血）

| 项 | 结果 |
|---|---|
| 4 个变体 01~04 | 全都装上 `NpcAiBaiXiongJing`，招数与冷却一致 |
| 普攻 | 10 次命中，最小间隔 4.07s，伤害 595 |
| 主动神通 | 3 次命中，最小间隔 12.26s，伤害 1079 |
| 属性 | 全程 **物理**，0 次非物理；**没有任何飞弹**（`NpcProjectile` 数恒为 0） |
| 站位（3.2 米那版） | 命中距离 3.19 ~ 3.19 米，0 次被闪避 |
| 站位（2.5 米现版） | 站定 **2.495 米**，命中记录 2.496 米；挥砍窗口内武器盒**盖住玩家人心**（已出图） |
| 真掉血 | 关掉无敌后 4.5 秒把 420 血的玩家打死（`已死亡=True`） |

### 两个测试踩到的坑（不是代码 bug，但很浪费时间）

1. **测试留下的 NPC 会挡住后来的**。`exec_runtime_script` 之间 **Play Mode 是延续的**，
   我在几轮测试里 `Instantiate` 的白熊精没被清掉，新熊的「前方球探障」正好打在旧熊身上
   → 新熊卡在原地不动、状态停在「接近」，看着像 AI 坏了。
   **现象**：`前方被挡=True` 但前方啥也没有 → 先把场上的测试怪全清掉再测。
2. **玩家被打死之后，所有敌对 NPC 都会永久待机**（`决策()` 第一句就是
   `玩家生命.已死亡 → 待机`）。测战斗要么给玩家开无敌，要么 `回满()` 清掉死亡标记。
   注意 `当前气血` 每次 Update 会被夹回 `气血上限`，所以**不能靠直接把血写大来抗打**。

## 19. 命中特效：所有物理攻击最起码要有一个

**需求**：写 AI 时，**所有物理属性的攻击**都要有一个**基础命中特效**；
部分高级攻击可以用更高级的。特殊（法术）攻击各自有各自的表现，不强制。

### 机制（三条伤害路径**以前全都没有特效**）

| 路径 | 以前 | 现在 |
|---|---|---|
| `立即命中()`（够近就命中，**白熊精走的就是这条**） | **完全没特效** | 在玩家胸口放 |
| `在目标结算()`（飞弹命中） | 只在**自己配了** `命中特效路径` 时才放 | 走统一入口，物理攻击自动兜底 |
| ~~`武器判定()`（近战·武器网格）~~ | — | **已删**（见 §18：不做网格判定了） |

**出伤和特效是同一个时刻**（都在 `攻击中()` 的 `出手进度` 那一下），所以看起来就是"打到身上的那一下"。

核心就两个方法（都在 `NpcAiBase`）：

```csharp
// 这一招该放哪个特效（空 = 不放）
取命中特效路径(配置)：
    1. 这一招自己配了 命中特效路径 → 用它（★ 高级攻击走这条）
    2. 没配、但是【物理】伤害     → 用 物理基础命中特效（默认 FX/Hit_Physical）
    3. 特殊伤害又没配             → 不放

// 在命中点放出来；朝向 = 从命中点指向攻击者（火星朝攻击者溅）
生成命中特效(配置, 命中点, 朝向)
生成命中特效(配置, ICombatTarget)      // 打在某个单位身上，自动取判定点 + 朝向
```

两个开关：
- `NpcAiBase.物理基础命中特效`（默认 `FX/Hit_Physical`）—— 全 NPC 共用的基础特效
- `NpcAttackConfig.用命中特效`（默认**勾着**）—— 只有"一秒打十几下"的多段攻击才需要关掉
- `NpcAttackConfig.命中特效缩放 / 命中特效存活` 已有，继续可用

> **只在 `命中 && 伤害 > 0` 时才放** —— 被闪避就不放，这样"打没打中"一眼能看出来。

### 特效是自己做的（不依赖第三方特效包）

菜单 **修仙 / 构建命中特效**（`Assets/Editor/Builders/HitFxBuilder.cs`），全部程序化生成：

| 产出 | 说明 |
|---|---|
| `Assets/resources/FX/Hit_Physical.prefab` | **基础命中**：细四芒星闪光（0.62）+ 15 颗拉伸火星 |
| `Assets/resources/FX/Hit_Physical_Heavy.prefab` | **重击**：更大星芒（1.15）+ **26 颗**火星（更快更久） |
| `Assets/resources/FX/Textures/*.png` | 程序化画的贴图：星芒（64px）/ 光点（32px） |
| `Assets/resources/FX/Materials/*.mat` | 加色混合粒子材质 |

朝向约定：**预制体的 +Z 指向"攻击者"那一侧**（和飞弹命中特效一致），
所以调用方传 `Quaternion.LookRotation(攻击者位置 - 命中点)`。
两个粒子层都是 `playOnAwake` + 世界空间 + 一次性爆发，销毁交给调用方。

> **重击曾经有一个「冲击光环」，用户说有点突兀，已经删掉了**
> （贴图 / 材质 / 粒子层全删了）。现在重击和基础的区别只靠
> **星芒更大 + 火星更多更快**。要加环回来的话注意：环和星芒同心会读成"白色齿轮"，
> 而且环晚 0.03s 出现才像冲击波。

### 高级攻击怎么用更高级的（白熊精就是例子）

```csharp
普攻.命中特效路径 = 普攻命中特效;        // 留空 → 自动用基础物理特效
神通.命中特效路径 = 重击命中特效;        // "FX/Hit_Physical_Heavy"
神通.命中特效缩放 = 1.15f;
```
实测解析结果：`Attack1（物理）→ FX/Hit_Physical`、`Attack2（物理）→ FX/Hit_Physical_Heavy` ✓

### 三个坑

1. **星芒和光环不要同心叠**。第一版基础和重击都做了"星芒 + 光环"，
   两者同心叠在一起，40ms 那帧看着像个**白色齿轮** ✗
   （后来环干脆整个删了，见上。）
2. **`清掉飞弹配置()` 以前把 `命中特效路径` 也一起清了**。
   白熊精神通刚设好的重击特效被自己抹掉、又退回基础特效
   （实测解析出来是 `FX/Hit_Physical`，不查根本发现不了）✗
   命中特效**不是飞弹专属**，不能清。
3. 星芒的尖要**细**（`σ≈0.03`）。太粗 = 齿轮感。
4. **截图要隔一帧**：粒子是 `Instantiate` 之后**下一帧**才模拟出来的，
   出伤那一帧立刻 `Camera.Render()` 拍到的是**还没有粒子**的空画面。

### 玩家侧已经有，不用管

`BasicSword01HitEffect`（飞剑穿过敌人炸一簇火花）、
`BasicRemoteAttack01`（弹道 / 闪光 / 命中三条路径都有）—— 侧重点不同，没并到这套里。

## 20. 冰魄怪：近战普攻 + 冰弹神通

**一句话**：`Attack1` 是**物理**近战（右手爪，2 秒一次，动画 60% 出伤），
`Attack2` 是**特殊**冰弹（动画 63% 从**左手**射出，12 秒冷却）。

文件：`Assets/Scripts/Npc/Ai/NpcAiBingPoGuai.cs`（一个类覆盖 `BingPoGuai_01~04`）。

### 招式

| 招 | 动画 | 属性 | 类别 | 倍率 | 冷却 | 出手进度 | 打出去的是 |
|---|---|---|---|---|---|---|---|
| 普攻 | `Attack1`（1.20s） | **物理** | 普通攻击 | 1.0 | **2 秒** | **60%** | 前伸的右手爪（近战） |
| 主动神通 | `Attack2`（1.67s） | **特殊** | 普通攻击 | 2.0 | **12 秒** | **63%** | 左手射出的**冰弹** |
| — | `Attack3` | 不用 | | | | | |

- **普攻**：策划原话「用前伸的右手的网格来做伤害判定，动画的 60% 处是出伤实际，给命中特效」。
  实现上**不做网格判定**（理由见 §18），直接 `出手进度 = 0.60`，
  **出伤 + 命中特效都在 60% 那一刻**，特效自动用物理基础特效 `FX/Hit_Physical`。
- **冰弹**：`伤害属性 = 特殊` + `攻击类别 = 普通攻击` = 策划要的「**特殊普通攻击**」。

### 冰弹的 5 个要素

| 要素 | 做法 |
|---|---|
| **1 出生点** | 左手骨骼末端：挂点 `Bip01 L Hand` + 偏移 `(0, 0, 0.55)`（模型里没有手指骨，只能近似） |
| **2 飞行路径** | **生成时朝玩家方向直线飞、匀速、不追踪**（`NpcProjectile.设置直线飞行`） |
| **3 命中效果** | 对玩家造成 **特殊 · 普通攻击** |
| **4 能否被阻挡** | **不和场景碰撞**（墙挡不住），只检测 `拦截层` = 图层「**拦截**」（新建的 index 8）。<br>以后技能神通 / 法宝 / 灵阵放到那一层就能打掉它 |
| **5 消灭点** | 飞出 `消灭距离 = 22 米` 就自行**淡化消散** |

> **新建了图层「拦截」（index 8）**：项目原来只有 Default / TransparentFX / Ignore Raycast / Water / UI，
> 没有任何能表达"技能神通 / 法宝 / 灵阵"的层，所以"可以被它们阻挡"这条**根本没法落地**。
> 层建好了，等那些系统做出来放上去即可。
> 代码里 `取拦截层()` 对"图层不存在"做了保护（返回 0 = 谁都拦不住，不会 `1 << -1` 溢出）。

### 冰属性观感

弹道 / 枪口闪光 / 命中爆炸**全部用 QFX 的 Winter 系列**（这套包的 Winter 就是冰系配色，偏冰蓝）：

- `QFX/ProjectilesFX/VFX_Prefabs/Projectiles_Particles/VFX_Winter_Projectile_Only`
- `.../Flashes/VFX_Winter_Flash`
- `.../Impacts/VFX_Winter_Impact`

### 实测（30 秒）

| 项 | 结果 |
|---|---|
| AI 装配 | 4 个变体全装 `NpcAiBingPoGuai` |
| 普攻 | 命中 595（**物理·普通攻击**），30 秒 8 次 ≈ 3.2 秒一下（2 + 1.2 + 0.25） |
| 冰弹 | 命中 1031（**特殊·普通攻击**）✓ |
| 冰弹出生 | 在左手前方出现，**与玩家的夹角 0°**（直线朝玩家飞）✓ |
| 拦截层 | 256（= 图层 8「拦截」）✓ |

> ⚠️ **一个待定细节**：基类 `生成子弹` 会在出膛点再往前推一个 `出膛余量()`
> （= 施法者碰撞体半径 + 0.35，冰魄怪这里约 **1.2 米**），目的是"别一出生就撞到自己"。
> 所以冰弹**实际出现的位置比左手末端远约 1.2 米**，不是紧贴手。
> 这是所有 NPC 飞弹的公共行为；要让冰弹紧贴手出来就得给"出膛余量"加个每招覆盖，**等用户定**。

### 动画实测：把每个片段的「手伸到哪儿」量出来了

用户说「**冰魄怪的 attack1 是不是有问题，看着动作怪怪的**」，所以把控制器和骨头位置都量了一遍。
方法：`anim.Play(动作名, 0, t)` + `anim.Update(0.0001f)` 把姿态**精确定格**在任意进度，
再量左右手相对胸口在正前方的投影（`Vector3.Dot(手 - 胸, forward)`），单位米。

**测出来是（放大前的 prefab，1.2 倍）：**

| 进度 | Attack1 右手 | Attack1 左手 | Attack2 右手 | Attack2 左手 |
|---|---|---|---|---|
| 0% | 0.14 | 0.18 | 0.14 | 0.18 |
| 20% | 0.06 | **0.52** | 0.75 | 0.72 |
| 29% | 0.07 | **0.57** ← 左手最远 | −0.32 | −0.45 |
| 49% | 0.08 | 0.45 | **−0.73** ← 双手收到最后（蓄力） | −0.69 |
| 59% | **0.68** ← 右手最远 | 0.42 | −0.02 | −0.05 |
| 69% | 0.53 | 0.49 | **0.95** ← 双手推到最远 | **0.97** |
| 98% | 0.22 | 0.21 | 0.24 | 0.27 |

**结论（四条）：**

1. **Attack1 的动画和出伤时机是对的** —— 右手在 **59%** 伸到最远（0.68 米），
   配置的 `出手进度 = 0.60` 正好压在这个峰值上 ✓ 不是 Attack1 坏了。
2. **★ Attack3 和 Attack2 是同一个动画** —— 两条曲线的小数点后两位完全一样
   （0.75/0.59/…/0.95/0.97）。冰魄怪的 `Attack3` 动作名已经清空、不会用，所以无害，
   但**以后要给它加第三招时别直接拿 Attack3 当"另一招"**。
3. **Attack2 的 `出手进度 = 0.63` 比手的峰值（69%）早约 0.1 秒** ——
   冰魂是在双手**还在蓄力→前推的路上**出来的。想让冰弹"跟着手一起推出去"就改成 **0.69**。
   （**没动**，属于手感调参，等用户定。）
4. **★★ 上面这些测量的坐标系本身被 bug 污染过，别当铁证** ——
   见下一节：AI 在攻击动画播放期间每帧把根节点转回目标，把动画自带的**转体抵消掉了**。
   量出来的"往侧下方挥"有相当一部分是这个抵消的产物。

### ★★ 根因：动画的「转体」烘在 Root Motion 的根旋转里，被 `applyRootMotion = false` 丢掉了

**用户一步步逼出来的**（原话）：
> 「正常应该是向前挥出的手臂会附带一个转体……但是你可能设计了怪的 ai 要时刻朝向玩家，所以导致了实际上右手刺歪了」
> →（改完还是歪）「是不是动画或者骨骼什么的没有挂对，感觉后面刺出时是对的，但是中间扭动好像有点问题，
> **他手臂向后蓄力时身体没有转过来，侧身送出手臂时身体也没有转过来，好像身体转不过来一样**」
> →「最开始是对着正下方的，经过扭动，最后对着右下方，然后攻击用的手指向了最开始的方向，
> **说明动画是没问题的，只要攻击过程不转向玩家就行**」

**最后那句「身体转不过来」是钥匙。** 量下来：

| 项 | 结果 |
|---|---|
| `Animator.avatar` | **`BingPoGuaiAvatar`（Humanoid）** |
| Attack1 的根路径曲线 | `RootT.x/y/z` + **`RootQ.x/y/z/w`** + 一堆肌肉曲线（`Spine Twist Left-Right` …） |
| `Bip01` 根骨骼自身的偏航（Play 定格 / 自然播，两种方式） | **全程 0°** |

也就是说**「转体」不在骨骼上，在根变换的 `RootQ` 里**。而 `NpcAiBase.关掉RootMotion()`
当年为了治「Wound/Attack 位移累积、挨一下飞天」，把 `applyRootMotion` 直接设成了 `false` ——
**那等于把 `RootT` 和 `RootQ` 一起丢掉**，于是转体没了、手刺歪 ✗

> 当年那个 `false` 治的是**位移**（RootT），治过头把**旋转**（RootQ）也一起杀了。

**修法：`applyRootMotion = true` + 自己实现 `OnAnimatorMove()`，只吃旋转、丢掉位移。**

```csharp
// Awake
接管RootMotion();      // 设 applyRootMotion = true

/// 只要实现了 OnAnimatorMove，Unity 就不再自动应用 root motion —— 由我们决定怎么消费
protected virtual void OnAnimatorMove()
{
    if (动画 == null || !强制关闭RootMotion) return;
    if (状态 != NpcAiState.攻击 || !攻击期间锁定朝向) return;
    if (自己 != null && 自己.IsDead) return;

    transform.rotation *= 动画.deltaRotation;   // ★ 只吃旋转
    // deltaPosition 一律丢掉 → 不会「挨一下飞天」
}
```

- `applyRootMotion` 必须是 **true** —— 不是要它自动应用，而是**只有 true，Unity 才会调 `OnAnimatorMove`**
- **攻击中不能锁 `transform.rotation`**（我第一版加过硬锁，那会把刚找回来的转体又摁掉）

**实测**：运行时 `applyRootMotion = True` ✓｜攻击期间根节点**开始跟着动画转了** ✓
｜受击 1.5 秒内位移 **0.000 米**（没飞天）✓

### 配套：控制器过渡时长 0.3s → 0.1s

`Assets/Editor/Tools/NpcAnimTransitionFixer.cs`，菜单
**修仙 / NPC 资产 / 压短动画过渡（起手别再边跑边挥）**。
控制器在 **`Assets/Animations/NPC/`**（101 个，**不在 `Assets/resources` 下**），实测改了 **704 条**。

两个毛病都是这 0.3 秒造成的：
1. 跑动中起手时，攻击动画前 30% 和跑步姿势混着播（`normalizedTime` 已经是 0.29）
2. **交叉淡入会把上一个状态的根旋转（待机 ≈ 0°）和攻击的根旋转混在一起** ——
   累计出来的转体角度不对

> ✅ **用户确认修好了**（2026-09-22）：「欧克，现在对了」。
>
> 压短过渡之后单次 Attack1 累计出来的根偏航能到 **±70~90°** —— 那不是 bug，
> 就是这段动画真的转了这么大一圈（大范围挥爪 + 转身），配合根旋转才是完整动作。
> `NpcAiBase.强制关闭RootMotion` 取消勾选 = 回到"完全不吃 root motion"的老行为，可 A/B。

### 走过的弯路：先以为是「AI 边播边转向抵消了转体」

用户第一轮判断是：「AI 要时刻朝向玩家 → 动画的转体被抵消 → 手刺歪」。
当时按这个改了（`攻击期间锁定朝向`：攻击期间 `转向()` 不生效 + 起手瞬时对准），
实测确实把 AI 的转向挡住了（把玩家挪到怪的右侧 4 米，旧行为 12 帧转 +40.6°，改完全程 0°），
**但用户看下来还是歪** —— 因为真正的转体不是被 AI 挡的，是**根本没播出来**（见上一节的 RootQ）。

现在这套闸门**保留**（它本身是对的：AI 确实不该在攻击动画期间抢转向），
但**去掉了那版加的「硬锁 `transform.rotation = 出手朝向`」** ——
那会把 Root Motion 刚转出来的身体又摁回去 ✗

### 全量审计：**其他 NPC 有没有同样的问题**（用户追问后查的）

修复写在 `NpcAiBase` 里 → **所有有 AI 的 NPC 自动吃到**。但有三件事是每个 NPC 各自不同的，
所以逐个查了一遍（260 个 NPC prefab / 104 个控制器）：

| 分类 | 数量 | 说明 |
|---|---|---|
| **Humanoid（有 Avatar）** | **250** | 转体在 `RootQ` 里 → 全部吃到这次修复 |
| Generic（**没有 Avatar**） | 10 | `RongYanGuai_01~04`、`ShuangTouShe_01~04`、`Chibang_01`、`thxr` |
| 攻击片段**带根旋转**的控制器 | **67** | 这些就是「转体原来播不出来」的，这次全部修好 |
| 攻击片段**不带根旋转**的控制器 | 33 | 狗/猴/驴/母鸡/兔子/各种坐骑/典狱官/管理员 —— **本来就是无转体的动作，行为完全不变**（无回归） |

**10 个 Generic 的单独查过**：

- **双头蛇**：骨架是 Biped（`Bip01` 上有曲线），根运动走的是骨骼路径而不是 `RootQ`。
  实测（放玩家正前方 8 米）能走过来打人，**根 yaw 摆幅 0.0°**、y 漂移 0.076 米 → 没有乱转、没有飞天 ✓
- **熔岩怪**：整个模型只有 `Bone01/02/03` 三条曲线 —— **根本没有转体可言**，同样 0.0° ✓
- 结论：Generic 的这几个**既没被修坏、也没有可修的转体**

**抽查（4 种骨架/曲线组合，各放 8 米外）**：

| NPC | 骨架 | 会来打 | 接近到 | y 漂移 | 根 yaw 摆幅 |
|---|---|---|---|---|---|
| 双头蛇·幼 | Generic | 是 | 3.2 米 | 0.076 | **0.0°** |
| 熔岩怪·幼 | Generic | 是 | 3.2 米 | 0.076 | **0.0°** |
| 蛤蟆精·幼 | Humanoid | 是 | 3.2 米 | 0.076 | 累计 ~444°（9 秒内 4 次攻击） |
| 典狱官 | Humanoid | 否 | 3.4 米 | 0.073 | 0.0° |

> **典狱官不来打是正常的**：它是**人类**，初始好感 ≥ 阈值 → `视玩家为敌 = false` → 不会主动打玩家（设计如此）。
> **「y 漂移 0.07」是贴地微调**，不是飞天（飞天会是几米级）。

**过渡时长**：`Assets/Animations/NPC` **和** `Assets/Animations/NPC_Creature` 两个目录
都压过了 —— 第一次只扫了前者，漏掉 `NPC_Creature` 的 **22 条**（熔岩怪/双头蛇那些）。
现在 **>0.1s 的过渡 = 0 条** ✓

**顺带挖出一个坏资产**：`Assets/resources/NPC/Human/LeiGong/LeiGong_02.prefab`
—— **里面有丢失的脚本**（`SaveAsPrefabAsset` 直接报 "You are trying to save a Prefab with a missing script" 拒绝保存），
`NpcInstance.定义` 也是空的，而且它是**唯一一个 prefab 上还开着 root motion** 的。
定义为空 → `NpcInstance.Start` 不会给它装 AI → 没人接管 → **它一旦播 Wound/Attack 就会飞天**。
它不在真灵列表里（定义为空），所以暂时没影响，**但它是坏资产，要么修要么删 —— 等用户定**。
（顺手把 `NpcAssetFixer.处理RootMotion` 改成遇到"有丢失脚本"的 prefab 就跳过并单独列出来，
不再让一个坏 prefab 把整批修复带崩。）

### 两招的运动轨迹（实测，根节点完全不转时）

| 招 | 右手 本地「前」 | 右手 本地「右」 | 读起来像 |
|---|---|---|---|
| **Attack1**（普攻） | 0.07 → **0.67**（60%）→ 0.51 | 0.73 → **1.13**（72%） | 手往右侧甩出去 1.1 米、前伸只有 0.67 米 → 配合根旋转才是完整的一记横扫 |
| **Attack2**（神通） | −0.33（蓄力）→ **0.91** | 0.37 → **0.32** | **干净的双手正直前推**：前 0.9 米、侧向只有 0.3 米 |

> 注意这是**根旋转被丢掉时**量的，所以只有"半套动作"。
> 好消息：横扫一样能打到人 —— 实测（缩到 0.6 倍的真灵 vs 白熊精·王）
> 命中那一刻**手到靶面 = 0.00 米** ✓

> **这条规矩对所有 NPC 生效**（妖魔 / 人类 / 野兽 / 战阵真灵），
> 因为闸门写在 `NpcAiBase.转向()` 里 —— 以后新写的 AI 自动就有。

## 21. NPC表：一个蒙皮版本一行 + 数值逐档递推

**背景**：素材里同一个怪有多个蒙皮版本（`BaiXiongJing_01~04`、`BingPoGuai_01~04`……），
而 NPC表里**一个家族只有一行** → 4 个 prefab 共用同一份 `NpcDefinition`，
等级 / 属性完全一样，也没法单独调。用户要求「按不同的蒙皮版本完善 NPC表」。

### 工具：`NpcVariantSplitter`（可反复跑）

菜单 **修仙 / 配置表 / NPC 按蒙皮版本拆分并递推数值**
（只做数值：**修仙 / 配置表 / NPC 数值按版本递推**）。
**改表前自动备份**到工程外 `D:/project：cultivation/_backup_NPC表_<时间>.csv.bak`（没有 git，改坏了能捞回来）。

**两步，都是幂等的**：

1. **拆分**：多蒙皮家族（prefab 数 > 1）里每个 prefab 一行 ——
   `id` = `<原id>_<NN>`、`名字` = `<原名><NN>`、`模型资源路径` = 它自己的路径；
   同时**删掉旧的共用定义**、把每个 prefab 的 `NpcInstance.定义` 接成自己那份。
   已拆过的家族（表里有 `<原id>_01`）**自动跳过** ✓
2. **递推**：每个家族的第 N 版**高一个大档**（用户定的），每次都以 `_01` 为基准重算 ✓
3. **命名**：给每个版本一个**由弱到强**的称呼（`白熊精01` → `白熊精·幼` … `白熊精·王`），
   见 §22 —— 这一步是后加的，改名字之前**先看 §22 的三个坑**

> ⚠️ 因为第 2 步是从 `_01` 重算的，**手工改过 02 之后的数值，重跑会被覆盖**。要单独调就改 `_01` 的基准。
>
> ⚠️ **手工配好的 4 个家族（白鹿精 / 赤蟒精 / 鬼气将 / 冥妖）在 `NpcVariantSplitter.手工家族` 名单里，
> 递推和命名都跳过**。以前是靠「名字不以 NN 结尾」隐式排除的，命名上线后这条失效了，见 §22 坑 1。

### 现状（实测）

| 项 | 值 |
|---|---|
| 多蒙皮家族 | **63 个**，占 207 个 prefab |
| demon / human 定义 | 96 → **207 个版本 + 33 个单版本 = 240** |
| NPC表数据行 | 110 → **254**（240 + 中立 3 + 兽类 10 + …） |
| 同一个定义被多个 prefab 共用 | **0**（兽类兔子除外，见下） |
| 战阵真灵列表 | **240** 个（所有 demon / human），按「妖魔 → 人类 → 同族相邻 → 由弱到强」排 |
| 名字以数字结尾的行 | **0**（全部换成了 §22 的称号） |

### 递推规律（**曲线是从现有表里拟合出来的，不是拍的**）

**境界**：第 N 版 = 基础 + 10 × 步长 × (N-1)，**高境界家族自动压缩步长**保证不越过满级 90：

- 基础 51 的 4 版 → **51 / 61 / 71 / 81**（正好一档一个）
- 基础 71 的 4 版 → **71 / 77 / 84 / 90**（压缩）
- 基础 81 的 3 版 → **81 / 86 / 90**（压缩）

**属性**（同一个大档内实测出来的比例）:

| 属性 | 每大档 |
|---|---|
| 气血 | ×1.75 |
| 攻击 | ×1.6489 |
| 防御 | ×1.6225 |
| 灵力 | ×1.6998 |
| 暴击 | +0.05 |
| 会心 | +0.04 |
| 真实伤害 / 闪避 / 冷却缩减 | +0.03 |
| 暴击抗性 / 会心抗性 / 真实 / 普攻伤害加成 / 普攻伤害减免 | +0.02 |
| 真实抗性 | +0.01 |
| 气血回复 / 灵力回复 | = 气血×0.01 / 灵力×0.02（跟着推） |
| 移动速度 / 攻速 / 暴击伤害 / 会心伤害 / 忽视闪避 / 治疗加成 / 各类法术加成减免 | **不变** |

实测示例：白熊精 01~04 = 境界 **51/61/71/81**，
气血 6565/11489/20105/35184，攻击 367/605/998/1645，暴击 0.25/0.3/0.35/0.4 ✓

> 用拟合比例而不是查表：非大档的等级（86/77/84…）本来就没有现成数值，
> 而且这样一次性覆盖全部属性。代价是**落到整档上有 ≤0.2% 取整偏差**
> （境界81 的气血表里是 35186，递推出来 35184）。
> 想完全不偏就得改成"整档查表 + 非整档插值"，等有需要再说。

### 手工定过的没被动

`白鹿精·幼 / 白鹿精 / 白鹿精·王`（`demon_bailujing_01/02/03`，境界 1/4/10，
气血 1225/3752/11489）本来就是**三份独立定义**，不在拆分范围内 ✓ **一点没动**。
和它同类的还有 **赤蟒精**（21/51/71）、**鬼气将**（31/51/71）、**冥妖**（31/41/51/61）——
这 4 个家族现在写在 `NpcVariantSplitter.手工家族` 里，递推和命名都绕过它们。

### 遗留（用户没要求，我没动手）

1. **6 份 `mount_*` 孤儿资产**（翅膀 / 狼骑 / 鹿坐骑 / 麒麟坐骑 / 熊猫坐骑 / 战骑）：
   `Assets/Data/Generated/NpcDefinition` 里有、但 NPC表里**没有对应的行**（名字还是默认的"新NPC"）。
   它们没有模型路径、也不在真灵列表里。**坐骑系统要做的话得先把这些行补回表里**。
2. **兽类（动物）没拆**：实测只有兔子（`beast_tuzi`）是两个蒙皮共用一份定义
   （`TuZi_01` / `TuZi_02`）。用户这次说的是 demon / human，所以兽类我没动 —— 要拆说一声。
3. 导入器**不清理孤儿资产**（表里删了行，`Assets/Data/Generated` 里的资产还在）。

## 22. NPC 变体的称呼：由弱到强（不许叫 01/02/03）

**一句话**：拆分脚本本来给每个蒙皮版本起名「`白熊精01` / `白熊精02`」，
用户要求「**不要直接起 01，02，03，给这些 npc 一个由弱到强的称呼，编号越低越弱、越高越强**」。
现在变成 **`白熊精·幼 / 白熊精·壮 / 白熊精·悍 / 白熊精·王`**。

### 两套称号（顺序就是强弱，别乱调）

| 类型 | 由弱 → 到强（8 档） |
|---|---|
| **妖魔** | 幼 · 少 · 壮 · 猛 · 凶 · 悍 · 霸 · 王 |
| **人类** | 微末 · 寻常 · 出众 · 拔萃 · 超群 · 绝顶 · 盖世 · 无双 |

> **人类为什么不用「初学 / 小成 / 宗师」那套**：人类那一列里**平民和神仙是混着的** ——
> 既有 老头 / 李村医 / 山城土匪，也有 嫦娥 / 哪吒 / 杨戬。
> 用修士进阶的词会出来「**嫦娥·初学**」「**老头·大家**」这种一眼别扭的名字 ✗
> （我先用了那套，截图一看就不对，换成了气势档次的词 —— 凡人和神仙都读得通。）
>
> 想换词：只改 `NpcVariantSplitter.妖魔称号` / `人类称号` 两个数组，再跑一次
> **修仙 / 配置表 / NPC 称号按版本命名** 就会整表重排（见下面「可以反复跑」）。

### 怎么分档

家族内按**境界升序**排名，把 8 个称号**均匀铺开**：
第一个 → 称号[0]，最后一个 → 称号[7]。
即 `idx = round(第几个 × 7 / (共几个 − 1))`，所以不会重词：

| 版本数 | 铺出来的称号 |
|---|---|
| 2 | 幼 / 王（微末 / 无双） |
| 3 | 幼 / 凶 / 王 |
| 4 | 幼 / 壮 / 悍 / 王 |
| 5 | 幼 / 少 / 猛 / 悍 / 王 |
| 8（山城土匪） | 八个全用上 |

实测：`山城土匪·微末 → 寻常 → 出众 → 拔萃 → 超群 → 绝顶 → 盖世 → 无双`（境界 31→90）✓

### 三个坑

1. **★【坑·已修】不能再靠「名字以 NN 结尾」来认变体行。**
   原来是靠这个条件做到两件事：认拆分行 + **顺带排除手工家族**。
   名字一改成「白熊精·壮」，这个条件就永远不成立 ——
   再跑一次递推会**整表跳过**（看着像"没坏"），而一旦有人把它去掉，
   又会**把白鹿精的 1/4/10 冲成 1/11/21、赤蟒精的 21/51/71 冲成 21/41/61** ✗
   现在改成：**只看 id**（`<基>_NN`），手工家族用**显式名单**排除
   （`NpcVariantSplitter.手工家族` = `demon_bailujing / demon_chimangjin /
   demon_guiqijiang / demon_mingyao`）。
2. **只改「本步骤管得着」的行**，所以手工起过名的一个都不动：
   冥妖·一 / 鬼气将·帅 / 火狼 / 土地公 / 铁匠……
   > 判定见 `名字可重排()`：`<原名>NN`（以数字结尾）或者
   > `<原名>·<梯子上已有的称号>`。第二种是为了**能换一套词重跑**。
   > 注意：**换词时如果新梯子和旧梯子的词完全不重叠，就认不出来**了 ——
   > 这时得先从备份（`D:/project：cultivation/_backup_NPC表_*.csv.bak`）恢复再跑
   > （我这次换人类的词就是这么做的）。
3. **`手工家族` 要连递推一起排除**，不只是不重命名。
   她们的境界是手工分档的（白鹿精 1/4/10、赤蟒精 21/51/71），
   递推会按「每版一大档」重算，直接冲掉。

### 结果

- 改了 **207 行 / 67 个家族**；`名字以数字结尾的行 = 0`
- 手工家族一分未动：白鹿精 1/4/10、赤蟒精 21/51/71、鬼气将 31/51/71、冥妖 31/41/51/61 ✓
- 名字同时是**真灵列表里的显示名**（`NpcDefinition.名字` → `DisplayName`），
  也是击杀日志里的名字 —— 所以列表里看到的就是「白熊精·悍」这种

### 顺带补的英文译名（用户确认过）

素材里有 6 行的 `名字` 一直是**英文没翻译**，命名步会把称号直接缀在英文后面
（`LieQuan·凶`）。用户拍板译名之后，在 `NpcVariantSplitter.名字修正` 里加了一张对照表：

| 原名 | 译名 | 涉及 |
|---|---|---|
| `LieQuan` | **猎犬** | `demon_liequan_01/02/03` → 猎犬·幼 / 猎犬·凶 / 猎犬·王 |
| `XiaoYaoZi` | **逍遥子** | `npc_xiaoyaozi` → 逍遥子；`human_xiaoyaozi_01/02` → 逍遥子·微末 / 逍遥子·无双 |

> 表是**键=英文家族名**，命中后连「不成家族的单个 NPC」（`npc_xiaoyaozi` 的名字就是光秃秃的
> `XiaoYaoZi`）也一起改 —— 所以 `命名()` 里除了家族那一轮，还有第二轮**整表精确匹配**。
> 改完键就再也匹配不上，反复跑没问题。
>
> 实测：改 6 行；全表 `含 ASCII 名字的行 = 0`；
> 真灵列表里 `逍遥子 / 逍遥子·微末 / 逍遥子·无双` 三条**挨在一起**
> （家族名都取模型文件夹 `XiaoYaoZi`，所以虽然是 `npc_xiaoyaozi` 和 `human_xiaoyaozi_*`
> 两个 id 命名空间，排序上仍归一族）✓

---

## 23. 坐骑资产导入（2026-09-23）

用户从 `D:\project：cultivation\somenews` 给了 3 套「爱给网」下载的龙/兽模型，
**是给以后的坐骑系统备的料**。要求：把**能用的部分**导入 `Assets/resources/NPC/Mount/`，
**每份资产一个文件夹**。

### 导入了什么

| 源目录 | 导入为 | 内容 |
|---|---|---|
| 古风上古神龙模型环绕飞行骨骼绑定动画 | `NPC/Mount/ShenLong_01/` | `01-环绕飞行.fbx` + `01-环绕飞行.fbm/`（15 张 png）+ `Materials/`（8 个） |
| 带骨骼绑定动画巨龙 神龙 神话龙 恶龙 飞龙 | `NPC/Mount/JuLong_01/` | `drgon.fbx` + `dragon.png`（漫反射）+ `dragon-No.png`（法线）+ `Materials/` |
| 碧水兽_3ds Max动作源文件8个 | `NPC/Mount/BiShuiShou_01/` + `BiShuiShou_01_Animation/` | 基础 `Bishuishou.fbx` + `Texture/`（5 tga）+ 8 个动作 FBX |

**没导入**（引擎读不了，留在 `somenews` 当源文件）：`.c4d`（2 个，158 MB）、`.max`（10 个）、
`.blend`（1 个，16 MB）。合计 190 MB 没进工程。

### 命名：中文原名 → 拼音 ASCII

跟项目里既有的 `LangQi_01` / `XiongMaoZuoQi` / `ZhanQi_01` 保持一致：

```
古风上古神龙 → ShenLong_01        巨龙 → JuLong_01        碧水兽 → BiShuiShou_01
```

⚠️ **神龙的 FBX 和 `.fbm` 贴图目录名字故意没改**：`01-环绕飞行.fbx` 内部按
**相对路径**引用 `01-环绕飞行.fbm\T_xxx.png`，改名两边就对不上了。文件夹是 ASCII
（`ShenLong_01/`），代码路径干净就够。

### 三个资产的实测与踩到的坑

| | 骨架 | 尺寸 | 材质 | 片段 |
|---|---|---|---|---|
| **神龙** | Generic | 1.79 × 1.51 × **5.95** 米 | 8 个，**全部** 自动接上 D + N | 1 个（原名 `CINEMA_4D___`） |
| **巨龙** | Generic | ~~0.11 米~~ → **3.64 × 3.70 × 3.32** 米 | 1 个，**贴图没自动接上** | **6 个**：`ZhanLi` `XingZou` `PuTongGongJi` `JiNengGongJi1/2` `SiWang` |
| **碧水兽** | Generic | 2.06 × 2.15 × 2.02 米 | 1 个，接上了 D | 基础模型 0 个；8 个动作各 1 个 |

#### ★【坑 1】巨龙小了 **25 倍**，而且看起来像"碎掉了"

刚导入时巨龙的包围盒只有 **0.11 米**（11 厘米），`capture_asset` 渲出来是一堆**白色尖片**，
第一眼像网格坏了。实际查下来：

- `bindposes` 53 个对 53 根骨骼，**没有一个是 null**，全部是"旋转 + 极小平移" → **绑定是完整自洽的**
- `fileScale = 0.01`（FBX 按厘米），`globalScale = 1` → 原始网格只有 **19 个单位**
- 那些"尖片"其实是它**展开的破烂翅膀** —— 模型本来就这么做的，套上贴图后一眼就是条龙

**修法**：`ModelImporter.globalScale = 25` → 3.64 米。**凡是显示"碎成片"的先量包围盒和 bindpose，
别急着判死刑**；先对齐尺寸 + 接上贴图再看。

#### ★【坑 2】`.fbm` 文件夹不能改名，但引擎能自动接上

3ds Max 导出 FBX 时会生成同名 `.fbm` 文件夹放贴图，FBX 内部按相对路径引用。
**保持原样复制过去**，Unity 的 `ImportViaMaterialDescription` 就能自动把 `_MainTex` / `_BumpMap`
都接上（神龙 8 个材质 8 个都接上了，连法线都对）。
`Hair` 那个只有 `_D`、没有 `_N`，所以它没有法线 —— 那是素材本身就没有，不是导错。

#### 【坑 3】巨龙的贴图要手动接

它的 FBX 里材质叫 `BossLeiLong`，但贴图文件叫 `dragon.png` / `dragon-No.png`，**名字对不上**，
所以 Unity 没搜到。手动建材质接上即可（见下）。
另外 `dragon-No.png` 要**手动把 Texture Type 改成 NormalMap**，否则当普通贴图用会很难看。

### 收尾做了哪些规范化

1. **提外部材质**：项目约定材质放 `<资产>/Materials/*.mat`（FBX 内嵌的不好改）。
   神龙 8 个（`MI_MB1SuiguangMd00501Body` → `ShenLong_01_Body` …）、
   巨龙 1 个（`JuLong_01.mat`）、碧水兽 1 个（`BiShuiShou_01.mat`），
   用 `ModelImporter.AddRemap(new AssetImporter.SourceAssetIdentifier(typeof(Material), 原名), 新材质)`
   重映射回去。
2. **法线贴图**：`dragon-No.png`、`Pet_Bishuishou_N.tga` 都设成 `TextureImporterType.NormalMap`。
3. **循环标记**：`CINEMA_4D___`（神龙）/`ZhanLi`/`XingZou`/`Idle*`/`FightRun` 设 `loopTime = true`；
   攻击、死亡、出生、升级等一次性动作 `false`。

> ⚠️ **片段名一个都别改**。用户明确说过「**我还没有为这三份资产的动画改名**」——
> 命名是他的事。我第一次擅自把神龙的 `CINEMA_4D___` 改成了 `FeiXing`，已还原。
> 还原办法：`mi.clipAnimations = new ModelImporterClipAnimation[0]; mi.SaveAndReimport();`
> —— 清掉片段覆盖后 Unity 会**从 FBX 的 take 重新生成**，拿回来的是真正的原始名字
> （比手打猜测靠谱，`CINEMA_4D___` 后面到底几个下划线我原本是猜的）。
> 改完片段名记得同步：控制器里的**状态名** + prefab 上 `NpcAnimator.动作列表`。

### 骨架类型为什么是 Generic（和现有坐骑不一样）

现有坐骑（`LangQi_01` / `ZhanQi_01`）是 **Human** + `avatarSetup = CreateFromThisModel`，
动作 FBX 用 `CopyFromOther` 指向基础模型的 Avatar。
**但龙/兽必须用 Generic** —— Human 要求有人形骨架映射（头/脊柱/双臂/双腿），
套在四足龙身上是错的。所以这三套保持 Unity 自动判定的 **Generic**。

> Generic 的 `avatarSetup = NoAvatar`、`Animator.avatar = null` **是正常的**，不是漏配：
> Generic 片段按**层级路径**绑定（实测 8 个片段各 360~390 条曲线，**失配 0 条**），
> 不需要 Avatar。**别按现有坐骑那样硬塞一个 Avatar 进去**。

### 建的架子：控制器 + prefab（用户同意先留着）

**不是统一模板** —— 每套按**它自己的动画数量**配（用户强调过"每个坐骑都会有独立的脚本，
动画的数量也不同"）：

| 资产 | 控制器（`Assets/Animations/NPC/`） | 状态数 | prefab |
|---|---|---|---|
| 神龙 | `ShenLong.controller` | 1 | `ShenLong_01.prefab` |
| 巨龙 | `JuLong.controller` | 6 | `JuLong_01.prefab` |
| 碧水兽 | `BiShuiShou.controller` | 8 | `BiShuiShou_01.prefab` |

控制器结构照抄现有 `LangQi.controller`：参数 `Action`(Int) + `Speed`(Float)，
一个动作一个状态，靠 `AnyState → 状态` 且 `Action Equals <索引>` 切换，
`writeDefaultValues = true`（和 `LangQi` 一致；§16.2 那个坑是**状态没 motion** 才发作，
这里每个状态都有片段，所以安全）。

prefab 根上：`Animator`（`applyRootMotion = false`、`cullingMode = AlwaysAnimate`）
+ `NpcAnimator`（`动作列表` 按索引顺序填好）+ `CapsuleCollider`。
**没挂 `NpcInstance` / `NpcIndicator`** —— 那要配定义资产和 NPC 表，属于坐骑系统本身。

### 还没做的（属于"坐骑系统"本身，等以后做）

- `NpcInstance` + `NpcIndicator` + NPC 表行
- 每个坐骑**各自的脚本**
- 贴图没用上的通道：碧水兽的 `_E`（自发光）/ `_S`（高光）/ `_M`（遮罩）

**实测 1**（`mount_all_posed.png`）：三只摆一排、各自套上动作采样后都能正常渲染。
**实测 2**（`mount_anim_test.mp4`）：接上控制器后逐个动作播一遍，**姿势确实各不相同** ——

```
碧水兽  Idle        包围盒 2.05 × 1.55 × 2.04      巨龙  ZhanLi        2.87 × 2.71 × 1.72
        FightRun            1.93 × 1.26 × 2.12            XingZou       3.17 × 3.27 × 2.33
        HuDong              1.81 × 1.62 × 1.76            SiWang        1.92 × 3.85 × 1.87
        Birth               1.39 × 2.73 × 0.95            （6 个全不同）
        Die                 1.05 × 2.34 × 1.04
```

> 【坑·用户报的】"**播什么动画都是待机的样子**" —— 根因是**当时根本没有控制器**
> （我只导了资产），不是片段坏了。另外 Animation 窗口对纯动画 FBX 会显示
> 「No model is available for preview」也**是正常的**：那 8 个动作 FBX
> `SkinnedMesh=0 MeshFilter=0 材质数=0`，里面没有模型可显示，得把基础模型拖进预览区。

---

## 24. 坐骑表 + 坐骑页（2026-09-23）

用户给了概念图「坐骑ui（i键唤出，默认境界）」，要求先实现 UI，
再「**对照着那个被动神通的表以及现有的坐骑资产，弄一个坐骑的表，表头基本一致的**」。

### 24.1 坐骑表（`Assets/Data/Tables/坐骑表.csv`）

**表头 = 被动神通表 的写法照搬**，只把神通专属的几列换成坐骑的：

| | 列 |
|---|---|
| 身份列（换成坐骑的） | `坐骑id` `坐骑名称` `坐骑品阶` `修炼门槛` `模型资源路径` `待机动作` `介绍` |
| 增益列（**逐字照抄被动神通表**，15 列） | `气血增益` `攻击增益` `防御增益` `灵力增益` `移动速度增益` `暴击增益` `暴击抗性增益` `闪避增益` `冷却缩减增益` `攻速增益` `气血回复增益` `灵力回复增益` `普攻伤害减免增益` `主动法术伤害减免增益` `被动法术伤害减免增益` |

共 22 列 × 9 行（现有 9 套坐骑资产）。**列名 = 字段名**，属性列由
`TableSpec("坐骑表", typeof(MountDefinition), "增益", "增益")` 自动识别
（剥掉「增益」后缀 → 匹配 `AttributeUtil.GetDisplayName`）。

> ⚠️ **数值是我按品阶编的占位值**（门槛 1/10/20/25/30/35/45/60/75 由弱到强），
> 用户还没定数值，要调就改 CSV 再跑一次
> **修仙 / 从配置表生成资产**，别去改 `Assets/Data/Generated` 下的资产。

9 套坐骑：`灵翅(黄品1)` `狼骑(玄品10)` `灵鹿(玄品20)` `碧水兽(地品25)` `熊猫坐骑(地品30)`
`战骑(地品35)` `麒麟坐骑(天品45)` `巨龙(天品60)` `上古神龙(仙品75)`。

### 24.2 ★【坑】坐骑 prefab 不能用 `NpcPrefabs.加载`

`NpcPrefabs.加载` 靠「挑带 `NpcInstance` 的那个」来避开同名 FBX 的坑（见 §5.7），
但**坐骑 prefab 上没有 `NpcInstance`**，用它会把 FBX 挑出来。

坐骑的判别条件换成 **`NpcAnimator`**：实测三个新导坐骑的 **FBX 根节点上没有 `Animator`**
（`Animator: 无`），而组装好的坐骑 prefab 一定有 `Animator` + `NpcAnimator`。
所以 `MountPrefabs.加载` 的优先级是：**带 `NpcAnimator` → 带 `Animator` → 第一个（并警告）**。

### 24.3 坐骑页（`UIMountPage.cs` + `CharacterPanelBuilder.BuildMountPage`）

布局照概念图：

```
┌──────────────────────────────────┐
│      坐骑 idle 动画展示（3D）      │   ← RawImage + RenderTexture
├────────────────────┬─────────────┤
│ 拥有的坐骑 [滚动条] │ 选中坐骑信息 │
└────────────────────┴─────────────┘
```

**3D 预览不用加图层**：在 **y = −4000** 开一块「口袋场地」，把坐骑实例、
3 盏点光、一台相机都放那儿。相机 `clearFlags = SolidColor`、`farClipPlane = 60`，
游戏世界在 4000 米外进不了视锥 → **只画得到坐骑**，不需要改 `TagManager`。

> ⚠️ **灯必须自带**：那地方是空的，没灯坐骑全黑。要用**点光**（range 18~26），
> **不能用平行光** —— 平行光照亮全世界，会把游戏场景一起点亮 ✗
>
> ⚠️ **预览实例的 Animator 必须 `cullingMode = AlwaysAnimate`**：
> Unity 会按「离相机远」剔除 Animator，表现是**姿势冻住不动**
> （本工程踩过一次，见 Animator culling 那个坑）。

页签顺序按新概念图**重排过**：`背包 / 境界 / 神通 / 法宝 / 灵阵 / 战阵 / 坐骑`
（原来是 `境界…背包`，`背包` 从最后挪到最前，`坐骑` 新增在最后）。

### 24.4 【坑】面板打开时 `timeScale = 0`

`CharacterPanelUI` 打开时会把时间停住。写验证脚本时
**`yield return new WaitForSeconds(...)` 会永远等不到**（`yield return null` 还正常），
表现是脚本卡死直到 bridge 超时。**改用 `WaitForSecondsRealtime`。**

### 24.5 实测

`I` 唤出 → 切「坐骑」页：**只激活 1 个页面**、列表 **9 行**（按门槛由低到高）、
滚动条正常、四个坐骑来回点都能换预览：

```
点「灵翅」    → 预览模型=预览_灵翅     非背景像素 6.5%
点「巨龙」    → 预览模型=预览_巨龙     非背景像素 6.0%
点「上古神龙」→ 预览模型=预览_上古神龙 非背景像素 1.9%   ← 蛇形龙细长，占比本来就小
点「碧水兽」  → 预览模型=预览_碧水兽   非背景像素 3.4%
```

证据：`screenshots/mount_page.png`。

> 信息栏那个**品阶色方块**（天品是淡紫）**不是丢贴图**：`MountDefinition.图标` 没填时
> `UIEntryInfo` 用 `UIEntryRow.TierColor(品阶)` 当占位底，和其它页一个行为。
> 想要真图标就往表里加一列 `图标`（`Sprite` 字段已经有了）。

---

## 25. ★【坑·已修】玩家对象被误改名成「f」→ **存档系统静默失效**（2026-09-23）

**现象**：`3C_Testbed` / `Sect` 两个场景里的玩家对象（挂着 `PlayerVitals` / `PlayerController`
/ `PlayerCultivation` … 那 16 个组件、82 个子物体的根）**名字变成了「f」**。
多半是在 Hierarchy 里点中名字误触打进去的。

**后果比看上去严重得多** —— 全项目有 **5 处按名字找玩家**：

| 位置 | 找不到会怎样 |
|---|---|
| **`SaveSystem.cs:147`（`应用到角色`）** | **静默 `return`** → 读档 = 什么都没恢复 ✗ |
| **`SaveSystem.cs:244`（`从角色采集`）** | **静默 `return`** → 存档 = 空档 ✗ |
| `TopDownCamera.cs:56` | 自动找目标的兜底失效（现在靠显式 `target` 引用，暂时没暴露） |
| `SwordProbe.cs:21`、`DataTableImporter.cs:521`、`VillageSceneBuilder.cs:53` | 各自的编辑器工具失效 |

> **最阴的是存档那两处**：`var 玩家 = GameObject.Find("Player"); if (玩家 == null) return;`
> —— 不报错、不警告，表现成"读档没反应"，很容易被当成存档格式坏了去查别的地方。

**修法**：把两个场景里的「f」改回 **`Player`**（`go.name = "Player"` + `Undo.RecordObject`），
保存。改名**不会**破坏任何引用 —— `TopDownCamera.target` 那种是**对象引用**不是名字，
实测改完 `target` 仍指着同一个对象。

**实测（改名后）**：

```
从角色采集 → 位置(-83.60, 14.16, 89.90) 气血420 灵气160   ← 采集有效 = 找到玩家了
把气血改成 7、灵气改成 3
应用到角色 → 位置 0.00 米不差，气血 420，灵气 160          ← 全部还原 ✓
日志：[存档] 已恢复修为…  [存档] 已恢复战阵…
```

> ⚠️ **别随便改玩家对象的名字**。代码里认的是**字符串 `"Player"`**，不是 tag
> （目前玩家对象 tag 是 `Untagged`，项目里也没人用 Player tag）。
> 真要改名，必须同时改那 5 处 `GameObject.Find("Player")`。

> **顺带记一个脚本层的坑**：`EditorSceneManager.OpenScene()` **不会弹保存提示，
> 会直接丢掉未保存的改动**（菜单里打开才会问）。我在排查时因此弄丢过一次用户手转的相机角度
> —— **脚本里开关场景之前一定先 `EditorSceneManager.SaveOpenScenes()`**。

---

## 26. 修炼小屋界面（`CultivationUI`）：装配 + 两个坑（2026-09-23）

用户 09/22 做好的「修炼小屋」三页界面（闭关修炼 / 境界突破 / 转修功法），
代码和 prefab 都在，但**接不上**。排查过程记下来。

### 26.1 装配关系（三件套）

```
场景里的「cultivation room」
  └─ StationInteractable  （类型=修炼，交互距离 4 米，有 BoxCollider）
       └─ 界面预制体 = Assets/Prefabs/CultivationUI.prefab
                         └─ 挂 CultivationUI 脚本，UI 全部由代码在 Awake 里搭

玩家
  └─ StationInteractor    （右键交互；靠近显示「右键 修炼」提示）
```

`StationInteractor.打开界面()` 的逻辑：**`界面预制体 != null` 就 Instantiate 它，
否则弹一块「界面尚未设计」的占位幕布**。

> **诊断口诀**：右键后看到「界面尚未设计，这里是空白幕布占位」= `界面预制体` 是空的，
> **不是 UI 坏了**。

装设施用编辑器菜单 **`Cultivation/设施/安装交互`**（`StationSetup.cs`，
用的是活动场景，没有硬编码路径，可以放心跑）。
但它**只装 `StationInteractable`，不接 `界面预制体`** —— 那一步得手动接。

### 26.2 ★【坑·我犯的】`LoadAll<T>()` 是按**资产名**排序的，把境界表打乱了

`DataTableImporter` 里我写的是：

```csharp
修炼.境界表 = LoadAll<RealmDefinition>().ToArray();   // ✗ 名字序！
```

`LoadAll` 内部 `result.Sort((a,b) => string.Compare(a.name, b.name, ...))`
—— 于是境界表变成 **元婴第10层 / 元婴第1层 / … / 化神十层 / 炼气第1层**（按名字）。
而 `PlayerCultivation.查境界()` 里有个 **`else break`**，隐含要求数组**按等级升序**：

```csharp
if (d.等级 <= 最佳.等级) continue;
if (d.累计灵气 <= S) 最佳 = d; else break;    // ★ 顺序一乱就提前退出
```

**实测后果**：玩家明明是「炼气第1层」，`查境界(0)` 却返回 **「元婴第10层」**
→ 修炼小屋的「转修后境界预估」显示成「元婴第10层」，看起来像转修功能坏了。

> 最阴的是**主界面显示是对的** —— `当前境界` 用的是**全表扫描**（不依赖顺序），
> 所以「炼气第1层」正常，只有 `查境界` 这一条路径错。两个函数用了两种写法，
> 只有一种踩了坑。

**两处都修了**：
1. 接线时**显式按等级排**：`境界们.Sort((a,b) => a.等级.CompareTo(b.等级));`
2. `查境界` 改成**与顺序无关**：全表扫「累计灵气 ≤ S 且等级最高」的那一个，
   去掉 `else break`。90 条数据，开销可以忽略。

> **规矩**：凡是「按顺序取第 N 项 / 提前 break」的表，接线时都要显式排序，
> **不要相信 `LoadAll` / `Resources.LoadAll` 的顺序**。

### 26.3 ★【坑】团结引擎的 guid 有**两种写法**，别拿文件搜索去判"引用断了"

`CultivationUI.prefab` 里 `m_Script` 写的是 **32 位十六进制** guid
（`a30c15ee55cf63c469f41e699bb558c9`），而 `CultivationUI.cs.meta` 里是**长格式**
（`Xnkf5iu...=64 字符`）。

我拿"扫所有 .meta 找这个 guid"的方式判断，**误判成"脚本引用断了"**，
于是把 prefab 里的 guid 换成长格式 —— **反而把好的弄坏了**
（Unity 报 `Could not extract GUID in text file ... at line 45`）。

> 团结引擎：**`.meta` 用长格式、YAML 里的引用用 32 位格式，是同一根 guid 的两种写法。**
> 判断引用有没有断，**要看 Unity 自己怎么说**（`PrefabUtility.LoadPrefabContents` +
> `GetComponent<T>()` 是不是 null），别靠文件搜索比对。
> 之前那个「Mixusage Warning」里 32 位/长格式混用也是同一件事。

### 26.4 【坑】父物体换了，坐标就白算了

`搭转修页` 里标题和功法行都是**列表块**的子物体，锚的是**列表块左下角**（高 h−86=550），
不是页根（高 636）。我第一版按页根算成 `h−46=590`，结果标题被推到框外面去了。

原来的坐标是标题 `h−86−46=504`、第一行 `h−86−56=494` —— **两者重叠**，
标题被行整个盖住（概念图里标题是有的）。

修正后：标题 504、行从 `h−186=450` 起，间隔 54。实测
`标题 504~536 / 行0 450~496 / 行1 396~442`，不再重叠 ✓

### 26.5 【改动】转修页加了明确反馈

原来 `点确认转修` **成功时一个字都不写**，失败时只把提示塞进「转修后境界预估」那行小字
—— 用户很容易当成"点了没反应"。现在三种情况都有明确文字：

| 操作 | 界面显示 |
|---|---|
| 没点功法行就点确认 | `请先在上面点一门功法` |
| 点了当前正在修的那门 | `已经在修「太虚炼气诀」了` |
| 转修成功 | `已转修到「太虚炼气诀」　当前境界：炼气第1层` |

> ⚠️ 反馈要**写在 `刷新()` 之后** —— `刷新()` 会把那行重算成预估，先写会被盖掉。

### 26.6 实测：转修两个方向都通

```
低→高：[修炼] 转修（低→高）：太虚炼气诀 → 青云剑诀｜K 1.0 → 2.6｜总灵气 0 → 0（扣 5% 损耗）
高→低：[修炼] 转修（高→低）：K 2.6 → 1.0｜境界不提升（封顶 1 级）｜总灵气 0 → 0
```
转完之后：界面「功法信息」跟着换、列表里的 `▶`（当前功法标记）跟着挪、
预估按新功法重算 ✓

> 转修改的是 `UIPanelData.当前功法`（**场景对象的字段**），所以**退出 Play 就还原** ——
> 真存档要靠 `SaveSystem`。别把「转完再进 Play 又变回去了」当成 bug。

### 26.7 Sect 场景里没有修炼小屋

```
Sect.scene     cultivation room = 0 个    StationInteractable = 0 个    CultivationUI = 0 个
3C_Testbed     cultivation room + 4 台设施齐全，修炼界面已接上
```
要把修炼小屋搬进 Sect，需要把屋子模型放进去 + 跑 `Cultivation/设施/安装交互` + 接 `界面预制体`。

---

## 27. ★【大坑·已修】换功法只改了界面，**实际功法没换**（2026-09-23）

用户报：「转修没换过来，只是修炼小屋里显示的换了，**实际的功法没换**」。
根因是**同一个「当前功法」在三个地方各存了一份，谁都不通知谁**：

| 位置 | 存的是什么 | 谁在用 |
|---|---|---|
| `UIPanelData.当前功法` | **唯一真来源** | 界面、`PlayerAbilityLoader`、读档 |
| `PlayerCombatStats.当前功法` | **自己的一份拷贝** | **玩家实际战斗属性的汇总** |
| 玩家身上挂着哪个普攻组件 | 隐含的第四份 | 实际打出来的招式 |

转修只改了第 1 个，于是：界面换了、**属性还在按旧功法算**、**普攻还是旧的那套**。

### 27.1 【修法一】`PlayerCombatStats` 自动跟随

它原来的 `当前功法` 是**纯 Inspector 手填字段，全项目没有任何代码写它**。
实测：界面写着「青云剑诀」，它却还停在 09-21 手填的「太虚炼气诀」，
汇总攻击一直是 57 不动 → 看起来就像"转修没生效"。

现在 `Update()` 里每帧比一次引用，不一致就跟 `PlayerCultivation.当前功法` 同步并 `Recalculate()`：

```csharp
void Update()
{
    if (修炼 == null) 修炼 = GetComponent<PlayerCultivation>();
    if (修炼 != null && 修炼.当前功法 != 当前功法) 同步功法();
}
```

> **为什么用「每帧比较」而不是事件**：换功法的路径不止一条（转修 / 读档 / 以后的剧情），
> 谁都有可能忘了发通知。一次引用比较而已，但能彻底消灭这类静默不一致。

### 27.2 【修法二】普攻组件改成「启用/停用」，不销毁重建

`PlayerAbilityLoader` 原来是「需要 → `AddComponent`、不需要 → `Destroy`」。
两个致命问题：

1. **手工挂在玩家身上的那份永远不会被卸载** —— 代码里 `if (GetComponent(type) != null) continue;`
   （"已经手工挂着了，不重复装"），而卸载循环只遍历**自己装的** `已装载` 字典。
   → 换功法之后**新旧两套普攻同时生效** ✗
2. **真用 `AddComponent` 新建的话配置全丢** —— `BasicSword01` 身上有
   `剑模型资源`(base_sword.FBX)、模型朝向补偿、悬浮偏移、绕行长短轴/周期、瞄准高度……
   全变默认值 ✗

**改成：只在「配置表里登记过的能力类名」范围内切换 `enabled`**，
谁挂的、配了什么一概不动。停用前对 `YufengFlight` 调 `强制落地()`。

配套：`BasicSword01` 加 `OnDisable()` 收掉剑体。
剑体是**懒创建**的（`更新悬浮` 里 `if (剑体 == null) 确保飞剑存在()`），
所以停用收掉、再启用会自动重新生成 ✓
> 只在 `OnDestroy` 收的话，停用之后那把剑会**冻在原地继续挂着** ✗

### 27.3 【缺配置】功法→普攻的绑定少了一条

`PlayerAbilityLoader.普攻方法表` 里只有 `basic_sword_01 → BasicSword01`，
**`basic_remoteattack_01` 根本没登记** —— 而
`太虚炼气诀.普攻方法id = basic_remoteattack_01`。所以转到太虚炼气诀时
**一个普攻都装不上**。已补上 `basic_remoteattack_01 → BasicRemoteAttack01`。

> 两个普攻脚本的自给自足程度不一样，记一下：
> · `BasicRemoteAttack01`：**专为运行时 AddComponent 设计** —— `Awake` 自接
>   `玩家战斗属性`/`目标管理器`/`动画`，还自己 `Resources.Load("技能动作/普攻_远程_01")`。
>   所以新建出来就是能用的 ✓
> · `BasicSword01`：同样会 `Resources.Load("Weapons/base_sword")` 兜底模型，
>   但**悬浮/绕行/瞄准那套参数是 Inspector 里调过的值**，重建就没了 —— 所以它必须
>   以「手工挂 + 切 enabled」的方式用 ✓

### 27.4 实测（全链路）

```
① 刚进 Play（青云剑诀）  修炼.功法=青云剑诀  CombatStats.功法=青云剑诀  一致=True
                        汇总 攻击=72 气血=370   BasicSword01=启用  Remote=不存在  飞剑实例=1
② 转修到太虚炼气诀      [PlayerAbilityLoader] 停用 BasicSword01
                        [PlayerAbilityLoader] 启用 BasicRemoteAttack01
                        修炼.功法=太虚炼气诀  CombatStats.功法=太虚炼气诀  一致=True
                        汇总 攻击=57 气血=420   BasicSword01=停用  Remote=启用  飞剑实例=0
③ 再转回青云剑诀        [PlayerAbilityLoader] 启用 BasicSword01 / 停用 BasicRemoteAttack01
                        攻击=72 气血=370 复现 ✓
```

> ⚠️ **`当前功法` 只能有一个真来源（`UIPanelData.当前功法`）**。
> 以后再加任何"跟着功法走"的东西（特效、音效、被动加成），
> **一律从 `PlayerCultivation.当前功法` 读**，不要再存第二份拷贝。

---

## 28. ★【已修】功法等级 ↔ 境界等级 的换算全错（2026-09-23）

用户让我翻一遍"项目里已有的脚本"，看功法转修相关的东西是不是早就实现过。
翻的结论：**`灵气转换` 和 `难度系数` 确实早就实现好了，坏的是"等级换算"这一层** ——
同一个问题在文件里有**三套各算一套的算法**，互相差一级。

### 28.1 【坑】`当前境界` 和 `查境界` 是两套算法，差一级

```csharp
当前境界  →  d.等级 + 1      // 语义：等级 n 的那一行，显示成第 n+1 层
查境界(S) →  d.等级          // ← 没 +1
```

后果：预估转修后境界**永远比实际低一级**，看起来像"预估不准"。

**修法**：`当前境界` 改成**委托** `查境界(当前基准灵气)`，
只留一套算法；`查境界` 内部统一 `推定 = Mathf.Min(90, d.等级 + 1)`。

> 教训：**两个地方都要把"等级"翻译成"显示名"时，让它俩调同一个函数**，
> 别各写一遍 —— 这种 ±1 的错不会报错、不会崩，只会静静地偏。

### 28.2 【坑】`查境界` 依赖表的**遍历顺序**（和 §26.2 同源）

`查境界` 原本是 `foreach (var d in 表) { if (S >= d.累计灵气) 命中 = d; else break; }`，
靠 `break` 提前退出 —— **一旦表不是按等级升序就全错**。
`LoadAll<RealmDefinition>()` 是按**资产名**排序的（§26.2），所以这个 `else break`
在某些资产命名下会提前退出，`查境界(0)` 能查出「元婴第10层」。

**修法**：改成与顺序无关的写法（扫全表、取满足条件的最大等级）。

### 28.3 【坑】"境界不提升"其实涨了一级

高→低转修的封顶原本是：

```csharp
总灵气 = Math.Min(总灵气, (long)(取累计(旧等级) * 新K));   // ✗
```

但表里 **`累计灵气(n)` = 从 1 级升到 **n+1** 级所需**（§28.1 的 `+1` 规则）。
所以要**停在** L 级，必须用 `取累计(L − 1)`：

```csharp
int 封顶等级 = Mathf.Min(旧等级, Mathf.Max(1, 新功法.最高可修炼境界));
long 上限S  = 取累计(封顶等级 - 1);                      // ✓
总灵气 = Math.Min(总灵气, (long)(上限S * 新K));
```

同时 `预估转修后境界` 也复制了这段（原本各写一份），**现在两边共用同一套封顶等级**，
否则界面预估和实际结果又会不一样。

### 28.4 【改】`PlayerCombatStats.功法等级` 改为「跟着境界走」

用户 09-23 定：**功法等级 = 当前境界等级**，并被当前功法的 `最高可修炼境界` 压住。

```csharp
int 期望功法等级 => Mathf.Clamp(修炼.等级, 0, Mathf.Max(1, 当前功法.最高可修炼境界));
```

`Update()` 里每帧比较 `修炼.当前功法 != 当前功法 || 期望功法等级 != 功法等级` 才同步，
所以**转修 / 读档 / 以后的剧情**任何一条路径改功法，属性都会自己跟上（理由见 §27.1）。

### 28.5 实测（3C_Testbed，Play）

```
功法表：太虚炼气诀 上限9   青云剑诀 上限12

① 初始           境界=炼气第1层 等级=1   CombatStats.功法=青云剑诀 功法等级=1  攻击=48 气血=330
                 （★ 修复前这一格是 攻击=72 —— 属性一直按 5 级在算）
② 灵气堆到 20 级  境界=金丹第2层 等级=22  CombatStats.功法等级=12  ← 被青云上限 12 压住 ✓
③ 转修太虚(上限9) 预估=炼气第9层  → 实际=炼气第9层   一致=True ✓（★ 修复前差一级）
④ 转回青云       预估=炼气第5层  → 实际=炼气第5层   一致=True ✓
```

### 28.6 【明文决定】`修炼门槛` **只当显示用**，不拦转修

`GongFaDefinition.修炼门槛`（"可开始修炼的最低境界序号"）目前的实际情况是：

* `PlayerHud.cs:330` 显示成「可修炼境界：门槛 ~ 最高」
* `MountDefinition.修炼门槛` 在坐骑页当排序键（`UIPanelData.cs:566`）
* **`转修功法` 里从头到尾没校验过它**（`PlayerCultivation.cs:301` 只判了 null 和"已经在修了"）

也就是境界 5 的人能转修一门写着门槛 10 的功法。

**用户 2026-09-23 决定：保持现状，门槛纯展示，不做拦截。**
→ 这是**有意为之，不是 bug**，别再当漏洞翻出来修。
（今天两门功法门槛都是 1，所以这个洞本来也观察不到。）

### 28.7 顺带确认：`BasicRemoteAttack01` 运行时新建是**能用的**

`PlayerAbilityLoader` 在运行时 `AddComponent` 出 `BasicRemoteAttack01` 时会打一条警告
「新建出来的是默认配置，记得手动配一遍」。**对这个类属于虚惊** —— 它不读 `面板数据`，
全部自己拿：

* `玩家战斗属性 = GetComponent<PlayerCombatStats>()` → 攻速系数 / 神识范围 / 伤害
* `目标管理器   = GetComponent<NpcTargeting>()`
* `动画         = GetComponent<PlayerAnimationController>()`
* `普攻动作     = Resources.Load<AnimationClip>("技能动作/普攻_远程_01")`（`Awake` 里兜底）

所以"手工挂"的硬要求**只对 `BasicSword01` 成立**（它的悬浮/绕行/瞄准参数是调过的值，§27.2）。

---

## 29. ★【大坑·已修】`enabled = false` 拦不住 **C# 事件** → 转修后冒出废飞剑（2026-09-23）

用户报：「青云剑诀在**转修其他功法后，第一次普攻**会召唤出一把**没有用的飞剑**」。

### 29.1 现象与定位

飞剑本身是好的（§27.2 收剑逻辑生效了），坏的是**它被谁造出来**：

```
【B】转修到太虚：数剑=0  剑.enabled=False      ← 收剑正常 ✓
【C】锁一次目标：数剑=1  剑.enabled=False      ← ★ 冒烟证据：
                                                  组件明明是停用的，飞剑却是它生成的
```

因为 `Update()` 不跑，**没有任何东西驱动这把剑** —— 它就冻在悬浮位置当废铁。

### 29.2 根因：**Unity 消息看 `enabled`，C# 事件不看**

```csharp
void Awake()   { 目标管理器.LockChanged += 处理锁定变化; }   // ✗ 订阅
void OnDestroy(){ 目标管理器.LockChanged -= 处理锁定变化; }   // ✗ 只在销毁时退订
```

`NpcTargeting.LockChanged` 是个**普通 C# 事件**。
`MonoBehaviour.enabled = false` 只让 Unity **不再发消息**（`Update` / `OnTrigger…`），
**委托链是你自己挂的，它照调不误。**

而换功法时 `PlayerAbilityLoader` 走的是 `enabled = false`（§27.2，为了保住 Inspector 配置），
于是停用期间回调还活着，玩家一锁目标就：

```
处理锁定变化 → 进入飞行() → 确保飞剑存在()      // BasicSword01.cs:356
```

**为什么偏偏是"第一次普攻"**：普攻必须先锁定目标，
锁定 = 触发 `LockChanged` = 唯一能走到 `确保飞剑存在()` 的路 ✗

### 29.3 修法

订阅/退订**跟着启用状态走**，`Awake` 只解析引用：

```csharp
void Awake()   { if (目标管理器 == null) 目标管理器 = GetComponent<NpcTargeting>(); }  // 不订阅
void OnEnable(){ if (目标管理器 != null) 目标管理器.LockChanged += 处理锁定变化; }
void OnDisable(){ if (目标管理器 != null) 目标管理器.LockChanged -= 处理锁定变化; 收起飞剑(); }
```

再加一道兜底（`处理锁定变化` 开头 `if (!enabled) return;`）。

### 29.4 实测（回归三连）

```
起点（转走再转回青云）：功法=青云剑诀  剑.enabled=True  飞剑=True
【C】转修到太虚后锁目标 → 数剑=0                              ✓ 不再冒废飞剑
【D】转回青云           → 数剑=1 飞剑重生=True                ✓ 懒创建照常
【E】锁目标            → 数剑=1                              ✓ 不重复生成
① 青云·重新锁定 → 飞剑位移=2.231                            ✓ 订阅挪了位置没改坏普攻
② 太虚·远程普攻 → [basic_remoteattack_01] 对锁定单位「练功木桩」出弹
                  命中「练功木桩」会心 60.36                 ✓ 转修后普攻正常
```

> ② 顺带纠正一个**假警报**：切过去之后查 `可以出手=False` 曾让我以为普攻坏了，
> 其实是它**已经自动出手打完了**，`冷却剩余=2.37` 是那 3 秒冷却。看日志别被这个骗了。

### 29.5 【通用规则】以后凡是在 `Awake` 里 `+=` 事件，都要问一句

> **这个组件会不会被 `enabled = false` 停用？如果会，订阅就必须放在 `OnEnable`。**
>
> 同类要一起检查的还有：`Invoke` / `InvokeRepeating`（**同样不看 `enabled`**，
> 只有 `OnDisable` 里手动 `CancelInvoke` 才停）、以及别人持有的**回调委托 / Action 字段**。
> 区分口诀：**Unity 消息（Update/OnEnable/OnTrigger）看 `enabled`；你自己挂的委托一律不看。**