# 备份与恢复说明（2026-09-23 事故）

## 一、发生了什么

`3C_Testbed.scene` 的内容被 **`Sect.scene` 的内容顶掉了** —— 原来的环境对象
（`Ground` / `Props` / `Props_Trees` / `Props_Rocks` / `Props_Small`）不见了，
玩家对象也从原来的 `Player` 变成了 `f`。

### 根因：项目代码里的"硬编码场景路径"地雷

```csharp
// Assets/Editor/Data/DataTableImporter.cs
const string ScenePath = "Assets/Scenes/3C_Testbed.scene";

static void EditorSceneManager_MarkAndSave()
{
    var scene = EditorSceneManager.GetActiveScene();   // ← 当前打开的场景
    EditorSceneManager.SaveScene(scene, ScenePath);    // ← 却存进硬编码的 3C_Testbed！
}
```

`DemoDataSetup.cs` 里是**一模一样的写法**。

**只要在开着 `Sect` 的时候触发了这两个保存，Sect 就会被整个写进 `3C_Testbed.scene`。**
而 `DataTableImporter.ImportAll()`（导入配置表的菜单）第 187 行就会调它 ——
也就是说：**开着 Sect 点一次"从配置表生成资产"，3C_Testbed 就没了。**

> **已修**（2026-09-23）：两处都改成 `SaveScene(scene)` —— **不传 path，存回自己的路径**。
> 并且当前场景不是 3C_Testbed 时会打印一行日志说明存的是谁。
>
> **规矩**：`EditorSceneManager.SaveScene` 永远不要传 path。
> 想存别的场景就显式 `OpenScene` 过去，别用"活动场景 + 硬编码路径"这种组合。

---

## 二、能不能救回来：**能**

### 干净的原始场景还在这儿

```
.dsh\_backup\20260921-104433-fentian\3C_Testbed.scene     1814.2 KB
```

对比证据：

| | 09-21 备份 | 当前 3C_Testbed |
|---|---|---|
| 玩家对象名 | **`Player`**（原始正确） | `Player`（事故后是 `f`，刚被我改回） |
| `Ground` | ✅ 有 | ❌ 没有 |
| `Props` | ✅ 有 | ❌ 没有 |
| GameObject 数 | 617 | 607 |
| 独立名字数 | 166 | 208 |

**当前 `3C_Testbed` 的名字集合是 `Sect` 的超集**（Sect 的 201 个名字全在里面，
当前只比 Sect 多 6 个 = 新加的坐骑页 `Page_坐骑` / `MountList` / `Preview` /
`MountShow` / `Tab_坐骑` / `Tip`）—— 这是"Sect 被写进了 3C_Testbed"的铁证。

### 两条恢复路线

**路线 A（推荐，干净）：用 09-21 备份重建 3C_Testbed**

1. 把当前 `3C_Testbed.scene` 挪走留底
2. 把 `.dsh\_backup\20260921-104433-fentian\3C_Testbed.scene` 复制成
   `Assets/Scenes/3C_Testbed.scene`
3. 打开它，重跑这几个生成器把界面补回来（都是秒级、可重复执行）：
   - `修仙 / 生成游戏界面 HUD`
   - `修仙 / 构建角色面板 UI`
   - `修仙 / 从配置表生成资产`（顺带 `RewirePanelData`）
4. 相机机位按需重设（`pitch=50 / yaw=45`）

- **能拿回**：整个原始环境、玩家、NPC 摆位……09-21 那一刻的场景状态
- **会丢**：09-21 → 09-23 之间在场景里做的**手工摆位**改动（脚本/资产不受影响，
  它们在 `Assets/` 里，不在场景里）

**路线 B（保守，手工合并）：只把环境搬回来**

在当前 `3C_Testbed` 里删掉 Sect 带来的环境对象，再把备份里的
`Ground` / `Props` / `Props_Trees` / `Props_Rocks` / `Props_Small` 拷进来。

- **能保住**：最近所有改动
- **风险**：需要人工判断哪些对象属于"Sect 的环境"，容易漏删/多删

> 我建议 **A**：环境是唯一不可再生的东西，而界面全部是生成器产出的、随时能重跑。

---

## 三、备份到 GitHub

### 现状：我这里推不上去

| 障碍 | 情况 |
|---|---|
| **没装 git** | `C:\Program Files\Git` 不存在（PATH 里那条是卸载后的残留） |
| **没有外网** | 这个 shell 连不出去（系统代理 `127.0.0.1:7890` 没在跑，直连也失败） |
| **没有凭据** | 推送要你的 GitHub 登录 / token，我没有也不该有 |

所以我把**能做的都做好了**，你只需要动两下。

### 方式一：网页上传（不用装任何东西，现在就能做）

`_backup_zip\` 下已经打好 3 个包，**每个都小于 GitHub 网页 25 MB 的上传上限**：

| 包 | 大小 | 内容 |
|---|---|---|
| `01-代码-场景-配置-20260923.zip` | 1.76 MB | Scripts / Editor / Data / Scenes / ProjectSettings / Packages / 所有文档 |
| `02-动画-字体-UI资源-20260923.zip` | 7.88 MB | Animations / Fonts / UIResources / Avatar |
| `03-场景历史备份-20260923.zip` | 0.31 MB | **含 09-21 那份原始 3C_Testbed** |

操作：打开 https://github.com/Rihyokin/project-cultivation →
`Add file` → `Upload files` → 把这 3 个 zip 拖进去 → `Commit changes`。

### 方式二：正经用 git（推荐，之后每次改完双击一下就行）

1. 装 Git for Windows：https://git-scm.com/download/win
2. 双击仓库根目录的 **`上传到GitHub.cmd`**

脚本会自己 `init` → `add` → `commit` → `remote add` → `push`，
`.gitignore` 已经排除了 `Library`（3.8 GB）、`Temp`、`Logs`、`screenshots`、
`.codely-*`，以及 `somenews`（里面有 118 MB 的 `.c4d`，超 GitHub 单文件 100 MB 上限）。

> ⚠️ **首次推送约 2 GB**（`Assets` 里有 1.7 GB 的三方资源包），慢是正常的。
> 嫌大就把 `.gitignore` 里 `cultivation/Assets/resources/` 那行取消注释，
> 只备份自己写的东西。
>
> ⚠️ `somenews` 里的 `.c4d / .max / .blend` 源文件**没有进仓库**（超限），
> 需要的话用网盘单独存一份。能用的部分（fbx + 贴图）已经导入
> `Assets/resources/NPC/Mount/` 了。

---

## 四、本地快照（已经做了）

```
.dsh\_snapshot\20260923-195614\     1885 个文件 / 30.1 MB
    Assets\{Scenes,Scripts,Editor,Data,Animations,Avatar,Prefabs,DemoData,UIResources,Fonts,Shaders}
    ProjectSettings\  Packages\
    _dsh_backup\   ← 含 09-21 的 3C_Testbed.scene
    *.md / *.txt / *.bak
```

这是不依赖任何外部工具的本地保底。

---

## 五、这次暴露的其他问题（建议一起处理）

1. **`SaveSystem` 静默失效**：`SaveSystem.cs:147 / :244` 用
   `GameObject.Find("Player")` 找玩家，找不到就 `return`，**不报错**。
   玩家对象被误改名成 `f` 期间，**存档/读档等于没执行**。已把名字改回 `Player`，
   但建议把这几处换成更稳的写法（缓存 + `FindObjectOfType<PlayerVitals>()` 兜底）。
2. **`TopDownCamera.Awake()` 里的 `GameObject.Find("Player")`** 是死代码
   （`target` 已被显式指定）。
3. **`EditorSceneManager.OpenScene()` 不弹保存提示，会直接丢未保存的改动**
   （菜单里打开才会问）。脚本里开关场景前必须先 `SaveOpenScenes()`。
