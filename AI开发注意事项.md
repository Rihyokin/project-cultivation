# AI 开发注意事项

> 这份是**开工前的入口文档**：下次要接着做 NPC 的 AI，从这里开始看。
> 踩坑的细节在 `开发注意事项.md` §十五 / §十六，本文只讲**怎么干活**。

---

## 0. 一句话现状

**AI 骨架已经搭完并且验证通过了**，现在处于「逐个 NPC 写专属 AI」的阶段。

已经能用的东西：

| 层 | 位置 | 状态 |
|---|---|---|
| AI 基类 | `Assets/Scripts/Npc/Ai/NpcAiBase.cs` | ✅ 感知 / 状态机 / 移动 / 动画降级 / 出手 / 受击 / 死亡 / 收服预留 |
| 类型层 | `NpcAiAnimal.cs`（兽）/ `NpcAiCombatant.cs`（会打人的公共）→ `NpcAiDemon.cs` / `NpcAiHuman.cs` | ✅ 三型行为都实测过 |
| 物种层 | **10 只兽类全做完** | ✅ `NpcAiGou` / `HouZi` / `LingHe` / `MaoLv` / `MuJi` / `ShuiLan` / `SongShu` / `TuZi` / `XiaoJi` / `XiaoLu` |
| 子弹 | `NpcProjectile.cs` | ✅ 能跑；特效资产还没导入，填路径即用 |
| 通用消散 | `NpcDissolveEffect.cs` | ✅ **纯代码生成**（粒子炸开 + 各自随机渐隐），不需要美术资产 |

> **兽类物种层文件很薄**（每个 ~40 行），只重写 `取默认参数()` 调性格数值，行为逻辑全在基类。
> 「待机花样（啄食 / 刨地）」这类**共性行为**是放在 `NpcAiAnimal`（类型层）上的，
> 所以 10 只动物一加就全都有了 —— **加新动物时先想「这条是不是所有动物都要」，是就放类型层。**

### 死亡表现的两条规则

1. **没有死亡动画的 NPC（野兽那类）→ 死亡瞬间立刻隐藏模型**（`没有死亡动画就立即隐藏模型`，默认开）。
   不隐藏的话模型会保持站姿一直杵到消亡延迟结束，和粒子对不上，很出戏。
   重生时 `显示模型()` 会还回来。
2. **有没有死亡动画是自动判断的**（`播("Death")` 成不成功），不用手配。
   有 Death 的（妖魔/人类）正常播动画，播完再按 `死亡后销毁延迟` 收摊。

消散大小用 `死亡消散特效缩放` 调，**基准值是 3**（在基类上，所有 NPC 共用）。
它同时放大**粒子大小、初速和散布范围**，再乘上各自的身躯半径 ——
所以基准一致、观感按体型自动分档：母鸡 0.77 米、狗 1.55 米、白鹿精 2.49 米、白熊精 3.93 米。

实测过的完整链路：妖魔 接近→攻击→命中玩家（`420 → 18`）、野兽 跟随/躲避/挨打逃跑、
人类 好感掉到阈值以下翻脸、母鸡 啄食/跟随/躲避/死亡粒子消散。

---

## 1. 从哪入手：加一个 NPC 的专属 AI

**只做一件事：新建 `Assets/Scripts/Npc/Ai/NpcAi<模型名>.cs`。**

装配是**反射自动发现**的（`NpcAiBase.物种脚本()`），不需要改任何注册表、不需要改 prefab。

匹配规则（`NpcAiBase.找物种脚本`）按这个顺序：

1. 精确相等
2. 前缀：`MuJi_01`、`MuJi_01(Clone)`、`MuJi ` 
3. 下划线包含：`beast_muji`（定义 id 的写法）

> 所以「模型名」指的是 **prefab 文件名 / 定义 id 里的那段**，不是中文名
> （中文名 `母鸡` 匹配不到，因为关键字是 ASCII）。

### 步骤清单

```
1. 查资料：这个 NPC 的 prefab 在 resources/NPC/<类>/<名字>/，动画控制器在 Assets/Animations/NPC/<名字>.controller
   → 先在 Unity 里看它到底有哪些状态（Idle/Walk/Run/Attack1-3/Death/Wound/Specialidle…）
2. 决定继承谁：
   · 不会打人、只会躲/跟随        → 继承 NpcAiAnimal
   · 会主动打玩家                → 继承 NpcAiCombatant（或直接 NpcAiDemon / NpcAiHuman）
   · 别的                        → 继承 NpcAiBase
3. 新建 NpcAi<模型名>.cs，重写「取默认参数()」填这个 NPC 的数
   （索敌范围 / 攻击距离 / 移动倍率 / 死亡消散特效…）
4. 在「决策()」里写它独一无二的行为；只用到已有状态就不用重写
5. 编译 → Play 里 Instantiate 那个 prefab → 看 Console 是不是装上了你这个类
6. 验收：见第 4 节的清单
```

### 母鸡（`NpcAiMuJi`）就是模板

它演示了三种典型改法，照抄即可：

| 想做什么 | 怎么做 |
|---|---|
| 换这套默认数值 | 重写 `取默认参数()`，先 `base.取默认参数()` 再改 |
| 让待机有花样（母鸡啄食） | 重写 `执行待机()`，在 `Idle` 和 `Specialidle` 之间按时长交替 |
| 加自己的玩法（下蛋） | 重写 `Update()`，先 `base.Update()` 再写自己的逻辑 + 抛事件给外部 |

---

## 2. 关键约定（别踩）

- **类名必须是 `NpcAi<模型名>`** —— 前缀不是 `NpcAi` 就发现不了；
  叫 `Animal` / `Demon` / `Human` / `Combatant` 的会被当成类型默认层，不参与物种匹配
- **不要改 prefab 上的数值**：`确保()` 只给「代码自动装的」灌默认值；
  prefab 上手动放了 AI 就原样保留，所以在 prefab 上调参是安全的
- **`类型` 列决定回落**：物种匹配不上时按 `NPC表.csv` 的 `类型` 装
  （兽类/妖魔/人类/中立/未指定；中立和未指定**不装 AI**）
- **动画会自动降级**：`播首个可用("Run","Walk","Idle")` —— 所以没 Run 的动物不会报错，
  但**填动作名前先确认它真有**，否则那条出手方式会被跳过
- **伤害在动画中途结算**：`NpcAttackConfig.出手进度`（默认 0.4）—— 想对齐起手动作就调它
- **改配置表要跑导入器**（进 Play 前会自动检查并警告），`类型`/`索敌范围` 两列在 CSV 里

### 各类型的好感阈值速查

| 类型 | 视玩家为敌 | 备注 |
|---|---|---|
| 兽类 | **永不** | 好感 > 60 跟随、< 10 躲避、挨打逃跑 3 秒 |
| 妖魔 | 好感 **≤ −10** | 表里妖魔初始就是 −10 及更低 |
| 人类 | 好感 **≤ 9** | = 「10 及以上才友好」 |
| 中立 | 不参战 | 村民 / 设施 / 练功木桩 |

**每次受击 −20 好感**（在 `NpcInstance.ReceiveAttack` 里，`每次受击降好感`）。
⚠️ 是**每次伤害结算**都掉 —— 焚天炎术那种多段 AoE 会掉很多次。

---

## 3. 必须先知道的坑

全都写在 `开发注意事项.md`，动手前扫一眼这几条就够：

| 坑 | 在哪 |
|---|---|
| **改了配置表不会自动生效**，必须跑导入器 | §0.5 |
| **Animator 状态 `motion = null` + `writeDefaultValues = true` → 骨骼被重置、蒙皮塌陷** | §16.1 / 16.2 |
| **武器包围盒不能算进碰撞体**（按 `_01` / `wuqi` 命名挑身躯） | §16.3 |
| **闪避公式**：`闪避 ÷ (闪避 + 忽视闪避 + 闪避基准)` | §16.5 |
| **NPC 身上只有 `CapsuleCollider`**，移动只能用 `transform.position`，别加 CharacterController | §8.2 |
| **水平探路别拿整条胶囊扫**（会一直命中地面） | §8.3 |
| **`SphereCast` 从自己体内出发会命中自己** | §15.6 |
| **攻击距离是「中心距」**，填小了会「贴着脸打不着」死循环 | §15.7 |
| 好感度是**运行时副本**，写回资产会把表改脏 | §15.5 |
| **`Animator.avatar` 为空 → 动画完全不播**（状态照切、蒙皮不动，极难发现）| §16.7 |
| **玩家 `已死亡` 没清 → 全场 NPC 集体永久待机**（含新召唤的，最像"状态机挂了"）| §15.10 |
| **测试时用 `回满()` 补血**：它以前不清死亡标记，会制造"AI 坏了"的假象 | §15.10 |

---

## 4. 验收清单（每个 NPC 都要过一遍）

在 Play Mode 里 `Instantiate` 那个 prefab，检查：

```
[ ] AI 装配成了我自己写的那个类（不是回落成 NpcAiAnimal/Demon/Human）
[ ] 动作表里我引用的动作名都存在（Console 没有 "没有动作「X」" 的警告）
[ ] **玩家是活的** —— 先打印 `PlayerVitals.已死亡`！玩家一死所有敌对 NPC 静默停摆，
    现象极像"AI 状态机挂了"。测试补血用 `复活()`（`回满()` 现在也会清标记了）
[ ] **交战中的待机是 `Fight` 不是 `Idle`**（`NpcAiCombatant.取待机动作名()`）
[ ] **待机/战斗待机中挨打 → 第 1 帧就播 `Wound`**，且 Wound 期间不会偷偷接近/攻击
[ ] **生成的敌对 NPC 会先转向面对玩家再出手**（`朝向玩家够正()`）
[ ] **骨骼真的在动** —— 别只看状态在切！`animator.GetBoneTransform(HumanBodyBones.LeftHand)`
    隔 0.12 秒采样 6 次，最大位移 > 0.01 米才算在播（`avatar` 为空时状态照切、蒙皮不动 —— 看 §16.7）
[ ] 待机时动画在循环播放，不是卡住不动（片段接上没有 —— 看 §16.1）
[ ] 模型没有陷进地板 / 浮空（看 §16.2、§16.3）
[ ] 走起来姿势正常、不会原地卡住（别被自己的碰撞体挡住）
[ ] 好感度拉到 60+/10- 两边，行为切对了
[ ] 打它一下：好感 −20、受击动画播了、该逃跑的逃跑了
[ ] 打死它：死亡动画 / 粒子消散播了，尸体按时销毁（或按配置重生）
[ ] 玩家打它、它打玩家，伤害数字飘出来了
```

**现成的验证手法**（不用模拟输入）：

```csharp
var go = Instantiate(Resources.Load<GameObject>("NPC/Animal/MuJi/MuJi_01"), 位置, Quaternion.identity);
var ai = go.GetComponent<NpcAiBase>();        // Start 之后自动装上
ai.状态 / ai.视玩家为敌 / ai.到玩家距离        // 看状态
ai.出手 += (a, 配置, 结果) => Debug.Log(配置.动作名 + " " + 结果.伤害);
go.GetComponent<NpcInstance>().改变好感度(±N); // 拨好感看行为切换
go.GetComponent<NpcAnimator>().CurrentAction; // 看当前在播什么
```

---

## 5. 还没做的（下次可以挑）

| 事项 | 说明 |
|---|---|
| **兽类 10 只全做完了** | `NpcAiGou` / `HouZi` / `LingHe` / `MaoLv` / `MuJi` / `ShuiLan` / `SongShu` / `TuZi` / `XiaoJi` / `XiaoLu` |
| **妖魔 54 / 人类 43 还没做** | 都还在用类型默认 AI（`NpcAiDemon` / `NpcAiHuman`） |
| **模型尺寸要体检** | 动物的模型是外部导入的，**根缩放各不一样（6~14）**。`XiaoLu_01` 就漏设成 1，结果只有 **0.13 米**高、比小鸡还小两倍 —— **单看它自己完全正常**，跟同类摆一起才看得出来。菜单 **修仙 / NPC 资产 / 检查模型尺寸** 按同类型中位数找离群值 |
| **仙鹤已从表里剔除** | 它不是目标 NPC。`NPC表.csv` 的 `beast_xianhe` 行、生成资产、`NpcAiXianHe.cs` 都删了。**模型和动画资产还留着**（`resources/NPC/Animal/XianHe*`、`Animations/NPC/XianHe.controller`），要彻底清掉跟美术说一声 |
| **死亡消散是通用的** | `NpcDissolveEffect` 按体型自动缩放，所有 NPC 共用。想给某个 NPC 专属特效就填 `死亡消散特效路径` |
| **涨好感没有途径** | 好感只降不升，所以「兽类 > 60 跟随」在实际游戏里触发不了，需要一个投喂 / 安抚交互 |
| **灵阵 / 法宝没接 AI** | `ActiveSkillKind.未实现`，技能栏 3/4 格按下去只给提示 |
| **收服 / 组队** | `NpcAiBase.收服()` / `放生()` 留好了钩子，`执行被收服()` 还是空的（要接「跟随主人 + 打玩家的锁定目标」） |
| **人类对话 / 剧情** | `NpcAiHuman.现在可对话` 有了，但没接对话系统 |
| **子弹特效** | `NpcProjectile` 能跑，等特效资产导入后填 `子弹特效路径` |
| **母鸡下蛋** | `NpcAiMuJi` 里实现了（默认关），没配蛋的预制体 |
| **群体行为** | 鸡群 / 狼群一起行动、共享仇恨，现在每个 NPC 各管各的 |

---

## 6. 文件地图

```
Assets/Scripts/Npc/
├── NpcInstance.cs        数据载体 + 好感度（运行时副本）+ 挨打掉好感 + 死亡/重生
├── NpcIndicator.cs       选中绿环 / 锁定红环 / 血条 / 受伤飘字
├── NpcAnimator.cs        按名字切动作（控制器用 Action:Int 驱动）
├── NpcTargeting.cs       鼠标选中 / 锁定管理器（挂在 Player 上）
├── NpcBodyBounds.cs      找「身躯」渲染体（排除武器）—— 贴地和碰撞体都用它
├── DamagePopup.cs        受伤飘字
└── Ai/
    ├── NpcAiBase.cs          ★ 基类 + NpcAiState + NpcAttackConfig + 物种装配
    ├── NpcAiCombatant.cs     会打人的两类的公共逻辑
    ├── NpcAiAnimal.cs        兽类
    ├── NpcAiDemon.cs         妖魔
    ├── NpcAiHuman.cs         人类
    ├── NpcAiMuJi.cs          ★ 母鸡（专属 AI 的模板）
    └── NpcProjectile.cs      施法子弹

Assets/Editor/Tools/NpcAssetFixer.cs   ▸ 菜单「修仙 / NPC 资产」：修动画控制器 + 修碰撞体
```
